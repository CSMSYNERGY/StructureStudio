// GENERATED FILE — do not edit. Compiled from structure-studio.component.js (sha256 53e25e2ca895)
// by scripts/compile.mjs using vendored babel-standalone 7.23.9. Rebuild: npm run compile
;(function () {
if (window.__ssBootBlocked) return; // the boot guard neutralises compiled scripts via this flag
"use strict";var _excluded=["style","wrapStyle"];function _classCallCheck(instance,Constructor){if(!(instance instanceof Constructor)){throw new TypeError("Cannot call a class as a function");}}function _defineProperties(target,props){for(var i=0;i<props.length;i++){var descriptor=props[i];descriptor.enumerable=descriptor.enumerable||false;descriptor.configurable=true;if("value"in descriptor)descriptor.writable=true;Object.defineProperty(target,_toPropertyKey(descriptor.key),descriptor);}}function _createClass(Constructor,protoProps,staticProps){if(protoProps)_defineProperties(Constructor.prototype,protoProps);if(staticProps)_defineProperties(Constructor,staticProps);Object.defineProperty(Constructor,"prototype",{writable:false});return Constructor;}function _callSuper(t,o,e){return o=_getPrototypeOf(o),_possibleConstructorReturn(t,_isNativeReflectConstruct()?Reflect.construct(o,e||[],_getPrototypeOf(t).constructor):o.apply(t,e));}function _possibleConstructorReturn(self,call){if(call&&(_typeof(call)==="object"||typeof call==="function")){return call;}else if(call!==void 0){throw new TypeError("Derived constructors may only return object or undefined");}return _assertThisInitialized(self);}function _assertThisInitialized(self){if(self===void 0){throw new ReferenceError("this hasn't been initialised - super() hasn't been called");}return self;}function _isNativeReflectConstruct(){try{var t=!Boolean.prototype.valueOf.call(Reflect.construct(Boolean,[],function(){}));}catch(t){}return(_isNativeReflectConstruct=function _isNativeReflectConstruct(){return!!t;})();}function _getPrototypeOf(o){_getPrototypeOf=Object.setPrototypeOf?Object.getPrototypeOf.bind():function _getPrototypeOf(o){return o.__proto__||Object.getPrototypeOf(o);};return _getPrototypeOf(o);}function _inherits(subClass,superClass){if(typeof superClass!=="function"&&superClass!==null){throw new TypeError("Super expression must either be null or a function");}subClass.prototype=Object.create(superClass&&superClass.prototype,{constructor:{value:subClass,writable:true,configurable:true}});Object.defineProperty(subClass,"prototype",{writable:false});if(superClass)_setPrototypeOf(subClass,superClass);}function _setPrototypeOf(o,p){_setPrototypeOf=Object.setPrototypeOf?Object.setPrototypeOf.bind():function _setPrototypeOf(o,p){o.__proto__=p;return o;};return _setPrototypeOf(o,p);}function _regeneratorRuntime(){"use strict";/*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/facebook/regenerator/blob/main/LICENSE */_regeneratorRuntime=function _regeneratorRuntime(){return e;};var t,e={},r=Object.prototype,n=r.hasOwnProperty,o=Object.defineProperty||function(t,e,r){t[e]=r.value;},i="function"==typeof Symbol?Symbol:{},a=i.iterator||"@@iterator",c=i.asyncIterator||"@@asyncIterator",u=i.toStringTag||"@@toStringTag";function define(t,e,r){return Object.defineProperty(t,e,{value:r,enumerable:!0,configurable:!0,writable:!0}),t[e];}try{define({},"");}catch(t){define=function define(t,e,r){return t[e]=r;};}function wrap(t,e,r,n){var i=e&&e.prototype instanceof Generator?e:Generator,a=Object.create(i.prototype),c=new Context(n||[]);return o(a,"_invoke",{value:makeInvokeMethod(t,r,c)}),a;}function tryCatch(t,e,r){try{return{type:"normal",arg:t.call(e,r)};}catch(t){return{type:"throw",arg:t};}}e.wrap=wrap;var h="suspendedStart",l="suspendedYield",f="executing",s="completed",y={};function Generator(){}function GeneratorFunction(){}function GeneratorFunctionPrototype(){}var p={};define(p,a,function(){return this;});var d=Object.getPrototypeOf,v=d&&d(d(values([])));v&&v!==r&&n.call(v,a)&&(p=v);var g=GeneratorFunctionPrototype.prototype=Generator.prototype=Object.create(p);function defineIteratorMethods(t){["next","throw","return"].forEach(function(e){define(t,e,function(t){return this._invoke(e,t);});});}function AsyncIterator(t,e){function invoke(r,o,i,a){var c=tryCatch(t[r],t,o);if("throw"!==c.type){var u=c.arg,h=u.value;return h&&"object"==_typeof(h)&&n.call(h,"__await")?e.resolve(h.__await).then(function(t){invoke("next",t,i,a);},function(t){invoke("throw",t,i,a);}):e.resolve(h).then(function(t){u.value=t,i(u);},function(t){return invoke("throw",t,i,a);});}a(c.arg);}var r;o(this,"_invoke",{value:function value(t,n){function callInvokeWithMethodAndArg(){return new e(function(e,r){invoke(t,n,e,r);});}return r=r?r.then(callInvokeWithMethodAndArg,callInvokeWithMethodAndArg):callInvokeWithMethodAndArg();}});}function makeInvokeMethod(e,r,n){var o=h;return function(i,a){if(o===f)throw new Error("Generator is already running");if(o===s){if("throw"===i)throw a;return{value:t,done:!0};}for(n.method=i,n.arg=a;;){var c=n.delegate;if(c){var u=maybeInvokeDelegate(c,n);if(u){if(u===y)continue;return u;}}if("next"===n.method)n.sent=n._sent=n.arg;else if("throw"===n.method){if(o===h)throw o=s,n.arg;n.dispatchException(n.arg);}else"return"===n.method&&n.abrupt("return",n.arg);o=f;var p=tryCatch(e,r,n);if("normal"===p.type){if(o=n.done?s:l,p.arg===y)continue;return{value:p.arg,done:n.done};}"throw"===p.type&&(o=s,n.method="throw",n.arg=p.arg);}};}function maybeInvokeDelegate(e,r){var n=r.method,o=e.iterator[n];if(o===t)return r.delegate=null,"throw"===n&&e.iterator["return"]&&(r.method="return",r.arg=t,maybeInvokeDelegate(e,r),"throw"===r.method)||"return"!==n&&(r.method="throw",r.arg=new TypeError("The iterator does not provide a '"+n+"' method")),y;var i=tryCatch(o,e.iterator,r.arg);if("throw"===i.type)return r.method="throw",r.arg=i.arg,r.delegate=null,y;var a=i.arg;return a?a.done?(r[e.resultName]=a.value,r.next=e.nextLoc,"return"!==r.method&&(r.method="next",r.arg=t),r.delegate=null,y):a:(r.method="throw",r.arg=new TypeError("iterator result is not an object"),r.delegate=null,y);}function pushTryEntry(t){var e={tryLoc:t[0]};1 in t&&(e.catchLoc=t[1]),2 in t&&(e.finallyLoc=t[2],e.afterLoc=t[3]),this.tryEntries.push(e);}function resetTryEntry(t){var e=t.completion||{};e.type="normal",delete e.arg,t.completion=e;}function Context(t){this.tryEntries=[{tryLoc:"root"}],t.forEach(pushTryEntry,this),this.reset(!0);}function values(e){if(e||""===e){var r=e[a];if(r)return r.call(e);if("function"==typeof e.next)return e;if(!isNaN(e.length)){var o=-1,i=function next(){for(;++o<e.length;)if(n.call(e,o))return next.value=e[o],next.done=!1,next;return next.value=t,next.done=!0,next;};return i.next=i;}}throw new TypeError(_typeof(e)+" is not iterable");}return GeneratorFunction.prototype=GeneratorFunctionPrototype,o(g,"constructor",{value:GeneratorFunctionPrototype,configurable:!0}),o(GeneratorFunctionPrototype,"constructor",{value:GeneratorFunction,configurable:!0}),GeneratorFunction.displayName=define(GeneratorFunctionPrototype,u,"GeneratorFunction"),e.isGeneratorFunction=function(t){var e="function"==typeof t&&t.constructor;return!!e&&(e===GeneratorFunction||"GeneratorFunction"===(e.displayName||e.name));},e.mark=function(t){return Object.setPrototypeOf?Object.setPrototypeOf(t,GeneratorFunctionPrototype):(t.__proto__=GeneratorFunctionPrototype,define(t,u,"GeneratorFunction")),t.prototype=Object.create(g),t;},e.awrap=function(t){return{__await:t};},defineIteratorMethods(AsyncIterator.prototype),define(AsyncIterator.prototype,c,function(){return this;}),e.AsyncIterator=AsyncIterator,e.async=function(t,r,n,o,i){void 0===i&&(i=Promise);var a=new AsyncIterator(wrap(t,r,n,o),i);return e.isGeneratorFunction(r)?a:a.next().then(function(t){return t.done?t.value:a.next();});},defineIteratorMethods(g),define(g,u,"Generator"),define(g,a,function(){return this;}),define(g,"toString",function(){return"[object Generator]";}),e.keys=function(t){var e=Object(t),r=[];for(var n in e)r.push(n);return r.reverse(),function next(){for(;r.length;){var t=r.pop();if(t in e)return next.value=t,next.done=!1,next;}return next.done=!0,next;};},e.values=values,Context.prototype={constructor:Context,reset:function reset(e){if(this.prev=0,this.next=0,this.sent=this._sent=t,this.done=!1,this.delegate=null,this.method="next",this.arg=t,this.tryEntries.forEach(resetTryEntry),!e)for(var r in this)"t"===r.charAt(0)&&n.call(this,r)&&!isNaN(+r.slice(1))&&(this[r]=t);},stop:function stop(){this.done=!0;var t=this.tryEntries[0].completion;if("throw"===t.type)throw t.arg;return this.rval;},dispatchException:function dispatchException(e){if(this.done)throw e;var r=this;function handle(n,o){return a.type="throw",a.arg=e,r.next=n,o&&(r.method="next",r.arg=t),!!o;}for(var o=this.tryEntries.length-1;o>=0;--o){var i=this.tryEntries[o],a=i.completion;if("root"===i.tryLoc)return handle("end");if(i.tryLoc<=this.prev){var c=n.call(i,"catchLoc"),u=n.call(i,"finallyLoc");if(c&&u){if(this.prev<i.catchLoc)return handle(i.catchLoc,!0);if(this.prev<i.finallyLoc)return handle(i.finallyLoc);}else if(c){if(this.prev<i.catchLoc)return handle(i.catchLoc,!0);}else{if(!u)throw new Error("try statement without catch or finally");if(this.prev<i.finallyLoc)return handle(i.finallyLoc);}}}},abrupt:function abrupt(t,e){for(var r=this.tryEntries.length-1;r>=0;--r){var o=this.tryEntries[r];if(o.tryLoc<=this.prev&&n.call(o,"finallyLoc")&&this.prev<o.finallyLoc){var i=o;break;}}i&&("break"===t||"continue"===t)&&i.tryLoc<=e&&e<=i.finallyLoc&&(i=null);var a=i?i.completion:{};return a.type=t,a.arg=e,i?(this.method="next",this.next=i.finallyLoc,y):this.complete(a);},complete:function complete(t,e){if("throw"===t.type)throw t.arg;return"break"===t.type||"continue"===t.type?this.next=t.arg:"return"===t.type?(this.rval=this.arg=t.arg,this.method="return",this.next="end"):"normal"===t.type&&e&&(this.next=e),y;},finish:function finish(t){for(var e=this.tryEntries.length-1;e>=0;--e){var r=this.tryEntries[e];if(r.finallyLoc===t)return this.complete(r.completion,r.afterLoc),resetTryEntry(r),y;}},"catch":function _catch(t){for(var e=this.tryEntries.length-1;e>=0;--e){var r=this.tryEntries[e];if(r.tryLoc===t){var n=r.completion;if("throw"===n.type){var o=n.arg;resetTryEntry(r);}return o;}}throw new Error("illegal catch attempt");},delegateYield:function delegateYield(e,r,n){return this.delegate={iterator:values(e),resultName:r,nextLoc:n},"next"===this.method&&(this.arg=t),y;}},e;}function asyncGeneratorStep(gen,resolve,reject,_next,_throw,key,arg){try{var info=gen[key](arg);var value=info.value;}catch(error){reject(error);return;}if(info.done){resolve(value);}else{Promise.resolve(value).then(_next,_throw);}}function _asyncToGenerator(fn){return function(){var self=this,args=arguments;return new Promise(function(resolve,reject){var gen=fn.apply(self,args);function _next(value){asyncGeneratorStep(gen,resolve,reject,_next,_throw,"next",value);}function _throw(err){asyncGeneratorStep(gen,resolve,reject,_next,_throw,"throw",err);}_next(undefined);});};}function _typeof(o){"@babel/helpers - typeof";return _typeof="function"==typeof Symbol&&"symbol"==typeof Symbol.iterator?function(o){return typeof o;}:function(o){return o&&"function"==typeof Symbol&&o.constructor===Symbol&&o!==Symbol.prototype?"symbol":typeof o;},_typeof(o);}function _toConsumableArray(arr){return _arrayWithoutHoles(arr)||_iterableToArray(arr)||_unsupportedIterableToArray(arr)||_nonIterableSpread();}function _nonIterableSpread(){throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");}function _iterableToArray(iter){if(typeof Symbol!=="undefined"&&iter[Symbol.iterator]!=null||iter["@@iterator"]!=null)return Array.from(iter);}function _arrayWithoutHoles(arr){if(Array.isArray(arr))return _arrayLikeToArray(arr);}function _createForOfIteratorHelper(o,allowArrayLike){var it=typeof Symbol!=="undefined"&&o[Symbol.iterator]||o["@@iterator"];if(!it){if(Array.isArray(o)||(it=_unsupportedIterableToArray(o))||allowArrayLike&&o&&typeof o.length==="number"){if(it)o=it;var i=0;var F=function F(){};return{s:F,n:function n(){if(i>=o.length)return{done:true};return{done:false,value:o[i++]};},e:function e(_e2){throw _e2;},f:F};}throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");}var normalCompletion=true,didErr=false,err;return{s:function s(){it=it.call(o);},n:function n(){var step=it.next();normalCompletion=step.done;return step;},e:function e(_e3){didErr=true;err=_e3;},f:function f(){try{if(!normalCompletion&&it["return"]!=null)it["return"]();}finally{if(didErr)throw err;}}};}function _extends(){_extends=Object.assign?Object.assign.bind():function(target){for(var i=1;i<arguments.length;i++){var source=arguments[i];for(var key in source){if(Object.prototype.hasOwnProperty.call(source,key)){target[key]=source[key];}}}return target;};return _extends.apply(this,arguments);}function ownKeys(e,r){var t=Object.keys(e);if(Object.getOwnPropertySymbols){var o=Object.getOwnPropertySymbols(e);r&&(o=o.filter(function(r){return Object.getOwnPropertyDescriptor(e,r).enumerable;})),t.push.apply(t,o);}return t;}function _objectSpread(e){for(var r=1;r<arguments.length;r++){var t=null!=arguments[r]?arguments[r]:{};r%2?ownKeys(Object(t),!0).forEach(function(r){_defineProperty(e,r,t[r]);}):Object.getOwnPropertyDescriptors?Object.defineProperties(e,Object.getOwnPropertyDescriptors(t)):ownKeys(Object(t)).forEach(function(r){Object.defineProperty(e,r,Object.getOwnPropertyDescriptor(t,r));});}return e;}function _defineProperty(obj,key,value){key=_toPropertyKey(key);if(key in obj){Object.defineProperty(obj,key,{value:value,enumerable:true,configurable:true,writable:true});}else{obj[key]=value;}return obj;}function _toPropertyKey(t){var i=_toPrimitive(t,"string");return"symbol"==_typeof(i)?i:String(i);}function _toPrimitive(t,r){if("object"!=_typeof(t)||!t)return t;var e=t[Symbol.toPrimitive];if(void 0!==e){var i=e.call(t,r||"default");if("object"!=_typeof(i))return i;throw new TypeError("@@toPrimitive must return a primitive value.");}return("string"===r?String:Number)(t);}function _slicedToArray(arr,i){return _arrayWithHoles(arr)||_iterableToArrayLimit(arr,i)||_unsupportedIterableToArray(arr,i)||_nonIterableRest();}function _nonIterableRest(){throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");}function _unsupportedIterableToArray(o,minLen){if(!o)return;if(typeof o==="string")return _arrayLikeToArray(o,minLen);var n=Object.prototype.toString.call(o).slice(8,-1);if(n==="Object"&&o.constructor)n=o.constructor.name;if(n==="Map"||n==="Set")return Array.from(o);if(n==="Arguments"||/^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n))return _arrayLikeToArray(o,minLen);}function _arrayLikeToArray(arr,len){if(len==null||len>arr.length)len=arr.length;for(var i=0,arr2=new Array(len);i<len;i++)arr2[i]=arr[i];return arr2;}function _iterableToArrayLimit(r,l){var t=null==r?null:"undefined"!=typeof Symbol&&r[Symbol.iterator]||r["@@iterator"];if(null!=t){var e,n,i,u,a=[],f=!0,o=!1;try{if(i=(t=t.call(r)).next,0===l){if(Object(t)!==t)return;f=!1;}else for(;!(f=(e=i.call(t)).done)&&(a.push(e.value),a.length!==l);f=!0);}catch(r){o=!0,n=r;}finally{try{if(!f&&null!=t["return"]&&(u=t["return"](),Object(u)!==u))return;}finally{if(o)throw n;}}return a;}}function _arrayWithHoles(arr){if(Array.isArray(arr))return arr;}function _objectWithoutProperties(source,excluded){if(source==null)return{};var target=_objectWithoutPropertiesLoose(source,excluded);var key,i;if(Object.getOwnPropertySymbols){var sourceSymbolKeys=Object.getOwnPropertySymbols(source);for(i=0;i<sourceSymbolKeys.length;i++){key=sourceSymbolKeys[i];if(excluded.indexOf(key)>=0)continue;if(!Object.prototype.propertyIsEnumerable.call(source,key))continue;target[key]=source[key];}}return target;}function _objectWithoutPropertiesLoose(source,excluded){if(source==null)return{};var target={};var sourceKeys=Object.keys(source);var key,i;for(i=0;i<sourceKeys.length;i++){key=sourceKeys[i];if(excluded.indexOf(key)>=0)continue;target[key]=source[key];}return target;}// structure-studio.component.js — the ONE shared designer module, loaded by BOTH
// index.html and portal.html via <script src=... type="text/babel">. Hand-mirrored
// twin of StructureStudio.jsx (the ES-module canon) in the babel/global-destructure
// dialect — every body/wrapper edit lands in BOTH files (see CLAUDE.md).
// Self-contained on purpose: Babel compiles each text/babel block independently, so
// nothing here leans on the host page's consts; the page mounts alias the globals
// published at the bottom. No createRoot here — mounting is the host page's job.
var _React=React,useState=_React.useState,useRef=_React.useRef,useCallback=_React.useCallback,useEffect=_React.useEffect,useMemo=_React.useMemo,Component=_React.Component;var _ReactDOM=ReactDOM,createPortal=_ReactDOM.createPortal;// Password input with a show/hide (eye) toggle. Forwards all input props; `wrapStyle`
// carries any flex/grid sizing onto the positioned wrapper so layouts are preserved.
function PasswordInput(_ref){var style=_ref.style,wrapStyle=_ref.wrapStyle,rest=_objectWithoutProperties(_ref,_excluded);var _useState=useState(false),_useState2=_slicedToArray(_useState,2),show=_useState2[0],setShow=_useState2[1];return/*#__PURE__*/React.createElement("div",{style:_objectSpread({position:"relative",width:"100%",boxSizing:"border-box"},wrapStyle)},/*#__PURE__*/React.createElement("input",_extends({},rest,{type:show?"text":"password",style:_objectSpread(_objectSpread({},style),{},{width:"100%",boxSizing:"border-box",paddingRight:38})})),/*#__PURE__*/React.createElement("button",{type:"button",tabIndex:-1,"aria-label":show?"Hide password":"Show password",title:show?"Hide password":"Show password",onMouseDown:function onMouseDown(e){return e.preventDefault();},onClick:function onClick(){return setShow(function(v){return!v;});},style:{position:"absolute",top:0,right:0,height:"100%",width:34,display:"flex",alignItems:"center",justifyContent:"center",background:"transparent",border:"none",padding:0,margin:0,cursor:"pointer",color:"#64748B"}},show?/*#__PURE__*/React.createElement("svg",{width:"18",height:"18",viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round","aria-hidden":"true"},/*#__PURE__*/React.createElement("path",{d:"M9.88 9.88a3 3 0 1 0 4.24 4.24"}),/*#__PURE__*/React.createElement("path",{d:"M10.73 5.08A10.43 10.43 0 0 1 12 5c7 0 10 7 10 7a13.16 13.16 0 0 1-1.67 2.68"}),/*#__PURE__*/React.createElement("path",{d:"M6.61 6.61A13.526 13.526 0 0 0 2 12s3 7 10 7a9.74 9.74 0 0 0 5.39-1.61"}),/*#__PURE__*/React.createElement("line",{x1:"2",x2:"22",y1:"2",y2:"22"})):/*#__PURE__*/React.createElement("svg",{width:"18",height:"18",viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round","aria-hidden":"true"},/*#__PURE__*/React.createElement("path",{d:"M2 12s3-7 10-7 10 7 10 7-3 7-10 7-10-7-10-7Z"}),/*#__PURE__*/React.createElement("circle",{cx:"12",cy:"12",r:"3"}))));}var createClient=window.supabase.createClient;// ─── Supabase project ───
// Single shared project across all white-label tenants. The anon key is browser-safe
// (RLS + capability RPCs); the service-role key never leaves the Edge Functions.
var SUPABASE_URL="https://jzeamjbhdrsbygdnphbm.supabase.co";var SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imp6ZWFtamJoZHJzYnlnZG5waGJtIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzczNDIwNDMsImV4cCI6MjA5MjkxODA0M30.YawJS7aiyTbQdwVnzndyKwD2ejNGYhdBSiectURvxwY";// Address-autocomplete key used when a tenant's config row doesn't carry its own
// googleMapsApiKey.
var DEFAULT_GOOGLE_MAPS_API_KEY="AIzaSyDEKe7mODI2xKnUQ5-z7L0ZZnUfBgE6dok";// ─── Default tenant ───
// When neither ?client= nor a tenant subdomain nor a design owner resolves a
// client, the loader fetches this client's config from public.client_configs.
// There is no in-source config copy — the table row is the source of truth.
// To change Junior Barns, edit the row, not this file.
var DEFAULT_CLIENT_ID="junior-barns";// ─── STRUCTURE STUDIO ENGINE ───
var WALL_THICKNESS=6;// Built-in annotation tools that are merged into ITEMS for every client.
// Distinct from `layoutItems` in config (which the client controls): these
// are universal drawing aids — a free-text note and a freeform line.
var BUILT_IN_TOOLS={textNote:{label:"Note",color:"#0F172A",icon:"📝",shortLabel:"Note",noteType:true,width:4,height:1},line:{label:"Line",color:"#475569",icon:"📏",shortLabel:"Line",lineType:true,width:4,height:0}};// Legacy render safety-net. When a tenant HIDES a built-in option (deactivates it in
// client_layout_items so it drops out of get_config's layoutItems), its already-placed items on
// SAVED designs would otherwise lose their render config and vanish. This provides the standard
// built-in door/window/ramp definitions for RENDERING ONLY (noPalette → never a placeable tool),
// so old quotes keep drawing correctly forever. Spread FIRST in ITEMS so a tenant that still has
// the item ACTIVE overrides it with their own config (and it shows in the palette as normal); it
// only fills the gap for a HIDDEN item. Dimensions/colors mirror the layout_item_types master.
var LEGACY_LAYOUT_FALLBACK={singleDoor:{label:"Single Door (36\")",icon:"🚪",color:"#D97706",width:3,height:0.5,shortLabel:"SD",wallOnly:true,noPalette:true},doubleDoor:{label:"Double Door (60\")",icon:"🚪🚪",color:"#B45309",width:5,height:0.5,shortLabel:"DD",wallOnly:true,noPalette:true},window:{label:"Window (24\")",icon:"🪟",color:"#0EA5E9",width:2,height:0.5,shortLabel:"W",wallOnly:true,noPalette:true}// NOTE: ramp is NOT here — it's fully self-contained now (SIMPLE_RAMP_CFG below), decoupled from
// the built-in `ramp` layout item, so a tenant's ramp works whether or not that legacy row exists.
};// Title-case a building-style name for display (designs store either the label
// "Farmland" or the lowercase key "cabin").
function capWords(s){return String(s||"").replace(/\b\w/g,function(c){return c.toUpperCase();});}// Board-and-batten door glyph for the palette buttons (single + double), modeled on the
// real shed doors: cream frame, vertical planks, a mid cross-rail, black T-hinges, and a
// latch. Replaces the generic door emoji so the button reads as the actual product.
function DoorIcon(_ref2){var _ref2$double=_ref2["double"],_double=_ref2$double===void 0?false:_ref2$double;var FRAME="#ECE4D3",PANEL="#B99A82",PLANK="#8B7058",IRON="#2C2A28";var leaf=function leaf(x,w,hingeLeft){var planks=[];for(var i=1;i<=3;i++){var px=x+w*i/4;planks.push(/*#__PURE__*/React.createElement("line",{key:i,x1:px,y1:2.6,x2:px,y2:15.4,stroke:PLANK,strokeWidth:0.4}));}var hx=hingeLeft?x+0.5:x+w-1.8;return/*#__PURE__*/React.createElement("g",{key:x},/*#__PURE__*/React.createElement("rect",{x:x,y:1,width:w,height:16,rx:0.5,fill:FRAME,stroke:"#B5A98E",strokeWidth:0.7}),/*#__PURE__*/React.createElement("rect",{x:x+1.1,y:2.2,width:w-2.2,height:13.6,fill:PANEL}),planks,/*#__PURE__*/React.createElement("rect",{x:x+1.1,y:8.1,width:w-2.2,height:1.2,fill:FRAME}),/*#__PURE__*/React.createElement("rect",{x:hx,y:3.4,width:1.3,height:0.9,fill:IRON}),/*#__PURE__*/React.createElement("rect",{x:hx,y:12.8,width:1.3,height:0.9,fill:IRON}));};if(_double){return/*#__PURE__*/React.createElement("svg",{width:18,height:16,viewBox:"0 0 20 18",style:{display:"block"},"aria-hidden":"true"},leaf(0.5,9,true),leaf(10.5,9,false),/*#__PURE__*/React.createElement("rect",{x:9.2,y:8.3,width:1.6,height:0.9,fill:IRON}));}return/*#__PURE__*/React.createElement("svg",{width:11,height:16,viewBox:"0 0 12 18",style:{display:"block"},"aria-hidden":"true"},leaf(0.5,11,true),/*#__PURE__*/React.createElement("rect",{x:9.4,y:8.2,width:1.4,height:0.9,fill:IRON}));}// Closest point distance from (px,py) to the segment (x1,y1)-(x2,y2)
function _distToSeg(px,py,x1,y1,x2,y2){var dx=x2-x1,dy=y2-y1;var lenSq=dx*dx+dy*dy;var t=lenSq?((px-x1)*dx+(py-y1)*dy)/lenSq:0;t=Math.max(0,Math.min(1,t));var cx=x1+t*dx,cy=y1+t*dy;return Math.sqrt((px-cx)*(px-cx)+(py-cy)*(py-cy));}// Treat each wall as a line segment; pick the wall closest to the click.
// Returns null only when the click is too far from any wall to be reasonable.
function getWallFromClick(x,y,pW,pH,mgX,mgY){var ix=x-mgX,iy=y-mgY,T=80;var walls=[{wall:"north",d:_distToSeg(ix,iy,0,0,pW,0)},{wall:"south",d:_distToSeg(ix,iy,0,pH,pW,pH)},{wall:"west",d:_distToSeg(ix,iy,0,0,0,pH)},{wall:"east",d:_distToSeg(ix,iy,pW,0,pW,pH)}];walls.sort(function(a,b){return a.d-b.d;});return walls[0].d<=T?walls[0].wall:null;}function snapToWall(wall,cx,cy,iW,iH,pW,pH,mgX,mgY){var hw=iW/2;switch(wall){case"north":return{x:Math.max(mgX+hw,Math.min(cx,mgX+pW-hw)),y:mgY,rotation:0,wall:wall};case"south":return{x:Math.max(mgX+hw,Math.min(cx,mgX+pW-hw)),y:mgY+pH,rotation:0,wall:wall};case"west":return{x:mgX,y:Math.max(mgY+hw,Math.min(cy,mgY+pH-hw)),rotation:90,wall:wall};case"east":return{x:mgX+pW,y:Math.max(mgY+hw,Math.min(cy,mgY+pH-hw)),rotation:90,wall:wall};default:return{x:cx,y:cy,rotation:0,wall:null};}}function snapToWallInterior(wall,cx,cy,iW,iH,pW,pH,mgX,mgY){switch(wall){case"north":return{x:Math.max(mgX+iW/2,Math.min(cx,mgX+pW-iW/2)),y:mgY+iH/2,rotation:0,wall:wall};case"south":return{x:Math.max(mgX+iW/2,Math.min(cx,mgX+pW-iW/2)),y:mgY+pH-iH/2,rotation:0,wall:wall};case"west":return{x:mgX+iH/2,y:Math.max(mgY+iW/2,Math.min(cy,mgY+pH-iW/2)),rotation:90,wall:wall};case"east":return{x:mgX+pW-iH/2,y:Math.max(mgY+iW/2,Math.min(cy,mgY+pH-iW/2)),rotation:90,wall:wall};default:return{x:cx,y:cy,rotation:0,wall:null};}}// Always returns a wall — used as a fallback when the click is ambiguous or far
// from the plan. Uses segment distance so corner clicks resolve to the closer side.
function getNearestWall(x,y,pW,pH,mgX,mgY){var ix=x-mgX,iy=y-mgY;var walls=[{wall:"north",d:_distToSeg(ix,iy,0,0,pW,0)},{wall:"south",d:_distToSeg(ix,iy,0,pH,pW,pH)},{wall:"west",d:_distToSeg(ix,iy,0,0,0,pH)},{wall:"east",d:_distToSeg(ix,iy,pW,0,pW,pH)}];walls.sort(function(a,b){return a.d-b.d;});return walls[0].wall;}function checkDoorCollision(ni,nc,existing,itemTypes,sc){if(!ni.wall)return false;var niw=(ni.widthFt||nc.width)*sc;var _iterator=_createForOfIteratorHelper(existing),_step;try{for(_iterator.s();!(_step=_iterator.n()).done;){var it=_step.value;var c=itemTypes[it.type];if(!c||!c.wallOnly||it.type==="window")continue;// Only check doors on the same wall
if(it.wall!==ni.wall)continue;var iw=(it.widthFt||c.width)*sc;// Check overlap along the wall axis
if(ni.wall==="north"||ni.wall==="south"){if(Math.abs(ni.x-it.x)<niw/2+iw/2+4)return true;}else{if(Math.abs(ni.y-it.y)<niw/2+iw/2+4)return true;}}}catch(err){_iterator.e(err);}finally{_iterator.f();}return false;}// Does a wall-mounted item (door / window / rough opening) at `sn` overlap a WORKBENCH on the
// same wall? checkDoorCollision above deliberately only compares wallOnly items to each other, and
// a workbench is wallSnap, so it is skipped there — which meant the invariant was enforced in one
// direction only: dragging a workbench into a door showed "A door is blocking this wall!", while
// dragging the DOOR onto the workbench silently succeeded and produced exactly the layout that
// toast exists to prevent, rasterized into the PDF and sent to the shop. Same math as the
// workbench-side check, read from the other side.
function checkWorkbenchOverlap(sn,widthFtPx,existing,itemTypes,sc){if(!sn.wall)return false;var isH=sn.wall==="north"||sn.wall==="south";var candPos=isH?sn.x:sn.y;var candHalf=widthFtPx/2;var _iterator2=_createForOfIteratorHelper(existing),_step2;try{for(_iterator2.s();!(_step2=_iterator2.n()).done;){var ob=_step2.value;if(ob.type!=="workbench"||ob.wall!==sn.wall)continue;var obHalf=(ob.widthFt||itemTypes[ob.type]&&itemTypes[ob.type].width)*sc/2;var obPos=isH?ob.x:ob.y;if(Math.abs(candPos-obPos)<candHalf+obHalf-2)return true;}}catch(err){_iterator2.e(err);}finally{_iterator2.f();}return false;}function parseSize(s){if(!s)return null;var m=s.match(/(\d+)\s*[x×✕]\s*(\d+)/i);// accept Unicode ×/✕ size labels too, not just ASCII x — else the building silently stays at the default size (audit #F2)
return m?{w:parseInt(m[1]),h:parseInt(m[2])}:null;}// Check if a loft (edges in ft) has both ends of at least one axis attached to walls or other lofts
function checkLoftAttached(l,r,t,b,bldgW,bldgH,otherLoftEdges){var tol=0.3;var atWall=function atWall(val,wallVal){return Math.abs(val-wallVal)<tol;};var touchesLoft=function touchesLoft(edge,oKey,pMin,pMax,opMin,opMax){return otherLoftEdges.some(function(o){return Math.abs(edge-o[oKey])<tol&&pMin<o[opMax]-tol&&pMax>o[opMin]+tol;});};var leftOk=atWall(l,0)||touchesLoft(l,"r",t,b,"t","b");var rightOk=atWall(r,bldgW)||touchesLoft(r,"l",t,b,"t","b");if(leftOk&&rightOk)return true;var topOk=atWall(t,0)||touchesLoft(t,"b",l,r,"l","r");var bottomOk=atWall(b,bldgH)||touchesLoft(b,"t",l,r,"l","r");if(topOk&&bottomOk)return true;return false;}// ─── Page geometry, as a pure function of the building size ───────────────────
// The component derives scale/mgX/mgY from bldgW/bldgH on every render. Reflowing
// a layout after a size change needs the SAME derivation for the OLD size, to turn
// stored page-pixels back into feet — so the formula lives here, called twice
// (old, new), instead of being inlined once in the render body.
var SS_PAGE={W:850,H:1100,TEXT_AREA_H:340,TOP_LABEL_PAD:30,BOT_LABEL_PAD:30,RAMP_SPACE_FT:2};// Narrowest canvas ROW that can carry the plan and the docked 3D panel side by side:
// plan floor 560 + gap 12 + panel floor 320 + row padding 40 = 932, rounded to 960.
// Measured on the ROW, never on window.innerWidth: the portal sidebar is 240px and
// collapses to 68px at max-width:900px, so the designer host is ~661px at a 901px
// viewport but ~832px at 900px — a viewport threshold would switch the dock ON for
// the narrower layout and OFF for the wider one.
var SS_DOCK_MIN_ROW_W=960;function pageGeom(bldgW,bldgH){var visibleH=SS_PAGE.H-SS_PAGE.TEXT_AREA_H;var scale=Math.min(SS_PAGE.W*0.70/bldgW,visibleH*0.70/bldgH,(visibleH-SS_PAGE.TOP_LABEL_PAD-SS_PAGE.BOT_LABEL_PAD)/(bldgH+2*SS_PAGE.RAMP_SPACE_FT));var pW=bldgW*scale,pH=bldgH*scale;return{scale:scale,pW:pW,pH:pH,mgX:(SS_PAGE.W-pW)/2,mgY:SS_PAGE.RAMP_SPACE_FT*scale+SS_PAGE.TOP_LABEL_PAD};}// Where a ramp sits for a given door position — the ramp is derived, never placed.
// Uses the ramp's OWN depth (heightFt) rather than the 2 ft default, so a catalog
// ramp with a longer run stays where it was drawn.
function rampPosFor(rmp,sn,g){var depthPx=(rmp&&rmp.heightFt||SS_PAGE.RAMP_SPACE_FT)*g.scale;if(sn.wall==="north")return{x:sn.x,y:g.mgY-depthPx/2,rotation:0,wall:"north"};if(sn.wall==="south")return{x:sn.x,y:g.mgY+g.pH+depthPx/2,rotation:0,wall:"south"};if(sn.wall==="west")return{x:g.mgX-depthPx/2,y:sn.y,rotation:90,wall:"west"};if(sn.wall==="east")return{x:g.mgX+g.pW+depthPx/2,y:sn.y,rotation:90,wall:"east"};return null;}// Items the customer sizes themselves, so shrinking one to fit a smaller building
// preserves their intent. A DOOR is not in this list on purpose: a 6 ft double door
// quietly becoming 4 ft would change what they are buying.
var SS_SHRINKABLE={workbench:true,roughOpening:true,loft:true};var SS_WALL_ORDER={north:["south","east","west"],south:["north","east","west"],east:["west","north","south"],west:["east","north","south"]};/**
 * Move every placed item into the equivalent legal position for a new building size.
 *
 * PURE — returns { items, events } and mutates nothing, so the caller can inspect the
 * events and decide whether to commit. That is what makes "revert the size change when
 * something cannot be placed" possible without ever leaving a half-reflowed layout.
 *
 * events: { id, type, label, kind } where kind is "movedWall" | "resized" | "blocked".
 * Sliding along the SAME wall is deliberately not an event — it is what the customer
 * expects when a building grows or shrinks, and reporting it would bury the two things
 * they do need to know.
 */function reflowItems(items,prev,next,ITEMS){var A=pageGeom(prev.w,prev.h),B=pageGeom(next.w,next.h);var events=[];var byId=new Map();var placed=[];// already-reflowed items, for collision tests
var labelOf=function labelOf(it){return ITEMS[it.type]&&ITEMS[it.type].label||it.type;};// Try to seat a wall item at `wantFt` along `wall`, sliding to the nearest clear spot.
// Returns the snap result, or null when the wall has no room.
var seat=function seat(it,cfg,wall,wantFt,wFt,hFt){var isH=wall==="north"||wall==="south";var wallLen=isH?next.w:next.h;var half=wFt/2;if(wFt>wallLen)return null;var lo=half,hi=wallLen-half;var start=Math.max(lo,Math.min(wantFt,hi));// Search outward from the wanted spot in 0.25 ft steps, nearest first.
for(var d=0;d<=hi-lo+0.25;d+=0.25){for(var _i=0,_arr=d===0?[start]:[start-d,start+d];_i<_arr.length;_i++){var c=_arr[_i];if(c<lo-0.001||c>hi+0.001)continue;var px=(isH?B.mgX:B.mgY)+c*B.scale;var sn=cfg.wallSnap?snapToWallInterior(wall,isH?px:B.mgX,isH?B.mgY:px,wFt*B.scale,hFt*B.scale,B.pW,B.pH,B.mgX,B.mgY):snapToWall(wall,isH?px:B.mgX,isH?B.mgY:px,wFt*B.scale,hFt*B.scale,B.pW,B.pH,B.mgX,B.mgY);var cand=_objectSpread(_objectSpread(_objectSpread({},it),sn),{},{widthFt:wFt,heightFt:hFt});if(checkDoorCollision(cand,_objectSpread(_objectSpread({},cfg),{},{width:wFt}),placed,ITEMS,B.scale))continue;if(checkWorkbenchOverlap(sn,wFt*B.scale,placed,ITEMS,B.scale))continue;return sn;}}return null;};// ── Pass 1: everything except ramps (which are derived from their door) ──
var _iterator3=_createForOfIteratorHelper(items),_step3;try{for(_iterator3.s();!(_step3=_iterator3.n()).done;){var it=_step3.value;var cfg=ITEMS[it.type];// Annotations live in PAGE space, deliberately independent of the building —
// moving them with the plan would be a regression, not a completion.
if(!cfg||cfg.lineType||it.type==="textNote"||it.type==="line"){byId.set(it.id,it);continue;}if(cfg.doorSnap)continue;// pass 2
var isWall=(cfg.wallOnly||cfg.wallSnap)&&it.wall;if(isWall){var isH=it.wall==="north"||it.wall==="south";var wantFt=isH?(it.x-A.mgX)/A.scale:(it.y-A.mgY)/A.scale;var _hFt=it.heightFt||cfg.height;var _wFt=it.widthFt||cfg.width;var wallLen=isH?next.w:next.h;if(_wFt>wallLen&&SS_SHRINKABLE[it.type]){var shrunk=Math.max(it.type==="roughOpening"?0.5:2,wallLen);if(shrunk!==_wFt){_wFt=shrunk;events.push({id:it.id,type:it.type,label:labelOf(it),kind:"resized",to:_wFt});}}var sn=seat(it,cfg,it.wall,wantFt,_wFt,_hFt);var movedWall=false;if(!sn){var _iterator5=_createForOfIteratorHelper(SS_WALL_ORDER[it.wall]||[]),_step5;try{for(_iterator5.s();!(_step5=_iterator5.n()).done;){var w2=_step5.value;var isH2=w2==="north"||w2==="south";sn=seat(it,cfg,w2,(isH2?next.w:next.h)/2,_wFt,_hFt);if(sn){movedWall=true;break;}}}catch(err){_iterator5.e(err);}finally{_iterator5.f();}}if(!sn){events.push({id:it.id,type:it.type,label:labelOf(it),kind:"blocked"});byId.set(it.id,it);continue;}if(movedWall)events.push({id:it.id,type:it.type,label:labelOf(it),kind:"movedWall",to:sn.wall});var _nit=_objectSpread(_objectSpread(_objectSpread({},it),sn),{},{widthFt:_wFt});byId.set(it.id,_nit);placed.push(_nit);continue;}if(it.type==="loft"){var _wFt2=it.widthFt||cfg.width,_hFt2=it.heightFt||cfg.height;var _cxFt=(it.x-A.mgX)/A.scale,_cyFt=(it.y-A.mgY)/A.scale;// A loft placed by clicking is seeded widthFt = bldgW (a snapshot of the building
// width). If it spanned wall to wall, it should still span wall to wall.
if(Math.abs(_wFt2-prev.w)<0.35){_wFt2=next.w;_cxFt=next.w/2;}else _cxFt=_cxFt*(next.w/prev.w);_cyFt=_cyFt*(next.h/prev.h);var wasW=_wFt2,wasH=_hFt2;_wFt2=Math.max(2,Math.min(_wFt2,next.w));_hFt2=Math.max(2,Math.min(_hFt2,next.h));if(_wFt2>next.w||_hFt2>next.h){events.push({id:it.id,type:it.type,label:labelOf(it),kind:"blocked"});byId.set(it.id,it);continue;}if(_wFt2!==wasW||_hFt2!==wasH)events.push({id:it.id,type:it.type,label:labelOf(it),kind:"resized",to:_wFt2});_cxFt=Math.max(_wFt2/2,Math.min(_cxFt,next.w-_wFt2/2));_cyFt=Math.max(_hFt2/2,Math.min(_cyFt,next.h-_hFt2/2));var _nit2=_objectSpread(_objectSpread({},it),{},{x:B.mgX+_cxFt*B.scale,y:B.mgY+_cyFt*B.scale,widthFt:_wFt2,heightFt:_hFt2});byId.set(it.id,_nit2);placed.push(_nit2);continue;}// Free-floating: keep it inside the new box.
var wFt=it.widthFt||cfg.width,hFt=it.heightFt||cfg.height;var cxFt=(it.x-A.mgX)/A.scale*(next.w/prev.w);var cyFt=(it.y-A.mgY)/A.scale*(next.h/prev.h);cxFt=Math.max(wFt/2,Math.min(cxFt,next.w-wFt/2));cyFt=Math.max(hFt/2,Math.min(cyFt,next.h-hFt/2));var nit=_objectSpread(_objectSpread({},it),{},{x:B.mgX+cxFt*B.scale,y:B.mgY+cyFt*B.scale});byId.set(it.id,nit);placed.push(nit);}// ── Pass 2: ramps follow their door, or go with it ──
}catch(err){_iterator3.e(err);}finally{_iterator3.f();}var _iterator4=_createForOfIteratorHelper(items),_step4;try{for(_iterator4.s();!(_step4=_iterator4.n()).done;){var _it=_step4.value;var _cfg=ITEMS[_it.type];if(!_cfg||!_cfg.doorSnap)continue;var door=byId.get(_it.snapDoorId);var pos=door&&door.wall?rampPosFor(_it,door,B):null;if(!pos)continue;// orphaned ramp is dropped, as when its door is deleted
byId.set(_it.id,_objectSpread(_objectSpread({},_it),pos));}}catch(err){_iterator4.e(err);}finally{_iterator4.f();}return{items:items.map(function(it){return byId.get(it.id);}).filter(Boolean),events:events};}// Point on a note box's border in the direction of a target — where the note's
// leader (pointer) line starts, so the dashed line begins at the pill's edge
// instead of its center. If the target is inside the box, returns the target
// itself (degenerate line; callers skip drawing when start ≈ end).
function noteEdgePoint(cx,cy,w,h,tx,ty){var dx=tx-cx,dy=ty-cy;if(!dx&&!dy)return{x:cx,y:cy};var t=Math.min(dx?w/2/Math.abs(dx):Infinity,dy?h/2/Math.abs(dy):Infinity,1);return{x:cx+dx*t,y:cy+dy*t};}// Determine which positional wall (north/south/east/west) is the FRONT
// based on door placement. Double door wins over single door.
function getFrontWall(items){// Catalog fixture doors count as doors too; a double-leaf (built-in doubleDoor, or a
// fixture whose operation is "double") wins over a single, same as before.
var doubles=items.filter(function(i){return i.wall&&(i.type==="doubleDoor"||i.type==="fixtureDoor"&&i.operation==="double");});if(doubles.length>0)return doubles[0].wall;var singles=items.filter(function(i){return i.wall&&(i.type==="singleDoor"||i.type==="fixtureDoor");});if(singles.length>0)return singles[0].wall;return null;}// ── Fixtures catalog (Options → Doors) → placeable designer tools + rendering ──────
// Each active catalog door (from get_fixtures) becomes a palette tool keyed `fx:<id>`
// (wallOnly, carrying its own width in feet). Placing one creates a stable `fixtureDoor`
// item that SNAPSHOTS the door's spec (name/width/price/swing/operation) so a later catalog
// edit never changes a saved design. FIXTURE_DOOR_CFG is the render cfg for those placed
// items; `noPalette` keeps it out of the tool row (only the fx: tools are shown).
var FIXTURE_DOOR_COLOR="#D97706";// matches the built-in Single Door glyph
var FIXTURE_DOOR_COLOR_DOUBLE="#B45309";// matches the built-in Double Door glyph
// Amber like the built-in doors, darker for a double so it reads the same as doubleDoor.
function fixtureDoorColor(item){return item&&item.operation==="double"?FIXTURE_DOOR_COLOR_DOUBLE:FIXTURE_DOOR_COLOR;}var FIXTURE_DOOR_CFG={label:"Door",color:FIXTURE_DOOR_COLOR,wallOnly:true,width:3,height:0.5,shortLabel:"DOOR",noPalette:true,isFixtureDoor:true};// The single "Door" palette tool. Arming it and clicking a wall opens the door picker
// (below) instead of placing immediately — the shopper chooses WHICH door (and its swing/
// operation where more than one is offered) in the popup.
var DOOR_PICKER_CFG={label:"Door",color:FIXTURE_DOOR_COLOR,wallOnly:true,width:3,height:0.5,shortLabel:"DOOR",isDoorPicker:true};// Custom ramps (custom mode). The "Ramp" tool attaches to a door (doorSnap) and opens the ramp
// picker. A placed custom ramp is a normal type:"ramp" item — so it reuses ALL the existing ramp
// machinery (render, door-snap follow, delete-cascade, z-order) — but carries the chosen style's
// own width/length + a priced snapshot (vs the simple built-in ramp which takes the door's width).
var FIXTURE_RAMP_COLOR="#0284C7";var RAMP_PICKER_CFG={label:"Ramp",color:FIXTURE_RAMP_COLOR,icon:"⬛",doorSnap:true,width:3,height:2,shortLabel:"RAMP",isRampPicker:true};// Simple ramp — a fully self-contained option (render + placement), NO longer the built-in `ramp`
// layout item. Auto-widths to the door it attaches to (handled in handleClick's doorSnap branch,
// same as before). Stone color matches the old built-in so already-placed ramps look identical.
// ITEMS.ramp is ALWAYS this cfg (so every placed type:"ramp" renders), placeable only when the
// tenant offers a simple ramp (rampSettings.enabled + simple mode).
var SIMPLE_RAMP_CFG={label:"Ramp",color:"#78716C",icon:"⬛",doorSnap:true,width:3,height:3,shortLabel:"RAMP",isSimpleRamp:true};// Catalog windows. The "Window" tool is wall-placed (like the door picker). A placed catalog
// window is a normal type:"window" item — so it reuses the built-in window's render (mullions,
// wall bar), collision, and payload — but carries the chosen style's width + a priced snapshot
// (built-in windows have no fixtureItemId; that's how the two are told apart in pricing).
var FIXTURE_WINDOW_COLOR="#0EA5E9";var WINDOW_PICKER_CFG={label:"Window",color:FIXTURE_WINDOW_COLOR,icon:"🪟",wallOnly:true,width:2,height:0.5,shortLabel:"WIN",isWindowPicker:true};function fixtureInitialSwing(fx){if(fx.swingIn&&fx.swingOut)return fx.swingDefault||"in";if(fx.swingIn)return"in";if(fx.swingOut)return"out";return null;}function fixtureInitialOperation(fx){if(fx.opSlideUp)return"slideup";if(fx.opDouble)return"double";if(fx.opRight&&fx.opLeft)return fx.opDefault||"right";if(fx.opRight)return"right";if(fx.opLeft)return"left";return null;}function buildFixtureTools(fixtures){var out={};(Array.isArray(fixtures)?fixtures:[]).forEach(function(fx){if(!fx||fx.category&&fx.category!=="door")return;var wIn=Number(fx.widthIn)||36;out["fx:".concat(fx.id)]={label:fx.name||"Door",color:FIXTURE_DOOR_COLOR,icon:"🚪",wallOnly:true,width:wIn/12,height:0.5,shortLabel:(fx.name||"DOOR").toUpperCase().slice(0,10),fixture:fx};});return out;}// Swing/operation-aware door glyph. `out` combines the wall side (like the built-in
// singleDoor/doubleDoor) with the door's in/out swing (in = mirror of out). Hinge side
// comes from operation (right/left); "double" = two leaves; "slideup" = a segmented
// garage/roll-up panel with no arc.
function fixtureDoorOut(item){var outBase=item.wall==="north"||item.wall==="east";return item.swing==="in"?!outBase:outBase;}function fixtureDoorSVG(item,iw,color){var stroke=color+"60",op=item.operation,out=fixtureDoorOut(item);if(op==="slideup"){return/*#__PURE__*/React.createElement("g",null,[-iw/4,0,iw/4].map(function(lx,k){return/*#__PURE__*/React.createElement("line",{key:k,x1:lx,y1:-5,x2:lx,y2:5,stroke:"#FFF",strokeWidth:1.5});}));}if(op==="double"){var _r=iw*0.4,s=out?-1:1;return/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("path",{d:"M ".concat(-iw/2+_r," 0 A ").concat(_r," ").concat(_r," 0 0 ").concat(out?0:1," ").concat(-iw/2," ").concat(s*_r),fill:"none",stroke:stroke,strokeWidth:1.5,strokeDasharray:"4 3"}),/*#__PURE__*/React.createElement("path",{d:"M ".concat(iw/2-_r," 0 A ").concat(_r," ").concat(_r," 0 0 ").concat(out?1:0," ").concat(iw/2," ").concat(s*_r),fill:"none",stroke:stroke,strokeWidth:1.5,strokeDasharray:"4 3"}),/*#__PURE__*/React.createElement("line",{x1:0,y1:-5,x2:0,y2:5,stroke:"#FFF",strokeWidth:1.5}));}var r=iw*0.8,rightHinge=op==="right",ey=out?-r:r;var sx=rightHinge?iw/2-r:-iw/2+r,ex=rightHinge?iw/2:-iw/2;var sweep=rightHinge?out?1:0:out?0:1;return/*#__PURE__*/React.createElement("path",{d:"M ".concat(sx," 0 A ").concat(r," ").concat(r," 0 0 ").concat(sweep," ").concat(ex," ").concat(ey),fill:"none",stroke:stroke,strokeWidth:1.5,strokeDasharray:"4 3"});}function fixtureDoorCanvas(ctx,item,iw,color){var op=item.operation,out=fixtureDoorOut(item);if(op==="slideup"){ctx.strokeStyle="#FFF";ctx.lineWidth=1.5;[-iw/4,0,iw/4].forEach(function(lx){ctx.beginPath();ctx.moveTo(lx,-5);ctx.lineTo(lx,5);ctx.stroke();});return;}ctx.strokeStyle=color+"60";ctx.lineWidth=1.5;ctx.setLineDash([4,3]);if(op==="double"){var _r2=iw*0.4;ctx.beginPath();ctx.arc(-iw/2,0,_r2,0,out?-Math.PI/2:Math.PI/2,out);ctx.stroke();ctx.beginPath();ctx.arc(iw/2,0,_r2,Math.PI,out?3*Math.PI/2:Math.PI/2,!out);ctx.stroke();ctx.setLineDash([]);ctx.strokeStyle="#FFF";ctx.lineWidth=1.5;ctx.beginPath();ctx.moveTo(0,-5);ctx.lineTo(0,5);ctx.stroke();return;}var r=iw*0.8,rightHinge=op==="right";if(rightHinge){ctx.beginPath();ctx.arc(iw/2,0,r,Math.PI,out?3*Math.PI/2:Math.PI/2,!out);ctx.stroke();}else{ctx.beginPath();ctx.arc(-iw/2,0,r,0,out?-Math.PI/2:Math.PI/2,out);ctx.stroke();}ctx.setLineDash([]);}// Door sizes are stored in inches; show them as feet/inches on the plan + in the picker.
function fmtFtIn(inches){var n=Number(inches);if(!isFinite(n)||n<=0)return"";var ft=Math.floor(n/12),inch=Math.round((n-ft*12)*100)/100;if(ft===0)return inch+'"';if(inch===0)return ft+"'";return ft+"'"+inch+'"';}// Door placement picker. Doors are grouped by STYLE (exact name): one card per style; picking a
// style with more than one size reveals a size chooser, then swing/operation where more than one
// is offered, then place.
function DoorPicker(_ref3){var doors=_ref3.doors,showPricing=_ref3.showPricing,onCancel=_ref3.onCancel,onPlace=_ref3.onPlace;var styles=useMemo(function(){var m=new Map();doors.forEach(function(d){var k=d.name||"Door";if(!m.has(k))m.set(k,{name:k,imageUrl:d.imageUrl||null,sizes:[]});var g=m.get(k);g.sizes.push(d);if(!g.imageUrl&&d.imageUrl)g.imageUrl=d.imageUrl;});return _toConsumableArray(m.values());},[doors]);var _useState3=useState(styles.length===1?styles[0]:null),_useState4=_slicedToArray(_useState3,2),style=_useState4[0],setStyle=_useState4[1];var _useState5=useState(styles.length===1&&styles[0].sizes.length===1?styles[0].sizes[0]:null),_useState6=_slicedToArray(_useState5,2),sel=_useState6[0],setSel=_useState6[1];var _useState7=useState(null),_useState8=_slicedToArray(_useState7,2),swing=_useState8[0],setSwing=_useState8[1];var _useState9=useState(null),_useState10=_slicedToArray(_useState9,2),operation=_useState10[0],setOperation=_useState10[1];useEffect(function(){if(!sel){setSwing(null);setOperation(null);return;}setSwing(fixtureInitialSwing(sel));setOperation(fixtureInitialOperation(sel));},[sel]);var pickStyle=function pickStyle(st){setStyle(st);setSel(st.sizes.length===1?st.sizes[0]:null);};var swingOpts=sel?[sel.swingIn&&"in",sel.swingOut&&"out"].filter(Boolean):[];var opOpts=sel?[sel.opRight&&"right",sel.opLeft&&"left",sel.opDouble&&"double",sel.opSlideUp&&"slideup"].filter(Boolean):[];var OP_LABEL={right:"Right",left:"Left","double":"Double",slideup:"Slide up"};var money=function money(n){return"$"+Number(n).toLocaleString();};var chip=function chip(key,on,label,onClick){return/*#__PURE__*/React.createElement("div",{key:key,onClick:onClick,style:{padding:"6px 14px",borderRadius:20,fontSize:13,fontWeight:600,cursor:"pointer",border:"2px solid ".concat(on?FIXTURE_DOOR_COLOR:"#E2E8F0"),background:on?"#FEF3C7":"#FFF",color:on?"#92400E":"#334155"}},label);};return/*#__PURE__*/React.createElement("div",{onClick:onCancel,style:{position:"fixed",inset:0,background:"rgba(15,23,42,0.45)",zIndex:9000,display:"flex",alignItems:"center",justifyContent:"center",padding:16}},/*#__PURE__*/React.createElement("div",{onClick:function onClick(e){return e.stopPropagation();},style:{background:"#FFF",borderRadius:14,width:"min(560px, 96vw)",maxHeight:"88vh",overflow:"auto",padding:20,boxShadow:"0 20px 60px rgba(0,0,0,0.3)",fontFamily:"system-ui, -apple-system, sans-serif"}},/*#__PURE__*/React.createElement("div",{style:{fontSize:17,fontWeight:800,color:"#1E293B",marginBottom:4}},"Choose a door"),/*#__PURE__*/React.createElement("div",{style:{fontSize:13,color:"#64748B",marginBottom:14}},style&&style.sizes.length>1?"Pick a size.":"Pick a door to place on this wall."),/*#__PURE__*/React.createElement("div",{style:{display:"grid",gridTemplateColumns:"repeat(auto-fill, minmax(150px, 1fr))",gap:10,marginBottom:16}},styles.map(function(st){var on=style&&style.name===st.name;var one=st.sizes.length===1?st.sizes[0]:null;var sub=one?"".concat(fmtFtIn(one.widthIn)," \xD7 ").concat(fmtFtIn(one.heightIn)).concat(showPricing&&one.price!=null?" \xB7 ".concat(money(one.price)):""):"".concat(st.sizes.length," sizes");return/*#__PURE__*/React.createElement("div",{key:st.name,onClick:function onClick(){return pickStyle(st);},style:{border:"2px solid ".concat(on?FIXTURE_DOOR_COLOR:"#E2E8F0"),borderRadius:10,overflow:"hidden",cursor:"pointer",background:"#FFF"}},st.imageUrl?/*#__PURE__*/React.createElement("img",{src:st.imageUrl,alt:"",style:{width:"100%",height:90,objectFit:"cover",display:"block"}}):/*#__PURE__*/React.createElement("div",{style:{height:90,background:"#F1F5F9",display:"flex",alignItems:"center",justifyContent:"center",fontSize:26}},"\uD83D\uDEAA"),/*#__PURE__*/React.createElement("div",{style:{padding:"8px 10px"}},/*#__PURE__*/React.createElement("div",{style:{fontSize:13,fontWeight:700,color:"#1E293B"}},st.name),/*#__PURE__*/React.createElement("div",{style:{fontSize:11.5,color:"#64748B"}},sub)));})),style&&style.sizes.length>1&&/*#__PURE__*/React.createElement("div",{style:{marginBottom:14}},/*#__PURE__*/React.createElement("div",{style:{fontSize:12,fontWeight:700,color:"#334155",marginBottom:6}},"Size"),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:8,flexWrap:"wrap"}},style.sizes.map(function(d){return chip(d.id,sel&&sel.id===d.id,"".concat(fmtFtIn(d.widthIn)," \xD7 ").concat(fmtFtIn(d.heightIn)).concat(showPricing&&d.price!=null?" \xB7 ".concat(money(d.price)):""),function(){return setSel(d);});}))),sel&&swingOpts.length>1&&/*#__PURE__*/React.createElement("div",{style:{marginBottom:12}},/*#__PURE__*/React.createElement("div",{style:{fontSize:12,fontWeight:700,color:"#334155",marginBottom:6}},"Swing"),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:8}},swingOpts.map(function(o){return chip(o,swing===o,o==="in"?"In-swing":"Out-swing",function(){return setSwing(o);});}))),sel&&opOpts.length>1&&/*#__PURE__*/React.createElement("div",{style:{marginBottom:14}},/*#__PURE__*/React.createElement("div",{style:{fontSize:12,fontWeight:700,color:"#334155",marginBottom:6}},"Operation"),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:8,flexWrap:"wrap"}},opOpts.map(function(o){return chip(o,operation===o,OP_LABEL[o],function(){return setOperation(o);});}))),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:8,justifyContent:"flex-end",marginTop:4}},/*#__PURE__*/React.createElement("button",{onClick:onCancel,style:{padding:"9px 16px",borderRadius:8,border:"1px solid #CBD5E1",background:"#FFF",color:"#334155",fontWeight:600,cursor:"pointer"}},"Cancel"),/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return sel&&onPlace(sel,swing,operation);},disabled:!sel,style:{padding:"9px 18px",borderRadius:8,border:"none",background:sel?FIXTURE_DOOR_COLOR:"#CBD5E1",color:"#FFF",fontWeight:700,cursor:sel?"pointer":"default"}},"Place door"))));}// Map a positional wall to a display label (FRONT/BACK/LEFT/RIGHT)
// based on which wall is currently FRONT. Returns null if no front set.
//
// VIEWPOINT: a customer standing OUTSIDE the building, in front of the doors, looking
// at them. LEFT/RIGHT are that person's left and right — what they'd say pointing at
// the real building — NOT what a plan reader sees on screen. For a north- or
// south-facing front those two are mirror images of each other, which is exactly what
// makes this easy to get wrong.
//
// Facing the front wall, the customer's left is the direction they face rotated 90°
// counter-clockwise on the compass:
//
//   front   customer faces   LEFT    RIGHT
//   north   south            east    west
//   south   north            west    east
//   east    west             south   north
//   west    east             north   south
//
// (Plan orientation, per getWallFromClick: north = top of the canvas, south = bottom,
// west = left, east = right.)
//
// FIXED 2026-07-26 (reported by Junior Barns): the north and south rows were inverted.
// They described an INSIDE-looking-out viewpoint while east/west already used
// outside-looking-in, so a door on the north or south wall reported its sides
// backwards while an end-wall door read correctly — an inconsistency, not a uniform
// flip. This function is the single source of truth for the on-screen labels, the
// exported PNG/PDF, and the `wall` field in the submit payload, so it drives the
// emailed estimate too. Check any edit against the table above, not against a
// screenshot of the plan.
function getDisplayLabel(positionalWall,frontWall){if(!frontWall||!positionalWall)return null;var map={north:{north:"FRONT",south:"BACK",east:"LEFT",west:"RIGHT"},south:{south:"FRONT",north:"BACK",west:"LEFT",east:"RIGHT"},east:{east:"FRONT",west:"BACK",south:"LEFT",north:"RIGHT"},west:{west:"FRONT",east:"BACK",north:"LEFT",south:"RIGHT"}};return map[frontWall][positionalWall];}// How to name a wall in a sentence to the customer. getDisplayLabel is relative to the
// FRONT, and there is no front until a door is placed — it returns null, which rendered as
// "moved to the null wall". Naming a direction we cannot actually derive would be worse
// than not naming one, so say "another wall" until a door defines the front.
function wallPhrase(positionalWall,frontWall){var d=getDisplayLabel(positionalWall,frontWall);return d?"the "+d.toLowerCase()+" wall":"another wall";}// A ramp sits centered on the OUTSIDE of its door, so its placement is fully
// derived from the door's position/wall + the ramp's depth. One source of
// truth used by ramp placement AND by the 2D/3D door drags, so ramps follow
// their door wherever it goes.
function rampPlacementForDoor(door,rampDepthFt,pW,pH,mgX,mgY,scale){var dPx=(rampDepthFt||2)*scale;switch(door.wall){case"north":return{x:door.x,y:mgY-dPx/2,rotation:0,wall:"north"};case"south":return{x:door.x,y:mgY+pH+dPx/2,rotation:0,wall:"south"};case"west":return{x:mgX-dPx/2,y:door.y,rotation:90,wall:"west"};case"east":return{x:mgX+pW+dPx/2,y:door.y,rotation:90,wall:"east"};default:return null;}}// ─── Layout add-on pricing (browser mirror of submit-estimate's pushItem) ─────────
// Compute one display row per priceable placed item type, applying the SAME 7
// pricing_method formulas the edge function uses so the prices shown on the plan match
// the emailed estimate to the penny. Needs C.layoutPricing ({key:{rate,method,byStyle}})
// and C.sizePricing ({styleKey:{sizeLabel:{basePrice,widthFt,lengthFt}}}) — both are
// present only when the tenant's show_pricing is on (else {} → returns no rows).
var LAYOUT_PRICE_ORDER=["singleDoor","doubleDoor","window","workbench","loft","ramp"];function normSizeLabel(s){return String(s||"").toLowerCase().replace(/[×✕]/g,"x").replace(/\s+/g,"");}function fmtMoney2(n){var v=Number(n)||0;var s="$"+Math.abs(v).toLocaleString("en-US",{minimumFractionDigits:2,maximumFractionDigits:2});return v<0?"−"+s:s;}// Building / Paint Colors / Roof summary for the "Details" section, in the SAME order they appear
// on the GHL estimate (Building, Paint, Roof). Prices use the same sizePricing + color rate/method
// the estimate uses (total is null when show_pricing is off). The Roof row shows only when the
// tenant offers roof colors (some color flagged shingle/metal).
// Origin allowlist for the postMessage prefill/config listeners — same-origin,
// the structurestudio.app family, and localhost (dev). Without it, a malicious
// page that frames or window.opens the designer can inject selections/contact or
// remount the app with an attacker-controlled config. If a tenant ever embeds the
// designer from its own domain via postMessage, add that origin here.
function ssAllowedOrigin(origin){try{if(!origin||origin==="null")return false;if(origin===window.location.origin)return true;var h=new URL(origin).hostname;return h==="structurestudiosuite.com"||h.endsWith(".structurestudiosuite.com")||h==="structurestudio.app"||h.endsWith(".structurestudio.app")||h==="localhost"||h==="127.0.0.1";}catch(_unused){return false;}}function computeSelectionRows(sel,paintColors,C,items){var styleKey=sel&&sel.style;var showP=!!(C&&C.showPricing);var colors=Array.isArray(C&&C.colors)?C.colors:[];var szMap=C&&C.sizePricing&&styleKey?C.sizePricing[styleKey]:null;var szRow=null;if(szMap&&sel&&sel.size){szRow=szMap[sel.size];if(!szRow){var want=normSizeLabel(sel.size);for(var k in szMap){if(normSizeLabel(k)===want){szRow=szMap[k];break;}}}}var bW=szRow&&szRow.widthFt!=null?Number(szRow.widthFt):0;var bL=szRow&&szRow.lengthFt!=null?Number(szRow.lengthFt):0;var buildingArea=bW*bL,buildingPerimeter=2*(bW+bL);var buildingPrice=szRow&&szRow.basePrice!=null?Number(szRow.basePrice):0;var charge=function charge(c){if(!c)return 0;var rate=Number(c.rate)||0;if(rate<=0)return 0;switch(c.pricingMethod||"each"){case"sqft_building":return rate*buildingArea;case"perimeter_building":return rate*buildingPerimeter;case"pct_building_price":return rate/100*buildingPrice;case"pct_estimate_total":return 0;default:return rate;}};var pick=function pick(label,pred){var v=String(label||"").trim();if(!v)return null;var list=colors.filter(pred);return list.find(function(c){return c.label===v;})||list.find(function(c){return c.allowCustom;})||null;};var styleLabel=((C&&C.buildingStyles||[]).find(function(s){return s.value===styleKey;})||{}).label||styleKey||"";var rows=[];// Declined included items are itemized UNDER the building line (one per line), same as the GHL
// estimate — the building line's bold label is just the style + size, and the gray detail lists
// the original price + each declined item; the credits reduce the building total.
var layoutPricing=C&&C.layoutPricing||{};var resolveLp=function resolveLp(key){var lp=layoutPricing[key];if(!lp)return null;var ov=lp.byStyle&&styleKey?lp.byStyle[styleKey]:null;return{rate:Number(ov&&ov.rate!=null?ov.rate:lp.rate)||0,method:ov&&ov.method||lp.method||"each"};};var stEntry=(C&&C.buildingStyles||[]).find(function(s){return s.value===styleKey;});var pickSize=function pickSize(map){if(!map||_typeof(map)!=="object"||!(sel&&sel.size))return null;if(map[sel.size]!=null)return map[sel.size];var want=normSizeLabel(sel.size);for(var _k in map){if(normSizeLabel(_k)===want)return map[_k];}return null;};var rawQ=stEntry?pickSize(stEntry.sizeInclusionQty):null;var qmap=rawQ&&_typeof(rawQ)==="object"&&!Array.isArray(rawQ)?rawQ:null;var legacyArr=!qmap&&stEntry?pickSize(stEntry.sizeInclusions):null;var includedNow={};if(qmap)includedNow=qmap;else if(Array.isArray(legacyArr)){var _iterator6=_createForOfIteratorHelper(legacyArr),_step6;try{for(_iterator6.s();!(_step6=_iterator6.n()).done;){var _k2=_step6.value;includedNow[_k2]=1;}}catch(err){_iterator6.e(err);}finally{_iterator6.f();}}var declinedKeys=sel&&Array.isArray(sel.declinedItems)?sel.declinedItems:[];var declinedLines=[];var declinedTotal=0;if(sel&&sel.size){var _iterator7=_createForOfIteratorHelper(declinedKeys),_step7;try{var _loop=function _loop(){var k=_step7.value;if(includedNow[k]==null)return 0;// continue
// Catalog fixture inclusion (key = fixture id): credit its snapshot price × the included qty.
var fxDecl=(Array.isArray(C.fixtures)?C.fixtures:[]).find(function(f){return String(f.id)===k;});if(fxDecl){var q0=Math.max(1,Number(includedNow[k])||1);var credit0=Math.round((fxDecl.price!=null?Number(fxDecl.price):0)*q0*100)/100;if(credit0<=0)return 0;// continue
declinedLines.push("".concat(fxDecl.name||"Item"," declined (\u2212").concat(fmtMoney2(credit0),")"));declinedTotal+=credit0;return 0;// continue
}var rp=resolveLp(k);if(!rp||!(rp.rate>0))return 0;// continue
var q=rp.method==="pct_estimate_total"?1:Math.max(1,Number(includedNow[k])||1);var unitValue=rp.rate;switch(rp.method){case"sqft_building":unitValue=rp.rate*buildingArea;break;case"perimeter_building":unitValue=rp.rate*buildingPerimeter;break;case"pct_building_price":unitValue=rp.rate/100*buildingPrice;break;default:break;}unitValue=Math.round(unitValue*100)/100;var credit=Math.round(unitValue*q*100)/100;if(credit<=0)return 0;// continue
var label=C.layoutItems&&C.layoutItems[k]&&C.layoutItems[k].label||LEGACY_LAYOUT_FALLBACK[k]&&LEGACY_LAYOUT_FALLBACK[k].label||k;declinedLines.push("".concat(label," declined (\u2212").concat(fmtMoney2(credit),")"));declinedTotal+=credit;},_ret;for(_iterator7.s();!(_step7=_iterator7.n()).done;){_ret=_loop();if(_ret===0)continue;}}catch(err){_iterator7.e(err);}finally{_iterator7.f();}}// Under-placed included area items (loft, and any sqft_option inclusion): a smaller placed
// area than the included amount credits the shortfall (mirrors submit-estimate's pushItem,
// which credits sqft_option under-placement). Only when actually placed and not declined — a
// fully-absent include is handled by the decline flow, not auto-credited. Kept method-scoped
// to sqft_option so it stays in lock-step with the edge (lineal_ft is NOT under-credited).
if(sel&&sel.size&&Array.isArray(items)){var _loop2=function _loop2(_k3){if(declinedKeys.includes(_k3))return 0;// continue
var rpk=resolveLp(_k3);if(!rpk||rpk.method!=="sqft_option"||!(rpk.rate>0))return 0;// continue
var incQ=Number(includedNow[_k3])||0;if(incQ<=0)return 0;// continue
var placedSqft=Math.round(items.filter(function(i){return i.type===_k3;}).reduce(function(s,i){return s+(Number(i.widthFt)||0)*(Number(i.heightFt)||0);},0));if(placedSqft>0&&placedSqft<incQ){var credit=Math.round(rpk.rate*(incQ-placedSqft)*100)/100;if(credit>0){var lbl=C.layoutItems&&C.layoutItems[_k3]&&C.layoutItems[_k3].label||LEGACY_LAYOUT_FALLBACK[_k3]&&LEGACY_LAYOUT_FALLBACK[_k3].label||_k3;declinedLines.push("".concat(lbl," smaller than included: ").concat(incQ-placedSqft," sq ft credited (\u2212").concat(fmtMoney2(credit),")"));declinedTotal+=credit;}}},_ret2;for(var _k3 in includedNow){_ret2=_loop2(_k3);if(_ret2===0)continue;}}declinedTotal=Math.round(declinedTotal*100)/100;var styleSize=[styleLabel,sel&&sel.size].filter(Boolean).join(" ")||"—";var buildingDetail=declinedLines.length?["Original building price: ".concat(fmtMoney2(buildingPrice))].concat(declinedLines).join("\n"):"";rows.push({key:"building",label:styleSize,detail:buildingDetail,total:showP?Math.max(0,buildingPrice-declinedTotal):null});var painted=sel&&sel.paint==="Painted";var pDetail="Unpainted",pTotal=0;if(painted){var body=pick(paintColors&&paintColors.body,function(c){return c.siding;});var trim=pick(paintColors&&paintColors.trim,function(c){return c.trim;});var seen={};[body,trim].forEach(function(c){if(c&&c.id&&!seen[c.id]){seen[c.id]=1;pTotal+=charge(c);}});pDetail="Body: ".concat(paintColors&&paintColors.body||"TBD",", Trim: ").concat(paintColors&&paintColors.trim||"TBD");}rows.push({key:"paint",label:"Paint Colors",detail:pDetail,total:showP?pTotal:null});var offersRoof=colors.some(function(c){return c.shingle||c.metal;});if(offersRoof){var rt=sel&&sel.roofType||"";var rDetail="No roof selected",rTotal=0;if(rt){var rc=pick(sel&&sel.roofColor,function(c){return rt==="Metal"?c.metal:c.shingle;});rTotal=charge(rc);rDetail=sel&&sel.roofColor?"".concat(rt," \u2014 ").concat(sel.roofColor):"".concat(rt," \u2014 (color TBD)");}rows.push({key:"roof",label:"Roof",detail:rDetail,total:showP?rTotal:null});}return rows;}// Only allow a design's image_url to be used as a clickable href when it is an
// https Supabase-storage (or same-origin) URL. image_url is stored verbatim by the
// anon-granted save_design RPC, so a hostile caller could stash a javascript: or
// off-site phishing URL; gate it before it reaches an <a href>. Returns null if unsafe. (audit #F8)
function ssSafeUrl(u){try{var url=new URL(u,window.location.origin);if(url.protocol!=="https:")return null;var h=url.hostname;return h===window.location.hostname||h.endsWith(".supabase.co")?u:null;}catch(_unused2){return null;}}function computeLayoutPricingRows(items,sel,customOptions,C,paintColors){if(!C||!C.showPricing||!C.layoutPricing)return{rows:[]};var pricing=C.layoutPricing;var styleKey=sel&&sel.style;// Resolve rate + method for an item_key: a per-style override wins over the default,
// matching submit-estimate's layoutRates precedence.
var resolve=function resolve(key){var lp=pricing[key];if(!lp)return null;var ov=lp.byStyle&&styleKey?lp.byStyle[styleKey]:null;return{rate:Number(ov&&ov.rate!=null?ov.rate:lp.rate)||0,method:ov&&ov.method||lp.method||"each"};};// Building geometry + base price for the building-dependent methods, from the selected
// size (0 when the size isn't priced/matched — same $0 the estimate would show).
var szMap=C.sizePricing&&styleKey?C.sizePricing[styleKey]:null;var szRow=null;if(szMap&&sel&&sel.size){szRow=szMap[sel.size];if(!szRow){var want=normSizeLabel(sel.size);for(var k in szMap){if(normSizeLabel(k)===want){szRow=szMap[k];break;}}}}var bW=szRow&&szRow.widthFt!=null?Number(szRow.widthFt):0;var bL=szRow&&szRow.lengthFt!=null?Number(szRow.lengthFt):0;var buildingArea=bW*bL;var buildingPerimeter=2*(bW+bL);var buildingPrice=szRow&&szRow.basePrice!=null?Number(szRow.basePrice):0;// Roll placed items into counts + per-measure quantities. Ramps split two ways: CUSTOM ramps
// (a catalog style with a snapshot price) price like fixture doors; SIMPLE ramps (the built-in
// ramp) price from the tenant's single ramp price when set, else the legacy layout "ramp" rate.
var rampSettings=C.rampSettings||null;var rampSimplePriced=!!(rampSettings&&rampSettings.price!=null);var singleDoors=0,doubleDoors=0,builtinWindows=0,lofts=0,loftSqft=0;var workbenchFt=[];var customRamps=[],simpleRamps=[];var customWindows=[];var _iterator8=_createForOfIteratorHelper(items),_step8;try{for(_iterator8.s();!(_step8=_iterator8.n()).done;){var _it3=_step8.value;if(_it3.type==="singleDoor")singleDoors++;else if(_it3.type==="doubleDoor")doubleDoors++;// Catalog windows (own snapshot price) price below like fixture doors; built-in windows
// (no fixtureItemId) keep pricing via the layout "window" rate.
else if(_it3.type==="window"){if(_it3.fixtureItemId&&_it3.price!=null)customWindows.push(_it3);else builtinWindows++;}else if(_it3.type==="workbench")workbenchFt.push(Number(_it3.widthFt)||0);else if(_it3.type==="loft"){lofts++;loftSqft+=(Number(_it3.widthFt)||0)*(Number(_it3.heightFt)||0);}else if(_it3.type==="ramp"){if(_it3.fixtureItemId&&_it3.price!=null)customRamps.push(_it3);else simpleRamps.push(_it3);}}}catch(err){_iterator8.e(err);}finally{_iterator8.f();}loftSqft=Math.round(loftSqft);var totalWorkbenchFt=workbenchFt.reduce(function(s,f){return s+f;},0);var measures={singleDoor:{count:singleDoors},doubleDoor:{count:doubleDoors},window:{count:builtinWindows},workbench:{count:workbenchFt.length,lengthFt:totalWorkbenchFt},loft:{count:lofts,optionSqft:loftSqft},// Legacy layout "ramp" row applies only to simple ramps that AREN'T priced by the new ramp
// settings — otherwise ramps price below (custom by snapshot, simple by ramp settings).
ramp:{count:rampSimplePriced?0:simpleRamps.length}};var lineFor=function lineFor(rate,method,m){var count=m.count||0;switch(method){case"lineal_ft":return{qty:m.lengthFt!=null?m.lengthFt:count,total:rate*(m.lengthFt!=null?m.lengthFt:count),unit:fmtMoney2(rate)+" / ft"};case"sqft_option":return{qty:m.optionSqft!=null?m.optionSqft:count,total:rate*(m.optionSqft!=null?m.optionSqft:count),unit:fmtMoney2(rate)+" / sq ft"};case"sqft_building":return{qty:count,total:rate*buildingArea*count,unit:fmtMoney2(rate)+" / sq ft of building"};case"perimeter_building":return{qty:count,total:rate*buildingPerimeter*count,unit:fmtMoney2(rate)+" / ft of perimeter"};case"pct_building_price":return{qty:count,total:rate/100*buildingPrice*count,unit:rate+"% of building price"};case"pct_estimate_total":return{qty:count,total:null,pct:rate,unit:rate+"% of subtotal"};case"each":default:return{qty:count,total:rate*count,unit:fmtMoney2(rate)+" each"};}};// Included quantities for this style+size (part of the base price → not re-charged; only the
// amount placed BEYOND the inclusion is charged, matching submit-estimate's pushItem).
var incForRows=function(){var st=(C.buildingStyles||[]).find(function(s){return s.value===styleKey;});if(!st||!sel||!sel.size)return{};var pick=function pick(map){if(!map||_typeof(map)!=="object")return null;if(map[sel.size]!=null)return map[sel.size];var want=normSizeLabel(sel.size);for(var _k4 in map){if(normSizeLabel(_k4)===want)return map[_k4];}return null;};var q=pick(st.sizeInclusionQty);if(q&&_typeof(q)==="object"&&!Array.isArray(q)){var _o={};for(var _k5 in q)_o[_k5]=Math.max(1,Number(q[_k5])||1);return _o;}var arr=pick(st.sizeInclusions);var o={};if(Array.isArray(arr)){var _iterator9=_createForOfIteratorHelper(arr),_step9;try{for(_iterator9.s();!(_step9=_iterator9.n()).done;){var _k6=_step9.value;o[_k6]=1;}}catch(err){_iterator9.e(err);}finally{_iterator9.f();}}return o;}();var rows=[];var deferred=[];var nonPctSubtotal=0;var _iterator10=_createForOfIteratorHelper(LAYOUT_PRICE_ORDER),_step10;try{for(_iterator10.s();!(_step10=_iterator10.n()).done;){var key=_step10.value;var m=measures[key];if(!m||!m.count)continue;var rp=resolve(key);if(!rp)continue;var label=C.layoutItems&&C.layoutItems[key]&&C.layoutItems[key].label||LEGACY_LAYOUT_FALLBACK[key]&&LEGACY_LAYOUT_FALLBACK[key].label||key;// Net out the included quantity for this item (loft = sq ft, others = count).
var _inc3=incForRows[key]||0;var placedMeasure=rp.method==="lineal_ft"?m.lengthFt||0:rp.method==="sqft_option"?m.optionSqft||0:m.count||0;var _chargeable3=Math.max(0,placedMeasure-_inc3);if(_inc3>0&&_chargeable3<=0){rows.push({key:key,label:label+" (included)",qty:placedMeasure,unit:"included",total:0,method:rp.method});continue;}var mNet=m;if(_inc3>0)mNet=rp.method==="lineal_ft"?_objectSpread(_objectSpread({},m),{},{lengthFt:_chargeable3}):rp.method==="sqft_option"?_objectSpread(_objectSpread({},m),{},{optionSqft:_chargeable3}):_objectSpread(_objectSpread({},m),{},{count:_chargeable3});var ln=lineFor(rp.rate,rp.method,mNet);// Measured inclusions (loft = sq ft, workbench = ft): show the TOTAL placed measure as the
// row quantity so it reads accurately, but keep charging only the excess beyond the included
// amount. "each" items (doors/windows) keep the netted count.
var measured=rp.method==="lineal_ft"||rp.method==="sqft_option";var dispQty=measured&&_inc3>0?placedMeasure:ln.qty;// Measured item with an inclusion (loft/workbench): spell out the full calc — placed, included,
// billable — WORD-FOR-WORD the same as the estimate's loft line, so the two match.
var unit=void 0;if(measured&&_inc3>0){var u2=rp.method==="sqft_option"?"sq ft":"ft";unit=["".concat(placedMeasure," ").concat(u2," placed"),"".concat(_inc3," ").concat(u2," included in base price"),"".concat(_chargeable3," ").concat(u2," billable @ ").concat(fmtMoney2(rp.rate),"/").concat(u2)].join(" · ");}else{unit=ln.unit;}var row={key:key,label:label,qty:dispQty,unit:unit,total:ln.total,method:rp.method};rows.push(row);if(ln.total==null)deferred.push({row:row,pct:ln.pct});else nonPctSubtotal+=ln.total;}// Catalog fixture doors (Options → Doors): each carries its OWN snapshotted price, not a
// per-key rate — so they price separately from the layout items above. Identical doors
// (same name + price) collapse into one line with a qty. Feeds the % base like any add-on.
// Grouped by fixture id so a size-inclusion nets the first N free (incForRows[fixtureId] = the
// qty the base price covers). Fully-included shows "(included)"; extras beyond it are charged.
}catch(err){_iterator10.e(err);}finally{_iterator10.f();}var fxGroups={};var _iterator11=_createForOfIteratorHelper(items),_step11;try{for(_iterator11.s();!(_step11=_iterator11.n()).done;){var _it4=_step11.value;if(_it4.type!=="fixtureDoor")continue;var _price2=_it4.price!=null?Number(_it4.price):0;var _fid5=_it4.fixtureItemId||"".concat(_it4.doorName||"Door","|").concat(_price2);if(!fxGroups[_fid5])fxGroups[_fid5]={label:_it4.doorName||"Door",price:_price2,qty:0,fid:_it4.fixtureItemId||null};fxGroups[_fid5].qty++;}}catch(err){_iterator11.e(err);}finally{_iterator11.f();}for(var fid in fxGroups){var g=fxGroups[fid];var inc=g.fid&&incForRows[g.fid]?Number(incForRows[g.fid]):0;var chargeable=Math.max(0,g.qty-inc);if(g.price>0&&inc>0&&chargeable<=0){rows.push({key:"fx:".concat(fid),label:g.label+" (included)",qty:g.qty,unit:"included",total:0,method:"each"});continue;}if(!(g.price>0))continue;// $0 / unpriced = free, no line
var total=Math.round(g.price*chargeable*100)/100;rows.push({key:"fx:".concat(fid),label:g.label,qty:chargeable,unit:fmtMoney2(g.price)+" each"+(inc>0?" \xB7 ".concat(inc," included"):""),total:total,method:"each"});nonPctSubtotal+=total;}// Catalog windows (Options → Windows): each carries its OWN snapshot price, grouped by style
// like doors. Built-in windows already priced above via the layout "window" rate.
var winGroups={};for(var _i2=0,_customWindows=customWindows;_i2<_customWindows.length;_i2++){var it=_customWindows[_i2];var price=it.price!=null?Number(it.price):0;var _fid=it.fixtureItemId||"".concat(it.windowName||"Window","|").concat(price);if(!winGroups[_fid])winGroups[_fid]={label:it.windowName||"Window",price:price,qty:0,fid:it.fixtureItemId||null};winGroups[_fid].qty++;}for(var _fid2 in winGroups){var _g=winGroups[_fid2];var _inc=_g.fid&&incForRows[_g.fid]?Number(incForRows[_g.fid]):0;var _chargeable=Math.max(0,_g.qty-_inc);if(_g.price>0&&_inc>0&&_chargeable<=0){rows.push({key:"win:".concat(_fid2),label:_g.label+" (included)",qty:_g.qty,unit:"included",total:0,method:"each"});continue;}if(!(_g.price>0))continue;// $0 / unpriced = free, no line
var _total=Math.round(_g.price*_chargeable*100)/100;rows.push({key:"win:".concat(_fid2),label:_g.label,qty:_chargeable,unit:fmtMoney2(_g.price)+" each"+(_inc>0?" \xB7 ".concat(_inc," included"):""),total:_total,method:"each"});nonPctSubtotal+=_total;}// Catalog ramps (Options → Ramps). Custom ramps carry their own snapshot price (grouped by
// style like doors); simple ramps price from the tenant's single ramp price — "each" per ramp,
// or "per_ft" × the attached door's width. Both feed the % base like any add-on.
var rampGroups={};for(var _i3=0,_customRamps=customRamps;_i3<_customRamps.length;_i3++){var _it2=_customRamps[_i3];var _price=_it2.price!=null?Number(_it2.price):0;var _fid3=_it2.fixtureItemId||"".concat(_it2.rampName||"Ramp","|").concat(_price);if(!rampGroups[_fid3])rampGroups[_fid3]={label:_it2.rampName||"Ramp",price:_price,qty:0,fid:_it2.fixtureItemId||null};rampGroups[_fid3].qty++;}for(var _fid4 in rampGroups){var _g2=rampGroups[_fid4];var _inc2=_g2.fid&&incForRows[_g2.fid]?Number(incForRows[_g2.fid]):0;var _chargeable2=Math.max(0,_g2.qty-_inc2);if(_g2.price>0&&_inc2>0&&_chargeable2<=0){rows.push({key:"ramp:".concat(_fid4),label:_g2.label+" (included)",qty:_g2.qty,unit:"included",total:0,method:"each"});continue;}if(!(_g2.price>0))continue;// $0 / unpriced = free, no line
var _total2=Math.round(_g2.price*_chargeable2*100)/100;rows.push({key:"ramp:".concat(_fid4),label:_g2.label,qty:_chargeable2,unit:fmtMoney2(_g2.price)+" each"+(_inc2>0?" \xB7 ".concat(_inc2," included"):""),total:_total2,method:"each"});nonPctSubtotal+=_total2;}if(rampSimplePriced&&simpleRamps.length){var rampPrice=Number(rampSettings.price)||0;var perFt=rampSettings.method==="per_ft";if(rampPrice>0){if(perFt){// Price per foot of the attached door's width. fixture doors carry their real width
// (widthIn); built-in doors fall back to the ramp's stored widthFt.
var totalFt=0;var _iterator12=_createForOfIteratorHelper(simpleRamps),_step12;try{var _loop3=function _loop3(){var r=_step12.value;var door=items.find(function(d){return d.id===r.snapDoorId;});var dw=Number(r.widthFt)||0;if(door&&door.type==="fixtureDoor"&&door.widthIn)dw=Number(door.widthIn)/12;totalFt+=dw;};for(_iterator12.s();!(_step12=_iterator12.n()).done;){_loop3();}}catch(err){_iterator12.e(err);}finally{_iterator12.f();}totalFt=Math.round(totalFt*100)/100;if(totalFt>0){var _total3=Math.round(rampPrice*totalFt*100)/100;rows.push({key:"ramp:simple",label:"Ramp",qty:totalFt,unit:fmtMoney2(rampPrice)+" / ft",total:_total3,method:"lineal_ft"});nonPctSubtotal+=_total3;}}else{var _total4=Math.round(rampPrice*simpleRamps.length*100)/100;rows.push({key:"ramp:simple",label:"Ramp",qty:simpleRamps.length,unit:fmtMoney2(rampPrice)+" each",total:_total4,method:"each"});nonPctSubtotal+=_total4;}}}// Resolve pct_estimate_total rows LAST against the same base the edge function uses:
// building (NET of declined-item credits — submit-estimate bakes them into the
// building line BEFORE the % pass) + paint/roof + all non-% add-ons + rough
// openings + custom options (delivery excluded, matching submit-estimate).
if(deferred.length){var roRate=(resolve("roughOpening")||{rate:0}).rate;var roCount=items.filter(function(i){return i.type==="roughOpening";}).length;var customTotal=(customOptions||[]).reduce(function(s,co){if(!co||!co.name||!String(co.name).trim())return s;var amt=parseFloat(co.amount)||0;var q=co.qty?Math.abs(parseInt(co.qty,10))||1:1;// abs: the edge bills |qty|
// Only POSITIVE custom options are line items in the % base; negatives are
// credits applied outside it, matching submit-estimate.
return s+Math.max(0,amt)*q;},0);// Paint + roof color charges are line items too, so the % base must include
// them exactly as submit-estimate does — otherwise the previewed % line is
// lower than the emailed estimate.
var selRowsForBase=computeSelectionRows(sel,paintColors,C,items);var selectionTaxable=selRowsForBase.filter(function(r){return r.key==="paint"||r.key==="roof";}).reduce(function(s,r){return s+(Number(r.total)||0);},0);var buildingRow=selRowsForBase.find(function(r){return r.key==="building";});var netBuilding=buildingRow&&buildingRow.total!=null?Number(buildingRow.total):buildingPrice;var base=netBuilding+selectionTaxable+nonPctSubtotal+roRate*roCount+customTotal;var _iterator13=_createForOfIteratorHelper(deferred),_step13;try{for(_iterator13.s();!(_step13=_iterator13.n()).done;){var d=_step13.value;d.row.total=d.pct/100*base*(d.row.qty||1);}// ×count: the server bills GHL line = qty×amount, so the preview must scale by count too or it under-shows (audit #F1)
}catch(err){_iterator13.e(err);}finally{_iterator13.f();}}// (Declined included items are no longer shown here — they're itemized under the building line
// by computeSelectionRows, matching the GHL estimate.)
return{rows:rows};}// Which placed items does a Details "Options on your plan" row cover? Built-in rows key by
// the item TYPE, but catalog rows carry synthetic keys — fx:/win:/ramp:<fixture id, or the
// "name|price" fallback> and "ramp:simple" — that match no item's type, and the layout
// "window"/"ramp" rows mean only the BUILT-IN subset (catalog ones price on their own rows).
// The row's × button and pick-one-to-remove mode both need this mapping; matching on
// `i.type === key` made the × a silent no-op for every catalog row — the customer could not
// remove a charged item from Details and might submit believing they had (audit 2026-08-20).
// Mirrors the grouping in computeLayoutPricingRows above — keep the two in sync.
function priceRowMatcher(key){var isCatalog=function isCatalog(i){return!!i.fixtureItemId&&i.price!=null;};// same test the grouping uses
if(key==="window")return function(i){return i.type==="window"&&!isCatalog(i);};if(key==="ramp"||key==="ramp:simple")return function(i){return i.type==="ramp"&&!isCatalog(i);};var sep=key.indexOf(":");if(sep<0)return function(i){return i.type===key;};var kind=key.slice(0,sep),fid=key.slice(sep+1);// Group id exactly as the row was built: the fixture id when the snapshot carries one,
// else the name|price fallback (String(): the row key came from template interpolation).
var gid=function gid(i,name){return String(i.fixtureItemId||"".concat(name,"|").concat(i.price!=null?Number(i.price):0));};if(kind==="fx")return function(i){return i.type==="fixtureDoor"&&gid(i,i.doorName||"Door")===fid;};if(kind==="win")return function(i){return i.type==="window"&&isCatalog(i)&&gid(i,i.windowName||"Window")===fid;};if(kind==="ramp")return function(i){return i.type==="ramp"&&isCatalog(i)&&gid(i,i.rampName||"Ramp")===fid;};return function(){return false;};}var idCounter=1;// 10-char short code in format SS-XXXXXXXXXX. Alphabet drops 0/O/I/1 to avoid
// look-alikes when read aloud or shared. 32^10 ≈ 2^50 combinations — the code is
// the capability for loading/saving a design via the RPCs, so it must not be
// guessable. (Legacy 6-char codes from before the RPC data path still load fine.)
var _SHORT_ALPHA="ABCDEFGHJKLMNPQRSTUVWXYZ23456789";function genShortCode(){var s="";for(var i=0;i<10;i++)s+=_SHORT_ALPHA[Math.floor(Math.random()*_SHORT_ALPHA.length)];return"SS-".concat(s);}// Pick readable text for a tenant-accent background: dark slate on light accents
// (mint, yellow), white on dark ones (navy, barn red). WCAG relative luminance,
// hex-only — the codebase already assumes hex accents (see the `${accent}50` shadows).
function textOnAccent(hex){var m=/^#?([0-9a-f]{6})$/i.exec(String(hex||"").trim());if(!m)return"#FFFFFF";var _map=[0,2,4].map(function(i){var c=parseInt(m[1].slice(i,i+2),16)/255;return c<=0.03928?c/12.92:Math.pow((c+0.055)/1.055,2.4);}),_map2=_slicedToArray(_map,3),r=_map2[0],g=_map2[1],b=_map2[2];return 0.2126*r+0.7152*g+0.0722*b>0.4?"#1E293B":"#FFFFFF";}// Progressive US phone formatter: "8163003600" -> "(816) 300-3600".
// Caps at 10 digits; partial inputs format as "(816", "(816) 30", etc.
// Display only — strip back to digits before sending to GHL.
function formatPhoneDisplay(v){var d=(v||"").replace(/\D/g,"").slice(0,10);if(d.length===0)return"";if(d.length<=3)return"(".concat(d);if(d.length<=6)return"(".concat(d.slice(0,3),") ").concat(d.slice(3));return"(".concat(d.slice(0,3),") ").concat(d.slice(3,6),"-").concat(d.slice(6));}// Lazy-load Google Maps JS API via the official inline bootstrap loader. Resolves
// to window.google. Rejects if no key. Idempotent: returns the same promise on
// subsequent calls.
//
// The bootstrap snippet (from Google's docs) synchronously installs
// window.google.maps.importLibrary, then defers the actual script download until
// importLibrary is first called. This is the only supported way to reach
// PlaceAutocompleteElement and other Places API (New) entry points; passing
// loading=async or libraries=places in the URL is NOT enough to expose
// importLibrary in practice.
// See: https://developers.google.com/maps/documentation/javascript/load-maps-js-api
var _googleMapsLoadPromise=null;function loadGoogleMapsPlaces(apiKey){if(typeof window==="undefined")return Promise.reject(new Error("no window"));if(!apiKey)return Promise.reject(new Error("Google Maps API key not configured"));if(window.google&&window.google.maps&&typeof window.google.maps.importLibrary==="function"){return Promise.resolve(window.google);}if(_googleMapsLoadPromise)return _googleMapsLoadPromise;_googleMapsLoadPromise=new Promise(function(resolve,reject){try{(function(g){var h,a,k,p="The Google Maps JavaScript API",c="google",l="importLibrary",q="__ib__",m=document,b=window;b=b[c]||(b[c]={});var d=b.maps||(b.maps={}),r=new Set(),e=new URLSearchParams(),u=function u(){return h||(h=new Promise(/*#__PURE__*/function(){var _ref4=_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee(f,n){var _m$querySelector;return _regeneratorRuntime().wrap(function _callee$(_context){while(1)switch(_context.prev=_context.next){case 0:_context.next=2;return a=m.createElement("script");case 2:e.set("libraries",_toConsumableArray(r)+"");for(k in g)e.set(k.replace(/[A-Z]/g,function(t){return"_"+t[0].toLowerCase();}),g[k]);e.set("callback",c+".maps."+q);a.src="https://maps.".concat(c,"apis.com/maps/api/js?")+e;d[q]=f;a.onerror=function(){return h=n(Error(p+" could not load."));};a.nonce=((_m$querySelector=m.querySelector("script[nonce]"))===null||_m$querySelector===void 0?void 0:_m$querySelector.nonce)||"";m.head.append(a);case 10:case"end":return _context.stop();}},_callee);}));return function(_x,_x2){return _ref4.apply(this,arguments);};}()));};d[l]?console.warn(p+" only loads once. Ignoring:",g):d[l]=function(f){for(var _len=arguments.length,n=new Array(_len>1?_len-1:0),_key=1;_key<_len;_key++){n[_key-1]=arguments[_key];}return r.add(f)&&u().then(function(){return d[l].apply(d,[f].concat(n));});};})({key:apiKey,v:"weekly"});resolve(window.google);}catch(err){_googleMapsLoadPromise=null;reject(err);}});return _googleMapsLoadPromise;}// ─── Per-option building-style scoping ───
// An option may declare `buildingStyles: ["Urban", "Northwood"]` to limit when
// it's shown. Without that field (or with an empty array) the option always
// applies. Unrestricted options also show before any style is picked; scoped
// options hide until the user picks a style they target.
function isOptionApplicable(opt,styleValue){if(!opt||!Array.isArray(opt.buildingStyles)||opt.buildingStyles.length===0)return true;return!!styleValue&&opt.buildingStyles.includes(styleValue);}// ─── 3D VIEW ENGINE ───
// Parametric 3D: every mesh below is computed from the same layout/catalog state
// the 2D plan renders from (bldgW/bldgH in feet, items in page coordinates, style
// + paint selections). No model assets, no 3D SaaS — Three.js (MIT) is lazy-loaded
// as a browser-native ES module only when the customer opens the 3D view, so the
// initial page load pays nothing. See STRUCTURESTUDIO_3D_PLAN.md.
var THREE_VERSION="0.167.0";// Browser-native dynamic import via Function indirection. The import() KEYWORD
// can't appear in this shared component body: Babel-standalone (index.html)
// rewrites it to an async require() wrapper that explodes at call time, and
// bundlers try to resolve the URL at build time. Constructing the import inside
// a Function keeps it invisible to both — the browser runs the real thing.
// (index.html already needs eval for Babel itself, so no new CSP constraint.)
function nativeImport(u){return new Function("u","return import(u)")(u);}var _threeLoadPromise=null;function loadThree(){if(_threeLoadPromise)return _threeLoadPromise;var base="https://esm.sh/three@".concat(THREE_VERSION);_threeLoadPromise=Promise.all([nativeImport("".concat(base)),nativeImport("".concat(base,"/examples/jsm/controls/OrbitControls.js"))]).then(function(_ref5){var _ref6=_slicedToArray(_ref5,2),THREE=_ref6[0],oc=_ref6[1];return{THREE:THREE,OrbitControls:oc.OrbitControls};})["catch"](function(err){_threeLoadPromise=null;throw err;});return _threeLoadPromise;}// ─── Building scan (094): read a phone LiDAR export, measure it, keep the parametric model ───
// A scan is a REFERENCE, never the customer-facing model. The file is one fused shell of
// triangles with no idea which part is a wall, so it cannot be edited, priced or quoted. What
// it IS good for is measurements — LiDAR output is metric — and, later, appearance. So we
// measure it and drive the parametric building from the numbers.
//
// Loaded separately from loadThree so a shopper opening the 3D view never pays for it. The
// esm.sh specifier MUST interpolate the same fully-pinned THREE_VERSION: the subpath build
// imports three by a RELATIVE path inside its own version directory, so an exact match is what
// guarantees ONE three instance. A looser range (three@0.167 resolves to 0.167.1) or
// ?external=three (emits a bare "three" specifier these no-build pages cannot resolve) both
// break instanceof in ways that surface as nonsense errors.
var _gltfLoadPromise=null;function loadGLTFLoader(){if(_gltfLoadPromise)return _gltfLoadPromise;var base="https://esm.sh/three@".concat(THREE_VERSION);_gltfLoadPromise=loadThree().then(function(bundle){return nativeImport("".concat(base,"/examples/jsm/loaders/GLTFLoader.js")).then(function(m){return{THREE:bundle.THREE,GLTFLoader:m.GLTFLoader};});})["catch"](function(err){_gltfLoadPromise=null;throw err;});return _gltfLoadPromise;}var SCAN_MAX_BYTES=60*1024*1024;// matches the models bucket cap in 094
var M_TO_FT=1/0.3048;// Cheap gate BEFORE any WebGL or geometry decode: the GLB container is a 12-byte header then
// length-prefixed chunks, and the JSON chunk alone (a few hundred KB of a 40 MB file) answers
// every question worth refusing on. Runs on any phone — no three.js, no GPU.
function scanInspectGlb(buf){var dv=new DataView(buf);if(buf.byteLength<20)return{err:"That file is too small to be a scan."};if(dv.getUint32(0,true)!==0x46546C67)return{err:"That is not a .glb file — its header does not say glTF. Re-export as GLB."};var version=dv.getUint32(4,true);if(version!==2)return{err:"That scan is glTF version ".concat(version,"; this needs version 2. Re-export as GLB 2.0.")};var off=12,json=null;while(off+8<=buf.byteLength){var len=dv.getUint32(off,true),type=dv.getUint32(off+4,true);var start=off+8;if(start+len>buf.byteLength)break;if(type===0x4E4F534A){json=new TextDecoder().decode(new Uint8Array(buf,start,len));break;}off=start+len+(4-len%4)%4;}if(!json)return{err:"That scan has no readable glTF data."};var doc;try{doc=JSON.parse(json);}catch(_e){return{err:"That scan's glTF data is malformed."};}// Compression extensions need decoder modules we deliberately do not ship. Saying which
// export setting to change beats a stack trace from inside the loader.
var needed=[].concat(doc.extensionsRequired||[],doc.extensionsUsed||[]);var hard=["KHR_draco_mesh_compression","EXT_meshopt_compression","KHR_texture_basisu"];var hit=hard.find(function(x){return needed.indexOf(x)!==-1;});if(hit)return{err:"That scan uses ".concat(hit,", which needs a decoder we don't load. Re-export it without compression (Draco/meshopt) or texture compression.")};// A GLB may legally point at external files. One that does would fire cross-origin fetches
// and arrive half-empty, so refuse it rather than measure a partial building.
var ext=[].concat((doc.buffers||[]).map(function(b){return b&&b.uri;}),(doc.images||[]).map(function(i){return i&&i.uri;})).filter(function(u){return typeof u==="string"&&!/^data:/i.test(u);});if(ext.length)return{err:"That export references separate files. Re-export as a single self-contained GLB."};return{doc:doc};}// Sample points ON THE SURFACE, area-weighted, not at the vertices. This is the correction
// that makes the whole thing work: occupancy and density are surface properties, and a scan
// exporter is free to emit a few huge triangles, so iterating POSITION would put four points
// on a whole wall and every histogram below would read noise. Triangle interiors sampled in
// proportion to area give a density that means something regardless of tessellation.
function scanSamplePoints(THREE,root,budget){root.updateMatrixWorld(true);var meshes=[];root.traverse(function(o){if(o.isMesh&&o.geometry&&o.geometry.getAttribute("position"))meshes.push(o);});if(!meshes.length)return null;var tri=[],areas=[];var a=new THREE.Vector3(),b=new THREE.Vector3(),c=new THREE.Vector3();var ab=new THREE.Vector3(),ac=new THREE.Vector3(),cr=new THREE.Vector3();var total=0;for(var _i4=0,_meshes=meshes;_i4<_meshes.length;_i4++){var m=_meshes[_i4];var pos=m.geometry.getAttribute("position");var idx=m.geometry.getIndex();var n=idx?idx.count:pos.count;for(var i=0;i+2<n;i+=3){var i0=idx?idx.getX(i):i,i1=idx?idx.getX(i+1):i+1,i2=idx?idx.getX(i+2):i+2;a.fromBufferAttribute(pos,i0).applyMatrix4(m.matrixWorld);b.fromBufferAttribute(pos,i1).applyMatrix4(m.matrixWorld);c.fromBufferAttribute(pos,i2).applyMatrix4(m.matrixWorld);ab.subVectors(b,a);ac.subVectors(c,a);var area=cr.crossVectors(ab,ac).length()*0.5;if(!(area>0))continue;tri.push(a.x,a.y,a.z,b.x,b.y,b.z,c.x,c.y,c.z);areas.push(area);total+=area;}}if(!tri.length||!(total>0))return null;var want=Math.max(2000,Math.min(budget||120000,200000));var pts=new Float64Array(want*3);var k=0;for(var t=0;t<areas.length&&k<want;t++){// Proportional allocation, with every triangle guaranteed at least one point so a small
// but meaningful face (a gable end) is never sampled away entirely.
var take=Math.max(1,Math.round(areas[t]/total*want));var o=t*9;for(var s=0;s<take&&k<want;s++){var u=Math.random(),v=Math.random();if(u+v>1){u=1-u;v=1-v;}// fold into the triangle
var w=1-u-v;pts[k*3]=tri[o]*w+tri[o+3]*u+tri[o+6]*v;pts[k*3+1]=tri[o+1]*w+tri[o+4]*u+tri[o+7]*v;pts[k*3+2]=tri[o+2]*w+tri[o+5]*u+tri[o+8]*v;k++;}}return{pts:pts,count:k,triangles:areas.length,area:total};}// Turn sampled surface points into the handful of numbers the parametric model needs, all in
// METRES until the single conversion at the end. The sequence matters: find the ground before
// any height, because bbox.min.y is always some artefact (a curb, drift, a dark hole) and
// using it puts every height 10-40 cm out.
function scanMeasure(sample){var pts=sample.pts,count=sample.count;if(!count)return{err:"That scan has no geometry to measure."};var minY=Infinity,maxY=-Infinity;for(var i=0;i<count;i++){var y=pts[i*3+1];if(y<minY)minY=y;if(y>maxY)maxY=y;}var span=maxY-minY;if(!(span>0.5))return{err:"That scan is too flat to be a building."};// GROUND = the densest horizontal slab in the lower part of the scan. In any yard capture
// that is the ground, and it is far more reliable than the lowest point.
var BIN=0.02,lowCut=minY+span*0.4;var bins=new Map();for(var _i5=0;_i5<count;_i5++){var _y=pts[_i5*3+1];if(_y>lowCut)continue;var k=Math.floor((_y-minY)/BIN);bins.set(k,(bins.get(k)||0)+1);}// Best 3-bin WINDOW, not single bin: a real lawn undulates a few centimetres,
// spreading the ground across neighbouring bins; a flat synthetic ground
// dominates any window with one bin, so fixtures measure identically.
var bestBin=-1,bestN=0;bins.forEach(function(n,k){var w3=n+(bins.get(k-1)||0)+(bins.get(k+1)||0);if(w3>bestN){bestN=w3;bestBin=k;}});var ground=bestBin>=0?minY+(bestBin+0.5)*BIN:minY;// WAIST CUT kills most junk for free: ground, grass, driveway, low clutter, the operator's
// feet. What is left is the building (plus anything tall next to it, which is why the
// builder confirms the numbers rather than us trusting them).
var waist=ground+1.2;var wall=[];for(var _i6=0;_i6<count;_i6++){var _y2=pts[_i6*3+1];if(_y2>=waist)wall.push(pts[_i6*3],_y2,pts[_i6*3+2]);}var wn=wall.length/3;if(wn<50)return{err:"That scan does not have enough of a building above ground level to measure."};var warnFlags=[];// COMPONENT ISOLATION: a fence, parked truck or tree clears the waist cut
// but does not touch the building. Rasterise the waist-up points onto a
// 15 cm plan grid, flood-fill 8-connected occupied cells, keep the biggest
// island. If that island holds under 60% of the points the scan is probably
// sparse/hole-riddled rather than cluttered — measure everything and warn,
// instead of silently measuring a fragment.
{var CELL=0.15;var mnx=Infinity,mnz=Infinity,mxx=-Infinity,mxz=-Infinity;for(var _i7=0;_i7<wn;_i7++){var x=wall[_i7*3],z=wall[_i7*3+2];if(x<mnx)mnx=x;if(x>mxx)mxx=x;if(z<mnz)mnz=z;if(z>mxz)mxz=z;}var gw=Math.max(1,Math.min(4096,Math.ceil((mxx-mnx)/CELL)+1));var gh=Math.max(1,Math.min(4096,Math.ceil((mxz-mnz)/CELL)+1));if(gw*gh<=2000000){var counts=new Int32Array(gw*gh);var cellIdx=new Int32Array(wn);for(var _i8=0;_i8<wn;_i8++){var cx=Math.min(gw-1,Math.max(0,Math.floor((wall[_i8*3]-mnx)/CELL)));var cz=Math.min(gh-1,Math.max(0,Math.floor((wall[_i8*3+2]-mnz)/CELL)));var idx=cz*gw+cx;cellIdx[_i8]=idx;counts[idx]++;}var label=new Int32Array(gw*gh);// 0 = unvisited/empty
var occupied=function occupied(idx){return counts[idx]>=3;};var nextLabel=0;var labelPts=[];// points per label (1-based)
var stack=[];for(var seed=0;seed<gw*gh;seed++){if(label[seed]||!occupied(seed))continue;nextLabel++;var acc=0;stack.length=0;stack.push(seed);label[seed]=nextLabel;while(stack.length){var c=stack.pop();acc+=counts[c];var _cx=c%gw,_cz=c/gw|0;for(var dz=-1;dz<=1;dz++)for(var dx=-1;dx<=1;dx++){if(!dx&&!dz)continue;var nx=_cx+dx,nz=_cz+dz;if(nx<0||nz<0||nx>=gw||nz>=gh)continue;var nIdx=nz*gw+nx;if(!label[nIdx]&&occupied(nIdx)){label[nIdx]=nextLabel;stack.push(nIdx);}}}labelPts[nextLabel]=acc;}var bestLabel=0,bestPts=0;for(var l=1;l<=nextLabel;l++){if(labelPts[l]>bestPts){bestPts=labelPts[l];bestLabel=l;}}if(bestLabel&&bestPts>=wn*0.6){var keep=[];for(var _i9=0;_i9<wn;_i9++){if(label[cellIdx[_i9]]===bestLabel)keep.push(wall[_i9*3],wall[_i9*3+1],wall[_i9*3+2]);}if(keep.length>=150){wall=keep;wn=keep.length/3;}}else if(bestLabel&&bestPts<wn*0.6){warnFlags.push("scattered");}}}// ORIENTED footprint. A phone's X/Z axes are wherever the AR session happened to start, so a
// building at an angle to them has an axis-aligned box up to 41% too big in BOTH directions.
// Sweep the yaw and keep the angle whose rectangle is smallest.
var bestA=0,bestArea=Infinity,bestW=0,bestD=0;for(var deg=0;deg<90;deg+=1){var t=deg*Math.PI/180,_cs=Math.cos(t),_sn=Math.sin(t);var lo1=Infinity,hi1=-Infinity,lo2=Infinity,hi2=-Infinity;for(var _i10=0;_i10<wn;_i10++){var _x3=wall[_i10*3],_z=wall[_i10*3+2];var u=_x3*_cs+_z*_sn,v=-_x3*_sn+_z*_cs;if(u<lo1)lo1=u;if(u>hi1)hi1=u;if(v<lo2)lo2=v;if(v>hi2)hi2=v;}var w=hi1-lo1,d=hi2-lo2,area=w*d;if(area<bestArea){bestArea=area;bestA=t;bestW=w;bestD=d;}}// EAVE vs PEAK from a SPAN profile in the building's own axes. Span, not cross-sectional
// area: a scan is a hollow shell full of holes (windows, the doorway, drop-out where
// tracking failed), so any area/flood-fill measure collapses at a random height. Walls hold
// their span; a roof narrows. The axis that collapses names the ridge, which gives the roof
// type for free.
var cs=Math.cos(bestA),sn=Math.sin(bestA);var peak=maxY;var BAND=0.05;var nb=Math.max(1,Math.ceil((peak-waist)/BAND));// PERCENTILE band spans (P98-P2), not min/max: doubled shells, stray tracking
// points and speckle otherwise widen every band, and the widened bands are
// exactly what the wall/eave/overhang separations below have to read.
var cnt=new Int32Array(nb);var bandOf=new Int32Array(wn);for(var _i11=0;_i11<wn;_i11++){var bi=Math.min(nb-1,Math.max(0,Math.floor((wall[_i11*3+1]-waist)/BAND)));bandOf[_i11]=bi;cnt[bi]++;}var bu=new Array(nb),bv=new Array(nb),fill=new Int32Array(nb);for(var b=0;b<nb;b++){if(cnt[b]){bu[b]=new Float64Array(cnt[b]);bv[b]=new Float64Array(cnt[b]);}}for(var _i12=0;_i12<wn;_i12++){var _b=bandOf[_i12];var _x4=wall[_i12*3],_z2=wall[_i12*3+2];bu[_b][fill[_b]]=_x4*cs+_z2*sn;bv[_b][fill[_b]]=-_x4*sn+_z2*cs;fill[_b]++;}var su=new Float64Array(nb),sv=new Float64Array(nb);var suWide=new Float64Array(nb),svWide=new Float64Array(nb);var uMid=new Float64Array(nb),vMid=new Float64Array(nb);for(var _b2=0;_b2<nb;_b2++){if(cnt[_b2]<5)continue;bu[_b2].sort();bv[_b2].sort();var p2=Math.floor(0.02*(cnt[_b2]-1)),p98=Math.ceil(0.98*(cnt[_b2]-1));su[_b2]=bu[_b2][p98]-bu[_b2][p2];uMid[_b2]=(bu[_b2][p98]+bu[_b2][p2])/2;sv[_b2]=bv[_b2][p98]-bv[_b2][p2];vMid[_b2]=(bv[_b2][p98]+bv[_b2][p2])/2;// A wider read (P99.9) for the OVERHANG bands only: the roof's outer rim
// is by construction the extreme of its band, and the 2% trim shaved a
// real 6 cm off it. Everything structural stays on the trimmed spans.
var p01=Math.floor(0.001*(cnt[_b2]-1)),p999=Math.ceil(0.999*(cnt[_b2]-1));suWide[_b2]=bu[_b2][p999]-bu[_b2][p01];svWide[_b2]=bv[_b2][p999]-bv[_b2][p01];}// WALL FOOTPRINT from the lowest wall bands, NOT the all-points sweep box:
// on any real building the roof overhangs the walls, so the sweep rectangle
// is the ROOF footprint - width and depth came out 2x the overhang too big,
// and the customer's building grew a size. The waist sits at 1.2m, safely
// under the overhang shadow of any wall a person can stand next to.
var lowBands=[];for(var _b3=0;_b3<nb&&lowBands.length<6;_b3++){if(cnt[_b3]>=5)lowBands.push(_b3);}if(!lowBands.length)return{err:"That scan does not have enough of a building above ground level to measure."};var med=function med(arr){var s=arr.slice().sort(function(a,q){return a-q;});return s[s.length-1>>1];};var wallU=med(lowBands.map(function(b){return su[b];}));var wallV=med(lowBands.map(function(b){return sv[b];}));// WALL TOP band walk with an UPPER bound too: near the eave the overhanging
// roof edge widens the band past the wall span, and the old ">= 92% only"
// rule happily called those roof bands "wall".
var wallTopBand=lowBands[0];for(var _b4=0;_b4<nb;_b4++){if(cnt[_b4]<5)continue;if(su[_b4]>=wallU*0.92&&su[_b4]<=wallU*1.06&&sv[_b4]>=wallV*0.92&&sv[_b4]<=wallV*1.06)wallTopBand=_b4;}var eaveY=waist+(wallTopBand+1)*BAND;// band-walk estimate; refined below
// Ridge axis: the axis that KEEPS its span above the wall top holds the ridge.
var uTop=0,vTop=0,tn=0;for(var _b5=wallTopBand+1;_b5<nb;_b5++){if(cnt[_b5]<5)continue;uTop+=su[_b5];vTop+=sv[_b5];tn++;}if(tn){uTop/=tn;vTop/=tn;}var ridgeAlongU=uTop>=vTop;var wallAcross=ridgeAlongU?wallV:wallU;var wallAlong=ridgeAlongU?wallU:wallV;// Roof bands: everything clearly above the wall top.
var roofY=[],roofS=[],roofAlong=[],roofMid=[];for(var _b6=wallTopBand+2;_b6<nb;_b6++){if(cnt[_b6]<5)continue;var sp=ridgeAlongU?sv[_b6]:su[_b6];if(!(sp>0))continue;roofY.push(waist+(_b6+0.5)*BAND);roofS.push(sp);roofAlong.push(ridgeAlongU?su[_b6]:sv[_b6]);roofMid.push(ridgeAlongU?vMid[_b6]:uMid[_b6]);}var lsFit=function lsFit(xs,ys,i0,i1){// least squares over [i0, i1)
var n=0,sx=0,sy=0,sxx=0,sxy=0;for(var _i13=i0;_i13<i1;_i13++){n++;sx+=xs[_i13];sy+=ys[_i13];sxx+=xs[_i13]*xs[_i13];sxy+=xs[_i13]*ys[_i13];}var denom=n*sxx-sx*sx;if(n<2||Math.abs(denom)<1e-9)return null;var slope=(n*sxy-sx*sy)/denom;var icept=(sy-slope*sx)/n;var sse=0;for(var _i14=i0;_i14<i1;_i14++){var e=ys[_i14]-(icept+slope*xs[_i14]);sse+=e*e;}return{slope:slope,icept:icept,sse:sse,n:n};};// PITCH from the roof's TAPER (see the fixture story in git history: a
// threshold eave biased the pitch a whole 1/12 step), and the TRUE EAVE from
// where the fitted roof line crosses the wall span - the roof plane passes
// through the wall-top corner whether or not it overhangs, so this crossing
// is overhang-immune where any band threshold is not.
var rise0=Math.max(0,peak-eaveY);var pitch=wallAcross>0.2?rise0/(wallAcross/2):0;var roofFit=lsFit(roofY,roofS,0,roofS.length);if(roofFit&&roofFit.slope<-1e-6&&roofFit.n>=3){pitch=-2/roofFit.slope;var cross=(wallAcross-roofFit.icept)/roofFit.slope;if(cross>waist&&cross<peak)eaveY=cross;}// OVERHANG. Across the ridge: bands just BELOW the eave hold the roof's
// overhanging edge, wider than the wall span - the widest of them names the
// edge. Along the ridge: a gable roof clears the end walls by the overhang
// at every roof height, so the mean along-span excess reads it directly.
var overhangM=0;{var maxBelow=wallAcross;for(var _b7=0;_b7<nb;_b7++){if(cnt[_b7]<5)continue;var _y3=waist+(_b7+0.5)*BAND;if(_y3<eaveY-0.7||_y3>eaveY)continue;var _sp=ridgeAlongU?svWide[_b7]:suWide[_b7];if(_sp>maxBelow)maxBelow=_sp;}var ovAcross=Math.max(0,(maxBelow-wallAcross)/2);var ovAlong=0;var alongN=0,alongSum=0;for(var _b8=wallTopBand+2;_b8<nb;_b8++){if(cnt[_b8]<5)continue;alongSum+=ridgeAlongU?suWide[_b8]:svWide[_b8];alongN++;}if(alongN)ovAlong=Math.max(0,(alongSum/alongN-wallAlong)/2);overhangM=(ovAcross+ovAlong)/2;if(Math.abs(ovAcross-ovAlong)>0.15)warnFlags.push("overhang");}var eave=Math.max(0.3,eaveY-ground);// gambrel refines eaveY below and re-derives this
var peakH=peak-ground;var rise=Math.max(0,peak-eaveY);// Classification. Midpoint drift separates what the old "both axes taper"
// proxy conflated: a symmetric gable's across-midpoint holds still, a
// saltbox's drifts toward the ridge, a mono-pitch shed's drifts at half the
// taper rate (the "ridge" IS one eave). A two-segment fit whose upper leg
// sheds span markedly faster than the lower is a gambrel - the farmland
// barn profile the one-line fit used to average into a wrong gable.
var roofType="gable";var ridgeOffset=0;var gambrel=null;if(rise<0.15){roofType="shed";pitch=0.25;}else if(roofS.length>=6){// Gambrel: best two-segment split vs the single line.
var best2=null;for(var _k7=3;_k7<=roofS.length-3;_k7++){var lo=lsFit(roofY,roofS,0,_k7);var hi=lsFit(roofY,roofS,_k7,roofS.length);if(!lo||!hi)continue;var sse=lo.sse+hi.sse;if(!best2||sse<best2.sse)best2={sse:sse,lo:lo,hi:hi};}var drift=lsFit(roofY,roofMid,0,roofMid.length);var halfTaper=roofFit?-roofFit.slope/2:0;var r=drift&&halfTaper>1e-6?drift.slope/halfTaper:0;if(best2&&roofFit&&best2.lo.slope<-1e-6&&best2.hi.slope<-1e-6&&best2.sse<roofFit.sse*0.5&&Math.abs(best2.hi.slope)>=Math.abs(best2.lo.slope)*1.7){roofType="gambrel";var kneeY=(best2.hi.icept-best2.lo.icept)/(best2.lo.slope-best2.hi.slope);var kneeSpan=best2.lo.icept+best2.lo.slope*kneeY;var crossG=(wallAcross-best2.lo.icept)/best2.lo.slope;var eaveG=crossG>waist&&crossG<peak?crossG:eaveY;var half=wallAcross/2;gambrel={kneeU:Math.max(0,Math.min(1,kneeSpan/wallAcross)),kneeRise:Math.max(0,Math.min(1,(kneeY-eaveG)/half)),ridgeRise:Math.max(0,Math.min(1.5,(peak-eaveG)/half))};eaveY=eaveG;eave=Math.max(0.3,eaveY-ground);// the single-line crossing sat above the true gambrel eave
pitch=-2/best2.lo.slope;}else if(Math.abs(r)>0.8){roofType="shed";pitch=wallAcross>0.2?rise/wallAcross:0.25;// shed rise spans the WHOLE across span
}else if(Math.abs(r)>0.16&&drift){var driftTotal=drift.slope*rise;ridgeOffset=Math.max(-0.35,Math.min(0.35,driftTotal/wallAcross));if(Math.abs(ridgeOffset)<0.05)ridgeOffset=0;}// Both axes tapering hard above the eave reads as a hip roof - we can't
// build one, so say so instead of silently calling it something else.
if(roofType==="gable"&&roofAlong.length>=3){var alongFit=lsFit(roofY,roofAlong,0,roofAlong.length);if(alongFit&&alongFit.slope<-0.5&&roofFit&&alongFit.slope<roofFit.slope*0.5)warnFlags.push("hip");}}// Plan-view centre of the WALL rectangle (the calibration turntable frames
// the scan with it): mid of the wall-band extents, rotated back to world.
var centerX=0,centerZ=0;{var luo=Infinity,huo=-Infinity,lvo=Infinity,hvo=-Infinity;lowBands.forEach(function(b){var p2=Math.floor(0.02*(cnt[b]-1)),p98=Math.ceil(0.98*(cnt[b]-1));if(bu[b][p2]<luo)luo=bu[b][p2];if(bu[b][p98]>huo)huo=bu[b][p98];if(bv[b][p2]<lvo)lvo=bv[b][p2];if(bv[b][p98]>hvo)hvo=bv[b][p98];});var cu=(luo+huo)/2,cvv=(lvo+hvo)/2;centerX=cu*cs-cvv*sn;centerZ=cu*sn+cvv*cs;}return{widthM:Math.max(wallU,wallV),depthM:Math.min(wallU,wallV),eaveM:eave,peakM:peakH,pitch:pitch,roofType:roofType,overhangM:overhangM,ridgeOffset:ridgeOffset,gambrel:gambrel,headingDeg:bestA*180/Math.PI,groundY:ground,sampled:count,centerX:centerX,centerZ:centerZ,warnFlags:warnFlags};}// Metres to the numbers a builder reads and the spec stores, rounded where rounding helps:
// footprint to the foot (then a caller can snap to a size they actually sell), wall height to
// the half foot, pitch to 0.01 rise-over-run. Pitch is NOT snapped to a 1/12 step: a real
// roof is rarely exactly n:12, and rounding a measured 0.62 up to 0.667 (8:12) visibly
// changes the building. The operator sees the measured value and can type an exact one. The plausibility gate catches a unit error, an unscaled photogrammetry
// export, or a failed isolation — all of which otherwise produce a confident wrong building.
function scanToFeet(m){var ft=function ft(v){return v*M_TO_FT;};var out={widthFt:Math.round(ft(m.widthM)),depthFt:Math.round(ft(m.depthM)),eaveFt:Math.round(ft(m.eaveM)*2)/2,peakFt:Math.round(ft(m.peakM)*2)/2,pitch:Math.max(0,Math.round(m.pitch*100)/100),roofType:m.roofType,overhangFt:Math.max(0,Math.min(3,Math.round(ft(m.overhangM||0)*10)/10)),ridgeOffset:m.ridgeOffset?Math.round(m.ridgeOffset*100)/100:0,gambrel:m.gambrel?{kneeU:Math.round(m.gambrel.kneeU*100)/100,kneeRise:Math.round(m.gambrel.kneeRise*100)/100,ridgeRise:Math.round(m.gambrel.ridgeRise*100)/100}:null,headingDeg:Math.round(m.headingDeg),sampled:m.sampled};var bad=[];if(out.widthFt<5||out.widthFt>100)bad.push("width ".concat(out.widthFt," ft"));if(out.depthFt<4||out.depthFt>100)bad.push("depth ".concat(out.depthFt," ft"));if(out.eaveFt<4||out.eaveFt>20)bad.push("wall height ".concat(out.eaveFt," ft"));if(out.peakFt<out.eaveFt)bad.push("a peak below the wall");var flagText={scattered:"the scan is scattered — its pieces don't connect, so clutter may be included",overhang:"the roof overhang reads differently along vs across the building — check it",hip:"the roof narrows on all four sides (hip roof?) — StructureStudio builds gable/gambrel/shed roofs"};var flags=(m.warnFlags||[]).map(function(f){return flagText[f]||f;});out.warn=bad.length?"These measurements look wrong (".concat(bad.join(", "),") \u2014 the scan may include the ground or something next to the building, or may not be to scale. Check them before saving."):flags.length?"Heads up: ".concat(flags.join("; "),"."):null;return out;}// Vertical dimensions the layout data model doesn't store yet (plan §6). Until
// wall_height_ft lives in building_sizes and opening heights live on items,
// every style renders with these defaults (feet):
var D3={WALL_H:8,// wall plate height
DOOR_H:6.5,// door opening height
RO_H:6.5,// rough-opening height
WINDOW_H:3,// window opening height
WINDOW_SILL:3.5,// window sill elevation
LOFT_ELEV:5.5,// loft platform top elevation
LOFT_T:0.35,// loft platform thickness
BENCH_H:3,// workbench top height
WALL_T:0.3,// wall thickness
FLOOR_T:0.35,// floor slab thickness (slab top = y 0)
ROOF_T:0.2,// roof panel thickness
OVERHANG:0.6// roof overhang past the walls
};// Built-in 3D appearance per building style, keyed by lowercased style value
// (plan §4.2). Each spec: roof { type: shed|gable|gambrel, pitch (rise/run),
// ridgeOffset (gable ridge shifted toward one eave — saltbox looks), overhang,
// gambrel knee/ridge fractions of the half-span }, siding ("batten" adds
// board-and-batten relief strips), colors (unpainted naturals for body/trim/
// roof). A tenant can override or define ANY style's appearance from its
// config row — buildingStyles[].d3 = { roof, siding, colors, wallHeightFt } —
// no code change needed (see d3ResolveStyleSpec).
var D3_STYLE_DEFAULTS={econo:{roof:{type:"shed",pitch:0.25,overhang:0.35},siding:null,colors:{}},urban:{roof:{type:"gable",pitch:0.33,overhang:0.85},siding:null,colors:{body:"#D6CCB6",trim:"#575044",roof:"#3E434A"}},northwood:{roof:{type:"gable",pitch:0.55,overhang:0.6},siding:"batten",colors:{body:"#C7B183",roof:"#4E5560"}},farmland:{roof:{type:"gambrel",kneeU:0.55,kneeRise:0.55,ridgeRise:0.8,overhang:0.5},siding:"batten",colors:{body:"#C2A377",trim:"#8A6F4D",roof:"#5D5348"}}};var D3_DEFAULT_ROOF={type:"gable",pitch:0.4};// ── CLADDING ──────────────────────────────────────────────────────────────────
// The customer-facing exterior material. Carolyn's direction (2026-08-18): call it
// CLADDING, not siding, and offer three genuinely different products rather than one
// texture rotated:
//   lap     horizontal boards, each course overlapping the one below (~6in exposure)
//   panel   vertical 4ft x 8ft sheets with grooves cut INTO the face (T1-11 / EWGWG)
//   agpanel metal sheets whose ribs stand OUT, overlapping at the rib (Panel-Loc Plus)
// `batten` is kept as a FOURTH, non-customer-facing id so the tenants whose styles
// already say "batten" (northwood, farmland) keep their exact current look.
//
// Before this, three sites disagreed about the vocabulary: the texture selector spoke
// lap|groove, the relief branch spoke batten|lap, and d3SidingOverride returned lap|null
// -- so "batten" silently drew the GROOVE texture, and there was no way to say "metal"
// at all. One table, one normalizer, and all three read from it.
//
// tileFtU/tileFtV are how many FEET one 512px texture tile covers, so the pattern is
// anchored to the BUILDING rather than to each wall's own 0..1 UV span.
var D3_CLADDING={lap:{id:"lap",label:"Lap Siding",tex:"lap",relief:"lap",stepFt:0.5,tileFtU:8.0,tileFtV:4.0,bump:0.45},panel:{id:"panel",label:"Panel Siding",tex:"groove",relief:null,tileFtU:4.0,tileFtV:8.0,bump:0.40},agpanel:{id:"agpanel",label:"Metal",tex:"agpanel",relief:"rib",stepFt:0.75,tileFtU:3.0,tileFtV:3.0,bump:0.60,metal:true},batten:{id:"batten",label:"Board & Batten",tex:"groove",relief:"batten",stepFt:1.5,tileFtU:4.0,tileFtV:8.0,bump:0.40}};// The customer-selectable set, in the order Carolyn listed them. `batten` is deliberately
// absent -- it is a legacy value we still RENDER, not one we offer.
var D3_CLADDING_CHOICES=["lap","panel","agpanel"];// Every value `building_styles.d3.siding` can already hold, mapped onto the table above.
// This is the whole backward-compatibility story and it needs NO migration: today `null`
// and `"batten"` differ only in relief, and `panel` carries no relief, so every existing
// tenant renders exactly as it does now.
function d3NormalizeCladding(v){var s=String(v==null?"":v).trim().toLowerCase();if(s==="lap"||s==="lapsiding"||s==="lap-siding")return"lap";if(s==="batten"||s==="board-and-batten"||s==="bnb")return"batten";if(s==="agpanel"||s==="ag"||s==="metal"||s==="panel-loc"||s==="panelloc")return"agpanel";return"panel";// null / "" / "groove" / "panel" / "t111" / anything unrecognised
}// Resolve a style's 3D appearance: tenant config override (the style entry's
// `d3` object) over the built-in per-style defaults, over the generic gable.
// sidingOverride (from d3SidingOverride) wins over everything — it's the
// customer's selected siding upgrade. customerWallHeightFt is the customer's
// wall-height pick from the 3D view (sel.wallHeight) and beats the style's
// default height, like IdeaRoom's wall-raise feature.
function d3ResolveStyleSpec(styleCfg,styleValue,globalWallHeightFt,sidingOverride,customerWallHeightFt){var key=String(styleValue||"").trim().toLowerCase();var base=D3_STYLE_DEFAULTS[key]||{};var o=styleCfg&&styleCfg.d3||{};return{roof:_objectSpread(_objectSpread(_objectSpread({},D3_DEFAULT_ROOF),base.roof||{}),o.roof||{}),siding:sidingOverride||(o.siding!==undefined?o.siding:base.siding||null),colors:_objectSpread(_objectSpread({},base.colors||{}),o.colors||{}),// The style's default roof MATERIAL (shingle|metal) — texture + surface
// response even before the customer picks a roof type; their pick wins.
roofMaterial:o.roofMaterial==="metal"||o.roofMaterial==="shingle"?o.roofMaterial:base.roofMaterial||null,wallHeightFt:customerWallHeightFt||o.wallHeightFt||styleCfg&&styleCfg.wallHeightFt||globalWallHeightFt||0};}// Carolyn (2026-07-02): horizontal lap siding is THE universal upgrade —
// vertical groove panel is standard everywhere. When the customer's selected
// options say "lap siding", the 3D walls switch to horizontal lap boards.
// Explicit config wins: `siding3d: { optionId, lapValue }` in the config blob
// names the option to watch. Without it, any selected option whose id or
// value reads as lap siding (e.g. a "Siding" option set to "Lap Siding")
// triggers the switch — so a tenant just adding the option works untouched.
function d3SidingOverride(config,sel){if(!config||!sel)return null;// The explicit Cladding pick wins over everything below. `sel.cladding` is set by the
// Cladding control in the 2D toolbar; the two legacy paths after it stay for tenants who
// already wired their own "Siding" option before Cladding existed.
if(sel.cladding&&D3_CLADDING[sel.cladding])return sel.cladding;var s3=config.siding3d;if(s3&&s3.optionId){var v=sel[s3.optionId];var lapVal=String(s3.lapValue||"Lap Siding").trim().toLowerCase();return v&&String(v).trim().toLowerCase()===lapVal?"lap":null;}for(var k in sel){var _v2=sel[k];if(typeof _v2!=="string")continue;if(/lap/i.test(_v2)&&(/sid/i.test(_v2)||/sid/i.test(k)))return"lap";}return null;}// Natural-material fallbacks for "No Paint" designs (and for palette values the
// browser can't parse — the paint palette stores display names, not hex).
var D3_COLORS={body:"#CDBA92",trim:"#8F7B55",roof:"#565C66",floor:"#B7AC99",ground:"#DCE2D8",door:"#77664C",glass:"#B9D8EA",loft:"#C2A67D",bench:"#8B7355",ramp:"#9AA0A6"};// Curated paint swatches for the 3D color picker. The LABEL is what lands in
// paintColors (and on the estimate — same free-text semantics as the 2D paint
// inputs); the css drives the live 3D material. Colors typed free-form in 2D
// still resolve through d3CssColor.
var D3_SWATCHES=[{label:"Barn Red",css:"#8B2E2E"},{label:"White",css:"#F2F1EA"},{label:"Tan",css:"#D2B48C"},{label:"Clay",css:"#B08D57"},{label:"Gray",css:"#9AA1A9"},{label:"Charcoal",css:"#3F444B"},{label:"Blue",css:"#4A6FA5"},{label:"Green",css:"#4F6F52"},{label:"Brown",css:"#6B4F3A"}];function d3SwatchCss(label,fallback){var s=D3_SWATCHES.find(function(x){return x.label===label;});return s?s.css:d3CssColor(label,fallback);}// Phase 5 (plan §6): vertical-dimension fields stamped onto items at placement
// so designs carry their own opening heights/sills/elevations. The 3D reads
// these with D3 fallbacks, so legacy designs (no fields) render identically.
function d3OpeningDefaults(type){if(type==="window")return{openingHeightFt:D3.WINDOW_H,sillFt:D3.WINDOW_SILL};if(type==="roughOpening")return{openingHeightFt:D3.RO_H};if(type==="singleDoor"||type==="doubleDoor")return{openingHeightFt:D3.DOOR_H};return{};}// Resolve a palette value ("Red", "Dark Gray", "#AA3322", …) to a CSS color the
// renderer can use. Tries the raw value, then a squashed lowercase form
// ("Dark Gray" → "darkgray"); falls back when neither parses.
function d3CssColor(v,fallback){if(!v||typeof v!=="string"||typeof document==="undefined")return fallback;var probe=new Option().style;probe.color=v.trim();if(probe.color)return v.trim();probe.color=v.trim().toLowerCase().replace(/\s+/g,"");return probe.color||fallback;}// Procedural surface textures for the 3D materials — no image assets (plan
// constraint: parametric only). Each returns a small tileable grayscale
// THREE.CanvasTexture that MULTIPLIES the material's base color (Lambert
// map × color), so the chosen paint / roof color still drives the hue while
// the pattern adds relief:
//   metal   → vertical standing-seam ribs (AgPanel / Panel-Loc look)
//   shingle → staggered horizontal shingle courses (Owens Corning Duration look)
//   lap     → horizontal lap-siding boards
//   groove  → vertical groove / board-and-batten panel (the standard siding look)
// Regenerated per build (cheap) so disposeShed3DModel frees them with the
// material — never a shared texture left disposed under a live model.
// Pattern canvases are rastered ONCE per session (module cache, the grass-
// singleton pattern below): a live drag re-mints only the cheap CanvasTexture
// wrapper per build, so dispose stays safe while the pixels — and the mipmap
// upload cost they used to re-pay every rebuilt frame — are paid once.
// Each kind rasters a COLOR canvas (multiplied by the material color, so it
// stays white-based) and a BUMP canvas (grayscale relief) at 512px, with a
// seeded PRNG so the variegation is identical every session — a quote PDF
// captured today must match one captured tomorrow.
var D3_TEX_KINDS=new Set(["metal","groove","lap","shingle","agpanel"]);var _d3TexCanvases={};function _d3Rng(seed){var a=seed>>>0;return function(){a|=0;a=a+0x6D2B79F5|0;var t=Math.imul(a^a>>>15,1|a);t=t+Math.imul(t^t>>>7,61|t)^t;return((t^t>>>14)>>>0)/4294967296;};}function _d3RasterKind(kind){var N=512;var color=document.createElement("canvas");color.width=color.height=N;var bump=document.createElement("canvas");bump.width=bump.height=N;var g=color.getContext("2d");var b=bump.getContext("2d");var rng=_d3Rng(kind.length*2654435761+20260815);g.fillStyle="#ffffff";g.fillRect(0,0,N,N);b.fillStyle="#808080";b.fillRect(0,0,N,N);if(kind==="shingle"){// Architectural asphalt: staggered variegated tabs, deep course shadows,
// faint vertical weather streaks.
var row=64,tab=86;for(var y=0,r=0;y<N+row;y+=row,r++){var off=r%2*(tab/2);for(var x=-tab;x<N+tab;x+=tab){var v=0.82+rng()*0.26;// per-tab lightness 0.82–1.08
var gray=Math.round(238*Math.min(1.04,v));g.fillStyle="rgb(".concat(gray,",").concat(gray,",").concat(gray,")");g.fillRect(x+off,y,tab-3,row-4);var bv=Math.round(112+rng()*48);b.fillStyle="rgb(".concat(bv,",").concat(bv,",").concat(bv,")");b.fillRect(x+off,y,tab-3,row-4);// tab gap
g.fillStyle="rgba(0,0,0,0.35)";g.fillRect(x+off+tab-3,y,3,row-4);b.fillStyle="#3a3a3a";b.fillRect(x+off+tab-3,y,3,row-4);}// course shadow + butt-edge highlight
g.fillStyle="rgba(0,0,0,0.42)";g.fillRect(0,y+row-4,N,4);g.fillStyle="rgba(255,255,255,0.10)";g.fillRect(0,y+row,N,2);b.fillStyle="#242424";b.fillRect(0,y+row-4,N,4);}for(var i=0;i<26;i++){// weather streaks
var _x5=rng()*N;g.fillStyle="rgba(0,0,0,".concat(0.02+rng()*0.04,")");g.fillRect(_x5,0,1+rng()*3,N);}}else if(kind==="metal"){// Standing seam: wide pans with soft cross-pan sheen, paired ribs at each
// seam, strong bump ridges so the sun catches them.
var pan=128;for(var _x6=0;_x6<N;_x6+=pan){var grad=g.createLinearGradient(_x6,0,_x6+pan,0);grad.addColorStop(0,"rgba(0,0,0,0.10)");grad.addColorStop(0.35,"rgba(255,255,255,0.09)");grad.addColorStop(0.65,"rgba(255,255,255,0.02)");grad.addColorStop(1,"rgba(0,0,0,0.14)");g.fillStyle=grad;g.fillRect(_x6,0,pan,N);g.fillStyle="rgba(0,0,0,0.30)";g.fillRect(_x6,0,3,N);// seam shadow
g.fillStyle="rgba(255,255,255,0.5)";g.fillRect(_x6+3,0,3,N);// rib light
g.fillStyle="rgba(0,0,0,0.18)";g.fillRect(_x6+6,0,2,N);b.fillStyle="#e8e8e8";b.fillRect(_x6+1,0,6,N);// rib ridge
b.fillStyle="#565656";b.fillRect(_x6+7,0,2,N);}}else if(kind==="agpanel"){// Panel-Loc Plus (Central States) — the reference Carolyn sent for WALL metal. This is
// NOT the standing-seam "metal" raster above: that one is the ROOF look and she
// explicitly rejected SmartBuild's metal wall view. The distinction that matters is the
// bump map — these ribs stand OUT (light), where groove's grooves are cut IN (dark).
// 4 major ribs per tile at tileFtU 3.0 puts them ~9in apart, matching a 36in-coverage
// panel, with a wider side-lap rib at the tile seam where sheets overlap.
var rib=N/4;for(var _x7=0;_x7<N;_x7+=rib){var _grad=g.createLinearGradient(_x7,0,_x7+rib,0);_grad.addColorStop(0,"rgba(0,0,0,0.13)");_grad.addColorStop(0.30,"rgba(255,255,255,0.07)");_grad.addColorStop(0.70,"rgba(255,255,255,0.02)");_grad.addColorStop(1,"rgba(0,0,0,0.10)");g.fillStyle=_grad;g.fillRect(_x7,0,rib,N);// the rib itself: shadow on the leading face, highlight on the crown
g.fillStyle="rgba(0,0,0,0.34)";g.fillRect(_x7,0,4,N);g.fillStyle="rgba(255,255,255,0.55)";g.fillRect(_x7+4,0,5,N);g.fillStyle="rgba(0,0,0,0.20)";g.fillRect(_x7+9,0,3,N);b.fillStyle="#efefef";b.fillRect(_x7+2,0,8,N);// ribs stand OUT
b.fillStyle="#4f4f4f";b.fillRect(_x7+10,0,2,N);// minor striations between ribs — Panel-Loc has them, standing seam does not
for(var k=1;k<=2;k++){var sx=_x7+rib*k/3;g.fillStyle="rgba(0,0,0,0.07)";g.fillRect(sx,0,1,N);b.fillStyle="#9a9a9a";b.fillRect(sx,0,1,N);}}// side-lap rib at the seam: wider, because that is where two sheets overlap
g.fillStyle="rgba(0,0,0,0.28)";g.fillRect(N-3,0,3,N);b.fillStyle="#f5f5f5";b.fillRect(N-3,0,3,N);}else if(kind==="lap"){// Horizontal lap boards: per-board tone, butt shadow, faint grain.
var board=64;for(var _y4=0;_y4<N;_y4+=board){var _v3=0.90+rng()*0.16;var _gray=Math.round(244*Math.min(1.03,_v3));g.fillStyle="rgb(".concat(_gray,",").concat(_gray,",").concat(_gray,")");g.fillRect(0,_y4,N,board);for(var _i15=0;_i15<7;_i15++){// grain streaks
g.fillStyle="rgba(0,0,0,".concat(0.015+rng()*0.03,")");g.fillRect(0,_y4+6+rng()*(board-12),N,1+rng());}g.fillStyle="rgba(0,0,0,0.34)";g.fillRect(0,_y4+board-5,N,5);// butt shadow
g.fillStyle="rgba(255,255,255,0.16)";g.fillRect(0,_y4,N,2);// top catch-light
var bg=b.createLinearGradient(0,_y4,0,_y4+board);// board tilt
bg.addColorStop(0,"#9a9a9a");bg.addColorStop(0.9,"#6f6f6f");bg.addColorStop(1,"#2f2f2f");b.fillStyle=bg;b.fillRect(0,_y4,N,board);}}else{// "groove" — vertical T1-11 style panel: grooves with soft shoulders + grain.
var gap=84;for(var _i16=0;_i16<30;_i16++){g.fillStyle="rgba(0,0,0,".concat(0.012+rng()*0.025,")");g.fillRect(rng()*N,0,1+rng()*2,N);}for(var _x8=0;_x8<N;_x8+=gap){g.fillStyle="rgba(0,0,0,0.10)";g.fillRect(_x8-3,0,3,N);g.fillStyle="rgba(0,0,0,0.30)";g.fillRect(_x8,0,4,N);g.fillStyle="rgba(255,255,255,0.12)";g.fillRect(_x8+4,0,2,N);b.fillStyle="#4a4a4a";b.fillRect(_x8,0,4,N);b.fillStyle="#6a6a6a";b.fillRect(_x8-2,0,2,N);}}return{color:color,bump:bump};}function d3MakeTexture(THREE,kind){if(!kind||typeof document==="undefined")return null;// Widened from a four-way chain when agpanel arrived (Carolyn's wall-metal cladding).
if(!D3_TEX_KINDS.has(kind))return null;if(!_d3TexCanvases[kind])_d3TexCanvases[kind]=_d3RasterKind(kind);var tex=new THREE.CanvasTexture(_d3TexCanvases[kind].color);tex.wrapS=tex.wrapT=THREE.RepeatWrapping;tex.anisotropy=8;// clamped to the GPU max at upload; kills grazing-angle smear on roofs
return tex;}// Matching relief map (same cached canvas family, same repeat contract as the
// color texture — the caller copies .repeat onto it).
function d3MakeBumpTexture(THREE,kind){if(!kind||typeof document==="undefined")return null;if(!_d3TexCanvases[kind])return null;// color texture is always made first
var tex=new THREE.CanvasTexture(_d3TexCanvases[kind].bump);tex.wrapS=tex.wrapT=THREE.RepeatWrapping;tex.anisotropy=8;return tex;}// ─── Catalog fixture photos as 3D textures ───
// A builder uploads a straight-on photo of THEIR door/window in the portal
// (Options → Doors/Windows); the 3D masks that photo onto the opening's slab, so
// the customer sees the real product instead of a generic panel. No model
// library to maintain on our side — the builder owns the photo.
//
// Three constraints drive this design:
//  1. `queueRebuild` disposes and rebuilds the dragged item's wall (and before
//     the scoped-rebuild work, the whole model) every animation frame during a
//     drag, so these textures must be shared across builds and must NOT be
//     freed by disposal (hence userData.ssShared, honored by disposeSubtree).
//  2. The renderer draws on demand only, so a photo arriving later has to ask for
//     a frame — d3OnFixtureTexSettle lets the viewer register that listener.
//  3. A texture whose image hasn't loaded renders BLACK. So the cache serves a 1x1
//     canvas in the parametric fill color until the photo arrives — a loading (or
//     permanently failed) photo looks exactly like the painted fallback.
//     The arriving photo becomes a BRAND-NEW texture object and every material
//     built against the placeholder is re-pointed at it. Verified the hard way on
//     three 0.167: swapping `texture.image` (or `texture.source.data`) and setting
//     needsUpdate does NOT re-upload an already-uploaded texture — the GPU kept
//     showing the 1x1 placeholder while `material.map.image.width` read 1024.
//     Only assigning a new texture to `material.map` takes effect. Do not
//     "optimize" this back into a mutation.
//     Re-pointing (rather than rebuilding the model) is also what keeps this
//     working in a BACKGROUNDED tab: requestAnimationFrame does not fire there, so
//     a rebuild-based repaint would never land and the customer would close the
//     view — and snapshot their quote — with blank doors.
// Photos are downscaled to <=1024px: a 4000px phone photo is ~48MB of GPU memory
// per opening, and the 3D has to survive a mid-range phone.
var _d3FxTexCache=new Map();// url → { tex, status: "loading" | "ready" | "error" }
var _d3FxMatBinds=new Map();// placeholder tex → Set<material> awaiting the photo
var _d3FxTexPending=0;var _d3FxTexWaiters=[];// one-shot resolvers (see d3WaitFixtureTextures)
var _d3FxTexListeners=new Set();// per-viewer settle callbacks
var D3_FX_TEX_MAX=1024;function _d3FxTexSettle(){_d3FxTexPending=Math.max(0,_d3FxTexPending-1);if(_d3FxTexPending===0){while(_d3FxTexWaiters.length){var r=_d3FxTexWaiters.pop();try{r();}catch(_e){/* noop */}}}_d3FxTexListeners.forEach(function(cb){try{cb();}catch(_e){/* a listener must never break the load */}});}// Shared, session-lived cache entry for a fixture photo URL: { tex, status }, where
// `tex` is null until the image has decoded. Never evicted — a disposed texture under a
// live model is exactly the hazard the grass-singleton note above warns about, and a
// tenant only has a handful of fixture photos.
//
// There is deliberately NO stand-in texture for the loading state. An earlier version
// served a 1x1 canvas in the door colour, which was wrong twice over: unlit, it landed
// within 12/255 of the SIDING colour, so a photo that never arrived read as a doorless
// wall rather than as the painted door it claims to fall back to. The painted door is now
// a real mesh that is always built, and the photo rides in front of it (see the fill
// branches) — so "loading", "failed" and "no photo" all look identical and correct.
function d3FixtureTexture(THREE,url){if(!url||typeof document==="undefined")return null;var hit=_d3FxTexCache.get(url);if(hit)return hit;var entry={tex:null,status:"loading"};_d3FxTexCache.set(url,entry);_d3FxTexPending++;var img=new Image();// Required: without CORS the downscale canvas is tainted and texImage2D throws.
// Supabase public buckets send Access-Control-Allow-Origin, so this is clean.
img.crossOrigin="anonymous";img.onload=function(){try{var w=img.naturalWidth||img.width,h=img.naturalHeight||img.height;var s=Math.min(1,D3_FX_TEX_MAX/Math.max(w,h));var dw=Math.max(1,Math.round(w*s)),dh=Math.max(1,Math.round(h*s));var out=document.createElement("canvas");out.width=dw;out.height=dh;// No fill behind it: builders upload background-REMOVED cut-outs (every photo in
// production is an RGBA PNG that is 50-65% fully transparent), and that alpha has
// to survive into the texture so the painted door shows through instead of black.
out.getContext("2d").drawImage(img,0,0,dw,dh);var photoTex=new THREE.CanvasTexture(out);if(THREE.SRGBColorSpace)photoTex.colorSpace=THREE.SRGBColorSpace;// photos are sRGB
photoTex.userData={ssShared:true};// disposeShed3DModel must skip this map
entry.tex=photoTex;entry.status="ready";// Show the photo on everything already built for it. Assigning a NEW texture is the
// only thing three 0.167 honours (see the note above), and revealing the mesh here
// rather than rebuilding keeps this working in a backgrounded tab, where rAF stops.
var waiting=_d3FxMatBinds.get(entry);if(waiting){waiting.forEach(function(b){b.mat.map=photoTex;b.mat.needsUpdate=true;if(b.mesh)b.mesh.visible=true;});_d3FxMatBinds["delete"](entry);}}catch(_e){entry.status="error";}// painted door stays — never a black slab
_d3FxTexSettle();};img.onerror=function(){entry.status="error";// The materials waiting on this photo keep their painted fallback — but
// the bind set must go, or it pins disposed materials forever (the load
// path deletes it on success; this was the missed error path).
_d3FxMatBinds["delete"](entry);_d3FxTexSettle();};img.src=url;return entry;}// Remember a photo mesh + material built while the image was still loading, so the photo
// can be revealed the moment it lands. Anything built after the load already has it.
function d3BindFixturePhoto(entry,mat,mesh){if(!entry||entry.status!=="loading")return mat;var set=_d3FxMatBinds.get(entry);if(!set){set=new Set();_d3FxMatBinds.set(entry,set);}set.add({mat:mat,mesh:mesh});return mat;}function d3FixtureTexturesPending(){return _d3FxTexPending;}// Resolves when every in-flight photo has settled, or when the cap elapses —
// the quote PDF must not be captured with placeholder doors, but it must also
// never hang on a dead image host.
function d3WaitFixtureTextures(timeoutMs){if(_d3FxTexPending===0)return Promise.resolve();return new Promise(function(resolve){var done=false;var fire=function fire(){if(!done){done=true;resolve();}};_d3FxTexWaiters.push(fire);setTimeout(fire,Math.max(0,timeoutMs||1500));});}function d3OnFixtureTexSettle(cb){_d3FxTexListeners.add(cb);return function(){return _d3FxTexListeners["delete"](cb);};}// ─── Environment (competitor-parity presentation: grass, sky, labels) ───
// SmartBuild-style scene dressing, all procedural canvases — no image assets.
// Grass: mottled two-green field tile. Sky: vertical gradient with soft cloud
// blobs, mapped onto a dome. Labels: flat text planes lying on the grass.
// The grass canvas is generated ONCE per session (module singleton): live-drag
// rebuilds create a fresh CanvasTexture per build (so dispose stays safe) from
// the SAME pixels — otherwise the noise re-rolls every rebuilt frame and the
// whole lawn shimmers while dragging an item.
var _d3GrassCanvas=null;function d3MakeGrassTexture(THREE){if(typeof document==="undefined")return null;if(!_d3GrassCanvas){var N=256,cv=document.createElement("canvas");cv.width=cv.height=N;var g=cv.getContext("2d");g.fillStyle="#7FA05B";g.fillRect(0,0,N,N);for(var i=0;i<240;i++){// mottled patches
g.fillStyle=Math.random()<0.5?"rgba(104,136,66,0.25)":"rgba(150,178,100,0.22)";g.beginPath();g.arc(Math.random()*N,Math.random()*N,4+Math.random()*14,0,Math.PI*2);g.fill();}for(var _i17=0;_i17<900;_i17++){// fine blade speckle
g.fillStyle=Math.random()<0.5?"rgba(70,96,44,0.35)":"rgba(172,198,122,0.30)";g.fillRect(Math.random()*N,Math.random()*N,1.3,2.2);}_d3GrassCanvas=cv;}var tex=new THREE.CanvasTexture(_d3GrassCanvas);tex.wrapS=tex.wrapT=THREE.RepeatWrapping;tex.anisotropy=8;// the lawn is the biggest grazing-angle surface on screen
return tex;}function d3MakeSkyTexture(THREE){if(typeof document==="undefined")return null;var W=512,H=512,cv=document.createElement("canvas");cv.width=W;cv.height=H;var g=cv.getContext("2d");var grad=g.createLinearGradient(0,0,0,H);grad.addColorStop(0,"#7EB3E0");grad.addColorStop(0.55,"#BAD4EA");grad.addColorStop(0.82,"#E8F0F6");grad.addColorStop(1,"#F2F5F3");g.fillStyle=grad;g.fillRect(0,0,W,H);for(var i=0;i<24;i++){// soft clouds in the upper band
var x=Math.random()*W,y=H*(0.10+Math.random()*0.42),r=20+Math.random()*40;var cg=g.createRadialGradient(x,y,0,x,y,r);cg.addColorStop(0,"rgba(255,255,255,0.55)");cg.addColorStop(1,"rgba(255,255,255,0)");g.fillStyle=cg;g.beginPath();g.ellipse(x,y,r*1.9,r*0.6,0,0,Math.PI*2);g.fill();}var tex=new THREE.CanvasTexture(cv);tex.wrapS=THREE.RepeatWrapping;tex.repeat.set(2,1);// tile twice around the dome
return tex;}// Flat text plane for on-ground dimension labels ("FRONT - 30'") and the N compass.
// Text canvases are cached by their text (a session sees a handful of distinct
// labels): rebuilds skip measureText/fillText and re-mint only the texture.
var _d3LabelCanvases=new Map();function d3MakeGroundLabel(THREE,text,hFt){if(typeof document==="undefined")return null;var cv=_d3LabelCanvases.get(text);if(!cv){var probe=document.createElement("canvas").getContext("2d");probe.font="700 44px Arial";cv=document.createElement("canvas");cv.width=Math.ceil(probe.measureText(text).width)+24;cv.height=64;var g=cv.getContext("2d");g.font="700 44px Arial";g.fillStyle="rgba(62,48,36,0.85)";g.textBaseline="middle";g.fillText(text,12,34);_d3LabelCanvases.set(text,cv);}var tex=new THREE.CanvasTexture(cv);var m=new THREE.Mesh(new THREE.PlaneGeometry(hFt*(cv.width/cv.height),hFt),new THREE.MeshBasicMaterial({map:tex,transparent:true,depthWrite:false}));m.rotation.x=-Math.PI/2;// lie flat on the grass
return m;}// Dev-only build timing: set window.__SS3D_DEBUG = true in the console and
// every model build records a "ss3d:rebuild" performance measure. The flag
// gates all work, so shipped pages pay nothing.
function d3TimedBuild(fn){if(typeof window==="undefined"||!window.__SS3D_DEBUG||typeof performance==="undefined"||!performance.mark)return fn();performance.mark("ss3d:rebuild:start");var out=fn();performance.mark("ss3d:rebuild:end");performance.measure("ss3d:rebuild","ss3d:rebuild:start","ss3d:rebuild:end");return out;}// Build the whole parametric building as one Group. World units are FEET; the
// footprint is centered on the origin with the floor top at y=0; +x = east and
// +z = south, matching the 2D plan (page-down = south). Returns { root, wallMat,
// roofGroup } so the viewer can ghost the shell for the "look inside" mode and
// dispose everything on close.
function buildShed3DModel(THREE,p){var bldgW=p.bldgW,bldgH=p.bldgH,items=p.items,itemTypes=p.itemTypes,bodyColor=p.bodyColor,trimColor=p.trimColor,frontWall=p.frontWall,scale=p.scale,mgX=p.mgX,mgY=p.mgY;// Wall height: config-driven when the tenant sets it (per-style wallHeightFt
// in the config blob / building_sizes.wall_height_ft once 016 is applied),
// else the D3 default (plan §6 gap #1).
var H=p.wallHeightFt||D3.WALL_H,T=D3.WALL_T;var root=new THREE.Group();// Standard (PBR) materials, not Lambert: with the ACES tone mapping the viewer
// sets, they are what makes painted siding read as PAINT and a metal roof as
// METAL instead of flat matte plastic — the single biggest realism lever.
// Roughness defaults high (wood/asphalt); callers override for metal.
var mat=function mat(color,extra){return new THREE.MeshStandardMaterial(_objectSpread({color:color,roughness:0.85,metalness:0.0},extra||{}));};var wallMat=mat(bodyColor);var trimMat=mat(trimColor);var roofMat=mat(p.roofColor||D3_COLORS.roof);// Catalog fixture photos. Resolved LIVE from the catalog by fixtureItemId
// rather than stamped on the item at placement (unlike price/name, which must
// not move under a saved quote): re-uploading a better photo improves designs
// already saved, and an archived fixture — dropped by get_fixtures — quietly
// falls back to the painted slab instead of leaving a hole.
var fxById=new Map((Array.isArray(p.fixtures)?p.fixtures:[]).map(function(fx){return[String(fx.id),fx];}));// Returns the shared cache ENTRY ({ tex, status }) — not a texture — because the caller
// needs to know whether the photo has decoded yet to decide if its layer is visible.
var fixturePhotoTex=function fixturePhotoTex(it){if(it.fixtureItemId==null)return null;// built-in door/window
var fx=fxById.get(String(it.fixtureItemId));if(!fx||!fx.imageUrl)return null;return d3FixtureTexture(THREE,fx.imageUrl);};// Cladding texture — multiplies the body color, so the customer's paint still drives
// the hue while the pattern supplies the relief. One entry in D3_CLADDING decides the
// raster, the relief style below, and the tile scale.
var clad=D3_CLADDING[d3NormalizeCladding(p.styleSpec&&p.styleSpec.siding)]||D3_CLADDING.panel;var wallKind=clad.tex;var wallTex=d3MakeTexture(THREE,wallKind);if(wallTex){// One tile per tileFt of WORLD FEET: wallBox rewrites every face's UVs to feet, so the
// repeat is a constant unit conversion, not a per-building guess. (The first cut set a
// building-averaged repeat against 0..1 face UVs, which anchored nothing -- every face
// still stretched its tiles to its own length; audit 2026-08-19.)
wallTex.repeat.set(1/clad.tileFtU,1/clad.tileFtV);wallMat.map=wallTex;var wallBump=d3MakeBumpTexture(THREE,wallKind);if(wallBump){wallBump.repeat.copy(wallTex.repeat);wallMat.bumpMap=wallBump;wallMat.bumpScale=clad.bump;}// Metal reads as metal only with the surface response, not the pattern alone — the
// same recipe the metal ROOF already uses.
if(clad.metal){wallMat.roughness=0.45;wallMat.metalness=0.35;}}var box=function box(m,w,h,d){return new THREE.Mesh(new THREE.BoxGeometry(w,h,d),m);};// Page px → world ft (plan §3): same scale/mg the 2D plan renders with.
var ftX=function ftX(px){return(px-mgX)/scale-bldgW/2;};var ftZ=function ftZ(py){return(py-mgY)/scale-bldgH/2;};// Environment: grass field to the horizon + on-ground dimension labels
// (SmartBuild-style landscape; the "Landscape" toggle hides this group).
var envGroup=new THREE.Group();var R2=Math.max(bldgW,bldgH);var groundR=Math.max(60,R2*7);// stays inside the viewer's sky dome
var grassTex=d3MakeGrassTexture(THREE);if(grassTex)grassTex.repeat.set(groundR/5,groundR/5);var ground=new THREE.Mesh(new THREE.CircleGeometry(groundR,64),new THREE.MeshLambertMaterial(_objectSpread({color:"#9DBE77"},grassTex?{map:grassTex}:{})));ground.rotation.x=-Math.PI/2;ground.position.y=-D3.FLOOR_T;envGroup.add(ground);// Dimension labels on the grass for the FRONT and LEFT edges (same display
// mapping the 2D labels use), plus a small N compass off the NW corner.
var fwLbl=frontWall||"south";var lblH=Math.max(1.3,Math.min(3.2,R2*0.055));["FRONT","LEFT"].forEach(function(want){var pos=["north","south","east","west"].find(function(w){return getDisplayLabel(w,fwLbl)===want;});if(!pos)return;var len=pos==="north"||pos==="south"?bldgW:bldgH;var lbl=d3MakeGroundLabel(THREE,"".concat(want," - ").concat(Math.round(len),"'"),lblH);if(!lbl)return;var off=2+lblH;var holder=new THREE.Group();if(pos==="north"){holder.position.set(0,0,-bldgH/2-off);holder.rotation.y=Math.PI;}else if(pos==="south"){holder.position.set(0,0,bldgH/2+off);}else if(pos==="west"){holder.position.set(-bldgW/2-off,0,0);holder.rotation.y=Math.PI/2;}else{holder.position.set(bldgW/2+off,0,0);holder.rotation.y=-Math.PI/2;}lbl.position.y=-D3.FLOOR_T+0.04;holder.add(lbl);envGroup.add(holder);});var nLbl=d3MakeGroundLabel(THREE,"N",Math.max(1.6,lblH*1.2));if(nLbl){nLbl.position.set(-bldgW/2-R2*0.35,-D3.FLOOR_T+0.04,-bldgH/2-R2*0.35);envGroup.add(nLbl);}root.add(envGroup);var floor=box(mat(D3_COLORS.floor),bldgW+0.2,D3.FLOOR_T,bldgH+0.2);floor.position.y=-D3.FLOOR_T/2;root.add(floor);// Wall frames: O = the wall's along=0 end (in x/z), U = unit vector along the
// wall, N = exterior normal. `along` runs west→east on N/S walls and
// north→south on E/W walls — exactly how the 2D snap logic measures items.
var WALLS={north:{len:bldgW,O:[-bldgW/2,-bldgH/2],U:[1,0],N:[0,-1]},south:{len:bldgW,O:[-bldgW/2,bldgH/2],U:[1,0],N:[0,1]},west:{len:bldgH,O:[-bldgW/2,-bldgH/2],U:[0,1],N:[-1,0]},east:{len:bldgH,O:[bldgW/2,-bldgH/2],U:[0,1],N:[1,0]}};var wallsGroup=new THREE.Group();// ghosted in "look inside" mode
var openingsGroup=new THREE.Group();// frames + door/window fills (stay solid)
// Place a box on wall `wf` spanning [a0,a1] along it and [y0,y1] vertically,
// `out` ft toward the exterior, `depth` ft thick (defaults to wall thickness).
var wallBox=function wallBox(m,wf,a0,a1,y0,y1,out,depth){var b=box(m,a1-a0,y1-y0,depth||T);// WORLD-FEET UVs -- the actual anchoring. Stock BoxGeometry UVs span 0..1 per face, so
// with a shared repeat every segment crammed the full tile count into its own face
// (a 2 ft strip beside a door drew the same course count as a 12 ft wall), and the
// gable cap -- whose ExtrudeGeometry UVs ARE in profile feet -- tiled 8-24x denser
// than the wall under it (audit 2026-08-19; setting wallTex.repeat alone never
// delivered what its comment claimed). Rewriting u/v to wall-run/height FEET makes
// the single wallMat repeat of 1/tileFt land every face at true scale, keeps the
// texture's drawn ribs in phase with the proud relief strips (both count feet from
// the wall origin), and lines the cap's courses up with the wall's at the plate line.
// The box's thin edge faces get stripe UVs out of this; they are 0.3 ft slivers
// buried in corner trim, invisible either way.
var uvA=b.geometry.attributes.uv,posA=b.geometry.attributes.position;var cu=(a0+a1)/2,cv=(y0+y1)/2;for(var i=0;i<uvA.count;i++)uvA.setXY(i,posA.getX(i)+cu,posA.getY(i)+cv);uvA.needsUpdate=true;var ac=(a0+a1)/2;b.position.set(wf.O[0]+wf.U[0]*ac+wf.N[0]*(out||0),(y0+y1)/2,wf.O[1]+wf.U[1]*ac+wf.N[1]*(out||0));if(wf.U[0]===0)b.rotation.y=Math.PI/2;return b;};// Opening vertical extent: item-stamped fields first (Phase 5 — placed items
// carry openingHeightFt/sillFt), D3 defaults for legacy designs.
// Catalog fixtures carry their real size as widthIn/heightIn instead of the
// Phase 5 stamps, so a 7 ft roll-up finally reads taller than a walk door.
// Everything is clamped to leave a header strip under the plate: the customer
// can pick 6 ft walls in this very modal, and an unclamped 6'8" door would
// push its casing into the roof and silently skip the header segment.
var openingSpan=function openingSpan(it){var inchesFt=function inchesFt(v){return Number(v)>0?Number(v)/12:null;};var maxTop=H-0.2;if(it.type==="window"){var wh=Math.min(it.openingHeightFt||inchesFt(it.heightIn)||D3.WINDOW_H,maxTop-0.35);var s0=it.sillFt!=null?it.sillFt:D3.WINDOW_SILL;if(s0+wh>maxTop)s0=Math.max(0.35,maxTop-wh);// tall window, short wall: drop the sill
return[s0,s0+wh];}if(it.type==="roughOpening")return[0,Math.min(it.openingHeightFt||D3.RO_H,maxTop)];// singleDoor / doubleDoor / fixtureDoor (every catalog door placement)
return[0,Math.min(it.openingHeightFt||inchesFt(it.heightIn)||D3.DOOR_H,maxTop)];};// One wall's segments + opening groups, buildable in isolation: the live
// drag rebuilds ONLY the wall(s) an item left and landed on (rebuildWalls on
// the returned model) — same code as the full build, so a partial rebuild's
// output is identical to a full one by construction.
var buildOneWall=function buildOneWall(wname,itemsNow){var wf=WALLS[wname];var ops=itemsNow.filter(function(it){var c=itemTypes[it.type];return c&&c.wallOnly&&it.wall===wname;}).map(function(it){var c=itemTypes[it.type];var w=it.widthFt||c.width;var along=wf.U[0]?(it.x-mgX)/scale:(it.y-mgY)/scale;var a=Math.max(w/2,Math.min(along,wf.len-w/2));var span=openingSpan(it);return{it:it,a:a,a0:a-w/2,a1:a+w/2,y0:span[0],y1:span[1]};}).sort(function(q,r){return q.a0-r.a0;});// Union overlapping opening ranges (e.g. a window over a door — the 2D
// collision check only guards door-vs-door) so wall segments never
// intersect a hole.
var ranges=[];ops.forEach(function(o){var last=ranges[ranges.length-1];if(last&&o.a0<last.a1-0.01){last.a1=Math.max(last.a1,o.a1);last.y0=Math.min(last.y0,o.y0);last.y1=Math.max(last.y1,o.y1);}else ranges.push({a0:o.a0,a1:o.a1,y0:o.y0,y1:o.y1});});// Panel-split walls (plan §4.4 fallback strategy, chosen over CSG for the
// no-build stack): full-height segments between openings, plus sill/header
// strips below/above each opening range. Segments live in a per-wall group
// tagged with the wall name so the 3D palette can place new items by
// clicking a wall.
var wg=new THREE.Group();wg.userData={wall:wname};var cursor=0;ranges.forEach(function(rg){if(rg.a0>cursor+0.01)wg.add(wallBox(wallMat,wf,cursor,rg.a0,0,H));if(rg.y0>0.01)wg.add(wallBox(wallMat,wf,rg.a0,rg.a1,0,rg.y0));if(rg.y1<H-0.01)wg.add(wallBox(wallMat,wf,rg.a0,rg.a1,rg.y1,H));cursor=rg.a1;});if(cursor<wf.len-0.01)wg.add(wallBox(wallMat,wf,cursor,wf.len,0,H));// Cladding relief on the exterior of the full-height segments (strips break at
// openings, like real cladding). Real geometry on top of the texture, so the material
// still reads at a grazing angle where a flat map flattens out.
//   batten  proud VERTICAL battens (board-and-batten)
//   lap     proud HORIZONTAL course lines, stepped by the same exposure the raster uses
//   rib     proud vertical metal RIBS (Panel-Loc): narrower and shallower than a batten
//   null    panel siding has its grooves cut INTO the face, so it gets NO proud strips.
//           The old code gave it battens, which is the opposite of the real product.
if(clad.relief){var relief=function relief(b0,b1){if(b1-b0<0.3)return;if(clad.relief==="batten"||clad.relief==="rib"){var bs=clad.stepFt;var halfW=clad.relief==="rib"?0.05:0.07;var depth=clad.relief==="rib"?0.05:0.1;for(var a=Math.ceil((b0+0.2)/bs)*bs;a<b1-0.2;a+=bs){wg.add(wallBox(wallMat,wf,a-halfW,a+halfW,0,H,T/2+0.03,depth));}}else{for(var y=clad.stepFt;y<H-0.15;y+=clad.stepFt){wg.add(wallBox(wallMat,wf,b0+0.03,b1-0.03,y-0.04,y+0.04,T/2+0.03,0.1));}}};var bc=0;ranges.forEach(function(rg){if(rg.a0>bc+0.01)relief(bc,rg.a0);bc=rg.a1;});if(bc<wf.len-0.01)relief(bc,wf.len);}var ogs=[];// Frames + fills per opening (trim-colored casing; doors get panels,
// windows get glass + muntins, rough openings stay empty). Each opening's
// meshes live in their own tagged group so the 3D drag can raycast-pick
// the item they belong to.
ops.forEach(function(o){var f=0.17;var og=new THREE.Group();og.userData={itemId:o.it.id,wallItem:true,wall:wname};og.add(wallBox(trimMat,wf,o.a0-f,o.a0,o.y0,o.y1+f,0,T+0.06));og.add(wallBox(trimMat,wf,o.a1,o.a1+f,o.y0,o.y1+f,0,T+0.06));og.add(wallBox(trimMat,wf,o.a0-f,o.a1+f,o.y1,o.y1+f,0,T+0.06));// A catalog fixture's own photo is masked onto the opening when the builder
// uploaded one — but it is LAYERED IN FRONT of the parametric door/glass, never
// instead of it, for two reasons that both bit us:
//   · builders upload background-REMOVED cut-outs (every photo in production is an
//     RGBA PNG, 50-65% fully transparent), so the parametric fill is what shows
//     through the cut-away parts. Without it those pixels rendered pure BLACK, and
//     that black door went onto the customer's quote.
//   · while the photo loads (or if it 404s), there is something correct on screen.
// MeshBasicMaterial for the photo, not Lambert: a photo already carries the light it
// was shot in, and shading it again reads as a dirty smudge. The photo stretches to
// the opening on purpose — one photo serves a door's 4/5/6 ft variants, which is the
// whole point of not keeping a model library.
var photoLayer=function photoLayer(entry,a0,a1,y0,y1,depth){// alphaTest discards the transparent surround (cheaper and better-sorted than
// blending it); transparent:true keeps the feathered edges of a soft cut-out.
var pm=new THREE.MeshBasicMaterial({map:entry.tex,transparent:true,alphaTest:0.06});var mesh=wallBox(pm,wf,a0,a1,y0,y1,0.02,depth);// 0.02 ft proud: no z-fight
mesh.visible=Boolean(entry.tex);// hidden until the photo has decoded
d3BindFixturePhoto(entry,pm,mesh);og.add(mesh);};if(o.it.type==="window"){og.add(wallBox(trimMat,wf,o.a0-f,o.a1+f,o.y0-f,o.y0,0,T+0.06));// Sill nose: a slightly wider, deeper board under the casing — the one
// horizontal shadow line that makes the window read as installed.
og.add(wallBox(trimMat,wf,o.a0-f-0.03,o.a1+f+0.03,o.y0-0.06,o.y0+0.02,T/2+0.04,0.16));// Sash: a slim dark inner ring set INTO the opening. The depth step
// between casing → sash → glass is what turns the old flat decal into
// an assembly (the SmartBuild teardown's biggest close-up win).
var s=0.09;var sashMat=mat("#3A3F45",{roughness:0.6});og.add(wallBox(sashMat,wf,o.a0,o.a0+s,o.y0,o.y1,0,T*0.5));og.add(wallBox(sashMat,wf,o.a1-s,o.a1,o.y0,o.y1,0,T*0.5));og.add(wallBox(sashMat,wf,o.a0,o.a1,o.y1-s,o.y1,0,T*0.5));og.add(wallBox(sashMat,wf,o.a0,o.a1,o.y0,o.y0+s,0,T*0.5));// Glass you can genuinely see through — the interior showing through
// the panes is what sells it. Slight blue-green tint, a whisper of
// metalness for sky glint; depthWrite off so the ghosted look-inside
// mode never sorts against it.
og.add(wallBox(mat("#BFE0E8",{transparent:true,opacity:0.22,roughness:0.05,metalness:0.4,side:THREE.DoubleSide,depthWrite:false}),wf,o.a0+s,o.a1-s,o.y0+s,o.y1-s,0,0.05));var winEntry=fixturePhotoTex(o.it);if(winEntry){photoLayer(winEntry,o.a0+0.05,o.a1-0.05,o.y0+0.05,o.y1-0.05,0.08);}else{// Muntin GRID sized to the sash (a lone cross read flat): 2-3 columns
// by width, double-hung rail across the middle.
var ww=o.a1-o.a0,wh=o.y1-o.y0;var cols=Math.max(2,Math.min(3,Math.round(ww/1.1)));for(var ci=1;ci<cols;ci++){var a=o.a0+ww*ci/cols;og.add(wallBox(trimMat,wf,a-0.03,a+0.03,o.y0+s,o.y1-s,0,0.09));}var midY=o.y0+wh/2;og.add(wallBox(trimMat,wf,o.a0+s,o.a1-s,midY-0.035,midY+0.035,0,0.09));}}else if(o.it.type==="singleDoor"||o.it.type==="doubleDoor"||o.it.type==="fixtureDoor"){var photoEntry=fixturePhotoTex(o.it);{var doorMat=mat(D3_COLORS.door);if(o.it.type==="doubleDoor"||o.it.operation==="double"){og.add(wallBox(doorMat,wf,o.a0+0.05,o.a-0.03,0.05,o.y1-0.05,0,0.16));og.add(wallBox(doorMat,wf,o.a+0.03,o.a1-0.05,0.05,o.y1-0.05,0,0.16));}else{if(o.it.operation==="slideup"&&!photoEntry){// Roll-up read: reuse the lap texture as ~1 ft horizontal panel
// seams, matching the segmented glyph the 2D plan draws.
var seamTex=d3MakeTexture(THREE,"lap");if(seamTex){seamTex.repeat.set(1,Math.max(2,Math.round(o.y1-0.1)));doorMat.map=seamTex;}}og.add(wallBox(doorMat,wf,o.a0+0.05,o.a1-0.05,0.05,o.y1-0.05,0,0.16));if(!photoEntry&&o.it.operation!=="slideup"){// Raised panels + a handle on a plain hinged slab — the relief
// shadows are what stop it reading as a painted rectangle.
var pa0=o.a0+0.22,pa1=o.a1-0.22,ph=o.y1-0.05;og.add(wallBox(doorMat,wf,pa0,pa1,ph*0.55,ph-0.18,0.1,0.05));og.add(wallBox(doorMat,wf,pa0,pa1,0.22,ph*0.45,0.1,0.05));og.add(wallBox(mat("#6B7280",{metalness:0.6,roughness:0.3}),wf,o.a1-0.38,o.a1-0.24,3.0,3.14,0.12,0.08));}}}// One photo layer even for a double or a roll-up: the photo already shows both
// leaves / the panel seams, so splitting it would draw them twice.
if(photoEntry)photoLayer(photoEntry,o.a0+0.05,o.a1-0.05,0.05,o.y1-0.05,0.16);}ogs.push(og);});return{wg:wg,ogs:ogs};};Object.keys(WALLS).forEach(function(wname){var built=buildOneWall(wname,items);wallsGroup.add(built.wg);built.ogs.forEach(function(og){return openingsGroup.add(og);});});// ── Roof (plan §4.2): a solid extruded profile in body color (its caps ARE
// the gable/gambrel end walls) with roof-colored overhanging slope slabs on
// top. Ridge runs front↔back so the gable end faces the customer's FRONT
// (derived from door placement, same rule the 2D labels use); the econo shed
// slope descends front→back. No doors yet → longer axis.
var roofGroup=new THREE.Group();var roofCfg=p.styleSpec&&p.styleSpec.roof||D3_DEFAULT_ROOF;var OV=roofCfg.overhang!=null?roofCfg.overhang:D3.OVERHANG;var fw=frontWall||(bldgH>=bldgW?"north":"west");var frontNS=fw==="north"||fw==="south";// Profile u-axis: the axis the roof profile spans across. For gable/gambrel
// the ridge is perpendicular to the front; for the shed the SLOPE runs
// front→back, so the axes swap.
var uAxisIsX=roofCfg.type==="shed"?!frontNS:frontNS;var S=uAxisIsX?bldgW:bldgH;// profile span
var L=uAxisIsX?bldgH:bldgW;// extrusion length
var prof=[];// profile polygon points [u, y], eave→ridge→eave
var slopes=[];// top edges [[A,B], …] that get roof slabs
if(roofCfg.type==="shed"){var rise=S*(roofCfg.pitch||0.25);var tallNeg=fw==="north"||fw==="west";var A=tallNeg?[-S/2,H+rise]:[-S/2,H];var B=tallNeg?[S/2,H]:[S/2,H+rise];prof.push([-S/2,H],A,B,[S/2,H]);slopes.push([A,B]);}else if(roofCfg.type==="gambrel"){var s2=S/2;var kU=s2*(roofCfg.kneeU||0.55);var kY=H+s2*(roofCfg.kneeRise||0.55);var rY=H+s2*(roofCfg.ridgeRise||0.8);prof.push([-s2,H],[-kU,kY],[0,rY],[kU,kY],[s2,H]);slopes.push([[-s2,H],[-kU,kY]],[[-kU,kY],[0,rY]],[[0,rY],[kU,kY]],[[kU,kY],[s2,H]]);}else{var _rise=S/2*(roofCfg.pitch||0.4);// ridgeOffset shifts the ridge toward one eave (saltbox-style asymmetry).
var ru=S*Math.max(-0.35,Math.min(0.35,roofCfg.ridgeOffset||0));prof.push([-S/2,H],[ru,H+_rise],[S/2,H]);slopes.push([[-S/2,H],[ru,H+_rise]],[[ru,H+_rise],[S/2,H]]);}// Drop consecutive duplicate points (the shed profile produces one) before
// building the shape; the open edge auto-closes along the wall-plate line.
var dedup=prof.filter(function(pt,i){return i===0||Math.abs(pt[0]-prof[i-1][0])>1e-6||Math.abs(pt[1]-prof[i-1][1])>1e-6;});var shape=new THREE.Shape();dedup.forEach(function(pt,i){return i===0?shape.moveTo(pt[0],pt[1]):shape.lineTo(pt[0],pt[1]);});var rg=new THREE.Group();// local space: x = profile u, y = up, z = 0..L along the ridge
// The gable END FACES are this extrusion's caps, and they are the wall above the plate
// line — so they must carry the CLADDING, or the siding visibly stops at the plate and
// the peak is a flat wedge. That is what Carolyn pointed at: "this needs to go on up,
// on the top" (2026-08-18).
//
// It used to be flat body color deliberately, because sampling the wall texture through
// profile-unit UVs "painted a smeared pale wedge". The fix is the material GROUP, not a
// different texture: ExtrudeGeometry emits group 0 = the two caps and group 1 = the swept
// sides, and its default WorldUVGenerator gives the caps UVs equal to profile-space
// (u, y) in FEET — the same world-anchored convention the walls now use — so wallMat
// tiles across the gable at true scale and in phase with the wall below it.
//
// gableMat stays and becomes the SOFFIT material (the swept underside), which keeps
// applyShellMode's existing ghost block and the live body-color write working unchanged.
// The guard matters: group ordering is a three.js internal, not a documented API, so a
// shape that does not produce exactly two groups falls back to today's behaviour rather
// than painting the soffit with cladding — which would look like the very defect this
// fixes, with a false green light.
var gableMat=mat(bodyColor);var gableGeom=new THREE.ExtrudeGeometry(shape,{depth:L,bevelEnabled:false});rg.add(new THREE.Mesh(gableGeom,gableGeom.groups&&gableGeom.groups.length===2?[wallMat,gableMat]:gableMat));// Roof texture: metal standing-seam vs shingle courses. The customer's
// roof-type pick wins; the STYLE's own roofMaterial (photo-derived, in d3)
// fills in before any pick — a bare flat-color slab was the single biggest
// "this looks fake" tell. Metal also gets a metal SURFACE (lower roughness,
// some metalness) so the sun actually glints off the pans.
var roofKind=p.roofType==="Metal"?"metal":p.roofType==="Shingle"?"shingle":p.styleSpec&&(p.styleSpec.roofMaterial==="metal"||p.styleSpec.roofMaterial==="shingle")?p.styleSpec.roofMaterial:"shingle";var roofTex=d3MakeTexture(THREE,roofKind);if(roofTex){roofTex.repeat.set(Math.max(2,Math.round(S/(roofKind==="metal"?1.5:2.5))),Math.max(2,Math.round(L/2.5)));roofMat.map=roofTex;roofMat.needsUpdate=true;var roofBump=d3MakeBumpTexture(THREE,roofKind);if(roofBump){roofBump.repeat.copy(roofTex.repeat);roofMat.bumpMap=roofBump;roofMat.bumpScale=roofKind==="metal"?0.5:0.35;}if(roofKind==="metal"){roofMat.roughness=0.45;roofMat.metalness=0.35;}else{roofMat.roughness=0.95;roofMat.metalness=0.0;}}var profPeak=-Infinity;dedup.forEach(function(pt){if(pt[1]>profPeak)profPeak=pt[1];});// A slope's endpoint is an INTERIOR JOINT when another slope shares it: a gable ridge,
// or a gambrel knee. Everything else is a free edge that should really overhang.
var jointPartnerAt=function jointPartnerAt(pt,self){return slopes.find(function(o){return o!==self&&(Math.abs(o[0][0]-pt[0])<1e-6&&Math.abs(o[0][1]-pt[1])<1e-6||Math.abs(o[1][0]-pt[0])<1e-6&&Math.abs(o[1][1]-pt[1])<1e-6);})||null;};slopes.forEach(function(sl){var A=sl[0],B=sl[1];var du=B[0]-A[0],dy=B[1]-A[1];var slen=Math.sqrt(du*du+dy*dy);var ux=du/slen,uy=dy/slen;var nx=-dy/slen,ny=du/slen;// 2D normal of the slope, pointing up-outward
// PER-END extensions. This slab used to be `slen + OV * 2` centred on the slope
// midpoint, i.e. it overhung the RIDGE by the full eave overhang as well as the eave.
// Both gable halves therefore crossed above the peak and poked through each other
// (~0.22 ft of visible crossed blades at pitch 0.4 / OV 0.6) -- what Carolyn meant by
// "we don't build roofs like that" (2026-08-18). The ridge cap below is only CAPW wide
// and shifted inward, so it could never hide it.
//
// A free edge keeps the real overhang. An interior joint extends exactly to the
// OFFSET-POLYGON VERTEX: both slabs' top faces sit `t` above their slope lines and
// intersect at Q = P + t*(n1+n2)/(1 + n1.n2); each slab runs to Q measured along its
// own direction, so the faces meet with no crossing and no gap. For a symmetric ridge
// this reduces to the old t*tan(a) (0.032 ft at pitch 0.4). It replaces a per-slope
// tan that was only valid for symmetric joints: at a gambrel KNEE the two angles
// differ, and per-slope tan gave 0.098/0.036 ft where the true meeting point needs
// 0.019 ft from both sides -- the crossed-blades bug again at one-sixth scale.
// Verified numerically against both profiles before landing (audit 2026-08-19).
var t=D3.ROOF_T/2+0.02;var jointExt=function jointExt(P,dirInX,dirInY){var o=jointPartnerAt(P,sl);if(!o)return OV;// free edge: the real eave overhang
var odu=o[1][0]-o[0][0],ody=o[1][1]-o[0][1];var olen=Math.sqrt(odu*odu+ody*ody)||1;var onx=-ody/olen,ony=odu/olen;var dot=nx*onx+ny*ony;var qx=t*(nx+onx)/(1+dot),qy=t*(ny+ony)/(1+dot);return Math.max(0,qx*dirInX+qy*dirInY);};var extA=jointExt(A,-ux,-uy);// the slab extends beyond A along -u
var extB=jointExt(B,ux,uy);var slab=box(roofMat,slen+extA+extB,D3.ROOF_T,L+OV*2);slab.rotation.z=Math.atan2(dy,du);var shift=(extB-extA)/2;// recentre: the ends no longer extend equally
slab.position.set((A[0]+B[0])/2+ux*shift+nx*t,(A[1]+B[1])/2+uy*shift+ny*t,L/2);rg.add(slab);// Eave fascia: a trim board hung on the slab's LOW edge, the finish
// carpentry that stops a roof reading as a floating slab. Positioned with
// the SAME normal offset the slab itself carries, or it hangs visibly
// detached at some angles. Only slopes that reach the wall plate get one
// (a gambrel's upper legs do not).
var lowEnd=A[1]<=B[1]?A:B;if(lowEnd[1]<=H+0.01){var towardLow=lowEnd===A?-1:1;var edgeU=lowEnd[0]+towardLow*ux*OV+nx*(D3.ROOF_T/2+0.02);var edgeY=lowEnd[1]+towardLow*uy*OV+ny*(D3.ROOF_T/2+0.02);var fascia=box(trimMat,0.14,0.4,L+OV*2);fascia.position.set(edgeU,edgeY-0.14,L/2);rg.add(fascia);}// Ridge cap: one angled board LYING ON each slope that reaches the peak
// (both halves of a gable, the upper legs of a gambrel) — a flat box can
// never seat on the V of two pitches, which is why the first cut floated.
var highEnd=A[1]>=B[1]?A:B;if(roofCfg.type!=="shed"&&highEnd[1]>=profPeak-0.01){var towardHigh=highEnd===A?-1:1;var CAPW=0.55;var capBoard=box(roofMat,CAPW,0.06,L+OV*2);capBoard.rotation.z=Math.atan2(dy,du);capBoard.position.set(highEnd[0]-towardHigh*ux*(CAPW/2-0.06)+nx*(D3.ROOF_T+0.05),highEnd[1]-towardHigh*uy*(CAPW/2-0.06)+ny*(D3.ROOF_T+0.05),L/2);rg.add(capBoard);}// Rake boards: trim running up the roof edge at each gable end. Without
// them the slab's raw end face reads unfinished — every rake on the
// SmartBuild reference building is boarded (teardown polish list).
// Same per-end extensions and recentre as the slab they trim. These kept the old
// symmetric `slen + OV*2` when the slab got its miter (052c79d), so two trim boards
// crossed 0.6 ft past the ridge at every gable end -- the exact crossed-blades look
// the miter had just removed, reintroduced in trim color (audit 2026-08-19).
[-OV+0.05,L+OV-0.05].forEach(function(z){var rake=box(trimMat,slen+extA+extB,0.32,0.1);rake.rotation.z=Math.atan2(dy,du);rake.position.set((A[0]+B[0])/2+ux*shift+nx*(D3.ROOF_T/2-0.08),(A[1]+B[1])/2+uy*shift+ny*(D3.ROOF_T/2-0.08),z);rg.add(rake);});});if(uAxisIsX){rg.position.z=-L/2;}else{rg.rotation.y=-Math.PI/2;rg.position.x=L/2;}roofGroup.add(rg);// Corner trim boards live in roofGroup so "look inside" hides them with the roof.
[[-bldgW/2,-bldgH/2],[bldgW/2,-bldgH/2],[-bldgW/2,bldgH/2],[bldgW/2,bldgH/2]].forEach(function(c){var post=box(trimMat,T+0.14,H,T+0.14);post.position.set(c[0],H/2,c[1]);roofGroup.add(post);});// ── Interior + attached items (plan §4.3, §4.5, §4.6) ──
// Same isolation as buildOneWall: rebuildInterior repopulates this group
// alone when a loft/workbench/ramp moves during a live drag.
var interiorGroup=new THREE.Group();var buildInterior=function buildInterior(itemsNow){return itemsNow.forEach(function(it){var c=itemTypes[it.type];if(!c)return;// Plan annotations, one more view of them: the note's text as a plaque
// lying at its plan position, the line as a thin strip between its
// endpoints. Text edits stay a 2D affordance.
if(c.noteType){var lbl=d3MakeGroundLabel(THREE,String(it.text||"Note").replace(/\s+/g," ").slice(0,60),0.9);if(lbl){var ng=new THREE.Group();ng.userData={itemId:it.id,floorItem:true};lbl.position.set(ftX(it.x),0.06,ftZ(it.y));ng.add(lbl);interiorGroup.add(ng);}return;}if(c.lineType){var x1=ftX(it.x1),z1=ftZ(it.y1),x2=ftX(it.x2),z2=ftZ(it.y2);var len=Math.sqrt((x2-x1)*(x2-x1)+(z2-z1)*(z2-z1));if(len>0.05){var _ng=new THREE.Group();_ng.userData={itemId:it.id,floorItem:true};var strip=box(mat("#475569"),len,0.05,0.12);strip.position.set((x1+x2)/2,0.05,(z1+z2)/2);strip.rotation.y=-Math.atan2(z2-z1,x2-x1);_ng.add(strip);interiorGroup.add(_ng);}return;}if(it.type==="loft"){var w=it.widthFt||c.width,d=it.heightFt||c.height;var elev=it.elevationFt||D3.LOFT_ELEV;// Phase 5 field, D3 fallback
var cx=ftX(it.x),cz=ftZ(it.y);var lg=new THREE.Group();lg.userData={itemId:it.id,floorItem:true};var plat=box(mat(D3_COLORS.loft),w,D3.LOFT_T,d);plat.position.set(cx,elev-D3.LOFT_T/2,cz);lg.add(plat);var ph=elev-D3.LOFT_T;[[-1,-1],[1,-1],[-1,1],[1,1]].forEach(function(sgn){var post=box(mat(D3_COLORS.bench),0.28,ph,0.28);post.position.set(cx+sgn[0]*(w/2-0.25),ph/2,cz+sgn[1]*(d/2-0.25));lg.add(post);});interiorGroup.add(lg);}else if(it.type==="workbench"){var _w=it.widthFt||c.width,_d=it.heightFt||c.height;var g=new THREE.Group();var benchMat=mat(D3_COLORS.bench);var top=box(benchMat,_w,0.22,_d);top.position.y=D3.BENCH_H-0.11;g.add(top);[[-1,-1],[1,-1],[-1,1],[1,1]].forEach(function(sgn){var leg=box(benchMat,0.18,D3.BENCH_H-0.22,0.18);leg.position.set(sgn[0]*(_w/2-0.15),(D3.BENCH_H-0.22)/2,sgn[1]*(_d/2-0.15));g.add(leg);});if(it.rotation===90)g.rotation.y=Math.PI/2;g.position.set(ftX(it.x),0,ftZ(it.y));g.userData={itemId:it.id,floorItem:true};interiorGroup.add(g);}else if(it.type==="ramp"){var wf=WALLS[it.wall];if(!wf)return;var _w2=it.widthFt||3;var along=wf.U[0]?(it.x-mgX)/scale:(it.y-mgY)/scale;var run=3,drop=D3.FLOOR_T;var _g3=new THREE.Group();var deck=box(mat(D3_COLORS.ramp),_w2,0.12,Math.sqrt(run*run+drop*drop));deck.rotation.x=Math.atan2(drop,run);// far end drops to grade
deck.position.set(0,-drop/2+0.06,run/2);_g3.add(deck);_g3.rotation.y=Math.atan2(wf.N[0],wf.N[1]);// local +z → exterior normal
_g3.position.set(wf.O[0]+wf.U[0]*along+wf.N[0]*(T/2),0,wf.O[1]+wf.U[1]*along+wf.N[1]*(T/2));interiorGroup.add(_g3);}// textNote / line: 2D annotations with no 3D representation (plan §4.7).
});};buildInterior(items);root.add(wallsGroup);root.add(openingsGroup);root.add(roofGroup);root.add(interiorGroup);// Sun shadows (SmartBuild's "Show Shadows"): solid building meshes cast,
// the grass receives. Transparent fills (glass) don't cast; labels neither.
var setShadowFlags=function setShadowFlags(grp){return grp.traverse(function(o){if(o.isMesh){o.castShadow=!(o.material&&o.material.transparent);o.receiveShadow=false;}});};setShadowFlags(root);envGroup.traverse(function(o){if(o.isMesh)o.castShadow=false;});ground.receiveShadow=true;floor.receiveShadow=true;// Scoped rebuilds for the live drag. sharedMats = the model-lifetime wall and
// trim materials that per-wall disposal must keep (their maps ride along, so
// the siding texture survives too). builtFrontWall lets the flush detect a
// FRONT flip, which needs the full path (roof + ground labels re-home).
var sharedMats=new Set([wallMat,trimMat]);var model={root:root,envGroup:envGroup,wallMat:wallMat,trimMat:trimMat,gableMat:gableMat,roofGroup:roofGroup,openingsGroup:openingsGroup,wallsGroup:wallsGroup,interiorGroup:interiorGroup,builtFrontWall:frontWall};model.rebuildWalls=function(names,itemsNow){names.forEach(function(wname){if(!WALLS[wname])return;var oldWg=wallsGroup.children.find(function(g){return g.userData&&g.userData.wall===wname;});if(oldWg){wallsGroup.remove(oldWg);disposeSubtree(oldWg,sharedMats);}openingsGroup.children.filter(function(g){return g.userData&&g.userData.wall===wname;}).forEach(function(og){openingsGroup.remove(og);disposeSubtree(og,sharedMats);});var built=buildOneWall(wname,itemsNow);setShadowFlags(built.wg);wallsGroup.add(built.wg);built.ogs.forEach(function(og){setShadowFlags(og);openingsGroup.add(og);});});};model.rebuildInterior=function(itemsNow){Array.from(interiorGroup.children).forEach(function(g){interiorGroup.remove(g);disposeSubtree(g,null);});buildInterior(itemsNow);setShadowFlags(interiorGroup);};return model;}// Forget pending fixture-photo binds whose materials were just disposed — a
// scoped drag rebuild otherwise leaves one dead bind per rebuilt frame while a
// photo is still in flight, and the arriving photo would write into thousands
// of disposed materials.
function d3UnbindFixtureMats(mats){_d3FxMatBinds.forEach(function(set,entry){set.forEach(function(b){if(mats.has(b.mat))set["delete"](b);});if(set.size===0)_d3FxMatBinds["delete"](entry);});}// Free one subtree's geometries and owned materials. `sharedMats` (a Set or
// null) names materials owned by the model as a whole — wall/trim during a
// per-wall rebuild — that must survive; shared fixture-photo maps are skipped
// the same way disposeShed3DModel always has (ssShared).
function disposeSubtree(obj,sharedMats){var mats=new Set();obj.traverse(function(o){if(o.geometry)o.geometry.dispose();if(o.material)(Array.isArray(o.material)?o.material:[o.material]).forEach(function(m){return mats.add(m);});});if(sharedMats)sharedMats.forEach(function(m){return mats["delete"](m);});// Fixture photo textures are shared across every rebuild (see d3FixtureTexture)
// — disposing one here would blank every catalog door on the first drag frame.
// Bump maps are per-build mints like color maps and dispose with them.
mats.forEach(function(m){if(m.map&&!(m.map.userData&&m.map.userData.ssShared))m.map.dispose();if(m.bumpMap&&!(m.bumpMap.userData&&m.bumpMap.userData.ssShared))m.bumpMap.dispose();m.dispose();});d3UnbindFixtureMats(mats);}// Free every geometry/material a buildShed3DModel() group holds — Three.js
// doesn't GC WebGL resources, and drag-to-move rebuilds the model live.
function disposeShed3DModel(model){disposeSubtree(model.root,null);}// Full-screen orbitable 3D view of the current design. Mounted only while open:
// builds the scene on mount, disposes every geometry/material on unmount (Three
// doesn't GC WebGL resources). Renders on demand — no animation loop, so an idle
// scene costs zero GPU. Calls onSnapshot({ url, w, h }) when the customer
// captures a view — and automatically on close if they never did — so the
// submit flow can add the 3D page to the quote PDF.
function Structure3DViewer(_ref7){var bldgW=_ref7.bldgW,bldgH=_ref7.bldgH,items=_ref7.items,itemTypes=_ref7.itemTypes,styleValue=_ref7.styleValue,painted=_ref7.painted,paintBody=_ref7.paintBody,paintTrim=_ref7.paintTrim,frontWall=_ref7.frontWall,scale=_ref7.scale,mgX=_ref7.mgX,mgY=_ref7.mgY,accent=_ref7.accent,style3d=_ref7.style3d,roofType=_ref7.roofType,roofColorHex=_ref7.roofColorHex,fixtures=_ref7.fixtures,paletteKeys=_ref7.paletteKeys,placeableDoors=_ref7.placeableDoors,placeableWindows=_ref7.placeableWindows,placeableRamps=_ref7.placeableRamps,paintEnabled=_ref7.paintEnabled,onPaintChange=_ref7.onPaintChange,onWallHeight=_ref7.onWallHeight,onItemAdd=_ref7.onItemAdd,onItemMove=_ref7.onItemMove,onItemDelete=_ref7.onItemDelete,onItemSelect=_ref7.onItemSelect,onSnapshot=_ref7.onSnapshot,onClose=_ref7.onClose;var canvasRef=useRef(null);var wrapRef=useRef(null);var engineRef=useRef(null);var capturedRef=useRef(false);var _useState11=useState("loading"),_useState12=_slicedToArray(_useState11,2),phase=_useState12[0],setPhase=_useState12[1];// loading | ready | error
var _useState13=useState(false),_useState14=_slicedToArray(_useState13,2),interior=_useState14[0],setInterior=_useState14[1];// SmartBuild-style view options: roof on/off, landscape (grass/sky/labels)
// on/off, and the 3×3 camera-preset popover.
var _useState15=useState(true),_useState16=_slicedToArray(_useState15,2),roofOn=_useState16[0],setRoofOn=_useState16[1];var _useState17=useState(true),_useState18=_slicedToArray(_useState17,2),envOn=_useState18[0],setEnvOn=_useState18[1];var _useState19=useState(false),_useState20=_slicedToArray(_useState19,2),viewsOpen=_useState20[0],setViewsOpen=_useState20[1];var _useState21=useState(false),_useState22=_slicedToArray(_useState21,2),shotTaken=_useState22[0],setShotTaken=_useState22[1];var _useState23=useState(false),_useState24=_slicedToArray(_useState23,2),closing=_useState24[0],setClosing=_useState24[1];// close-time auto-capture in progress (photo wait + readback can take ~2s)
// Armed palette tool for placing new wall items in 3D (§10.4). Mirrored into
// a ref so the native pointer handlers read the current value.
var _useState25=useState(null),_useState26=_slicedToArray(_useState25,2),tool3=_useState26[0],setTool3=_useState26[1];var tool3Ref=useRef(null);useEffect(function(){tool3Ref.current=tool3;var c=canvasRef.current;if(c)c.style.cursor=tool3?"crosshair":"";},[tool3]);// In-viewer catalog chooser (door/window/ramp picker tools). The 2D picker
// modals live outside the 3D modal's liveItems world, so choosing there
// could not rebuild this scene — the 3D flow keeps its own small sheet and
// commits through the same onItemAdd pipeline as every other placement.
var _useState27=useState(null),_useState28=_slicedToArray(_useState27,2),pick3=_useState28[0],setPick3=_useState28[1];// { kind: door|window|ramp, wall, ptx, pty, door }
// Refusal feedback (the 2D toasts' 3D counterpart) — shown in the hint slot.
var _useState29=useState(null),_useState30=_slicedToArray(_useState29,2),msg3=_useState30[0],setMsg3=_useState30[1];// The item selected by tapping in 3D — drives the footer's Remove button.
// { id, label } or null; kept in step with every onItemSelect report.
var _useState31=useState(null),_useState32=_slicedToArray(_useState31,2),sel3d=_useState32[0],setSel3d=_useState32[1];// Paint picker selection; labels flow into paintColors (same free-text
// semantics as the 2D paint inputs).
var _useState33=useState({body:painted?paintBody||"":"",trim:painted?paintTrim||"":""}),_useState34=_slicedToArray(_useState33,2),paintSel=_useState34[0],setPaintSel=_useState34[1];// Customer wall-height pick (IdeaRoom's wall-raise, per Carolyn 2026-07-02).
// Rebuilds the model live and commits to sel.wallHeight via onWallHeight.
var _useState35=useState(style3d&&style3d.wallHeightFt||D3.WALL_H),_useState36=_slicedToArray(_useState35,2),wallHSel=_useState36[0],setWallHSel=_useState36[1];var pickWallHeight=function pickWallHeight(h){setWallHSel(h);var e=engineRef.current;if(e&&e.setWallHeight)e.setWallHeight(h);if(onWallHeight)onWallHeight(h);capturedRef.current=false;// height change makes any earlier shot stale
setShotTaken(false);};var pickColor=function pickColor(kind,label){var next=kind==="none"?{body:"",trim:""}:_objectSpread(_objectSpread({},paintSel),{},_defineProperty({},kind,label));setPaintSel(next);var e=engineRef.current;if(e&&e.setLiveColors)e.setLiveColors(next.body,next.trim);if(onPaintChange)onPaintChange(next);capturedRef.current=false;// color change makes any earlier shot stale
setShotTaken(false);};useEffect(function(){var disposed=false;loadThree().then(function(bundle){var THREE=bundle.THREE,OrbitControls=bundle.OrbitControls;if(disposed||!canvasRef.current)return;// preserveDrawingBuffer stays OFF: it taxes every frame on tile-based
// mobile GPUs, and the only read-back — capture() — renders explicitly
// in the same task before toDataURL, which the spec guarantees is valid.
var renderer=new THREE.WebGLRenderer({canvas:canvasRef.current,antialias:true,preserveDrawingBuffer:false});// DPR policy: full sharpness at rest, capped resolution while a finger
// is down on a coarse-pointer device. A DPR-3 phone clamped to 2 still
// pushes ~1.3M pixels through MSAA on this full-screen canvas; during an
// orbit/drag nobody can see the extra sharpness, and the release renders
// one crisp frame at base DPR. Desktop pointers never change.
var baseDpr=Math.min(window.devicePixelRatio||1,2);var coarsePointer=typeof window.matchMedia==="function"&&window.matchMedia("(pointer: coarse)").matches;var interactDpr=Math.min(baseDpr,1.25);renderer.setPixelRatio(baseDpr);var setInteractDpr=function setInteractDpr(on){if(!coarsePointer||interactDpr===baseDpr)return;var want=on?interactDpr:baseDpr;if(renderer.getPixelRatio()!==want){renderer.setPixelRatio(want);if(!on)render();}};renderer.shadowMap.enabled=true;// SmartBuild-style sun shadows
renderer.shadowMap.type=THREE.PCFSoftShadowMap;// Filmic tone mapping is what lets the PBR materials read as real
// surfaces — flat Linear output was a big part of the "toy render" look.
renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.15;// The sun is a fixed directional light with a fixed ortho shadow camera,
// so the shadow map is VIEW-INDEPENDENT: orbiting re-renders the same
// depth map for nothing (a full 2048² PCFSoft pass per pointermove).
// Render it only when the scene actually changes - every mutation path
// funnels through queueRebuild's flush or applyShellMode, which set
// needsUpdate; setLiveColors doesn't (paint never moves geometry).
renderer.shadowMap.autoUpdate=false;renderer.shadowMap.needsUpdate=true;var scene=new THREE.Scene();scene.background=new THREE.Color("#E7EEF5");// Per-style appearance (roof profile/pitch/overhang, siding relief,
// natural material colors, wall height) resolved by the parent from the
// tenant's config + built-in defaults.
var spec=style3d||{roof:D3_DEFAULT_ROOF,siding:null,colors:{},wallHeightFt:0};// Live paint colors — swatch picks update these and recolor materials in
// place; drag rebuilds read the same vars so colors survive rebuilds.
// Unpainted falls back to the STYLE's natural material colors.
var liveBodyCss=painted?d3SwatchCss(paintBody,D3_COLORS.body):spec.colors.body||D3_COLORS.body;var liveTrimCss=painted?d3SwatchCss(paintTrim,D3_COLORS.trim):spec.colors.trim||D3_COLORS.trim;var roofCss=roofColorHex||spec.colors.roof||D3_COLORS.roof;var model=d3TimedBuild(function(){return buildShed3DModel(THREE,{bldgW:bldgW,bldgH:bldgH,wallHeightFt:spec.wallHeightFt,styleSpec:spec,roofColor:roofCss,roofType:roofType,items:items,itemTypes:itemTypes,bodyColor:liveBodyCss,trimColor:liveTrimCss,frontWall:frontWall,scale:scale,mgX:mgX,mgY:mgY,fixtures:fixtures});});scene.add(model.root);// Sky-tinted fill + warm sun: under ACES, white-on-white lighting reads
// as overcast plastic; a blue-ish ambient with a warm key is what makes
// painted siding and shingle relief look sunlit.
scene.add(new THREE.HemisphereLight(0xDCE9FF,0x8D8573,1.5));// Start the camera on the FRONT side (same wall the 2D labels call FRONT),
// nudged sideways for a three-quarter view; the sun follows the camera side.
var fw=frontWall||"south";var OUT={north:[0.35,-1],south:[0.35,1],west:[-1,0.35],east:[1,0.35]}[fw]||[0.35,1];var R=Math.max(bldgW,bldgH)*0.5+D3.WALL_H;// 34° lens (was 45°): the long-lens architectural look — less perspective
// distortion on the box, matching how buildings are photographed. dist
// carries the tan(22.5°)/tan(17°) ≈ 1.355 factor so framing is unchanged,
// and everything sized off dist (sky dome, far plane, orbit limits, sun)
// scales with it.
var dist=R*3.66;var outLen=Math.sqrt(OUT[0]*OUT[0]+OUT[1]*OUT[1]);var camX=OUT[0]/outLen*dist,camZ=OUT[1]/outLen*dist;var sun=new THREE.DirectionalLight(0xFFF3E0,2.6);sun.position.set(camX*0.8,dist*0.9,camZ*0.8);// Shadow camera sized to the building footprint (grass receives the shadow).
sun.castShadow=true;sun.shadow.mapSize.width=sun.shadow.mapSize.height=2048;var shR=Math.max(bldgW,bldgH)*1.6+8;sun.shadow.camera.left=-shR;sun.shadow.camera.right=shR;sun.shadow.camera.top=shR;sun.shadow.camera.bottom=-shR;sun.shadow.camera.near=0.5;sun.shadow.camera.far=dist*4;sun.shadow.bias=-0.0005;// Ortho shadow cameras bake their projection at construction — without
// this the bounds above are ignored and shadows clip to a ~10ft box.
sun.shadow.camera.updateProjectionMatrix();scene.add(sun);// far covers the sky dome even at controls.maxDistance (2.5d + 6.5d < 10d).
var camera=new THREE.PerspectiveCamera(34,1,0.1,dist*10);camera.position.set(camX,dist*0.5,camZ);// Sky dome: gradient + soft clouds on the inside of a hemisphere, slightly
// past the horizon so no gap shows between grass edge and sky. Hidden by
// the Landscape toggle along with the model's envGroup.
var skyTex=d3MakeSkyTexture(THREE);var sky=skyTex?new THREE.Mesh(new THREE.SphereGeometry(dist*6.5,32,16,0,Math.PI*2,0,Math.PI/2+0.14),new THREE.MeshBasicMaterial({map:skyTex,side:THREE.BackSide,depthWrite:false})):null;if(sky){sky.position.y=-0.5;scene.add(sky);}// SmartBuild-style camera presets (3×3 grid), azimuth relative to the
// FRONT wall so "F" always faces the side the 2D labels call FRONT.
var setViewPreset=function setViewPreset(relDeg,polDeg){var e=engineRef.current;if(!e)return;var t=e.controls.target;var r=e.camera.position.distanceTo(t)||dist;// Front azimuth resolved at CALL time — dragging the only door to a
// different wall re-homes the front (roof + labels already follow).
var liveFw=getFrontWall(liveItems)||fw;var az=({south:0,north:Math.PI,east:Math.PI/2,west:-Math.PI/2}[liveFw]||0)+relDeg*Math.PI/180;var phi=(90-polDeg)*Math.PI/180;// measured from straight up
e.camera.position.set(t.x+r*Math.sin(phi)*Math.sin(az),t.y+r*Math.cos(phi),t.z+r*Math.sin(phi)*Math.cos(az));e.camera.lookAt(t);e.controls.update();e.render();};// ── Drag-to-move wall items (first slice of Phase 6, plan §10.5) ──
// Doors/windows/rough openings drag along the walls exactly like the 2D
// drag: the pointer ray becomes page coordinates and runs the SAME module
// functions the 2D uses (getWallFromClick → getNearestWall → snapToWall),
// wall switching included; like 2D, no collision check while moving. The
// final position commits through onItemMove so the 2D plan, saved design,
// and estimate all see the move. The pointerdown handler is registered
// BEFORE OrbitControls is constructed, so stopImmediatePropagation keeps
// a grab on an item from also starting an orbit.
var canvas=canvasRef.current;var pWpx=bldgW*scale,pHpx=bldgH*scale;var liveItems=items;var dragging3=null;// { id, moved }
var lastHoverId=null;var rebuildScope=null;// accumulated for the next frame: { full, walls:Set, interior }
var canvasRect=null;// getBoundingClientRect cache - a layout read per pointermove forces layout of the whole page (the 2D SVG included)
var lastSentSelect=null;// last item id reported via onItemSelect - a no-op tap shouldn't re-render the parent
var raycaster=new THREE.Raycaster();var ndc=new THREE.Vector2();var dragPlane=new THREE.Plane();var dragHit=new THREE.Vector3();var highlight=new THREE.LineSegments(new THREE.EdgesGeometry(new THREE.BoxGeometry(1,1,1)),new THREE.LineBasicMaterial({color:0xFBBF24}));highlight.visible=false;scene.add(highlight);var applyShellMode=function applyShellMode(e){e.model.roofGroup.visible=!e.interior&&e.roofOn!==false;if(e.model.envGroup)e.model.envGroup.visible=e.envOn!==false;if(e.sky)e.sky.visible=e.envOn!==false;// Ghosted walls must stop casting shadows too — 14%-opacity walls
// throwing full-dark shadows reads as a rendering bug.
e.model.wallsGroup.traverse(function(o){if(o.isMesh)o.castShadow=!e.interior;});e.model.wallMat.transparent=!!e.interior;e.model.wallMat.opacity=e.interior?0.14:1;e.model.wallMat.depthWrite=!e.interior;e.model.wallMat.needsUpdate=true;if(e.model.gableMat){// The gable/end faces carry their own material (UV mismatch with the
// wall texture) but must ghost in step with the walls.
e.model.gableMat.transparent=!!e.interior;e.model.gableMat.opacity=e.interior?0.14:1;e.model.gableMat.depthWrite=!e.interior;e.model.gableMat.needsUpdate=true;}// Every caller changed what casts or shows shadow (rebuild flush, roof
// toggle, look-inside) - re-render the cached shadow map next frame.
e.renderer.shadowMap.needsUpdate=true;};var setRay=function setRay(ev){// The modal is position:fixed inset:0, so the rect only moves on a
// viewport resize - resize() invalidates, pointerdown re-primes.
if(!canvasRect)canvasRect=canvas.getBoundingClientRect();var rc=canvasRect;ndc.x=(ev.clientX-rc.left)/rc.width*2-1;ndc.y=-((ev.clientY-rc.top)/rc.height)*2+1;raycaster.setFromCamera(ndc,camera);};// Pick a movable item: wall openings (doors/windows/ROs) or floor items
// (lofts/workbenches). Ramps carry no itemId tag on purpose — 2D can't
// drag them either.
var pickItem3=function pickItem3(ev){var e=engineRef.current;if(!e)return null;setRay(ev);var hits=raycaster.intersectObjects([e.model.openingsGroup,e.model.interiorGroup],true);var _loop4=function _loop4(){var n=hits[h].object;while(n&&!(n.userData&&n.userData.itemId))n=n.parent;if(n){var it=liveItems.find(function(i){return i.id===n.userData.itemId;});if(it)return{v:it};}},_ret3;for(var h=0;h<hits.length;h++){_ret3=_loop4();if(_ret3)return _ret3.v;}return null;};// Pick a wall (for the 3D placement palette).
var pickWall3=function pickWall3(ev){var e=engineRef.current;if(!e)return null;setRay(ev);var hits=raycaster.intersectObjects([e.model.wallsGroup],true);for(var h=0;h<hits.length;h++){var n=hits[h].object;while(n&&!(n.userData&&n.userData.wall))n=n.parent;if(n)return{wall:n.userData.wall,point:hits[h].point};}return null;};// Vertical extent of an opening — item-stamped fields first (Phase 5), then a
// catalog fixture's own heightIn, then D3 defaults for legacy items. Mirrors
// the builder's openingSpan, INCLUDING its clamps, so drag highlights track
// a wall-height pick (spec.wallHeightFt is mutated live by setWallHeight).
var openSpanOf=function openSpanOf(it){var inchesFt=function inchesFt(v){return Number(v)>0?Number(v)/12:null;};var maxTop=(spec.wallHeightFt||D3.WALL_H)-0.2;if(it.type==="window"){var wh=Math.min(it.openingHeightFt||inchesFt(it.heightIn)||D3.WINDOW_H,maxTop-0.35);var s0=it.sillFt!=null?it.sillFt:D3.WINDOW_SILL;if(s0+wh>maxTop)s0=Math.max(0.35,maxTop-wh);return[s0,s0+wh];}if(it.type==="roughOpening")return[0,Math.min(it.openingHeightFt||D3.RO_H,maxTop)];return[0,Math.min(it.openingHeightFt||inchesFt(it.heightIn)||D3.DOOR_H,maxTop)];};var placeHighlight=function placeHighlight(it){var c=itemTypes[it.type]||{};if(c.wallOnly){var _w3=it.widthFt||c.width||3;var ns=it.wall==="north"||it.wall==="south";var len=ns?bldgW:bldgH;var along=ns?(it.x-mgX)/scale:(it.y-mgY)/scale;var a=Math.max(_w3/2,Math.min(along,len-_w3/2))-len/2;var sp=openSpanOf(it);highlight.scale.set(_w3+0.3,sp[1]-sp[0]+0.3,D3.WALL_T+0.4);highlight.rotation.y=ns?0:Math.PI/2;highlight.position.set(ns?a:it.wall==="west"?-bldgW/2:bldgW/2,(sp[0]+sp[1])/2,ns?it.wall==="north"?-bldgH/2:bldgH/2:a);return;}// Floor items: outline the loft platform / workbench body.
var w=it.widthFt||c.width||4;var d0=it.heightFt||c.height||2;var rot=it.rotation===90;var cx=(it.x-mgX)/scale-bldgW/2;var cz=(it.y-mgY)/scale-bldgH/2;highlight.rotation.y=0;if(it.type==="loft"){var elev=it.elevationFt||D3.LOFT_ELEV;highlight.scale.set(w+0.3,D3.LOFT_T+0.3,d0+0.3);highlight.position.set(cx,elev-D3.LOFT_T/2,cz);}else{highlight.scale.set((rot?d0:w)+0.3,D3.BENCH_H+0.3,(rot?w:d0)+0.3);highlight.position.set(cx,D3.BENCH_H/2,cz);}};// Queue a rebuild for the next frame, coalescing scopes: a drag passes
// { walls: [...] } / { interior: true } and only that part of the model
// is torn down and rebuilt (~a tenth of the full cost, which used to run
// EVERY drag frame). No argument = the full path — wall height, palette
// placement, and anything else structural. A FRONT flip is detected at
// flush time and upgrades a scoped rebuild to full, because the roof and
// ground labels re-home with the front.
var queueRebuild=function queueRebuild(scope){var full=!scope||scope.full===true;if(rebuildScope){if(full)rebuildScope.full=true;else{(scope.walls||[]).forEach(function(w){return rebuildScope.walls.add(w);});if(scope.interior)rebuildScope.interior=true;}return;}rebuildScope={full:full,walls:new Set(full?[]:scope.walls||[]),interior:!full&&Boolean(scope.interior)};requestAnimationFrame(function(){var sc=rebuildScope;rebuildScope=null;var e=engineRef.current;if(!e||!sc)return;// Recompute FRONT from the live items — dragging the only door to a
// different wall re-orients the roof exactly like the 2D labels.
var nf=getFrontWall(liveItems)||frontWall;if(sc.full||nf!==e.model.builtFrontWall){scene.remove(e.model.root);disposeShed3DModel(e.model);e.model=d3TimedBuild(function(){return buildShed3DModel(THREE,{bldgW:bldgW,bldgH:bldgH,wallHeightFt:spec.wallHeightFt,styleSpec:spec,roofColor:roofCss,roofType:roofType,items:liveItems,itemTypes:itemTypes,bodyColor:liveBodyCss,trimColor:liveTrimCss,frontWall:nf,scale:scale,mgX:mgX,mgY:mgY,fixtures:fixtures});});scene.add(e.model.root);}else{d3TimedBuild(function(){if(sc.walls.size)e.model.rebuildWalls(Array.from(sc.walls),liveItems);if(sc.interior)e.model.rebuildInterior(liveItems);});}applyShellMode(e);render();});};// Wall-height picks mutate the working spec and rebuild — walls, openings
// and roof all recompute from the new plate height.
var setWallHeight=function setWallHeight(h){spec.wallHeightFt=h||0;queueRebuild();};// Swatch picks recolor the live materials in place — no rebuild needed.
// Clearing paint returns to the style's natural material colors.
var setLiveColors=function setLiveColors(bodyLabel,trimLabel){liveBodyCss=bodyLabel?d3SwatchCss(bodyLabel,D3_COLORS.body):spec.colors.body||D3_COLORS.body;liveTrimCss=trimLabel?d3SwatchCss(trimLabel,D3_COLORS.trim):spec.colors.trim||D3_COLORS.trim;var e=engineRef.current;if(!e)return;e.model.wallMat.color.set(liveBodyCss);if(e.model.gableMat)e.model.gableMat.color.set(liveBodyCss);e.model.trimMat.color.set(liveTrimCss);render();};// ── 3D placement pipeline (every item class, §10.4) ──
// Each branch below is the 2D handleClick branch for that class, fed
// page coordinates from the 3D ray instead of an SVG click: same module
// snap/collision functions, same stamped fields, committed through the
// same onItemAdd callback. Never invent a 3D-only placement rule here.
var flash3=function flash3(m){setMsg3(m);setTimeout(function(){return setMsg3(function(cur){return cur===m?null:cur;});},4000);};var itemLabel3=function itemLabel3(type){return itemTypes[type]&&(itemTypes[type].shortLabel||itemTypes[type].label)||type;};var commitPlaced3=function commitPlaced3(ni){liveItems=liveItems.concat([ni]);if(onItemAdd)onItemAdd(ni);if(onItemSelect){lastSentSelect=ni.id;onItemSelect(ni.id);}setSel3d({id:ni.id,label:itemLabel3(ni.type)});capturedRef.current=false;setShotTaken(false);setTool3(null);queueRebuild();};// Remove the selected item — same one-liner semantics as the 2D delSel,
// ramp cascade included (a deleted door takes its ramp along).
var deleteItem3=function deleteItem3(id){if(!id)return;liveItems=liveItems.filter(function(i){return i.id!==id&&!(i.type==="ramp"&&i.snapDoorId===id);});if(onItemDelete)onItemDelete(id);if(onItemSelect){lastSentSelect=null;onItemSelect(null);}setSel3d(null);capturedRef.current=false;setShotTaken(false);queueRebuild();};// Catalog door/window from the in-viewer chooser (also the included-chip
// path): the FIXTURE stamps are what the estimate prices from — placing
// these as their raw tool key was the bug that made 3D-placed included
// fixtures invisible to pricing.
var placeFixture3=function placeFixture3(fx,type,ptx,pty){var w=getWallFromClick(ptx,pty,pWpx,pHpx,mgX,mgY)||getNearestWall(ptx,pty,pWpx,pHpx,mgX,mgY);var widthFt=(Number(fx.widthIn)||(type==="window"?24:36))/12;var sn=snapToWall(w,ptx,pty,widthFt*scale,0.5*scale,pWpx,pHpx,mgX,mgY);var ni;if(type==="window"){ni=_objectSpread(_objectSpread({id:idCounter++,type:"window"},sn),{},{widthFt:widthFt,heightFt:0.5,fixtureItemId:fx.id,windowName:fx.name||"Window",planLabel:fx.planLabel&&String(fx.planLabel).trim()||(fx.name||"WIN").toUpperCase().slice(0,6),price:fx.price!=null?fx.price:null,widthIn:Number(fx.widthIn)||null,heightIn:Number(fx.heightIn)||null});}else{var swing=fx.swingDefault||(fx.swingOut?"out":fx.swingIn?"in":null);var operation=fx.opDefault||(fx.opDouble?"double":fx.opSlideUp?"slideup":fx.opRight?"right":fx.opLeft?"left":null);ni=_objectSpread(_objectSpread({id:idCounter++,type:"fixtureDoor"},sn),{},{widthFt:widthFt,heightFt:0.5,fixtureItemId:fx.id,doorName:fx.name||"Door",planLabel:fx.planLabel&&String(fx.planLabel).trim()||(fx.name||"DOOR").toUpperCase().slice(0,6),price:fx.price!=null?fx.price:null,widthIn:Number(fx.widthIn)||null,heightIn:Number(fx.heightIn)||null,swing:swing,operation:operation});}if(checkDoorCollision(ni,{width:widthFt},liveItems,itemTypes,scale)){flash3("Something's already there — pick a different spot on the wall.");return false;}commitPlaced3(ni);return true;};var placeRamp3=function placeRamp3(fx,door){var rampDepth=(Number(fx.heightIn)||0)/12||2;// 2 = RAMP_SPACE_FT
var rp=rampPlacementForDoor(door,rampDepth,pWpx,pHpx,mgX,mgY,scale);if(!rp)return false;var ni=_objectSpread(_objectSpread({id:idCounter++,type:"ramp"},rp),{},{widthFt:(Number(fx.widthIn)||36)/12,heightFt:rampDepth,snapDoorId:door.id,fixtureItemId:fx.id,rampName:fx.name||"Ramp",planLabel:fx.planLabel&&String(fx.planLabel).trim()||(fx.name||"RAMP").toUpperCase().slice(0,6),price:fx.price!=null?fx.price:null,widthIn:Number(fx.widthIn)||null,heightIn:Number(fx.heightIn)||null});commitPlaced3(ni);return true;};var place3=function place3(tool,cfg,pageX,pageY){// Same dispatch order as 2D handleClick: pickers → included chips →
// doorSnap → wallSnap → loft → wallOnly built-ins.
if(cfg.isDoorPicker||cfg.isWindowPicker){var w=getWallFromClick(pageX,pageY,pWpx,pHpx,mgX,mgY)||getNearestWall(pageX,pageY,pWpx,pHpx,mgX,mgY);setPick3({kind:cfg.isDoorPicker?"door":"window",wall:w,ptx:pageX,pty:pageY});setTool3(null);return;}if(cfg.includedFixture&&!cfg.doorSnap){placeFixture3(cfg.includedFixture,cfg.includedFixture.category==="window"?"window":"fixtureDoor",pageX,pageY);return;}// Annotation tools — same stamps as the 2D branches; the page-bound
// clamps approximate PAGE_W/TEXT_BAND_TOP (page-layout values the
// viewer doesn't carry) closely enough to keep the item on the page.
if(cfg.noteType){commitPlaced3({id:idCounter++,type:tool,x:Math.max(20,Math.min(pageX,mgX*2+pWpx-20)),y:Math.max(20,Math.min(pageY,mgY+pHpx+40)),rotation:0,wall:null,widthPx:160,heightPx:40,text:"Note"});return;}if(cfg.lineType){var halfLenPx=(cfg.width||4)/2*scale;var cx=Math.max(20,Math.min(pageX,mgX*2+pWpx-20));var cy=Math.max(20,Math.min(pageY,mgY+pHpx+40));commitPlaced3({id:idCounter++,type:tool,wall:null,x1:Math.max(0,cx-halfLenPx),y1:cy,x2:Math.min(mgX*2+pWpx,cx+halfLenPx),y2:cy});return;}if(cfg.doorSnap){var doors=liveItems.filter(function(i){return i.type==="singleDoor"||i.type==="doubleDoor"||i.type==="fixtureDoor";});if(!doors.length){flash3("Place a door first, then add a ramp to it.");return;}var closest=null,minDist=Infinity;doors.forEach(function(d){var dx=pageX-d.x,dy=pageY-d.y;var dist=Math.sqrt(dx*dx+dy*dy);if(dist<minDist){minDist=dist;closest=d;}});if(!closest)return;if(liveItems.find(function(i){return i.type==="ramp"&&i.snapDoorId===closest.id;})){flash3("This door already has a ramp. Delete it first to replace.");return;}if(cfg.isRampPicker){setPick3({kind:"ramp",door:closest});setTool3(null);return;}if(cfg.includedFixture){placeRamp3(cfg.includedFixture,closest);return;}var doorCfg=itemTypes[closest.type];var rampDepth=2;// RAMP_SPACE_FT — visual run out from the door
var rp=rampPlacementForDoor(closest,rampDepth,pWpx,pHpx,mgX,mgY,scale);if(!rp)return;commitPlaced3(_objectSpread(_objectSpread({id:idCounter++,type:tool},rp),{},{widthFt:closest.widthFt||(doorCfg?doorCfg.width:3),heightFt:rampDepth,snapDoorId:closest.id}));return;}if(cfg.wallSnap){var nw=getNearestWall(pageX,pageY,pWpx,pHpx,mgX,mgY);var _sn2=snapToWallInterior(nw,pageX,pageY,cfg.width*scale,cfg.height*scale,pWpx,pHpx,mgX,mgY);var candidate=_objectSpread(_objectSpread({id:idCounter,type:tool},_sn2),{},{widthFt:cfg.width,heightFt:cfg.height});if(checkDoorCollision(candidate,cfg,liveItems,itemTypes,scale)){flash3("A door is blocking this wall — try a different wall, or move the door first.");return;}var isH=_sn2.wall==="north"||_sn2.wall==="south";var candPos=isH?_sn2.x:_sn2.y;var candHalf=cfg.width*scale/2;for(var i=0;i<liveItems.length;i++){var ob=liveItems[i];if(ob.type!=="workbench"||ob.wall!==_sn2.wall)continue;var obW=(ob.widthFt||itemTypes[ob.type].width)*scale/2;var obPos=isH?ob.x:ob.y;if(Math.abs(candPos-obPos)<candHalf+obW-2){flash3("Another workbench is in the way. Try a different spot on the wall.");return;}}idCounter++;// only burned on success, like 2D
commitPlaced3(candidate);return;}if(tool==="loft"){// Auto-span wall-to-wall at the clicked position — the 2D rule.
var loftH=cfg.height||4;var cyFtRound=Math.max(loftH/2,Math.min(Math.round((pageY-mgY)/scale),bldgH-loftH/2));var nL=0,nR=bldgW,nT=cyFtRound-loftH/2,nB=cyFtRound+loftH/2;var otherLofts=liveItems.filter(function(i){return i.type==="loft";});for(var _i18=0;_i18<otherLofts.length;_i18++){var o=otherLofts[_i18];var ow=(o.widthFt||cfg.width)/2,oh=(o.heightFt||cfg.height)/2;var ocx=(o.x-mgX)/scale,ocy=(o.y-mgY)/scale;if(nL<ocx+ow-0.1&&nR>ocx-ow+0.1&&nT<ocy+oh-0.1&&nB>ocy-oh+0.1){flash3("Can't place a loft overlapping another loft — click a different spot.");return;}}commitPlaced3({id:idCounter++,type:"loft",x:mgX+bldgW/2*scale,y:mgY+cyFtRound*scale,rotation:0,wall:null,widthFt:bldgW,heightFt:loftH,elevationFt:D3.LOFT_ELEV});return;}// wallOnly built-ins (config singleDoor/doubleDoor/window/roughOpening)
var w2=getWallFromClick(pageX,pageY,pWpx,pHpx,mgX,mgY)||getNearestWall(pageX,pageY,pWpx,pHpx,mgX,mgY);var sn=snapToWall(w2,pageX,pageY,cfg.width*scale,cfg.height*scale,pWpx,pHpx,mgX,mgY);var cand=_objectSpread(_objectSpread({id:-1,type:tool},sn),{},{widthFt:cfg.width,heightFt:cfg.height});if(checkDoorCollision(cand,cfg,liveItems,itemTypes,scale)){flash3("Something is already on that spot. Pick a clear part of the wall.");return;}if(checkWorkbenchOverlap(sn,cfg.width*scale,liveItems,itemTypes,scale)){flash3("A workbench is on that wall — place this somewhere else on the wall.");return;}commitPlaced3(_objectSpread(_objectSpread({id:idCounter++,type:tool},sn),{},{widthFt:cfg.width,heightFt:cfg.height},d3OpeningDefaults(tool)));};var onPtr3Down=function onPtr3Down(ev){if(ev.button!==undefined&&ev.button!==0)return;canvasRect=canvas.getBoundingClientRect();// re-prime the cache at gesture start
// Armed palette tool: click to place via place3 — wall items use the
// wall hit, floor items (workbench/loft/ramp) fall back to the ground
// plane, so every class the 2D palette offers places here too.
// Clicking the sky keeps the tool armed and lets the orbit through.
var tool=tool3Ref.current;if(tool){var cfg=itemTypes[tool];if(!cfg)return;var hitW=pickWall3(ev);var pageX=null,pageY=null;if(hitW){pageX=mgX+(hitW.point.x+bldgW/2)*scale;pageY=mgY+(hitW.point.z+bldgH/2)*scale;}else{setRay(ev);dragPlane.set(new THREE.Vector3(0,1,0),0);var p=raycaster.ray.intersectPlane(dragPlane,dragHit);var lim=Math.max(bldgW,bldgH)*4;if(p&&Math.abs(p.x)<=lim&&Math.abs(p.z)<=lim){pageX=mgX+(p.x+bldgW/2)*scale;pageY=mgY+(p.z+bldgH/2)*scale;}}if(pageX!==null){ev.stopImmediatePropagation();ev.preventDefault();place3(tool,cfg,pageX,pageY);}return;}var it=pickItem3(ev);if(!it){// An orbit is starting and hover raycasts pause while a button is
// held - drop any hover outline so it can't ride along stale.
if(lastHoverId!=null){lastHoverId=null;highlight.visible=false;render();}return;}ev.stopImmediatePropagation();ev.preventDefault();controls.enabled=false;dragging3={id:it.id,moved:false};setInteractDpr(true);lastHoverId=it.id;placeHighlight(it);highlight.visible=true;canvas.style.cursor="grabbing";try{canvas.setPointerCapture(ev.pointerId);}catch(_){/* synthetic pointer */}render();};// Apply a live position change during a drag: update the working items,
// move the outline, rebuild (throttled). `scope` names the wall(s)/
// interior the change touched so the rebuild stays scoped; omitted = full.
var commitLive=function commitLive(it,patch,scope){liveItems=liveItems.map(function(i){return i.id===it.id?_objectSpread(_objectSpread({},i),patch):i;});dragging3.moved=true;placeHighlight(liveItems.find(function(i){return i.id===it.id;}));queueRebuild(scope);};var onPtr3Move=function onPtr3Move(ev){if(dragging3){var it=liveItems.find(function(i){return i.id===dragging3.id;});if(!it)return;var c=itemTypes[it.type]||{};setRay(ev);if(c.wallOnly){// Doors/windows/ROs: pick against the horizontal plane at the
// opening's mid-height so the item lands on whichever wall the
// cursor is nearest — same feel as the 2D drag following the mouse.
var sp=openSpanOf(it);dragPlane.set(new THREE.Vector3(0,1,0),-((sp[0]+sp[1])/2));var p=raycaster.ray.intersectPlane(dragPlane,dragHit);var lim=Math.max(bldgW,bldgH)*4;if(!p||Math.abs(p.x)>lim||Math.abs(p.z)>lim){// Grazing angle — track against the item's current wall plane instead.
if(it.wall==="north")dragPlane.set(new THREE.Vector3(0,0,1),bldgH/2);else if(it.wall==="south")dragPlane.set(new THREE.Vector3(0,0,1),-bldgH/2);else if(it.wall==="west")dragPlane.set(new THREE.Vector3(1,0,0),bldgW/2);else dragPlane.set(new THREE.Vector3(1,0,0),-bldgW/2);p=raycaster.ray.intersectPlane(dragPlane,dragHit);if(!p)return;}var pageX=mgX+(p.x+bldgW/2)*scale;var pageY=mgY+(p.z+bldgH/2)*scale;var wFt=it.widthFt||c.width||3;var w=getWallFromClick(pageX,pageY,pWpx,pHpx,mgX,mgY)||getNearestWall(pageX,pageY,pWpx,pHpx,mgX,mgY);var sn=snapToWall(w,pageX,pageY,wFt*scale,(c.height||0.5)*scale,pWpx,pHpx,mgX,mgY);if(sn.x!==it.x||sn.y!==it.y||sn.wall!==it.wall){// The door's ramp follows live — same derived placement as 2D.
var ramp=liveItems.find(function(i){return i.type==="ramp"&&i.snapDoorId===it.id;});var rampMoved=false;if(ramp){var rp=rampPlacementForDoor(sn,ramp.heightFt,pWpx,pHpx,mgX,mgY,scale);if(rp){liveItems=liveItems.map(function(i){return i.id===ramp.id?_objectSpread(_objectSpread({},i),rp):i;});rampMoved=true;}}commitLive(it,sn,{walls:[it.wall,sn.wall],interior:rampMoved});}}else if(it.type==="workbench"){// Same rules as the 2D wallSnap drag: snap to the nearest wall's
// interior, blocked by doors and other benches on that wall.
dragPlane.set(new THREE.Vector3(0,1,0),-(D3.BENCH_H/2));var _p=raycaster.ray.intersectPlane(dragPlane,dragHit);if(!_p)return;var _pageX=mgX+(_p.x+bldgW/2)*scale;var _pageY=mgY+(_p.z+bldgH/2)*scale;var _wFt3=it.widthFt||c.width||6;var nw=getNearestWall(_pageX,_pageY,pWpx,pHpx,mgX,mgY);var _sn3=snapToWallInterior(nw,_pageX,_pageY,_wFt3*scale,(c.height||2)*scale,pWpx,pHpx,mgX,mgY);var others=liveItems.filter(function(i){return i.id!==it.id;});if(checkDoorCollision(_objectSpread(_objectSpread({},it),_sn3),_objectSpread(_objectSpread({},c),{},{width:_wFt3}),others,itemTypes,scale))return;var isH=_sn3.wall==="north"||_sn3.wall==="south";var candPos=isH?_sn3.x:_sn3.y;var candHalf=_wFt3*scale/2;for(var oi=0;oi<others.length;oi++){var ob=others[oi];if(ob.type!=="workbench"||ob.wall!==_sn3.wall)continue;var obW=(ob.widthFt||(itemTypes[ob.type]||{}).width||6)*scale/2;var obPos=isH?ob.x:ob.y;if(Math.abs(candPos-obPos)<candHalf+obW-2)return;}if(_sn3.x!==it.x||_sn3.y!==it.y||_sn3.wall!==it.wall)commitLive(it,_sn3,{interior:true});}else if(it.type==="loft"){// Same rules as the 2D free drag: integer-foot rounding, wall +
// loft edge snapping, overlap reject; unattached positions are
// allowed mid-drag (the 2D banner warns about them).
var elev=it.elevationFt||D3.LOFT_ELEV;dragPlane.set(new THREE.Vector3(0,1,0),-elev);var _p2=raycaster.ray.intersectPlane(dragPlane,dragHit);if(!_p2)return;var _wFt4=it.widthFt||c.width||6,hFt=it.heightFt||c.height||4;var halfW=_wFt4/2,halfH=hFt/2;var snapFt=1;var cxFt=Math.round(_p2.x+bldgW/2);var cyFt=Math.round(_p2.z+bldgH/2);if(cxFt-halfW<snapFt)cxFt=halfW;else if(cxFt+halfW>bldgW-snapFt)cxFt=bldgW-halfW;if(cyFt-halfH<snapFt)cyFt=halfH;else if(cyFt+halfH>bldgH-snapFt)cyFt=bldgH-halfH;var otherLofts=liveItems.filter(function(i){return i.type==="loft"&&i.id!==it.id;});var l=cxFt-halfW,r=cxFt+halfW,t=cyFt-halfH,b=cyFt+halfH;for(var _oi=0;_oi<otherLofts.length;_oi++){var o=otherLofts[_oi];var oW=(o.widthFt||6)/2,oH=(o.heightFt||4)/2;var oCx=(o.x-mgX)/scale,oCy=(o.y-mgY)/scale;var oL=oCx-oW,oR=oCx+oW,oT=oCy-oH,oB=oCy+oH;if(t<oB&&b>oT){if(Math.abs(r-oL)<snapFt)cxFt=oL-halfW;else if(Math.abs(l-oR)<snapFt)cxFt=oR+halfW;}if(l<oR&&r>oL){if(Math.abs(b-oT)<snapFt)cyFt=oT-halfH;else if(Math.abs(t-oB)<snapFt)cyFt=oB+halfH;}l=cxFt-halfW;r=cxFt+halfW;t=cyFt-halfH;b=cyFt+halfH;}cxFt=Math.max(halfW,Math.min(cxFt,bldgW-halfW));cyFt=Math.max(halfH,Math.min(cyFt,bldgH-halfH));var fL=cxFt-halfW,fR=cxFt+halfW,fT=cyFt-halfH,fB=cyFt+halfH;for(var _oi2=0;_oi2<otherLofts.length;_oi2++){var _o2=otherLofts[_oi2];var oW2=(_o2.widthFt||6)/2,oH2=(_o2.heightFt||4)/2;var oCx2=(_o2.x-mgX)/scale,oCy2=(_o2.y-mgY)/scale;if(fL<oCx2+oW2-0.1&&fR>oCx2-oW2+0.1&&fT<oCy2+oH2-0.1&&fB>oCy2-oH2+0.1)return;}var nx=mgX+cxFt*scale,ny=mgY+cyFt*scale;if(nx!==it.x||ny!==it.y)commitLive(it,{x:nx,y:ny},{interior:true});}ev.preventDefault();return;}// Hover affordance: grab cursor + amber outline over movable items
// (crosshair while a palette tool is armed). A held button means an
// orbit is in progress - skip the raycast entirely; OrbitControls owns
// the gesture and hover feedback would be invisible anyway.
if(tool3Ref.current)return;if(ev.buttons!==0)return;var hov=pickItem3(ev);canvas.style.cursor=hov?"grab":"";var hid=hov?hov.id:null;if(hid!==lastHoverId){lastHoverId=hid;if(hov)placeHighlight(hov);highlight.visible=!!hov;render();}};var onPtr3Up=function onPtr3Up(ev){if(!dragging3)return;var d=dragging3;dragging3=null;controls.enabled=true;setInteractDpr(false);canvas.style.cursor="";try{canvas.releasePointerCapture(ev.pointerId);}catch(_){/* not captured */}highlight.visible=false;lastHoverId=null;render();if(d.moved){var moved=liveItems.find(function(i){return i.id===d.id;});if(moved&&onItemMove)onItemMove(moved.id,{x:moved.x,y:moved.y,rotation:moved.rotation,wall:moved.wall});// A moved door commits its ramp's new derived position too.
var ramp=liveItems.find(function(i){return i.type==="ramp"&&i.snapDoorId===d.id;});if(ramp&&onItemMove)onItemMove(ramp.id,{x:ramp.x,y:ramp.y,rotation:ramp.rotation,wall:ramp.wall});// The design changed under any earlier shot — re-arm capture-on-close.
capturedRef.current=false;setShotTaken(false);}// Selection syncs to the 2D view (§10.5) — a tap selects, a drag
// selects what it moved. Tapping the already-selected item again is a
// no-op and shouldn't re-render the parent (unless it moved).
if(onItemSelect&&(d.moved||d.id!==lastSentSelect)){lastSentSelect=d.id;onItemSelect(d.id);}var selIt=liveItems.find(function(i){return i.id===d.id;});setSel3d(selIt?{id:selIt.id,label:itemLabel3(selIt.type)}:null);};canvas.addEventListener("pointerdown",onPtr3Down,true);canvas.addEventListener("pointermove",onPtr3Move);canvas.addEventListener("pointerup",onPtr3Up);canvas.addEventListener("pointercancel",onPtr3Up);var disposeInteraction=function disposeInteraction(){canvas.removeEventListener("pointerdown",onPtr3Down,true);canvas.removeEventListener("pointermove",onPtr3Move);canvas.removeEventListener("pointerup",onPtr3Up);canvas.removeEventListener("pointercancel",onPtr3Up);scene.remove(highlight);highlight.geometry.dispose();highlight.material.dispose();};var controls=new OrbitControls(camera,canvasRef.current);controls.target.set(0,D3.WALL_H*0.45,0);controls.maxPolarAngle=Math.PI*0.495;// never below the ground plane
controls.minDistance=R*1.1;controls.maxDistance=dist*2.5;controls.update();var render=function render(){return renderer.render(scene,camera);};// Orbit renders are rAF-coalesced: wheel and high-rate trackpads fire
// "change" faster than the display refreshes, and each render is a full
// frame. Only the orbit path goes through this - the fixture-photo
// settle and capture() below keep their DIRECT render calls, which must
// work in a backgrounded tab where rAF never fires.
var renderQueued=false;var scheduleRender=function scheduleRender(){if(renderQueued)return;renderQueued=true;requestAnimationFrame(function(){renderQueued=false;render();});};controls.addEventListener("change",scheduleRender);controls.addEventListener("start",function(){return setInteractDpr(true);});controls.addEventListener("end",function(){return setInteractDpr(false);});var resize=function resize(){var el=wrapRef.current;if(!el)return;canvasRect=null;// the cached pointer-math rect is stale now
renderer.setSize(el.clientWidth,el.clientHeight,false);camera.aspect=el.clientWidth/Math.max(1,el.clientHeight);camera.updateProjectionMatrix();render();};var ro=typeof ResizeObserver!=="undefined"?new ResizeObserver(resize):null;if(ro&&wrapRef.current)ro.observe(wrapRef.current);// The window listener is only the fallback for environments without
// ResizeObserver — wired together, every window resize ran resize()
// (and its render) twice.
if(!ro)window.addEventListener("resize",resize);// A fixture photo that arrives after the scene is built has already been swapped
// into its materials by the cache (see d3FixtureTexture) — all that is left is
// asking for a frame, since this renderer draws on demand. Deliberately a direct
// render, NOT queueRebuild: rAF does not fire in a backgrounded tab, and a
// customer who switches tabs must not come back to blank doors.
// Also un-arm any snapshot already taken: it shows placeholder slabs, so the
// close handler should replace it with the real thing.
var offFxTex=d3OnFixtureTexSettle(function(){var e2=engineRef.current;if(!e2)return;e2.render();// The shot taken before this photo landed shows a painted door, not the builder's.
// Drop it on BOTH sides: the modal's own button state AND the parent's stored shot,
// which otherwise kept the toolbar claiming "3D checked" for a stale image.
capturedRef.current=false;setShotTaken(false);onSnapshot(null);});engineRef.current={renderer:renderer,scene:scene,camera:camera,controls:controls,model:model,sky:sky,sun:sun,render:render,resize:resize,ro:ro,applyShellMode:applyShellMode,setViewPreset:setViewPreset,disposeInteraction:disposeInteraction,setLiveColors:setLiveColors,setWallHeight:setWallHeight,place3Fixture:placeFixture3,place3Ramp:placeRamp3,delete3:deleteItem3,offFxTex:offFxTex,baseDpr:baseDpr,interior:false,roofOn:true,envOn:true};// Dev-only: expose the engine for the perf-measurement protocol.
if(typeof window!=="undefined"&&window.__SS3D_DEBUG)window.__ss3dEngine=engineRef.current;resize();setPhase("ready");})["catch"](function(err){console.error("3D view failed to load:",err);if(!disposed)setPhase("error");});return function(){disposed=true;var e=engineRef.current;if(!e)return;engineRef.current=null;window.removeEventListener("resize",e.resize);if(e.ro)e.ro.disconnect();if(e.offFxTex)e.offFxTex();// no rendering into a disposed renderer, no setState after unmount
e.controls.dispose();e.disposeInteraction();disposeShed3DModel(e.model);if(e.sky){e.sky.geometry.dispose();if(e.sky.material.map)e.sky.material.map.dispose();e.sky.material.dispose();}if(e.sun&&e.sun.shadow&&e.sun.shadow.map)e.sun.shadow.map.dispose();// renderer.dispose() won't free the shadow target
e.renderer.dispose();};// The modal mounts fresh on every open and the 2D designer can't change
// underneath it, so the scene builds exactly once per mount.
// eslint-disable-next-line react-hooks/exhaustive-deps
},[]);// "Look inside": hide the roof, ghost the walls; door/window frames, floor
// and interior items stay solid so lofts/workbenches read clearly. Roof and
// Landscape toggles ride the same path so they survive live rebuilds too.
useEffect(function(){var e=engineRef.current;if(!e)return;e.interior=interior;// survives live rebuilds during drags
e.roofOn=roofOn;e.envOn=envOn;e.applyShellMode(e);e.render();},[interior,roofOn,envOn,phase]);var capture=function capture(){var e=engineRef.current;if(!e)return null;// The quote page must be sharp even if a touch interaction just lowered
// the DPR — restore base resolution before the frame we read back.
if(e.baseDpr&&e.renderer.getPixelRatio()!==e.baseDpr)e.renderer.setPixelRatio(e.baseDpr);e.render();// fresh buffer right before reading it back
try{var c=e.renderer.domElement;// A collapsed canvas (a hidden/backgrounded tab, or a resize race) makes toDataURL
// return the bare "data:," and a 0x0 page. That passed the caller's !shot check and
// became an empty page 2 of the quote with degenerate placement maths — better to
// report no snapshot and keep the plan-only PDF.
if(!c.width||!c.height)return null;var url=c.toDataURL("image/jpeg",0.9);if(!url||url.length<128)return null;return{url:url,w:c.width,h:c.height};}catch(_){return null;}};// This image becomes page 2 of the quote the customer signs against, so it must
// never be read while a fixture photo is still loading — that would ship a quote
// showing blank placeholder doors. Capped so a dead image host can't hang the
// shutter; a warm cache waits for nothing.
// The photo lands directly in the materials, and capture() renders before it reads
// the buffer, so waiting for the loads is the whole guard. No animation frame is
// involved on purpose — see the settle listener.
var awaitFixturePhotos=function awaitFixturePhotos(){return d3FixtureTexturesPending()>0?d3WaitFixtureTextures(1500):Promise.resolve();};var takeSnapshot=/*#__PURE__*/function(){var _ref8=_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee2(){var shot;return _regeneratorRuntime().wrap(function _callee2$(_context2){while(1)switch(_context2.prev=_context2.next){case 0:_context2.next=2;return awaitFixturePhotos();case 2:shot=capture();if(shot){_context2.next=5;break;}return _context2.abrupt("return");case 5:// also covers a close mid-await
capturedRef.current=true;onSnapshot(shot);setShotTaken(true);case 8:case"end":return _context2.stop();}},_callee2);}));return function takeSnapshot(){return _ref8.apply(this,arguments);};}();var handleClose=/*#__PURE__*/function(){var _ref9=_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee3(){var shot;return _regeneratorRuntime().wrap(function _callee3$(_context3){while(1)switch(_context3.prev=_context3.next){case 0:if(!closing){_context3.next=2;break;}return _context3.abrupt("return");case 2:if(!(!capturedRef.current&&phase==="ready")){_context3.next=8;break;}setClosing(true);_context3.next=6;return awaitFixturePhotos();case 6:shot=capture();if(shot)onSnapshot(shot);case 8:onClose();case 9:case"end":return _context3.stop();}},_callee3);}));return function handleClose(){return _ref9.apply(this,arguments);};}();return/*#__PURE__*/React.createElement("div",{style:{position:"fixed",inset:0,background:"rgba(15,23,42,0.9)",zIndex:1100,display:"flex",flexDirection:"column"}},/*#__PURE__*/React.createElement("div",{style:{display:"flex",alignItems:"center",justifyContent:"space-between",padding:"10px 16px",background:"#0F172A"}},/*#__PURE__*/React.createElement("div",{style:{color:"#F8FAFC",fontWeight:800,fontSize:15}},"3D Preview",/*#__PURE__*/React.createElement("span",{style:{color:"#94A3B8",fontWeight:600,fontSize:12,marginLeft:10}},bldgW,"\xD7",bldgH," ft \u2014 drag to orbit \xB7 drag items to move them \xB7 scroll to zoom")),/*#__PURE__*/React.createElement("button",{onClick:handleClose,disabled:closing,style:{background:"none",border:"none",color:closing?"#64748B":"#CBD5E1",fontSize:22,cursor:closing?"default":"pointer",lineHeight:1}},"\u2715")),/*#__PURE__*/React.createElement("div",{ref:wrapRef,style:{flex:1,position:"relative",minHeight:0}},/*#__PURE__*/React.createElement("canvas",{ref:canvasRef,style:{width:"100%",height:"100%",display:"block",touchAction:"none"}}),phase==="loading"&&/*#__PURE__*/React.createElement("div",{style:{position:"absolute",inset:0,display:"flex",alignItems:"center",justifyContent:"center",color:"#E2E8F0",fontWeight:700}},"Building your 3D model\u2026"),phase==="error"&&/*#__PURE__*/React.createElement("div",{style:{position:"absolute",inset:0,display:"flex",alignItems:"center",justifyContent:"center",color:"#FCA5A5",fontWeight:700,textAlign:"center",padding:20}},"Couldn't load the 3D viewer \u2014 check your connection, then close and try again."),closing&&/*#__PURE__*/React.createElement("div",{style:{position:"absolute",inset:0,display:"flex",alignItems:"center",justifyContent:"center",background:"rgba(15,23,42,0.45)",color:"#E2E8F0",fontWeight:700}},"Saving your 3D view\u2026"),pick3&&/*#__PURE__*/React.createElement("div",{style:{position:"absolute",left:"50%",bottom:14,transform:"translateX(-50%)",background:"#0F172A",border:"1px solid #334155",borderRadius:12,padding:12,zIndex:40,width:"min(540px, 92%)",maxHeight:300,overflowY:"auto",boxShadow:"0 12px 40px rgba(0,0,0,0.5)"}},/*#__PURE__*/React.createElement("div",{style:{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:8}},/*#__PURE__*/React.createElement("span",{style:{color:"#E2E8F0",fontSize:13,fontWeight:800}},pick3.kind==="door"?"Pick a door":pick3.kind==="window"?"Pick a window":"Pick a ramp"),/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return setPick3(null);},style:{background:"none",border:"none",color:"#94A3B8",fontSize:18,cursor:"pointer",lineHeight:1}},"\u2715")),/*#__PURE__*/React.createElement("div",{style:{display:"flex",flexWrap:"wrap",gap:8,justifyContent:"center"}},(pick3.kind==="door"?placeableDoors||[]:pick3.kind==="window"?placeableWindows||[]:placeableRamps||[]).map(function(fx){return/*#__PURE__*/React.createElement("button",{key:fx.id,onClick:function onClick(){var e=engineRef.current;if(!e)return;var ok=pick3.kind==="ramp"?e.place3Ramp(fx,pick3.door):e.place3Fixture(fx,pick3.kind==="window"?"window":"fixtureDoor",pick3.ptx,pick3.pty);if(ok!==false)setPick3(null);},style:{display:"flex",flexDirection:"column",alignItems:"center",gap:4,background:"#1E293B",border:"1px solid #334155",borderRadius:8,padding:8,cursor:"pointer",width:108}},fx.imageUrl?/*#__PURE__*/React.createElement("img",{src:fx.imageUrl,alt:"",style:{width:84,height:64,objectFit:"contain"}}):/*#__PURE__*/React.createElement("span",{style:{fontSize:28}},pick3.kind==="window"?"🪟":pick3.kind==="ramp"?"📐":"🚪"),/*#__PURE__*/React.createElement("span",{style:{color:"#CBD5E1",fontSize:11,fontWeight:700,textAlign:"center"}},fx.name),fx.widthIn?/*#__PURE__*/React.createElement("span",{style:{color:"#64748B",fontSize:10}},fx.widthIn,"\" wide"):null);})))),/*#__PURE__*/React.createElement("div",{style:{display:"flex",flexDirection:"column",gap:8,padding:"10px 16px",background:"#0F172A"}},/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:6,alignItems:"center",justifyContent:"center",flexWrap:"wrap"}},/*#__PURE__*/React.createElement("span",{style:{color:"#64748B",fontSize:11,fontWeight:800,textTransform:"uppercase",letterSpacing:"0.05em"}},"Add"),(paletteKeys||[]).map(function(k){return/*#__PURE__*/React.createElement("button",{key:k,onClick:function onClick(){var arming=tool3!==k;setTool3(arming?k:null);// Arming a tool flips the view to the side the item lands on
// (Ahsan 2026-08-18): interior items (bench/loft) are invisible
// behind walls with the roof on, and wall/exterior items are
// disorienting to place from the ghost view. Notes/lines are
// visible either way and force nothing. The Look inside button
// stays the manual toggle.
if(arming){var kc=itemTypes[k];if((kc.wallSnap||k==="loft")&&!interior)setInterior(true);else if((kc.wallOnly||kc.doorSnap)&&interior)setInterior(false);}},disabled:phase!=="ready",style:{background:tool3===k?accent:"#1E293B",color:tool3===k?"#FFF":"#CBD5E1",border:"1px solid #334155",borderRadius:7,padding:"6px 10px",fontSize:12,fontWeight:700,cursor:"pointer",opacity:phase==="ready"?1:0.5}},itemTypes[k].icon," ",itemTypes[k].shortLabel||itemTypes[k].label);}),tool3&&!msg3&&/*#__PURE__*/React.createElement("span",{style:{color:accent,fontSize:12,fontWeight:700}},"\u2190 ",itemTypes[tool3]&&itemTypes[tool3].doorSnap?"click near a door":itemTypes[tool3]&&(itemTypes[tool3].wallOnly||itemTypes[tool3].wallSnap)?"click a wall":"click the floor"),msg3&&/*#__PURE__*/React.createElement("span",{style:{color:"#FCA5A5",fontSize:12,fontWeight:700}},msg3),sel3d&&!tool3&&/*#__PURE__*/React.createElement("button",{onClick:function onClick(){var e=engineRef.current;if(e&&e.delete3)e.delete3(sel3d.id);},style:{background:"#7F1D1D",color:"#FECACA",border:"1px solid #991B1B",borderRadius:7,padding:"6px 10px",fontSize:12,fontWeight:700,cursor:"pointer"}},"\uD83D\uDDD1 Remove ",sel3d.label)),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:6,alignItems:"center",justifyContent:"center",flexWrap:"wrap"}},/*#__PURE__*/React.createElement("span",{style:{color:"#64748B",fontSize:11,fontWeight:800,textTransform:"uppercase",letterSpacing:"0.05em"}},"Wall height"),[6,7,8,9,10].map(function(h){return/*#__PURE__*/React.createElement("button",{key:h,onClick:function onClick(){return pickWallHeight(h);},disabled:phase!=="ready",style:{background:wallHSel===h?accent:"#1E293B",color:wallHSel===h?"#FFF":"#CBD5E1",border:"1px solid #334155",borderRadius:7,padding:"6px 10px",fontSize:12,fontWeight:700,cursor:"pointer",opacity:phase==="ready"?1:0.5}},h," ft");})),paintEnabled&&/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:14,alignItems:"center",justifyContent:"center",flexWrap:"wrap"}},["body","trim"].map(function(kind){return/*#__PURE__*/React.createElement("div",{key:kind,style:{display:"flex",gap:5,alignItems:"center"}},/*#__PURE__*/React.createElement("span",{style:{color:"#64748B",fontSize:11,fontWeight:800,textTransform:"uppercase",letterSpacing:"0.05em"}},kind),D3_SWATCHES.map(function(s){return/*#__PURE__*/React.createElement("button",{key:s.label,title:s.label,onClick:function onClick(){return pickColor(kind,s.label);},disabled:phase!=="ready",style:{width:20,height:20,borderRadius:99,background:s.css,cursor:"pointer",padding:0,border:paintSel[kind]===s.label?"2px solid #FBBF24":"1px solid #334155"}});}));}),/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return pickColor("none");},disabled:phase!=="ready",style:{background:"#1E293B",color:"#94A3B8",border:"1px solid #334155",borderRadius:7,padding:"4px 9px",fontSize:11,fontWeight:700,cursor:"pointer"}},"\u2715 No paint")),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:10,alignItems:"center",justifyContent:"center",flexWrap:"wrap"}},/*#__PURE__*/React.createElement("div",{style:{position:"relative"}},viewsOpen&&/*#__PURE__*/React.createElement("div",{style:{position:"absolute",bottom:"115%",left:"50%",transform:"translateX(-50%)",background:"#0F172A",border:"1px solid #334155",borderRadius:10,padding:7,display:"grid",gridTemplateColumns:"repeat(3, 52px)",gap:5,zIndex:5}},[{l:"↖ FL",a:-45,p:26},{l:"F",a:0,p:22},{l:"FR ↗",a:45,p:26},{l:"← L",a:-90,p:20},{l:"⬒ Top",a:0,p:86},{l:"R →",a:90,p:20},{l:"↙ BL",a:-135,p:26},{l:"B",a:180,p:22},{l:"BR ↘",a:135,p:26}].map(function(v){return/*#__PURE__*/React.createElement("button",{key:v.l,onClick:function onClick(){var e=engineRef.current;if(e&&e.setViewPreset)e.setViewPreset(v.a,v.p);setViewsOpen(false);},style:{background:"#1E293B",color:"#CBD5E1",border:"1px solid #334155",borderRadius:6,padding:"7px 2px",fontSize:11,fontWeight:700,cursor:"pointer"}},v.l);})),/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return setViewsOpen(function(v){return!v;});},disabled:phase!=="ready",style:{background:viewsOpen?accent:"#1E293B",color:viewsOpen?"#FFF":"#E2E8F0",border:"1px solid #334155",borderRadius:8,padding:"9px 14px",fontSize:13,fontWeight:700,cursor:"pointer",opacity:phase==="ready"?1:0.5}},"\uD83C\uDFA5 Views ",viewsOpen?"▾":"▴")),/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return setRoofOn(function(v){return!v;});},disabled:phase!=="ready",title:"Show/hide the roof",style:{background:roofOn?"#1E293B":"#475569",color:"#E2E8F0",border:"1px solid #334155",borderRadius:8,padding:"9px 14px",fontSize:13,fontWeight:700,cursor:"pointer",opacity:phase==="ready"?1:0.5}},"\u26FA Roof ",roofOn?"":"off"),/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return setEnvOn(function(v){return!v;});},disabled:phase!=="ready",title:"Show/hide grass, sky and labels",style:{background:envOn?"#1E293B":"#475569",color:"#E2E8F0",border:"1px solid #334155",borderRadius:8,padding:"9px 14px",fontSize:13,fontWeight:700,cursor:"pointer",opacity:phase==="ready"?1:0.5}},"\uD83C\uDF3F Landscape ",envOn?"":"off"),/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return setInterior(function(v){return!v;});},disabled:phase!=="ready",style:{background:"#1E293B",color:"#E2E8F0",border:"1px solid #334155",borderRadius:8,padding:"9px 16px",fontSize:13,fontWeight:700,cursor:"pointer",opacity:phase==="ready"?1:0.5}},interior?"🏠 Show exterior":"👁 Look inside"),/*#__PURE__*/React.createElement("button",{onClick:takeSnapshot,disabled:phase!=="ready",style:{background:accent,color:"#FFF",border:"none",borderRadius:8,padding:"9px 18px",fontSize:13,fontWeight:800,cursor:"pointer",opacity:phase==="ready"?1:0.5}},shotTaken?"✓ Added to quote — retake?":"📸 Use this view in my quote"))));}// ─── Docked view-only 3D (Carolyn, 2026-08-19) ────────────────────────────────
// Which part of the 3D an `items` change touched. buildOneWall selects on
// c.wallOnly ALONE; buildInterior builds EVERYTHING else — lofts, workbenches
// (wallSnap), ramps (doorSnap), notes (noteType, a ground plaque) and lines
// (lineType, a strip). Anything not attributable returns { full: true }: a full
// rebuild is ~11ms, a stale wall standing beside a corrected plan is a bug the
// customer sees. The identity compare is exact and O(n) because every 2D
// mutation is `setItems((p) => p.map((i) => i.id === X ? {...i, ...} : i))`,
// which returns the SAME object for every untouched row.
function d3ScopeForItemsChange(prev,next,itemTypes){if(prev===next)return null;var walls=new Set();var interior=false,full=false;var index=function index(arr){var m=new Map();(arr||[]).forEach(function(i){return m.set(i.id,i);});return m;};var pm=index(prev),nm=index(next);var touch=function touch(it){if(!it)return;var c=itemTypes[it.type];if(!c){full=true;return;}// unknown class — never guess
if(c.wallOnly){if(it.wall)walls.add(it.wall);else full=true;return;}interior=true;// loft | workbench | ramp | note | line
};var ids=new Set();pm.forEach(function(_v,k){return ids.add(k);});nm.forEach(function(_v,k){return ids.add(k);});ids.forEach(function(id){var a=pm.get(id),b=nm.get(id);if(a===b)return;touch(a);touch(b);});return full?{full:true}:{walls:Array.from(walls),interior:interior};}/**
 * Structure3DPanel — the 3D docked BESIDE the 2D plan instead of over it.
 *
 * Carolyn drew this on the 2026-08-19 call: the floor plan keeps the left of the
 * canvas, the 3D takes a column on the right, and the same toolbar button toggles
 * it. Scope, confirmed by Ahsan: the customer can ROTATE AROUND THE BUILDING AND
 * LOOK AT IT. Nothing more. No placing, no dragging, no wall height, no colors,
 * no snapshot, no roof/landscape/look-inside toggles — every one of those stays
 * the full-screen Structure3DViewer's job, and this file does not touch it.
 *
 * "View-only" is not a flag that could be flipped: this component simply never
 * registers pointer handlers, never builds a raycaster, and never caches a canvas
 * rect. That also means the whole class of scroll-stale-rect bugs cannot occur.
 *
 * Gating: rendered INSIDE the same `view3dOn` expression that gates the 3D button
 * — never hoisted above it — so a tenant without the grant cannot reach, render,
 * or discover it. There is no second switch to forget.
 *
 * Deliberate differences from the modal, all justified by a ~380px canvas:
 *   shadow map 2048 → 1024 (largest single GPU allocation, invisible at this size)
 *   DPR cap 2 → 1.75
 *   controls.enablePan → false (in a narrow panel a pan walks the building
 *     off-screen with no reset control to bring it back)
 *   forceContextLoss() on teardown — the modal omits it; the repo's throwaway
 *     GLB-scan renderer does call it, and this surface mounts far more often.
 */function Structure3DPanel(_ref10){var bldgW=_ref10.bldgW,bldgH=_ref10.bldgH,items=_ref10.items,itemTypes=_ref10.itemTypes,painted=_ref10.painted,paintBody=_ref10.paintBody,paintTrim=_ref10.paintTrim,frontWall=_ref10.frontWall,scale=_ref10.scale,mgX=_ref10.mgX,mgY=_ref10.mgY,style3d=_ref10.style3d,roofType=_ref10.roofType,roofColorHex=_ref10.roofColorHex,fixtures=_ref10.fixtures,suspended=_ref10.suspended,canEdit=_ref10.canEdit,onEdit=_ref10.onEdit,onClose=_ref10.onClose;var canvasRef=useRef(null);var wrapRef=useRef(null);var engineRef=useRef(null);var _useState37=useState("loading"),_useState38=_slicedToArray(_useState37,2),phase=_useState38[0],setPhase=_useState38[1];// loading | ready | error
// Latest props, readable from inside the mount effect's closures. Assigned
// during render (not in an effect) so a flush scheduled this frame already
// sees this frame's values — this is what avoids the frozen-props trap.
var pRef=useRef(null);pRef.current={bldgW:bldgW,bldgH:bldgH,items:items,itemTypes:itemTypes,painted:painted,paintBody:paintBody,paintTrim:paintTrim,frontWall:frontWall,scale:scale,mgX:mgX,mgY:mgY,style3d:style3d,roofType:roofType,roofColorHex:roofColorHex,fixtures:fixtures};// Every geometry input that is NOT `items`, flattened to a scalar string.
// d3ResolveStyleSpec returns a fresh object (with fresh nested roof/colors)
// on every parent render, so an object in a dep array would rebuild the whole
// model on every mousemove. Scalars cannot do that.
// Stringify the WHOLE resolved spec instead of hand-picking fields. d3ResolveStyleSpec
// returns a small plain object with stable key order, so this is still a cheap scalar —
// but unlike a field list it cannot drift out of step with what buildShed3DModel reads.
// The hand-picked version silently missed roofMaterial, roof.overhang and the gambrel
// knee numbers, so two styles differing only in those left a stale roof standing beside
// a corrected plan until some unrelated edit happened to force a full rebuild.
var geomSig=JSON.stringify([style3d,roofType||"",roofColorHex||"",painted?1:0,paintBody||"",paintTrim||"",scale,mgX,mgY]);useEffect(function(){var disposed=false;loadThree().then(function(bundle){var THREE=bundle.THREE,OrbitControls=bundle.OrbitControls;if(disposed||!canvasRef.current)return;var canvas=canvasRef.current;var renderer=new THREE.WebGLRenderer({canvas:canvas,antialias:true,preserveDrawingBuffer:false});renderer.setPixelRatio(Math.min(window.devicePixelRatio||1,1.75));renderer.shadowMap.enabled=true;renderer.shadowMap.type=THREE.PCFSoftShadowMap;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.15;// Same view-independent sun as the modal: orbiting must not re-render the
// depth map. Every geometry mutation below re-arms needsUpdate by hand.
renderer.shadowMap.autoUpdate=false;renderer.shadowMap.needsUpdate=true;var scene=new THREE.Scene();scene.background=new THREE.Color("#E7EEF5");var specOf=function specOf(p){return p.style3d||{roof:D3_DEFAULT_ROOF,siding:null,colors:{},wallHeightFt:0};};var bodyOf=function bodyOf(p){return p.painted?d3SwatchCss(p.paintBody,D3_COLORS.body):specOf(p).colors.body||D3_COLORS.body;};var trimOf=function trimOf(p){return p.painted?d3SwatchCss(p.paintTrim,D3_COLORS.trim):specOf(p).colors.trim||D3_COLORS.trim;};var roofOf=function roofOf(p){return p.roofColorHex||specOf(p).colors.roof||D3_COLORS.roof;};var buildArgs=function buildArgs(p,fw){var spec=specOf(p);return{bldgW:p.bldgW,bldgH:p.bldgH,wallHeightFt:spec.wallHeightFt,styleSpec:spec,roofColor:roofOf(p),roofType:p.roofType,items:p.items,itemTypes:p.itemTypes,bodyColor:bodyOf(p),trimColor:trimOf(p),frontWall:fw,scale:p.scale,mgX:p.mgX,mgY:p.mgY,fixtures:p.fixtures};};var p0=pRef.current;var fw0=p0.frontWall||"south";// The builder gets the RAW nullable frontWall, exactly as the modal does — fw0's
// "south" default exists for the camera lookup only. Defaulting it into the builder
// would give a doorless design a different roof here than in the full-screen view and
// the quote snapshot, and would leave model.builtFrontWall on a different domain from
// the value the flush below compares it against.
var model=d3TimedBuild(function(){return buildShed3DModel(THREE,buildArgs(p0,p0.frontWall));});scene.add(model.root);scene.add(new THREE.HemisphereLight(0xDCE9FF,0x8D8573,1.5));// Camera framing — identical maths to the modal so the docked view and the
// full-screen view read as the same building from the same angle. Every
// constant below derives ONLY from bldgW/bldgH, which is exactly why the
// parent remounts this component on a size change and on nothing else.
var OUT={north:[0.35,-1],south:[0.35,1],west:[-1,0.35],east:[1,0.35]}[fw0]||[0.35,1];var R=Math.max(p0.bldgW,p0.bldgH)*0.5+D3.WALL_H;var dist=R*3.66;var outLen=Math.sqrt(OUT[0]*OUT[0]+OUT[1]*OUT[1]);var camX=OUT[0]/outLen*dist,camZ=OUT[1]/outLen*dist;var sun=new THREE.DirectionalLight(0xFFF3E0,2.6);sun.position.set(camX*0.8,dist*0.9,camZ*0.8);sun.castShadow=true;sun.shadow.mapSize.width=sun.shadow.mapSize.height=1024;var shR=Math.max(p0.bldgW,p0.bldgH)*1.6+8;sun.shadow.camera.left=-shR;sun.shadow.camera.right=shR;sun.shadow.camera.top=shR;sun.shadow.camera.bottom=-shR;sun.shadow.camera.near=0.5;sun.shadow.camera.far=dist*4;sun.shadow.bias=-0.0005;sun.shadow.camera.updateProjectionMatrix();scene.add(sun);var camera=new THREE.PerspectiveCamera(34,1,0.1,dist*10);camera.position.set(camX,dist*0.5,camZ);// The sky dome stays. buildShed3DModel sizes the grass disc to "stay inside
// the viewer's sky dome" — drop the dome and the customer sees a green disc
// floating in flat grey.
var skyTex=d3MakeSkyTexture(THREE);var sky=skyTex?new THREE.Mesh(new THREE.SphereGeometry(dist*6.5,32,16,0,Math.PI*2,0,Math.PI/2+0.14),new THREE.MeshBasicMaterial({map:skyTex,side:THREE.BackSide,depthWrite:false})):null;if(sky){sky.position.y=-0.5;scene.add(sky);}var controls=new OrbitControls(camera,canvas);controls.target.set(0,D3.WALL_H*0.45,0);controls.maxPolarAngle=Math.PI*0.495;// never below the ground plane
controls.minDistance=R*1.1;controls.maxDistance=dist*2.5;// No pan. The modal leaves it on because a full-screen canvas can afford a
// lost building; a 380px panel with no reset control cannot.
controls.enablePan=false;controls.update();var render=function render(){return renderer.render(scene,camera);};var renderQueued=false;var scheduleRender=function scheduleRender(){if(renderQueued)return;renderQueued=true;requestAnimationFrame(function(){renderQueued=false;if(engineRef.current)render();});};controls.addEventListener("change",scheduleRender);var resize=function resize(){var el=wrapRef.current;if(!el)return;var w=el.clientWidth,h=el.clientHeight;if(!w||!h)return;// collapsed/hidden: nothing to size to
renderer.setSize(w,h,false);camera.aspect=Math.max(1,w)/Math.max(1,h);camera.updateProjectionMatrix();render();};var ro=typeof ResizeObserver!=="undefined"?new ResizeObserver(resize):null;if(ro&&wrapRef.current)ro.observe(wrapRef.current);if(!ro)window.addEventListener("resize",resize);// A builder's fixture photo that lands after the scene was built is already
// swapped into its materials by the cache — all that is left is asking for a
// frame. Direct render, not rAF: a backgrounded tab never fires rAF and the
// customer must not return to blank doors. Unlike the modal this NEVER
// touches snapshot state; the panel does not own a snapshot.
var offFxTex=d3OnFixtureTexSettle(function(){var e2=engineRef.current;if(e2)e2.render();});// ── Scoped, rAF-coalesced rebuilds ──
var pending=null;var flush=function flush(){var sc=pending;pending=null;var e=engineRef.current;if(!e||!sc)return;var p=pRef.current;var nf=getFrontWall(p.items)||p.frontWall;if(sc.full||nf!==e.model.builtFrontWall){scene.remove(e.model.root);disposeShed3DModel(e.model);e.model=d3TimedBuild(function(){return buildShed3DModel(THREE,buildArgs(p,nf));});scene.add(e.model.root);}else{d3TimedBuild(function(){if(sc.walls&&sc.walls.length)e.model.rebuildWalls(sc.walls,p.items);if(sc.interior)e.model.rebuildInterior(p.items);});}renderer.shadowMap.needsUpdate=true;// geometry moved: the cached depth map is stale
render();};var queue=function queue(scope){var full=!scope||scope.full===true;if(pending){if(full)pending.full=true;else{(scope.walls||[]).forEach(function(w){if(pending.walls.indexOf(w)===-1)pending.walls.push(w);});if(scope.interior)pending.interior=true;}return;}pending={full:full,walls:full?[]:(scope.walls||[]).slice(),interior:!full&&Boolean(scope.interior)};requestAnimationFrame(flush);};var builtItems=p0.items;engineRef.current={renderer:renderer,scene:scene,camera:camera,controls:controls,model:model,sky:sky,sun:sun,render:render,resize:resize,ro:ro,offFxTex:offFxTex,applyItems:function applyItems(next){var scope=d3ScopeForItemsChange(builtItems,next,pRef.current.itemTypes);builtItems=next;if(scope)queue(scope);},applyFull:function applyFull(){builtItems=pRef.current.items;queue({full:true});}};canvas.addEventListener("webglcontextlost",function(ev){ev.preventDefault();setPhase("error");});// preventDefault above is what asks the browser to restore the context. Without a
// matching restore listener it comes back and the panel stays stuck behind a dead
// error overlay for the rest of the session.
canvas.addEventListener("webglcontextrestored",function(){setPhase("ready");renderer.shadowMap.needsUpdate=true;render();// direct, not rAF — a backgrounded tab never fires rAF
});if(typeof window!=="undefined"&&window.__SS3D_DEBUG)window.__ss3dPanel=engineRef.current;resize();setPhase("ready");})["catch"](function(err){console.error("3D panel failed to load:",err);if(!disposed)setPhase("error");});return function(){disposed=true;var e=engineRef.current;if(!e)return;engineRef.current=null;window.removeEventListener("resize",e.resize);if(e.ro)e.ro.disconnect();if(e.offFxTex)e.offFxTex();e.controls.dispose();disposeShed3DModel(e.model);if(e.sky){e.sky.geometry.dispose();if(e.sky.material.map)e.sky.material.map.dispose();e.sky.material.dispose();}if(e.sun&&e.sun.shadow&&e.sun.shadow.map)e.sun.shadow.map.dispose();e.renderer.dispose();// The modal omits this. This surface toggles open and shut far more often,
// and browsers cap live WebGL contexts hard — dropping it deterministically
// is the difference between "the 3D stopped working after a while" and not.
if(e.renderer.forceContextLoss)e.renderer.forceContextLoss();};// Mount once per building size — the parent's `key` handles the rest. The shadow and
// orbit constants bake from bldgW/bldgH, so a size change genuinely needs a fresh scene.
// Be honest about one more thing that bakes at mount: the opening camera angle and the
// sun position derive from the FRONT wall. If the front re-homes later (dragging the only
// door to another wall) the geometry follows on the next rebuild but the camera keeps its
// current angle. That is deliberate — re-aiming the camera under someone who has just
// orbited to the view they wanted is worse than a slightly stale opening angle, and
// orbiting is this panel's whole affordance.
// eslint-disable-next-line react-hooks/exhaustive-deps
},[]);// Live follow: the plan moves, the 3D moves. `suspended` is in the dep array on
// purpose — releasing a drag flips it false and re-runs this effect, which is
// what flushes the edit even though onPtrUp never touches `items` itself.
useEffect(function(){var e=engineRef.current;if(e&&!suspended)e.applyItems(items);},[items,suspended]);// Style, cladding, roof, paint, zoom: rebuild in place, no remount, so a
// free-text roof colour cannot churn a WebGL context per keystroke.
useEffect(function(){var e=engineRef.current;if(e)e.applyFull();// eslint-disable-next-line react-hooks/exhaustive-deps
},[geomSig]);return/*#__PURE__*/React.createElement("div",{style:{display:"flex",flexDirection:"column",height:"100%",background:"#FFF",border:"1px solid #E2E8F0",borderRadius:12,boxShadow:"0 4px 24px rgba(0,0,0,0.08)",overflow:"hidden"}},/*#__PURE__*/React.createElement("div",{style:{display:"flex",alignItems:"center",gap:8,padding:"8px 10px 8px 12px",borderBottom:"1px solid #E2E8F0",background:"#F8FAFC",flexShrink:0}},/*#__PURE__*/React.createElement("span",{style:{fontSize:12,fontWeight:700,color:"#334155"}},"3D view"),/*#__PURE__*/React.createElement("span",{style:{fontSize:11,color:"#94A3B8"}},bldgW,"\xD7",bldgH," ft \xB7 drag to rotate \xB7 scroll to zoom"),/*#__PURE__*/React.createElement("button",{onClick:onClose,title:"Hide the 3D view","aria-label":"Hide the 3D view",style:{marginLeft:"auto",background:"transparent",border:"none",color:"#64748B",fontSize:16,lineHeight:1,cursor:"pointer",padding:"2px 4px"}},"\u2715")),/*#__PURE__*/React.createElement("div",{ref:wrapRef,style:{flex:1,position:"relative",minHeight:0,background:"#E7EEF5"}},/*#__PURE__*/React.createElement("canvas",{ref:canvasRef,style:{width:"100%",height:"100%",display:"block",touchAction:"none"}}),phase!=="ready"&&/*#__PURE__*/React.createElement("div",{style:{position:"absolute",inset:0,display:"flex",alignItems:"center",justifyContent:"center",color:"#64748B",fontSize:12,textAlign:"center",padding:16,pointerEvents:"none"}},phase==="error"?"The 3D view couldn't load. Close and open it again.":"Building the 3D view…")),canEdit&&/*#__PURE__*/React.createElement("div",{style:{padding:8,borderTop:"1px solid #E2E8F0",background:"#F8FAFC",flexShrink:0}},/*#__PURE__*/React.createElement("button",{onClick:onEdit,style:{background:"#7C3AED",color:"#FFF",border:"none",borderRadius:6,padding:"7px 12px",fontSize:12,fontWeight:700,cursor:"pointer",width:"100%"}},"\u26F6 Edit in 3D")));}// ─── MAIN COMPONENT ───
// Custom color dropdown: a native <select> can't render a color swatch per option, so this
// shows a color chip + name in the closed button and in each list row (matching the palette).
function ColorSelect(_ref11){var value=_ref11.value,colors=_ref11.colors,onPick=_ref11.onPick;var _useState39=useState(false),_useState40=_slicedToArray(_useState39,2),open=_useState40[0],setOpen=_useState40[1];var ref=useRef(null);useEffect(function(){if(!open)return;var h=function h(e){if(ref.current&&!ref.current.contains(e.target))setOpen(false);};document.addEventListener("mousedown",h);document.addEventListener("touchstart",h);return function(){document.removeEventListener("mousedown",h);document.removeEventListener("touchstart",h);};},[open]);var sel=colors.find(function(c){return c.label===value;});var chip=function chip(hex){return/*#__PURE__*/React.createElement("span",{style:{width:14,height:14,borderRadius:3,background:hex||"transparent",border:"1px solid rgba(0,0,0,0.25)",flexShrink:0,display:"inline-block"}});};return/*#__PURE__*/React.createElement("div",{ref:ref,style:{position:"relative",flex:1,minWidth:0}},/*#__PURE__*/React.createElement("button",{type:"button",onClick:function onClick(){return setOpen(function(o){return!o;});},style:{width:"100%",display:"flex",alignItems:"center",gap:6,border:"1px solid #CBD5E1",borderRadius:6,padding:"5px 8px",fontSize:12,background:"#FFF",cursor:"pointer",color:sel?"#334155":"#94A3B8"}},sel&&chip(sel.hex),/*#__PURE__*/React.createElement("span",{style:{flex:1,textAlign:"left",overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}},sel?sel.label:"Select…"),/*#__PURE__*/React.createElement("span",{style:{fontSize:10,color:"#94A3B8"}},"\u25BE")),open&&/*#__PURE__*/React.createElement("div",{style:{position:"absolute",top:"calc(100% + 2px)",left:0,right:0,zIndex:30,background:"#FFF",border:"1px solid #CBD5E1",borderRadius:6,boxShadow:"0 6px 18px rgba(0,0,0,0.15)",maxHeight:220,overflowY:"auto"}},colors.map(function(c){return/*#__PURE__*/React.createElement("div",{key:c.id||c.label,onClick:function onClick(){onPick(c.label);setOpen(false);},style:{display:"flex",alignItems:"center",gap:8,padding:"7px 10px",cursor:"pointer",fontSize:12,background:c.label===value?"#F1F5F9":"#FFF",color:"#334155"}},chip(c.hex),/*#__PURE__*/React.createElement("span",{style:{overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}},c.label));})));}// Lead-capture gate shown BEFORE the designer (the customer link is a lead-gen tool).
// Collects name + phone and fires a best-effort GHL lead capture (capture-lead edge fn) the
// moment they continue. Rendered by StructureStudioInner as a body-portaled overlay when
// !gatePassed && !isAdmin && !embedded — the designer renders BEHIND it, dimmed/blurred and
// marked inert (no pointer/keyboard/focus) until the gate is passed.
// NOTE: a phone-as-login "find my saved designs" flow was intentionally deferred — it needs
// SMS/OTP verification, else a low-entropy phone could expose a customer's saved address.
function LeadGate(_ref12){var config=_ref12.config,supabase=_ref12.supabase,accent=_ref12.accent,onPass=_ref12.onPass,onClose=_ref12.onClose;var _useState41=useState(""),_useState42=_slicedToArray(_useState41,2),name=_useState42[0],setName=_useState42[1];var _useState43=useState(""),_useState44=_slicedToArray(_useState43,2),phone=_useState44[0],setPhone=_useState44[1];var _useState45=useState(false),_useState46=_slicedToArray(_useState45,2),busy=_useState46[0],setBusy=_useState46[1];var digits=phone.replace(/\D/g,"");var valid=name.trim().length>0&&digits.length===10;var brand=config&&config.branding||{};var acc=accent||"#3D3672";var start=function start(){if(!valid||busy)return;setBusy(true);// Best-effort lead capture to the tenant's GHL — never block entry on it.
try{supabase.functions.invoke("capture-lead",{body:{clientId:config.clientId,name:name.trim(),phone:phone}});}catch(_e){}onPass({name:name.trim(),phone:phone});};var inp={width:"100%",boxSizing:"border-box",border:"1px solid #CBD5E1",borderRadius:8,padding:"10px 12px",fontSize:14,margin:"4px 0 12px"};return/*#__PURE__*/React.createElement("div",{style:{position:"fixed",inset:0,zIndex:9999,background:"rgba(15,23,42,0.42)",backdropFilter:"blur(2.5px)",WebkitBackdropFilter:"blur(2.5px)",display:"flex",alignItems:"center",justifyContent:"center",padding:16}},/*#__PURE__*/React.createElement("div",{style:{position:"relative",background:"#FFF",borderRadius:16,maxWidth:420,width:"100%",padding:24,boxShadow:"0 20px 60px rgba(0,0,0,0.3)",fontFamily:"'Segoe UI', system-ui, -apple-system, sans-serif"}},onClose&&/*#__PURE__*/React.createElement("button",{type:"button",onClick:onClose,"aria-label":"Close",title:"Close",style:{position:"absolute",top:10,right:12,background:"transparent",border:"none",fontSize:20,color:"#94A3B8",cursor:"pointer",lineHeight:1,padding:4}},"\xD7"),brand.logo?/*#__PURE__*/React.createElement("img",{src:brand.logo,alt:brand.companyName||"logo",style:{height:40,objectFit:"contain",marginBottom:12}}):/*#__PURE__*/React.createElement("div",{style:{fontWeight:800,fontSize:18,color:acc,marginBottom:12}},brand.companyName||"Design Studio"),/*#__PURE__*/React.createElement("div",{style:{fontSize:20,fontWeight:800,color:"#0F172A",marginBottom:4}},"Let's design your building"),/*#__PURE__*/React.createElement("div",{style:{fontSize:13,color:"#64748B",marginBottom:18}},"Enter your name and phone to get started."),/*#__PURE__*/React.createElement("label",{style:{fontSize:12,fontWeight:700,color:"#475569"}},"Name"),/*#__PURE__*/React.createElement("input",{value:name,onChange:function onChange(e){return setName(e.target.value);},placeholder:"Your name",style:inp,autoFocus:true}),/*#__PURE__*/React.createElement("label",{style:{fontSize:12,fontWeight:700,color:"#475569"}},"Phone"),/*#__PURE__*/React.createElement("input",{type:"tel",inputMode:"tel",value:formatPhoneDisplay(phone),onChange:function onChange(e){return setPhone(formatPhoneDisplay(e.target.value));},onKeyDown:function onKeyDown(e){return e.key==="Enter"&&start();},placeholder:"(555) 555-5555",style:_objectSpread(_objectSpread({},inp),{},{margin:"4px 0 16px"})}),/*#__PURE__*/React.createElement("button",{onClick:start,disabled:!valid||busy,style:{width:"100%",background:valid&&!busy?acc:"#94A3B8",color:"#FFF",border:"none",borderRadius:10,padding:"12px",fontSize:15,fontWeight:700,cursor:valid&&!busy?"pointer":"default"}},busy?"Starting…":"Start Designing →")));}// Ramp placement picker (custom mode). Like DoorPicker but no swing/operation: pick a ramp
// STYLE (exact name), then its size, then place it on the door the tool was dropped near.
function RampPicker(_ref13){var ramps=_ref13.ramps,showPricing=_ref13.showPricing,onCancel=_ref13.onCancel,onPlace=_ref13.onPlace;var styles=useMemo(function(){var m=new Map();ramps.forEach(function(d){var k=d.name||"Ramp";if(!m.has(k))m.set(k,{name:k,imageUrl:d.imageUrl||null,sizes:[]});var g=m.get(k);g.sizes.push(d);if(!g.imageUrl&&d.imageUrl)g.imageUrl=d.imageUrl;});return _toConsumableArray(m.values());},[ramps]);var _useState47=useState(styles.length===1?styles[0]:null),_useState48=_slicedToArray(_useState47,2),style=_useState48[0],setStyle=_useState48[1];var _useState49=useState(styles.length===1&&styles[0].sizes.length===1?styles[0].sizes[0]:null),_useState50=_slicedToArray(_useState49,2),sel=_useState50[0],setSel=_useState50[1];var pickStyle=function pickStyle(st){setStyle(st);setSel(st.sizes.length===1?st.sizes[0]:null);};var money=function money(n){return"$"+Number(n).toLocaleString();};var chip=function chip(key,on,label,onClick){return/*#__PURE__*/React.createElement("div",{key:key,onClick:onClick,style:{padding:"6px 14px",borderRadius:20,fontSize:13,fontWeight:600,cursor:"pointer",border:"2px solid ".concat(on?FIXTURE_RAMP_COLOR:"#E2E8F0"),background:on?"#E0F2FE":"#FFF",color:on?"#075985":"#334155"}},label);};return/*#__PURE__*/React.createElement("div",{onClick:onCancel,style:{position:"fixed",inset:0,background:"rgba(15,23,42,0.45)",zIndex:9000,display:"flex",alignItems:"center",justifyContent:"center",padding:16}},/*#__PURE__*/React.createElement("div",{onClick:function onClick(e){return e.stopPropagation();},style:{background:"#FFF",borderRadius:14,width:"min(560px, 96vw)",maxHeight:"88vh",overflow:"auto",padding:20,boxShadow:"0 20px 60px rgba(0,0,0,0.3)",fontFamily:"system-ui, -apple-system, sans-serif"}},/*#__PURE__*/React.createElement("div",{style:{fontSize:17,fontWeight:800,color:"#1E293B",marginBottom:4}},"Choose a ramp"),/*#__PURE__*/React.createElement("div",{style:{fontSize:13,color:"#64748B",marginBottom:14}},style&&style.sizes.length>1?"Pick a size.":"Pick a ramp for this door."),/*#__PURE__*/React.createElement("div",{style:{display:"grid",gridTemplateColumns:"repeat(auto-fill, minmax(150px, 1fr))",gap:10,marginBottom:16}},styles.map(function(st){var on=style&&style.name===st.name;var one=st.sizes.length===1?st.sizes[0]:null;var sub=one?"".concat(fmtFtIn(one.widthIn)," \xD7 ").concat(fmtFtIn(one.heightIn)).concat(showPricing&&one.price!=null?" \xB7 ".concat(money(one.price)):""):"".concat(st.sizes.length," sizes");return/*#__PURE__*/React.createElement("div",{key:st.name,onClick:function onClick(){return pickStyle(st);},style:{border:"2px solid ".concat(on?FIXTURE_RAMP_COLOR:"#E2E8F0"),borderRadius:10,overflow:"hidden",cursor:"pointer",background:"#FFF"}},st.imageUrl?/*#__PURE__*/React.createElement("img",{src:st.imageUrl,alt:"",style:{width:"100%",height:90,objectFit:"cover",display:"block"}}):/*#__PURE__*/React.createElement("div",{style:{height:90,background:"#F1F5F9",display:"flex",alignItems:"center",justifyContent:"center",fontSize:24}},"\u2B1B"),/*#__PURE__*/React.createElement("div",{style:{padding:"8px 10px"}},/*#__PURE__*/React.createElement("div",{style:{fontSize:13,fontWeight:700,color:"#1E293B"}},st.name),/*#__PURE__*/React.createElement("div",{style:{fontSize:11.5,color:"#64748B"}},sub)));})),style&&style.sizes.length>1&&/*#__PURE__*/React.createElement("div",{style:{marginBottom:14}},/*#__PURE__*/React.createElement("div",{style:{fontSize:12,fontWeight:700,color:"#334155",marginBottom:6}},"Size"),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:8,flexWrap:"wrap"}},style.sizes.map(function(d){return chip(d.id,sel&&sel.id===d.id,"".concat(fmtFtIn(d.widthIn)," \xD7 ").concat(fmtFtIn(d.heightIn)).concat(showPricing&&d.price!=null?" \xB7 ".concat(money(d.price)):""),function(){return setSel(d);});}))),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:8,justifyContent:"flex-end",marginTop:4}},/*#__PURE__*/React.createElement("button",{onClick:onCancel,style:{padding:"9px 16px",borderRadius:8,border:"1px solid #CBD5E1",background:"#FFF",color:"#334155",fontWeight:600,cursor:"pointer"}},"Cancel"),/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return sel&&onPlace(sel);},disabled:!sel,style:{padding:"9px 18px",borderRadius:8,border:"none",background:sel?FIXTURE_RAMP_COLOR:"#CBD5E1",color:"#FFF",fontWeight:700,cursor:sel?"pointer":"default"}},"Place ramp"))));}// Window placement picker. Like RampPicker (style → size, no swing/operation), but the placed
// item goes on a wall. "Choose a window" / "Place window".
function WindowPicker(_ref14){var windows=_ref14.windows,showPricing=_ref14.showPricing,onCancel=_ref14.onCancel,onPlace=_ref14.onPlace;var styles=useMemo(function(){var m=new Map();windows.forEach(function(d){var k=d.name||"Window";if(!m.has(k))m.set(k,{name:k,imageUrl:d.imageUrl||null,sizes:[]});var g=m.get(k);g.sizes.push(d);if(!g.imageUrl&&d.imageUrl)g.imageUrl=d.imageUrl;});return _toConsumableArray(m.values());},[windows]);var _useState51=useState(styles.length===1?styles[0]:null),_useState52=_slicedToArray(_useState51,2),style=_useState52[0],setStyle=_useState52[1];var _useState53=useState(styles.length===1&&styles[0].sizes.length===1?styles[0].sizes[0]:null),_useState54=_slicedToArray(_useState53,2),sel=_useState54[0],setSel=_useState54[1];var pickStyle=function pickStyle(st){setStyle(st);setSel(st.sizes.length===1?st.sizes[0]:null);};var money=function money(n){return"$"+Number(n).toLocaleString();};var chip=function chip(key,on,label,onClick){return/*#__PURE__*/React.createElement("div",{key:key,onClick:onClick,style:{padding:"6px 14px",borderRadius:20,fontSize:13,fontWeight:600,cursor:"pointer",border:"2px solid ".concat(on?FIXTURE_WINDOW_COLOR:"#E2E8F0"),background:on?"#E0F2FE":"#FFF",color:on?"#075985":"#334155"}},label);};return/*#__PURE__*/React.createElement("div",{onClick:onCancel,style:{position:"fixed",inset:0,background:"rgba(15,23,42,0.45)",zIndex:9000,display:"flex",alignItems:"center",justifyContent:"center",padding:16}},/*#__PURE__*/React.createElement("div",{onClick:function onClick(e){return e.stopPropagation();},style:{background:"#FFF",borderRadius:14,width:"min(560px, 96vw)",maxHeight:"88vh",overflow:"auto",padding:20,boxShadow:"0 20px 60px rgba(0,0,0,0.3)",fontFamily:"system-ui, -apple-system, sans-serif"}},/*#__PURE__*/React.createElement("div",{style:{fontSize:17,fontWeight:800,color:"#1E293B",marginBottom:4}},"Choose a window"),/*#__PURE__*/React.createElement("div",{style:{fontSize:13,color:"#64748B",marginBottom:14}},style&&style.sizes.length>1?"Pick a size.":"Pick a window for this wall."),/*#__PURE__*/React.createElement("div",{style:{display:"grid",gridTemplateColumns:"repeat(auto-fill, minmax(150px, 1fr))",gap:10,marginBottom:16}},styles.map(function(st){var on=style&&style.name===st.name;var one=st.sizes.length===1?st.sizes[0]:null;var sub=one?"".concat(fmtFtIn(one.widthIn)," \xD7 ").concat(fmtFtIn(one.heightIn)).concat(showPricing&&one.price!=null?" \xB7 ".concat(money(one.price)):""):"".concat(st.sizes.length," sizes");return/*#__PURE__*/React.createElement("div",{key:st.name,onClick:function onClick(){return pickStyle(st);},style:{border:"2px solid ".concat(on?FIXTURE_WINDOW_COLOR:"#E2E8F0"),borderRadius:10,overflow:"hidden",cursor:"pointer",background:"#FFF"}},st.imageUrl?/*#__PURE__*/React.createElement("img",{src:st.imageUrl,alt:"",style:{width:"100%",height:90,objectFit:"cover",display:"block"}}):/*#__PURE__*/React.createElement("div",{style:{height:90,background:"#F1F5F9",display:"flex",alignItems:"center",justifyContent:"center",fontSize:24}},"\uD83E\uDE9F"),/*#__PURE__*/React.createElement("div",{style:{padding:"8px 10px"}},/*#__PURE__*/React.createElement("div",{style:{fontSize:13,fontWeight:700,color:"#1E293B"}},st.name),/*#__PURE__*/React.createElement("div",{style:{fontSize:11.5,color:"#64748B"}},sub)));})),style&&style.sizes.length>1&&/*#__PURE__*/React.createElement("div",{style:{marginBottom:14}},/*#__PURE__*/React.createElement("div",{style:{fontSize:12,fontWeight:700,color:"#334155",marginBottom:6}},"Size"),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:8,flexWrap:"wrap"}},style.sizes.map(function(d){return chip(d.id,sel&&sel.id===d.id,"".concat(fmtFtIn(d.widthIn)," \xD7 ").concat(fmtFtIn(d.heightIn)).concat(showPricing&&d.price!=null?" \xB7 ".concat(money(d.price)):""),function(){return setSel(d);});}))),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:8,justifyContent:"flex-end",marginTop:4}},/*#__PURE__*/React.createElement("button",{onClick:onCancel,style:{padding:"9px 16px",borderRadius:8,border:"1px solid #CBD5E1",background:"#FFF",color:"#334155",fontWeight:600,cursor:"pointer"}},"Cancel"),/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return sel&&onPlace(sel);},disabled:!sel,style:{padding:"9px 18px",borderRadius:8,border:"none",background:sel?FIXTURE_WINDOW_COLOR:"#CBD5E1",color:"#FFF",fontWeight:700,cursor:sel?"pointer":"default"}},"Place window"))));}function StructureStudioInner(_ref15){var _INV_RANKS$designUnit,_INV_RANKS$designUnit2;var config=_ref15.config,_ref15$embedded=_ref15.embedded,embedded=_ref15$embedded===void 0?false:_ref15$embedded,_ref15$onSaved=_ref15.onSaved,onSaved=_ref15$onSaved===void 0?null:_ref15$onSaved,_ref15$openDesign=_ref15.openDesign,openDesign=_ref15$openDesign===void 0?null:_ref15$openDesign,_ref15$setup3d=_ref15.setup3d,setup3d=_ref15$setup3d===void 0?null:_ref15$setup3d,_ref15$view3d=_ref15.view3d,view3d=_ref15$view3d===void 0?false:_ref15$view3d,_ref15$calibrationOnl=_ref15.calibrationOnly,calibrationOnly=_ref15$calibrationOnl===void 0?false:_ref15$calibrationOnl;var C=config;// ── Which surface is this? THE discriminator between the two mounts of this module ──
//   embedded = true  → the Designer tab inside portal.html: business users building
//                      quotes for customers (discounts, delivery fees, full tooling).
//   embedded = false → the PUBLIC customer-facing page (index.html / tenant subdomains /
//                      the "try it" link on marketing sites): anonymous shed-shoppers.
// New surface differences gate on one of these two flags — never on a new prop, never on
// sniffing the URL. If a feature is for the business, use `embedded`; if it is lead- or
// customer-flavoured (contact gates, silent lead capture), use `customerFacing`.
var customerFacing=!embedded;// Is 3D on for this viewer? Two DIFFERENT authorities on purpose:
//   embedded (the portal designer) -> the `view3d` prop, which the portal derives from
//     featureOn("view_3d") -- i.e. entitlement, which also makes every operator true.
//   public page -> `C.view3d` from get_config (migration 110), sourced from the operator
//     grant table only. index.html is anonymous and can never call portal-billing, so it
//     cannot use the entitlement path at all.
// A tenant whose staff can see 3D in their portal does NOT automatically expose it to
// shoppers: that needs a real grant row, which is the point of the split.
var view3dOn=embedded?!!view3d:!!C.view3d;var doorFixtures=useMemo(function(){return(Array.isArray(C.fixtures)?C.fixtures:[]).filter(function(f){return f&&(f.category||"door")==="door";});},[C.fixtures]);var rampFixtures=useMemo(function(){return(Array.isArray(C.fixtures)?C.fixtures:[]).filter(function(f){return f&&(f.category||"")==="ramp";});},[C.fixtures]);var windowFixtures=useMemo(function(){return(Array.isArray(C.fixtures)?C.fixtures:[]).filter(function(f){return f&&(f.category||"")==="window";});},[C.fixtures]);// Internal-only fixtures: the rep (embedded) designer can place them, but the customer-facing page
// must NOT offer them as placement options. These "placeable" lists drive the PICKERS + picker
// buttons only; the full memos above still feed isArchivedItem / swap / render so an already-placed
// internal-only fixture keeps rendering for the customer and never reads as archived.
var placeableDoors=customerFacing?doorFixtures.filter(function(f){return!f.internalOnly;}):doorFixtures;var placeableRamps=customerFacing?rampFixtures.filter(function(f){return!f.internalOnly;}):rampFixtures;var placeableWindows=customerFacing?windowFixtures.filter(function(f){return!f.internalOnly;}):windowFixtures;// Ramp is self-contained now (SIMPLE_RAMP_CFG), driven by the Ramp settings — NOT the built-in
// `ramp` layout item. Custom mode → the ramp picker (catalog styles); simple mode + offered → the
// simple ramp tool; otherwise render-only (old ramps still draw, but no new placement).
var rampMode=C.rampSettings&&C.rampSettings.mode||"simple";var rampEnabled=!!(C.rampSettings&&C.rampSettings.enabled);var rampCustom=rampMode==="custom"&&placeableRamps.length>0;var _useState55=useState(function(){var init={style:"",size:"",roofType:"",roofColor:"",cladding:""};C.options.forEach(function(o){init[o.id]=o.type==="counter"?o.options[0]:"";});return init;}),_useState56=_slicedToArray(_useState55,2),sel=_useState56[0],setSel=_useState56[1];// Catalog fixtures the current size INCLUDES → a placement tool keyed by the fixture id. Each
// renders in the "included — place or decline" row and, when armed, drops that EXACT fixture on
// the next wall click (doors/windows) or door (ramps). Empty until a style+size is chosen; the
// built-in door/window/ramp keep their own catalog pickers.
var includedFixtureTools=function(){var out={};if(!sel.style||!sel.size)return out;var st=(C.buildingStyles||[]).find(function(s){return s.value===sel.style;});if(!st)return out;var pickInc=function pickInc(map){if(!map||_typeof(map)!=="object")return null;if(map[sel.size]!=null)return map[sel.size];var want=normSizeLabel(sel.size);for(var k in map){if(normSizeLabel(k)===want)return map[k];}return null;};var qmap=pickInc(st.sizeInclusionQty);if(!qmap||_typeof(qmap)!=="object"||Array.isArray(qmap)){var arr=pickInc(st.sizeInclusions);qmap={};if(Array.isArray(arr))arr.forEach(function(k){qmap[k]=1;});}var fixtures=Array.isArray(C.fixtures)?C.fixtures:[];var _loop5=function _loop5(k){var fx=fixtures.find(function(f){return String(f.id)===k;});if(!fx)return 0;// continue
// built-in keys (loft etc.) are handled by their own ITEMS entry
if(customerFacing&&fx.internalOnly)return 0;// continue
// internal-only: rep can place it, customer can't add/decline it
var cat=fx.category||"door";out[k]={label:fx.name||"Item",color:cat==="window"?FIXTURE_WINDOW_COLOR:cat==="ramp"?FIXTURE_RAMP_COLOR:FIXTURE_DOOR_COLOR,icon:cat==="window"?"🪟":cat==="ramp"?"⬛":"🚪",shortLabel:fx.planLabel&&String(fx.planLabel).trim()||(fx.name||"ITEM").toUpperCase().slice(0,4),wallOnly:cat!=="ramp",doorSnap:cat==="ramp",width:(Number(fx.widthIn)||36)/12,height:0.5,includedFixture:_objectSpread({},fx)// placement marker: drop THIS specific fixture
};},_ret4;for(var k in qmap){_ret4=_loop5(k);if(_ret4===0)continue;}return out;}();var ITEMS=_objectSpread(_objectSpread(_objectSpread(_objectSpread(_objectSpread(_objectSpread(_objectSpread({},LEGACY_LAYOUT_FALLBACK),C.layoutItems),BUILT_IN_TOOLS),{},{fixtureDoor:FIXTURE_DOOR_CFG},placeableDoors.length?{doorPicker:DOOR_PICKER_CFG}:{}),rampCustom?{rampPicker:RAMP_PICKER_CFG}:{}),{},{// Ramp is ALWAYS the self-contained SIMPLE_RAMP_CFG (overrides any built-in `ramp` layout item),
// so every placed ramp renders. Placeable only when the tenant offers a SIMPLE ramp; custom mode
// and not-offered are render-only (the picker handles custom placement).
ramp:_objectSpread(_objectSpread({},SIMPLE_RAMP_CFG),{},{noPalette:!(rampMode==="simple"&&rampEnabled)})},placeableWindows.length?{windowPicker:WINDOW_PICKER_CFG}:{}),includedFixtureTools);var _useState57=useState(null),_useState58=_slicedToArray(_useState57,2),swapId=_useState58[0],setSwapId=_useState58[1];// id of a placed catalog fixture being SWAPPED to another
var _useState59=useState(null),_useState60=_slicedToArray(_useState59,2),doorPick=_useState60[0],setDoorPick=_useState60[1];// { wall, ptx, pty } while the door picker modal is open
var _useState61=useState(null),_useState62=_slicedToArray(_useState61,2),rampPick=_useState62[0],setRampPick=_useState62[1];// { door } while the ramp picker modal is open
var _useState63=useState(null),_useState64=_slicedToArray(_useState63,2),windowPick=_useState64[0],setWindowPick=_useState64[1];// { wall, ptx, pty } while the window picker modal is open
// A PLACED item is "archived" (option retired) if: a catalog fixture whose fixture is no longer
// in the active list (get_fixtures drops archived), or a built-in whose layoutItems cfg is flagged
// archived (get_config keeps it, noPalette+archived). Archived items still render on the design;
// the rep is nudged to Swap them for a current option. Never blocks rendering.
var isArchivedItem=function isArchivedItem(it){if(!it)return false;if(it.fixtureItemId){var pool=it.type==="window"?windowFixtures:it.type==="ramp"?rampFixtures:doorFixtures;return!pool.some(function(f){return String(f.id)===String(it.fixtureItemId);});}var c=ITEMS[it.type];return!!(c&&c.archived);};var accent=C.branding.accentColor||"#D97706";// White-label initials for the logo placeholder shown when no logo is set.
var initials=(C.branding.companyName||"").split(" ").filter(Boolean).slice(0,2).map(function(w){return w[0];}).join("").toUpperCase()||"SS";// Admin gate: ?admin=1 surfaces the GHL credentials panel. The credentials never
// round-trip through the browser — admin types them in, the Edge Function stores
// them in Supabase, and customers' browsers never see them.
// Never true when embedded: the URL is the HOST page's (the portal), and
// /portal.html?admin=1 must not surface the operator panel inside a tenant portal.
var isAdmin=useMemo(function(){if(embedded)return false;if(typeof window==="undefined")return false;return new URLSearchParams(window.location.search).get("admin")==="1";},[embedded]);// The 3D calibration editor is reachable by an operator on the public page AND by the
// builder in their own portal (the host opts in by passing setup3d, and only does so for
// owner/admin roles). This must stay SEPARATE from isAdmin: isAdmin also unlocks the GHL
// credentials panel, and the whole reason it is forced false when embedded is that
// /portal.html?admin=1 must never surface that panel inside a tenant's portal.
var showCal3D=isAdmin||Boolean(setup3d);// Options the user currently sees. Options without scoping are always in the
// list; scoped options join/leave as the user picks/changes building style.
var visibleOptions=useMemo(function(){return C.options.filter(function(o){return isOptionApplicable(o,sel.style);});},[C.options,sel.style]);// Roof colors come from the same palette (shingle/metal flags). A roof type is only offered
// when the tenant has >=1 active color in it; roof pricing is the chosen color's rate
// (server-side), exactly like paint. Empty until the owner adds roof colors in the portal.
var roofColorsFor=function roofColorsFor(type){var list=Array.isArray(C.colors)?C.colors:[];return type==="Shingle"?list.filter(function(c){return c.shingle;}):type==="Metal"?list.filter(function(c){return c.metal;}):[];};var roofTypes=["Shingle","Metal"].filter(function(t){return roofColorsFor(t).length>0;});// Cladding is a FIXED triple (we ship a texture for each; a tenant cannot invent a
// fourth), so it is a constant list like roofTypes rather than a catalog read. Empty
// string = "builder's standard", i.e. fall through to the style's own d3.siding, which
// is what every existing design does today.
//
// Offered ONLY where 3D is on for this viewer. Cladding is visual-only in v1 -- the pick
// manifests nowhere but the 3D view (plus a line of text on the estimate) -- so on a
// tenant without the 3D grant it was a dropdown that visibly did nothing, shipped to
// every public designer without opt-in (audit 2026-08-19). Gating it on view3dOn keeps
// an ungranted tenant's page byte-identical to before cladding existed, and the control
// appears together with 3D as each builder is switched on.
var claddingChoices=view3dOn?D3_CLADDING_CHOICES:[];// The paint option renders inline beside the Roof Options (same row), not in
// the option list below — see the Size/Roof/Paint row and renderPaintFields.
var paintOpt=visibleOptions.find(function(o){return o.type==="counter"&&o.id==="paint";})||null;// When the building style changes, snap any now-inapplicable option back to
// its default so a stale "Painted" (etc.) selection can't be silently sent
// along in the submit payload.
useEffect(function(){setSel(function(prev){var changed=false;var next=_objectSpread({},prev);C.options.forEach(function(opt){if(isOptionApplicable(opt,prev.style))return;var def=opt.type==="counter"?opt.options[0]:"";if(next[opt.id]!==def){next[opt.id]=def;changed=true;}});return changed?next:prev;});},[sel.style,C.options]);// Phase 4a: which placeable items are INCLUDED (free) with the selected
// style+size — from get_config's per-style sizeInclusions map. Everything else
// is an "additional" (chargeable) option. Empty until a style+size is chosen.
// includedItemQty maps item key -> included quantity (loft = sq ft, doors = count)
// from the parallel sizeInclusionQty map; configs predating migration 039 fall
// back to quantity 1 per included key.
var includedItemQty=useMemo(function(){if(!sel.style||!sel.size)return{};var st=C.buildingStyles.find(function(s){return s.value===sel.style;});if(!st)return{};// Size labels can drift between "12x16" and "12×16" (CSV rewrite vs. saved design),
// so fall back to a normalized-label match like the sizePricing lookup does.
var pick=function pick(map){if(!map||_typeof(map)!=="object")return null;if(map[sel.size]!=null)return map[sel.size];var want=normSizeLabel(sel.size);for(var k in map){if(normSizeLabel(k)===want)return map[k];}return null;};var qmap=pick(st.sizeInclusionQty);if(qmap&&_typeof(qmap)==="object"&&!Array.isArray(qmap)){var _out={};for(var k in qmap)_out[k]=Math.max(1,Number(qmap[k])||1);return _out;}var arr=pick(st.sizeInclusions);var out={};if(Array.isArray(arr)){var _iterator14=_createForOfIteratorHelper(arr),_step14;try{for(_iterator14.s();!(_step14=_iterator14.n()).done;){var _k8=_step14.value;out[_k8]=1;}}catch(err){_iterator14.e(err);}finally{_iterator14.f();}}return out;},[sel.style,sel.size,C.buildingStyles]);var includedItemKeys=useMemo(function(){return Object.keys(includedItemQty);},[includedItemQty]);var _useState65=useState({name:"",phone:"",email:"",street:"",city:"",state:"",zip:""}),_useState66=_slicedToArray(_useState65,2),contact=_useState66[0],setContact=_useState66[1];// Lead-capture gate: shoppers give name + phone before designing (the customer link is a
// lead-gen tool). Bypassed for a returning shopper arriving via a saved-design link (?id=,
// which loads their contact), the operator preview (?admin=1), and once remembered in this
// browser. See <LeadGate/> rendered at the top of the return.
var _useState67=useState(function(){try{var params=new URLSearchParams(location.search);if(params.get("id")||params.get("admin")==="1")return true;if(localStorage.getItem("ss_gate_"+(C.clientId||"")))return true;}catch(_e){}return false;}),_useState68=_slicedToArray(_useState67,2),gatePassed=_useState68[0],setGatePassed=_useState68[1];// Default each side to the tenant's default palette color (e.g. "Unpainted"); a saved
// design overrides this from design.paint_colors on load.
var _useState69=useState(function(){var list=Array.isArray(C.colors)?C.colors:[];var dflt=function dflt(k){var d=list.find(function(c){return(k==="body"?c.siding:c.trim)&&c.isDefault;});return d?d.label:"";};return{body:dflt("body"),trim:dflt("trim")};}),_useState70=_slicedToArray(_useState69,2),paintColors=_useState70[0],setPaintColors=_useState70[1];// Tracks when the shopper picked an "allow custom" color and is typing an exact value
// (so the custom text box stays open even while paintColors.body/trim is momentarily "").
var _useState71=useState({body:false,trim:false}),_useState72=_slicedToArray(_useState71,2),paintCustom=_useState72[0],setPaintCustom=_useState72[1];// Roof: type (Shingle/Metal) + color live in `sel` (saved with the design); this tracks the
// transient "typing a custom roof color" state, same as paintCustom.
var _useState73=useState(false),_useState74=_slicedToArray(_useState73,2),roofCustom=_useState74[0],setRoofCustom=_useState74[1];var _useState75=useState([]),_useState76=_slicedToArray(_useState75,2),customOptions=_useState76[0],setCustomOptions=_useState76[1];var _useState77=useState({}),_useState78=_slicedToArray(_useState77,2),roDimensions=_useState78[0],setRoDimensions=_useState78[1];var _useState79=useState(10),_useState80=_slicedToArray(_useState79,2),bldgW=_useState80[0],setShedW=_useState80[1];var _useState81=useState(12),_useState82=_slicedToArray(_useState81,2),bldgH=_useState82[0],setShedH=_useState82[1];var _useState83=useState(null),_useState84=_slicedToArray(_useState83,2),activeTool=_useState84[0],setActiveTool=_useState84[1];var _useState85=useState([]),_useState86=_slicedToArray(_useState85,2),items=_useState86[0],setItems=_useState86[1];var _useState87=useState(null),_useState88=_slicedToArray(_useState87,2),selectedId=_useState88[0],setSelectedId=_useState88[1];var _useState89=useState(null),_useState90=_slicedToArray(_useState89,2),editingNoteId=_useState90[0],setEditingNoteId=_useState90[1];// note being typed in-place on the canvas
// Pick-one-to-remove mode ({ type }): entered from a Details row's × when several
// "each"-priced items of that type are placed — the plan highlights them and the
// rest of the page is blocked until the user clicks one (or cancels).
var _useState91=useState(null),_useState92=_slicedToArray(_useState91,2),pendingRemoval=_useState92[0],setPendingRemoval=_useState92[1];// "+ Add Delivery Fee" clicked — shows the delivery row before a value is typed.
var _useState93=useState(false),_useState94=_slicedToArray(_useState93,2),deliveryOpen=_useState94[0],setDeliveryOpen=_useState94[1];useEffect(function(){// Reopened designs restore sel.deliveryFee without deliveryOpen; latch the row
// open so clearing the amount mid-edit doesn't unmount the input underneath the
// user (only the row's × closes it).
if(!deliveryOpen&&String(sel.deliveryFee||"")!=="")setDeliveryOpen(true);},[deliveryOpen,sel.deliveryFee]);useEffect(function(){if(!pendingRemoval)return;// ESC cancels. Every other key is swallowed in the capture phase: the scrim only
// blocks POINTERS, so without this Tab+Enter could still fire buttons underneath
// it (another row's ×, even Get Quote) while the page looks blocked.
var onKey=function onKey(e){if(e.key==="Escape"){setPendingRemoval(null);return;}e.preventDefault();e.stopPropagation();};window.addEventListener("keydown",onKey,true);return function(){return window.removeEventListener("keydown",onKey,true);};},[pendingRemoval]);useEffect(function(){// Auto-exit pick mode if the last item of the target row disappears. priceRowMatcher,
// not a type compare: pendingRemoval.type is a Details ROW key, and catalog rows carry
// fx:/win:/ramp: keys that match no item's type — the type compare exited pick mode
// instantly for those rows (audit 2026-08-20).
if(pendingRemoval&&!items.some(priceRowMatcher(pendingRemoval.type)))setPendingRemoval(null);},[items,pendingRemoval]);var _useState95=useState(null),_useState96=_slicedToArray(_useState95,2),dragging=_useState96[0],setDragging=_useState96[1];var _useState97=useState(null),_useState98=_slicedToArray(_useState97,2),resizing=_useState98[0],setResizing=_useState98[1];var _useState99=useState(false),_useState100=_slicedToArray(_useState99,2),showExport=_useState100[0],setShowExport=_useState100[1];var _useState101=useState(null),_useState102=_slicedToArray(_useState101,2),exportUrl=_useState102[0],setExportUrl=_useState102[1];var _useState103=useState(false),_useState104=_slicedToArray(_useState103,2),submitting=_useState104[0],setSubmitting=_useState104[1];var _useState105=useState(false),_useState106=_slicedToArray(_useState105,2),submitted=_useState106[0],setSubmitted=_useState106[1];var _useState107=useState(null),_useState108=_slicedToArray(_useState107,2),submitError=_useState108[0],setSubmitError=_useState108[1];// After a successful save, holds { code, viewUrl, imageUrl } for the success screen
var _useState109=useState(null),_useState110=_slicedToArray(_useState109,2),savedDesign=_useState110[0],setSavedDesign=_useState110[1];// Current design's short code (set when a design is loaded or saved). Drives the
// "all designs on this estimate" version list shown in the editor + success screen.
var _useState111=useState(null),_useState112=_slicedToArray(_useState111,2),designCode=_useState112[0],setDesignCode=_useState112[1];// All versions of the current design (this estimate), newest first.
var _useState113=useState([]),_useState114=_slicedToArray(_useState113,2),estimateVersions=_useState114[0],setEstimateVersions=_useState114[1];// Which version is currently loaded in the editor (null = the latest). Marks "Viewing".
var _useState115=useState(null),_useState116=_slicedToArray(_useState115,2),viewingVersion=_useState116[0],setViewingVersion=_useState116[1];// Whether the "all designs on this estimate" dropdown is expanded (collapsed by default).
var _useState117=useState(false),_useState118=_slicedToArray(_useState117,2),versionsOpen=_useState118[0],setVersionsOpen=_useState118[1];// "Additional options" (custom line items) is collapsed by default behind a subtle toggle.
var _useState119=useState(false),_useState120=_slicedToArray(_useState119,2),additionalOpen=_useState120[0],setAdditionalOpen=_useState120[1];// Every enabled contact field filled, phone a real 10 digits — the same bar submitQuote
// enforces. Drives the public Details gate: a shopper sees quote details only after
// giving full contact info (which is what makes them a capturable lead).
var contactComplete=useMemo(function(){var req=["name","email","phone","street","city","state","zip"].filter(function(f){return C.contactFields.includes(f);});if(req.some(function(f){return!String(contact[f]||"").trim();}))return false;if(C.contactFields.includes("phone")&&String(contact.phone||"").replace(/\D/g,"").length!==10)return false;return true;},[contact,C.contactFields]);// The public Details section is locked until the contact form is complete. Content is
// ALSO gated on this (not just the click), so emptying a field after opening re-locks
// the details instead of leaving prices on screen behind a stale open state.
var detailsLocked=customerFacing&&!contactComplete;// Silent lead save, once per page load, the first time a shopper opens Details: they
// have just typed full contact info and asked to see prices — that IS a lead, even if
// they never press submit. Best-effort fire-and-forget (capture-lead validates and
// upserts into the tenant's GHL); it must never block or break the designer.
var leadCapturedRef=useRef(false);var captureLeadSilently=function captureLeadSilently(){if(!customerFacing||leadCapturedRef.current||!contactComplete)return;leadCapturedRef.current=true;try{supabase.functions.invoke("capture-lead",{body:{clientId:C.clientId,source:"details",// vs the gate's default — "asked for prices" ranks higher
name:String(contact.name||"").trim(),phone:String(contact.phone||"").trim(),email:String(contact.email||"").trim(),street:String(contact.street||"").trim(),city:String(contact.city||"").trim(),state:String(contact.state||"").trim(),zip:String(contact.zip||"").trim()}});}catch(_e){/* lead capture must never break the designer */}};// Draft-design capture (migration 063). The same Details-open moment that captures the
// lead also saves WHAT they designed, as a status='draft' designs row — so the portal
// can open a browsing lead's actual floor plan even though they never pressed submit.
// A later real submit reuses the same short_code (currentDesignIdRef) and save_design
// promotes the row to 'sent'. Silent and best-effort like the lead capture: no PDF is
// rendered, no URL is rewritten, nothing changes for the visitor.
var draftStateRef=useRef(null);// JSON of the last draft-saved payload (skip no-op re-saves)
var isDraftRef=useRef(false);// the row behind currentDesignIdRef is a draft, safe to re-save
var saveDraftSilently=function saveDraftSilently(){if(!customerFacing||!supabase)return;// Never write over a row we didn't create as a draft: someone re-opening a SUBMITTED
// design from a share link must not have it silently rewritten by browsing further.
if(currentDesignIdRef.current&&!isDraftRef.current)return;if(!sel.style&&!sel.size&&items.length===0)return;// nothing designed yet
var body={p_contact:contact,p_selections:sel,p_paint_colors:paintColors,p_items:items,p_custom_options:customOptions,p_ro_dimensions:roDimensions,p_bldg_w:bldgW,p_bldg_h:bldgH};var snapshot=JSON.stringify(body);if(snapshot===draftStateRef.current)return;// unchanged since the last draft save
var code=currentDesignIdRef.current||genShortCode();_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee4(){var _yield$supabase$rpc,error;return _regeneratorRuntime().wrap(function _callee4$(_context4){while(1)switch(_context4.prev=_context4.next){case 0:_context4.prev=0;_context4.next=3;return supabase.rpc("save_design",_objectSpread(_objectSpread({p_code:code,p_client_id:C.clientId},body),{},{p_image_url:null,// drafts carry no PDF; save_design preserves any existing one
p_status:"draft"// the ONLY status the RPC accepts from an anon caller
}));case 3:_yield$supabase$rpc=_context4.sent;error=_yield$supabase$rpc.error;if(!error){_context4.next=7;break;}return _context4.abrupt("return");case 7:// best-effort: a failed draft save is invisible by design
currentDesignIdRef.current=code;isDraftRef.current=true;draftStateRef.current=snapshot;_context4.next=14;break;case 12:_context4.prev=12;_context4.t0=_context4["catch"](0);case 14:case"end":return _context4.stop();}},_callee4,null,[[0,12]]);}))();};// ─── Inventory (migration 075) — embedded-only ───
// inventoryUnitRef: the unit a "Send estimate" flow came from; the submit success path
// links the new design back to it. inventoryMaster: non-null while an inventory MASTER
// design (status='inventory') is open — submit is blocked (a master must never become a
// customer estimate) and the save button flips to "Update Inventory Building".
var inventoryUnitRef=useRef(null);// The inventory unit an ALREADY-SAVED design was quoted from (designs.inventory_unit_id,
// read at load). Distinct from inventoryUnitRef, which arms a not-yet-submitted
// send-estimate flow — this one survives reopening the estimate later.
var _useState121=useState(null),_useState122=_slicedToArray(_useState121,2),designUnit=_useState122[0],setDesignUnit=_useState122[1];// { id, serial, lifecycle } | null
// Staff chose "Design a new build instead" on a locked estimate: the plan unlocks and
// the next submit saves a NEW version that is no longer tied to the unit.
var _useState123=useState(false),_useState124=_slicedToArray(_useState123,2),newBuildMode=_useState124[0],setNewBuildMode=_useState124[1];var _useState125=useState(null),_useState126=_slicedToArray(_useState125,2),inventoryMaster=_useState126[0],setInventoryMaster=_useState126[1];// { code, unitId, priceCents, locationId } | null
var _useState127=useState(null),_useState128=_slicedToArray(_useState127,2),invDialog=_useState128[0],setInvDialog=_useState128[1];// { busy, err, price, done } | null — price/confirm only (location is inline now)
// The inventory Save bar (inline location dropdown + button) appears ONLY for a NEW inventory
// build ("+ New inventory building" → openDesign.blank) or an OPENED inventory master — never on
// an ordinary customer design. Location is chosen inline, beside the Save button.
var _useState129=useState(false),_useState130=_slicedToArray(_useState129,2),inventoryNew=_useState130[0],setInventoryNew=_useState130[1];var _useState131=useState([]),_useState132=_slicedToArray(_useState131,2),invLocations=_useState132[0],setInvLocations=_useState132[1];// [{id, name, city}]
var _useState133=useState(""),_useState134=_slicedToArray(_useState133,2),invLocationId=_useState134[0],setInvLocationId=_useState134[1];// where this building sits
var invLocLoadedRef=useRef(false);// PLAN LOCK (Carolyn, 2026-08-02): "Building is BUILT". An estimate for an inventory
// building describes a structure that already physically exists, so its floor plan,
// size, style, roof and colours are not negotiable — only the money lines are (custom
// options, discount, delivery). Applies to the send-estimate flow AND to reopening that
// estimate later, on the public share link too. NEVER applies to the inventory MASTER
// itself (that is the builder editing their own building via "Update Inventory
// Building"), and staff can lift it deliberately with "Design a new build instead".
//
// …but only once the building actually EXISTS (migration 102). A unit can now be sold
// while it is still Requested or in the build queue, and locking those would mean the buyer
// of a building that has not been cut yet cannot change anything about it — the opposite of
// what a pre-build sale is for. So the lock keys on the ladder, not on the mere existence
// of a link. UNKNOWN FAILS TOWARD LOCKED: that is today's behaviour, so this can only ever
// loosen where we are sure. (Seven rungs since migration 105 — `accepted` retired; the
// built rank is 3 — see _shared/inventoryLifecycle.ts, which owns the ladder.)
var INV_BUILT_RANK=3;var INV_RANKS={requested:0,in_queue:1,scheduled_build:2,built:3,scheduled_delivery:4,at_location:5,delivered:6};var unitIsBuilt=!designUnit||!designUnit.lifecycle||((_INV_RANKS$designUnit=INV_RANKS[designUnit.lifecycle])!==null&&_INV_RANKS$designUnit!==void 0?_INV_RANKS$designUnit:INV_BUILT_RANK)>=INV_BUILT_RANK;var planLocked=Boolean((inventoryUnitRef.current||designUnit)&&!inventoryMaster&&!newBuildMode&&unitIsBuilt);// The canvas handlers are useCallbacks with their own dep arrays — reading the lock
// through a ref keeps them from capturing a stale value (and from re-creating on every
// lock change). Assigned during render on purpose: a useEffect sync would lag one
// render, and one render is long enough to drag an item on a building that is built.
var planLockedRef=useRef(false);planLockedRef.current=planLocked;// The pre-tax building total, mirroring the Details subtotal WITHOUT the customer-
// specific lines (delivery, discounts) — an asking price describes the building alone.
var inventoryQuotePrefill=function inventoryQuotePrefill(){try{var selRows=computeSelectionRows(sel,paintColors,C,items);var priceRows=C.showPricing?computeLayoutPricingRows(items,sel,customOptions,C,paintColors).rows:[];var roList=items.filter(function(i){return i.type==="roughOpening";});var lp=C.layoutPricing&&C.layoutPricing.roughOpening;var ov=lp&&lp.byStyle&&sel.style?lp.byStyle[sel.style]:null;var roRate=lp?Number(ov&&ov.rate!=null?ov.rate:lp.rate)||0:0;var customTotal=(customOptions||[]).reduce(function(s,r){var amt=Math.max(0,parseFloat(r&&r.amount)||0);var q=r&&r.qty?Math.abs(parseInt(r.qty,10))||1:1;return s+amt*q;},0);return Math.max(0,selRows.reduce(function(s,r){return s+(Number(r.total)||0);},0)+priceRows.reduce(function(s,r){return s+(Number(r.total)||0);},0)+(C.showPricing?roList.length*roRate:0)+customTotal);}catch(_e){return 0;}};// Load the tenant's locations once we enter an inventory context (new build or opened master),
// so the inline location dropdown by the Save button is ready. targetClientId names the tenant
// explicitly — the component's supabase client lacks portal.html's operator view-as injection.
useEffect(function(){if(!embedded||!supabase||invLocLoadedRef.current)return;if(!(inventoryNew||inventoryMaster))return;invLocLoadedRef.current=true;_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee5(){var _yield$supabase$funct,data;return _regeneratorRuntime().wrap(function _callee5$(_context5){while(1)switch(_context5.prev=_context5.next){case 0:_context5.prev=0;_context5.next=3;return supabase.functions.invoke("portal-settings",{body:{action:"list_locations",targetClientId:C.clientId}});case 3:_yield$supabase$funct=_context5.sent;data=_yield$supabase$funct.data;if(data&&Array.isArray(data.locations))setInvLocations(data.locations);_context5.next=10;break;case 8:_context5.prev=8;_context5.t0=_context5["catch"](0);case 10:case"end":return _context5.stop();}},_callee5,null,[[0,8]]);}))();},[embedded,supabase,inventoryNew,inventoryMaster,C.clientId]);var openInventoryDialog=function openInventoryDialog(){if(!sel.style||!sel.size){setSubmitError("Pick a Building Style and Size before saving to inventory.");return;}setSubmitError(null);var isUpdate=Boolean(inventoryMaster&&inventoryMaster.unitId);var prefill=isUpdate&&inventoryMaster.priceCents!=null?inventoryMaster.priceCents/100:inventoryQuotePrefill();// Location is chosen on the inline dropdown beside the Save button; this dialog only confirms price.
setInvDialog({busy:false,err:null,price:prefill>0?String(Math.round(prefill*100)/100):"",done:null});};var saveInventory=/*#__PURE__*/function(){var _ref18=_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee6(){var _invDialog$price,isUpdate,code,imageUrl,canvas,jpegDataUrl,jpegBin,jpegBytes,i,invPages,invShot3d,blob,filePath,up,priceStr,askingPriceCents,n,_yield$supabase$funct2,data,error,m,b;return _regeneratorRuntime().wrap(function _callee6$(_context6){while(1)switch(_context6.prev=_context6.next){case 0:if(!(!invDialog||invDialog.busy)){_context6.next=2;break;}return _context6.abrupt("return");case 2:setInvDialog(function(d){return _objectSpread(_objectSpread({},d),{},{busy:true,err:null});});_context6.prev=3;isUpdate=Boolean(inventoryMaster&&inventoryMaster.unitId);code=isUpdate?inventoryMaster.code:genShortCode();// Render + upload the plan PDF — the same steps submitQuote runs, deliberately
// duplicated rather than extracted: refactoring the money path for a reuse win
// is a bad trade. Best-effort: an upload failure must not lose the unit.
imageUrl=null;_context6.prev=7;canvas=renderExportCanvas();jpegDataUrl=canvas.toDataURL("image/jpeg",0.92);jpegBin=atob(jpegDataUrl.split(",")[1]);jpegBytes=new Uint8Array(jpegBin.length);for(i=0;i<jpegBin.length;i++)jpegBytes[i]=jpegBin.charCodeAt(i);// Multi-page builder (beta-2.0 replaced the single-page one): the 3D snapshot,
// when the builder took one, rides along as page 2 — same as submitQuote.
invPages=[{bytes:jpegBytes,w:canvas.width,h:canvas.height}];invShot3d=render3DSnapshotRef.current;if(invShot3d)invPages.push({bytes:dataUrlToBytes(invShot3d.url),w:invShot3d.w,h:invShot3d.h});blob=buildPdfFromJpegPages(invPages);filePath="".concat(C.clientId,"/").concat(code,"-").concat(Date.now(),".pdf");_context6.next=20;return supabase.storage.from("floor-plans").upload(filePath,blob,{upsert:false,contentType:"application/pdf",cacheControl:"0"});case 20:up=_context6.sent;if(!up.error)imageUrl=supabase.storage.from("floor-plans").getPublicUrl(filePath).data.publicUrl;_context6.next=26;break;case 24:_context6.prev=24;_context6.t0=_context6["catch"](7);case 26:// A typo must never become $0 on the lot. `Number("12,5o0")` is NaN, and the old
// `|| 0` turned that into a building publicly listed at $0.
priceStr=String((_invDialog$price=invDialog.price)!==null&&_invDialog$price!==void 0?_invDialog$price:"").replace(/[$,\s]/g,"");askingPriceCents=null;if(!(priceStr!=="")){_context6.next=34;break;}n=Number(priceStr);if(!(!Number.isFinite(n)||n<0)){_context6.next=33;break;}setInvDialog(function(d){return d&&_objectSpread(_objectSpread({},d),{},{busy:false,err:"Enter the asking price as a number, e.g. 8950 — or leave it blank."});});return _context6.abrupt("return");case 33:askingPriceCents=Math.round(n*100);case 34:_context6.next=36;return supabase.functions.invoke("portal-settings",{body:_objectSpread(_objectSpread({action:"save_inventory",targetClientId:C.clientId},isUpdate?{unitId:inventoryMaster.unitId}:{shortCode:code}),{},{imageUrl:imageUrl,askingPriceCents:askingPriceCents,locationId:invLocationId||null,selections:sel,paintColors:paintColors,items:items,customOptions:customOptions,roDimensions:roDimensions,bldgW:bldgW,bldgH:bldgH})});case 36:_yield$supabase$funct2=_context6.sent;data=_yield$supabase$funct2.data;error=_yield$supabase$funct2.error;if(!error){_context6.next=51;break;}m="Save failed — try again.";_context6.prev=41;_context6.next=44;return error.context.clone().json();case 44:b=_context6.sent;if(b&&b.error)m=b.error;_context6.next=50;break;case 48:_context6.prev=48;_context6.t1=_context6["catch"](41);case 50:throw new Error(m);case 51:if(!(!data||data.error)){_context6.next=53;break;}throw new Error(data&&data.error||"Save failed — try again.");case 53:setInvDialog(function(d){var _data$serial;return _objectSpread(_objectSpread({},d),{},{busy:false,done:{serial:(_data$serial=data.serial)!==null&&_data$serial!==void 0?_data$serial:null,updated:isUpdate}});});if(!isUpdate){// The design on screen IS now this unit's master — further saves are updates.
currentDesignIdRef.current=code;setDesignCode(code);setInventoryMaster({code:code,unitId:data.unitId,priceCents:askingPriceCents,locationId:invLocationId||null});}else{setInventoryMaster(function(m){return m&&_objectSpread(_objectSpread({},m),{},{priceCents:askingPriceCents,locationId:invLocationId||null});});}if(onSaved)onSaved();_context6.next=61;break;case 58:_context6.prev=58;_context6.t2=_context6["catch"](3);setInvDialog(function(d){return _objectSpread(_objectSpread({},d),{},{busy:false,err:_context6.t2.message||String(_context6.t2)});});case 61:case"end":return _context6.stop();}},_callee6,null,[[3,58],[7,24],[41,48]]);}));return function saveInventory(){return _ref18.apply(this,arguments);};}();// Details NEVER auto-opens. If the form drops back to incomplete (a cleared field, the
// address search resetting values), the section CLOSES — so re-completing the form can
// never resurface it without a fresh click. Without this, an earlier open survived the
// re-lock and the rows reappeared "by themselves" the moment the last field was filled.
useEffect(function(){if(detailsLocked&&additionalOpen)setAdditionalOpen(false);},[detailsLocked]);// The lead save keys off VISIBILITY, not the click handler: whatever path reveals the
// details, the contact is saved. The ref in captureLeadSilently keeps it once per load,
// and its customerFacing guard keeps the portal designer out entirely. The draft save
// rides the same moment: who they are (lead) and what they designed (draft) together.
useEffect(function(){if(additionalOpen&&!detailsLocked){captureLeadSilently();saveDraftSilently();}},[additionalOpen,detailsLocked]);var _useState135=useState(null),_useState136=_slicedToArray(_useState135,2),toast=_useState136[0],setToast=_useState136[1];// ─── 3D view state ───
var _useState137=useState(false),_useState138=_slicedToArray(_useState137,2),show3D=_useState138[0],setShow3D=_useState138[1];// ── Docked view-only 3D (Carolyn 2026-08-19) ──
// Mutually exclusive with show3D for free: the panel renders INSIDE the
// {!(show3D || adminCalPreview)} guard below, so two live WebGL contexts are
// structurally impossible rather than merely avoided.
var _useState139=useState(false),_useState140=_slicedToArray(_useState139,2),dock3D=_useState140[0],setDock3D=_useState140[1];// Boolean only. Storing the measured width would setState on every frame of a
// window drag, on top of the SVG re-render that already costs.
var _useState141=useState(false),_useState142=_slicedToArray(_useState141,2),dockCapable=_useState142[0],setDockCapable=_useState142[1];var canvasRowObsRef=useRef(null);// Callback ref, NOT useEffect([]) — the observed row unmounts whenever the
// full-screen 3D opens, and an effect-attached observer would keep watching a
// detached node, freezing dockCapable after the first trip through the editor.
var canvasRowRef=useCallback(function(node){if(canvasRowObsRef.current){canvasRowObsRef.current.disconnect();canvasRowObsRef.current=null;}if(!node||typeof ResizeObserver==="undefined")return;// Coarse pointer is a touch-trap guard, not a width proxy: an iPad in landscape
// is wide enough to dock, but the canvas sets touchAction:"none" for orbit, so a
// finger on the panel could never scroll the designer host — the only scroller.
var coarse=typeof window.matchMedia==="function"&&window.matchMedia("(pointer: coarse)").matches;var measure=function measure(){var w=node.clientWidth||0;// Zero means the row is not being rendered right now — the portal keeps the Designer
// mounted but hidden while the builder is on another tab. It does NOT mean "too
// narrow", and treating it that way collapsed the dock on every tab switch.
if(!w)return;// 40px of hysteresis on the way down, so an appearing scrollbar cannot
// oscillate the rule at the threshold.
setDockCapable(function(prev){return!coarse&&(prev?w>=SS_DOCK_MIN_ROW_W-40:w>=SS_DOCK_MIN_ROW_W);});};// Measure SYNCHRONOUSLY here, and let the observer handle changes after.
// ResizeObserver delivery rides the rendering steps, so its first callback is
// not guaranteed to land before the customer clicks the 3D button — and in a
// backgrounded tab it never lands at all. Waiting for it would silently open
// the full-screen modal on a wide screen that should have docked.
measure();var ro=new ResizeObserver(measure);ro.observe(node);canvasRowObsRef.current=ro;},[]);useEffect(function(){return function(){if(canvasRowObsRef.current)canvasRowObsRef.current.disconnect();};},[]);// Narrowing collapses the dock. Deliberately does NOT open the full-screen modal
// instead: a modal appearing because someone dragged a window edge is hostile,
// and the button is right there.
// PORTAL ONLY for now (Carolyn, 2026-08-20). index.html and portal.html load the SAME
// compiled bundle, so without the `embedded` term the dock would also appear on the
// anonymous public designer for any tenant holding a view_3d grant. `embedded` is true
// only when the portal's Designer tab is hosting us. Shipping it to the public page
// later is exactly this one term.
var dockOn=dockCapable&&embedded;useEffect(function(){if(!dockOn&&dock3D)setDock3D(false);},[dockOn,dock3D]);// Losing the grant mid-session must close the dock, not leave it rendering. The portal
// refetches entitlements on every session-token refresh, so view3dOn genuinely can flip
// true -> false with the designer still mounted — an operator revoking view_3d, or a
// grant simply reaching its expiry. The render site is gated on view3dOn as well; this
// clears the state sitting behind that gate so nothing stale survives a re-grant.
useEffect(function(){if(!view3dOn&&dock3D)setDock3D(false);},[view3dOn,dock3D]);// Latest captured 3D snapshot ({ url, w, h } — a JPEG data-URL) — becomes
// page 2 of the quote PDF on submit/download. Kept in a ref (it's large and
// never rendered); has3DSnapshot mirrors it for button labels.
var render3DSnapshotRef=useRef(null);var _useState143=useState(false),_useState144=_slicedToArray(_useState143,2),has3DSnapshot=_useState144[0],setHas3DSnapshot=_useState144[1];var svgRef=useRef(null);// After a drag or resize gesture ends, the trailing click on the SVG
// would otherwise re-run the hit test and deselect the item if the cursor
// ended outside its bounds. This ref signals "ignore the click that follows".
var justGesturedRef=useRef(false);// Gesture-movement tracking: a press only counts as a drag/resize (and thus
// swallows its trailing click) if the pointer actually moved past a jitter
// threshold. A stationary click must SURVIVE so clicking a selected note can
// enter in-place edit — an unconditional swallow made notes uneditable.
var movedRef=useRef(false);var gestureStartRef=useRef(null);// {x,y} in client px at pointer-down
// PostMessage listener
useEffect(function(){var handler=function handler(e){if(!ssAllowedOrigin(e.origin))return;if(e.data&&e.data.type==="structureConfig"){var d=e.data;setSel(function(p){var n=_objectSpread({},p);Object.keys(d).forEach(function(k){if(k!=="type"&&k in n)n[k]=d[k];});return n;});if(d.name||d.phone||d.email){setContact(function(p){return _objectSpread(_objectSpread({},p),{},{name:d.name||p.name,phone:d.phone||p.phone,email:d.email||p.email,street:d.street||p.street,city:d.city||p.city,state:d.state||p.state,zip:d.zip||p.zip});});if(d.name&&d.phone)setGatePassed(true);// host pre-satisfied the lead gate
}}};window.addEventListener("message",handler);return function(){return window.removeEventListener("message",handler);};},[]);// ─── Supabase client (browser-safe anon key, baked-in — config rows can't
// redirect the data connection) ───
var supabase=useMemo(function(){return createClient(SUPABASE_URL,SUPABASE_ANON_KEY);},[]);// Tracks the design currently being edited (set on load via ?id=, then on save)
var currentDesignIdRef=useRef(null);// GHL identifiers for the design currently being edited. When set (loaded from
// the saved row or returned by the Edge Function), the next submit becomes a
// PUT/update of the existing GHL estimate instead of a POST/create.
var ghlContactIdRef=useRef(null);var ghlEstimateIdRef=useRef(null);var ghlEstimateNumberRef=useRef(null);// Mirrors ghlEstimateIdRef in state so the submit button can re-render its label
// ("Get Quote" vs "Resubmit for Updated Estimate") when a design loads.
var _useState145=useState(false),_useState146=_slicedToArray(_useState145,2),hasExistingEstimate=_useState146[0],setHasExistingEstimate=_useState146[1];// Admin panel state — only used when isAdmin
var _useState147=useState(""),_useState148=_slicedToArray(_useState147,2),adminPwd=_useState148[0],setAdminPwd=_useState148[1];var _useState149=useState(""),_useState150=_slicedToArray(_useState149,2),adminLocId=_useState150[0],setAdminLocId=_useState150[1];var _useState151=useState(""),_useState152=_slicedToArray(_useState151,2),adminApiKey=_useState152[0],setAdminApiKey=_useState152[1];var _useState153=useState(false),_useState154=_slicedToArray(_useState153,2),adminBusy=_useState154[0],setAdminBusy=_useState154[1];var _useState155=useState(null),_useState156=_slicedToArray(_useState155,2),adminStatus=_useState156[0],setAdminStatus=_useState156[1];// {configured, ghlLocationIdMasked, updatedAt} | null
var _useState157=useState(null),_useState158=_slicedToArray(_useState157,2),adminMsg=_useState158[0],setAdminMsg=_useState158[1];// {ok, msg} | null
// 3D style calibration (operator): tune a style's d3 spec against its
// four-side reference photos, preview live, save to the config row.
var _useState159=useState(null),_useState160=_slicedToArray(_useState159,2),adminCal=_useState160[0],setAdminCal=_useState160[1];// { styleValue, spec, photos: [url×4] } | null
var _useState161=useState(null),_useState162=_slicedToArray(_useState161,2),adminCalMsg=_useState162[0],setAdminCalMsg=_useState162[1];// {ok, msg} | null
var _useState163=useState(false),_useState164=_slicedToArray(_useState163,2),adminCalBusy=_useState164[0],setAdminCalBusy=_useState164[1];var _useState165=useState(false),_useState166=_slicedToArray(_useState165,2),adminCalPreview=_useState166[0],setAdminCalPreview=_useState166[1];// Building scan (094): { busy, step, err, measured, file, status } for the selected style.
var _useState167=useState({busy:false,step:null,err:null,measured:null,file:null,status:"none",aiReady:null}),_useState168=_slicedToArray(_useState167,2),scan=_useState168[0],setScan=_useState168[1];// Prevents the size-change effect from clearing items when we're rehydrating
// a saved design (sel.size and items get set together).
var prevSizeRef=useRef("");// Outcomes of a size-change reflow. `reflowNote` is advisory (items moved wall or were
// shortened); `sizeBlock` is the refusal — { from, to, items } — shown when something
// could not be placed at all, with the size already reverted.
var _useState169=useState(null),_useState170=_slicedToArray(_useState169,2),reflowNote=_useState170[0],setReflowNote=_useState170[1];var _useState171=useState(null),_useState172=_slicedToArray(_useState171,2),sizeBlock=_useState172[0],setSizeBlock=_useState172[1];// Google Places "search for address" widget that auto-fills the four address
// fields below it. Uses google.maps.places.PlaceAutocompleteElement (Places API
// New) — the replacement for the deprecated Autocomplete class. The element
// brings its own input + dropdown; we mount it into a container <div> and
// listen for gmp-select. On selection we resolve the chosen place's address
// components and populate street / city / state / zip. When the key is empty,
// this is a no-op and the container renders empty (the row hides itself).
var attachStreetAutocomplete=useCallback(function(container){var mapsKey=C.googleMapsApiKey||DEFAULT_GOOGLE_MAPS_API_KEY;if(!container||!mapsKey)return;loadGoogleMapsPlaces(mapsKey).then(/*#__PURE__*/function(){var _ref19=_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee8(google){var _yield$google$maps$im,PlaceAutocompleteElement,pa,sizeTries,sizeToFields;return _regeneratorRuntime().wrap(function _callee8$(_context8){while(1)switch(_context8.prev=_context8.next){case 0:if(container.isConnected){_context8.next=2;break;}return _context8.abrupt("return");case 2:_context8.next=4;return google.maps.importLibrary("places");case 4:_yield$google$maps$im=_context8.sent;PlaceAutocompleteElement=_yield$google$maps$im.PlaceAutocompleteElement;pa=new PlaceAutocompleteElement({includedRegionCodes:["us"]});// Google's gmp-place-autocomplete now uses a CLOSED shadow root, so its
// inner input can't be styled from here (pa.shadowRoot is null). The HOST
// element is stylable from outside, though: give it the same light-gray
// border/radius as S.sel and pin its height to the sibling address fields
// so the search box lines up with them instead of Google's tall default.
pa.style.width="100%";pa.style.display="block";pa.style.boxSizing="border-box";pa.style.border="1px solid #CBD5E1";pa.style.borderRadius="6px";// Font properties inherit across the closed shadow boundary (Google's inner
// input uses font: inherit), so set them on the host to match S.sel.
pa.style.fontFamily="Arial, sans-serif";pa.style.fontSize="13px";pa.style.fontWeight="600";pa.style.color="#000";// Force a light theme so the search box matches the white sibling address
// fields — the gmp element otherwise defaults to a dark background, which
// looked like the brand color "bleeding" into the search box. color-scheme
// crosses the closed shadow boundary; backgroundColor covers the host.
pa.style.colorScheme="light";pa.style.backgroundColor="#FFF";container.replaceChildren(pa);sizeTries=0;sizeToFields=function sizeToFields(){var ref=document.querySelector('input[autocomplete="street-address"], input[autocomplete="postal-code"], input[autocomplete="address-level2"]');var h=ref?Math.round(ref.getBoundingClientRect().height):0;if(h){pa.style.height=h+"px";return;}if(sizeTries++<20)requestAnimationFrame(sizeToFields);else pa.style.height="28px";};requestAnimationFrame(sizeToFields);pa.addEventListener("gmp-select",/*#__PURE__*/function(){var _ref20=_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee7(ev){var place,comps,find,street,city,state,zip;return _regeneratorRuntime().wrap(function _callee7$(_context7){while(1)switch(_context7.prev=_context7.next){case 0:place=ev.placePrediction.toPlace();_context7.next=3;return place.fetchFields({fields:["addressComponents"]});case 3:comps=place.addressComponents||[];find=function find(type){var c=comps.find(function(x){return(x.types||[]).includes(type);});return c?c.longText||c.shortText||"":"";};street=[find("street_number"),find("route")].filter(Boolean).join(" ");city=find("locality")||find("sublocality")||find("postal_town");state=find("administrative_area_level_1");// full state name to match <select> options
zip=(find("postal_code")||"").replace(/\D/g,"").slice(0,5);setContact(function(p){return _objectSpread(_objectSpread(_objectSpread(_objectSpread(_objectSpread({},p),street?{street:street}:{}),city?{city:city}:{}),state?{state:state}:{}),zip?{zip:zip}:{});});case 10:case"end":return _context7.stop();}},_callee7);}));return function(_x10){return _ref20.apply(this,arguments);};}());case 23:case"end":return _context8.stop();}},_callee8);}));return function(_x9){return _ref19.apply(this,arguments);};}())["catch"](function(err){console.warn("[StructureStudio] Google Maps autocomplete unavailable:",err.message);});},[C.googleMapsApiKey]);// Auto-update building size, REFLOWING the layout instead of destroying it.
//
// This used to `setItems([])` on any size change — the customer lost their whole plan
// with no warning. Worse, it missed a case: changing the STYLE blanks the size, which
// recorded a blank into prevSizeRef, so the next size pick saw a falsy previous value
// and skipped the wipe. The items survived carrying page-pixels computed from the OLD
// scale/mgX/mgY, which left doors floating off their walls — and, because they then
// failed their own wallOnly rule, unable to be dragged back. That is the bug Carolyn
// reported. Comparing parsed DIMENSIONS rather than label strings closes it — PROVIDED
// the skip branch leaves the ref alone: it used to record sel.size (the blank) into the
// ref, so the follow-up size pick saw prev=null, skipped the reflow, and re-stranded
// every item exactly as described above. The ref now only ever holds the last size that
// actually took effect, so style-then-size reflows from the real old dimensions
// (audit 2026-08-20).
//
// reflowItems is pure, so the result is inspected BEFORE committing: if anything cannot
// be placed at all, the size change is reverted and the customer is told what to remove.
useEffect(function(){var p=parseSize(sel.size);if(!p)return;// blank/unparseable (a style click just blanked it): keep the last real size
var prev=parseSize(prevSizeRef.current);if(prev&&(prev.w!==p.w||prev.h!==p.h)&&items.length){var _reflowItems=reflowItems(items,prev,p,ITEMS),nextItems=_reflowItems.items,events=_reflowItems.events;var blocked=events.filter(function(e){return e.kind==="blocked";});if(blocked.length){// Nothing has changed yet — put the size back and let them decide.
setSizeBlock({from:prevSizeRef.current,to:sel.size,items:blocked});setSel(function(s){return _objectSpread(_objectSpread({},s),{},{size:prevSizeRef.current});});return;// prevSizeRef stays put; the revert re-runs this effect
}setItems(nextItems);setSelectedId(null);var notable=events.filter(function(e){return e.kind==="movedWall"||e.kind==="resized";});if(notable.length)setReflowNote(notable);}setShedW(p.w);setShedH(p.h);prevSizeRef.current=sel.size;},[sel.size]);// Snap a loaded layout back onto legal positions. Designs saved before the size-change
// reflow existed can carry items stranded off their walls by an old style-then-size
// change — they render floating and, failing their own placement rule, cannot be dragged
// back. Passing the SAME dimensions in and out makes the geometry conversion an identity,
// so nothing moves that was already legal; the clamps and collision checks do the repair.
// Silent by design: someone opening a link did not do anything to be told about.
var repairLoaded=function repairLoaded(loaded,sizeLabel){var d=parseSize(sizeLabel);if(!d||!loaded.length)return loaded;try{return reflowItems(loaded,d,d,ITEMS).items;}catch(_e){return loaded;}};// ─── Load a saved design by short code ───
// Shared by the public ?id= URL path and the portal's openDesign prop below — the two
// must hydrate identically (GHL refs, draft flag, optional version snapshot, id counter).
var loadDesignByCode=/*#__PURE__*/function(){var _ref21=_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee9(id,vParam){var isCancelled,_yield$supabase$rpc2,rows,error,data,design,_yield$supabase$rpc3,vrows,vrow,loadedItems,_args9=arguments;return _regeneratorRuntime().wrap(function _callee9$(_context9){while(1)switch(_context9.prev=_context9.next){case 0:isCancelled=_args9.length>2&&_args9[2]!==undefined?_args9[2]:function(){return false;};_context9.next=3;return supabase.rpc("load_design",{p_code:id});case 3:_yield$supabase$rpc2=_context9.sent;rows=_yield$supabase$rpc2.data;error=_yield$supabase$rpc2.error;data=Array.isArray(rows)?rows[0]:rows;// Returns TRUE only when the design actually loaded. Callers act on the result:
// the openDesign effect must not arm inventory state (unitId / asNew reset) against
// whatever design is still on the canvas when the RPC failed or the row is gone —
// that grafted unit B's id onto unit A's design and let an update overwrite B.
if(!(isCancelled()||error||!data)){_context9.next=9;break;}return _context9.abrupt("return",false);case 9:// The persistent portal designer can be sitting on the submit-success screen when an
// Open request arrives — without this the OLD design's success screen would keep
// covering the newly loaded one. No-op on the public ?id= path (fresh mount, false).
setSubmitted(false);setSubmitError(null);currentDesignIdRef.current=data.short_code;setDesignCode(data.short_code);// A re-opened draft may keep draft-saving; any other status locks the row against
// silent rewrites (saveDraftSilently refuses non-draft rows). Embedded mounts never
// draft-save at all (customerFacing guard), so there this flag is inert.
isDraftRef.current=data.status==="draft";// Inventory master (075)? Mark it: submit gets blocked and the inventory button
// flips to update mode. unitId is enriched by the openDesign effect (the portal
// sends it); a master reached any other way still blocks submit.
setInventoryMaster(data.status==="inventory"?{code:data.short_code,unitId:null,priceCents:null,locationId:null}:null);// Opening an existing design is never a NEW inventory build; a master's location is seeded
// by the openDesign enrichment below.
setInventoryNew(false);setInvLocationId("");// An estimate quoted FROM an inventory building carries the link on the row, so
// reopening it later (portal or public share link) locks the plan again. The serial
// is a nicety for the banner: readable to a signed-in tenant under inventory_units'
// owner-select policy, absent for an anon visitor — never let it block the lock.
setNewBuildMode(false);if(data.inventory_unit_id){// lifecycle stays null here on purpose. It is DERIVED from build_jobs + delivery_stops
// (see _shared/inventoryLifecycle.ts) — not a column this read could fetch, and
// re-deriving it in the browser would be a third copy of a rule that must not drift.
// Null means "unknown", which fails toward LOCKED — the behaviour this path has always
// had, and staff still have "Design a new build instead" to lift it deliberately. The
// Inventory tab, which does know, passes it through openDesign below.
setDesignUnit({id:data.inventory_unit_id,serial:null,lifecycle:null});supabase.from("inventory_units").select("serial").eq("id",data.inventory_unit_id).maybeSingle().then(function(_ref22){var u=_ref22.data;if(u&&!isCancelled())setDesignUnit(function(p){return _objectSpread(_objectSpread({},p||{}),{},{id:data.inventory_unit_id,serial:u.serial});});},function(){});}else{setDesignUnit(null);}// Hydrate GHL refs so a re-submit becomes an update of the same estimate.
ghlContactIdRef.current=data.ghl_contact_id||null;ghlEstimateIdRef.current=data.ghl_estimate_id||null;ghlEstimateNumberRef.current=data.ghl_estimate_number||null;setHasExistingEstimate(!!data.ghl_estimate_id);// Optionally open a specific saved version for review/resubmit. The design DATA
// comes from that version's snapshot; the GHL refs above stay from the current
// row so a resubmit updates the same one estimate rather than creating a new one.
design=data;if(!(Number.isFinite(vParam)&&vParam>0)){_context9.next=31;break;}_context9.next=27;return supabase.rpc("load_design_version",{p_code:id,p_version:vParam});case 27:_yield$supabase$rpc3=_context9.sent;vrows=_yield$supabase$rpc3.data;vrow=Array.isArray(vrows)?vrows[0]:vrows;if(!isCancelled()&&vrow)design=vrow;case 31:if(!isCancelled()){_context9.next=33;break;}return _context9.abrupt("return",false);case 33:setViewingVersion(Number.isFinite(vParam)&&vParam>0?vParam:null);setContact(data.contact||{name:"",email:"",phone:"",street:"",city:"",state:"",zip:""});// Pre-set prevSizeRef to what sel.size is ABOUT to become, so the size effect doesn't
// treat this load as a user size-change and wipe the items set below (same guard
// openVersion uses). "" (not the old size) because sel is REBUILT below, not merged.
prevSizeRef.current=(design.selections||{}).size||"";// Rebuild sel from pristine defaults rather than merging over the persistent portal
// designer's current selections: a design saved before an option existed (e.g. rows
// from before roofType/roofColor shipped) must not inherit the previously opened
// design's values for those keys. Mirrors the sel useState initializer.
setSel(function(){var base={style:"",size:"",roofType:"",roofColor:"",cladding:""};C.options.forEach(function(o){base[o.id]=o.type==="counter"?o.options[0]:"";});return _objectSpread(_objectSpread({},base),design.selections||{});});setPaintColors(design.paint_colors||{body:"",trim:""});setPaintCustom({body:false,trim:false});setCustomOptions(design.custom_options||[]);setRoDimensions(design.ro_dimensions||{});// Items must be set after sel.size has propagated; the prevSizeRef guard
// above keeps the size effect from wiping them.
loadedItems=repairLoaded(Array.isArray(design.items)?design.items:[],(design.selections||{}).size);setItems(loadedItems);// The persistent portal mount can carry a selection/note-edit from the PREVIOUS
// design; item ids are small integers that collide across designs, so a stale
// selectedId would put the Delete/Rotate toolbar on an arbitrary item of this one.
setSelectedId(null);setEditingNoteId(null);// Keep the global id counter ahead of any restored ids so the next placement can't
// reuse an existing id (which collided in select/drag/delete/resize).
idCounter=Math.max.apply(Math,[idCounter,0].concat(_toConsumableArray(loadedItems.map(function(i){return Number(i.id)||0;}))))+1;return _context9.abrupt("return",true);case 47:case"end":return _context9.stop();}},_callee9);}));return function loadDesignByCode(_x11,_x12){return _ref21.apply(this,arguments);};}();// ─── Load saved design from ?id=SS-XXXXXX on the URL ───
useEffect(function(){if(!supabase)return;// Embedded mounts never read the HOST page's URL — /portal.html?id=SS-… must not
// hydrate the in-portal designer with an arbitrary design code.
if(embedded)return;var params=new URLSearchParams(window.location.search);var id=params.get("id");if(!id)return;var cancelled=false;loadDesignByCode(id,parseInt(params.get("v")||"",10),function(){return cancelled;});return function(){cancelled=true;};// eslint-disable-next-line react-hooks/exhaustive-deps
},[supabase,embedded]);// ─── Open a design on demand (portal Designer tab) ───
// The portal's Designs/Contacts "Open" buttons hand the persistent embedded designer an
// { code, version } request instead of linking to the public page. Business users must
// NEVER review a customer's design on the public page: it silently captures leads and
// saves drafts (capture-lead / saveDraftSilently), so staff browsing there would corrupt
// the very activity Contacts reports. Embedded-only; the public page keeps its ?id= path.
// Each click sends a fresh object (identity change re-fires this even for the same code).
useEffect(function(){if(!embedded||!supabase||!openDesign)return;// "+ New inventory building" sends { blank: true } with no code: clear the canvas so
// a brand-new building never starts from the design that happened to be open. Without
// this, clicking New while another unit's MASTER was loaded left the submit bar saying
// "Update Inventory Building" — saving would have rewritten that other unit.
if(openDesign.blank){if(items.length>0||sel.style||sel.size){if(!window.confirm("Start a new building? This clears what's currently in the Designer tab."))return;}setItems([]);setSel(function(p){var n=_objectSpread({},p);Object.keys(n).forEach(function(k){return n[k]="";});return n;});setContact({name:"",phone:"",email:"",street:"",city:"",state:"",zip:""});setPaintColors({body:"",trim:""});setCustomOptions([]);setRoDimensions({});setSelectedId(null);setEditingNoteId(null);currentDesignIdRef.current=null;isDraftRef.current=false;draftStateRef.current=null;ghlContactIdRef.current=null;ghlEstimateIdRef.current=null;ghlEstimateNumberRef.current=null;inventoryUnitRef.current=null;setInventoryMaster(null);setDesignUnit(null);setNewBuildMode(false);setInventoryNew(true);// "+ New inventory building" → show the inventory Save bar + location dropdown
setInvLocationId("");setHasExistingEstimate(false);setDesignCode(null);setEstimateVersions([]);setViewingVersion(null);setSubmitted(false);setSubmitError(null);return;}if(!openDesign.code)return;// The persistent Designer tab may hold in-progress work — hand-built, or a previously
// opened design mid-edit. The old public links opened a NEW tab and could never
// destroy it; this in-place load can, so it asks first (the same courtesy the
// portal's openAccount extends before its remount discards the designer).
if(items.length>0||sel.style||sel.size){if(!window.confirm("Opening this design will replace what's currently in the Designer tab. Continue?"))return;}var cancelled=false;_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee10(){var loaded,_openDesign$unitSeria,_openDesign$unitLifec;return _regeneratorRuntime().wrap(function _callee10$(_context10){while(1)switch(_context10.prev=_context10.next){case 0:_context10.next=2;return loadDesignByCode(String(openDesign.code),Number(openDesign.version)||null,function(){return cancelled;});case 2:loaded=_context10.sent;if(!(cancelled||!loaded)){_context10.next=6;break;}if(!cancelled)setSubmitError("That design could not be opened — check your connection and try again.");return _context10.abrupt("return");case 6:if(openDesign.asNew){// "Send estimate" from Inventory: the unit's design becomes a FRESH estimate for
// a new customer — a new short_code is minted at submit, the contact starts
// blank, no GHL identity carries over, and the master itself stays untouched.
// Many customers can each get their own estimate on the same physical building.
currentDesignIdRef.current=null;setDesignCode(null);isDraftRef.current=false;draftStateRef.current=null;ghlContactIdRef.current=null;ghlEstimateIdRef.current=null;ghlEstimateNumberRef.current=null;setHasExistingEstimate(false);setViewingVersion(null);setContact({name:"",email:"",phone:"",street:"",city:"",state:"",zip:""});setInventoryMaster(null);// asNew is a NEW quote on that building: the lock comes from the armed ref, and
// designUnit (which tracks a SAVED row's link) must not also be set yet.
setDesignUnit(openDesign.inventoryUnitId?{id:openDesign.inventoryUnitId,serial:(_openDesign$unitSeria=openDesign.unitSerial)!==null&&_openDesign$unitSeria!==void 0?_openDesign$unitSeria:null,// The Inventory tab knows where the building is on the ladder, so it says so —
// that is what lets a not-yet-built unit be quoted with an editable plan.
lifecycle:(_openDesign$unitLifec=openDesign.unitLifecycle)!==null&&_openDesign$unitLifec!==void 0?_openDesign$unitLifec:null}:null);setNewBuildMode(false);inventoryUnitRef.current=openDesign.inventoryUnitId||null;}else if(openDesign.newBuild){// "Quote a new build for this customer" from a sold building's estimate list. Exactly
// what the in-designer "Design a new build instead" button does — the plan unlocks and
// unitToLink resolves to null at submit, so the new version reads New rather than
// inheriting the sold unit. No extra confirm here: openInDesigner already asked about
// replacing what is in the Designer tab, and the user clicked a button that says this.
inventoryUnitRef.current=null;setNewBuildMode(true);}else{inventoryUnitRef.current=null;if(openDesign.unit){// Inventory "Open": enrich the master marker so update mode knows its unit.
setInventoryMaster(function(m){var _openDesign$unit$aski,_openDesign$unit$loca;return m&&_objectSpread(_objectSpread({},m),{},{unitId:openDesign.unit.unitId,priceCents:(_openDesign$unit$aski=openDesign.unit.askingPriceCents)!==null&&_openDesign$unit$aski!==void 0?_openDesign$unit$aski:null,locationId:(_openDesign$unit$loca=openDesign.unit.locationId)!==null&&_openDesign$unit$loca!==void 0?_openDesign$unit$loca:null});});setInvLocationId(openDesign.unit.locationId||"");// seed the inline location dropdown
}}case 7:case"end":return _context10.stop();}},_callee10);}))();return function(){cancelled=true;};// eslint-disable-next-line react-hooks/exhaustive-deps
},[supabase,embedded,openDesign]);// On the submit-success screen, load every version of this design (this estimate) so the
// customer/rep can see and reopen all designs on the estimate. Capability read by code.
useEffect(function(){if(!supabase||!designCode){setEstimateVersions([]);return;}var cancelled=false;_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee11(){var _yield$supabase$rpc4,data,error;return _regeneratorRuntime().wrap(function _callee11$(_context11){while(1)switch(_context11.prev=_context11.next){case 0:_context11.next=2;return supabase.rpc("list_design_versions",{p_code:designCode});case 2:_yield$supabase$rpc4=_context11.sent;data=_yield$supabase$rpc4.data;error=_yield$supabase$rpc4.error;if(!(cancelled||error)){_context11.next=7;break;}return _context11.abrupt("return");case 7:setEstimateVersions(Array.isArray(data)?data:[]);case 8:case"end":return _context11.stop();}},_callee11);}))();return function(){cancelled=true;};},[supabase,designCode,submitted]);// Switch to another saved version in place (no page reload). Loads that version's design
// data and keeps the current GHL refs (same estimate), marking it as the one being viewed.
var openVersion=useCallback(/*#__PURE__*/function(){var _ref25=_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee12(version){var _yield$supabase$rpc5,vrows,error,vrow,vsel,loadedItems,p;return _regeneratorRuntime().wrap(function _callee12$(_context12){while(1)switch(_context12.prev=_context12.next){case 0:if(!(!supabase||!designCode)){_context12.next=2;break;}return _context12.abrupt("return");case 2:_context12.next=4;return supabase.rpc("load_design_version",{p_code:designCode,p_version:version});case 4:_yield$supabase$rpc5=_context12.sent;vrows=_yield$supabase$rpc5.data;error=_yield$supabase$rpc5.error;vrow=Array.isArray(vrows)?vrows[0]:vrows;if(!(error||!vrow)){_context12.next=10;break;}return _context12.abrupt("return");case 10:vsel=vrow.selections||{};// Pre-set prevSizeRef to this version's size so the size effect doesn't treat it as a
// user size-change and wipe the items we're loading (same guard the initial load uses).
prevSizeRef.current=vsel.size||prevSizeRef.current;// Rebuild from pristine defaults, NEVER merge over the live sel: a version saved
// before a key existed (cladding, roofType...) must not inherit whatever the user
// picked five clicks ago in the current session -- the same rule the design-load
// path already follows (audit 2026-08-19).
setSel(function(){var base={style:"",size:"",roofType:"",roofColor:"",cladding:""};C.options.forEach(function(o){base[o.id]=o.type==="counter"?o.options[0]:"";});return _objectSpread(_objectSpread({},base),vsel);});setPaintColors(vrow.paint_colors||{body:"",trim:""});setPaintCustom({body:false,trim:false});setCustomOptions(vrow.custom_options||[]);setRoDimensions(vrow.ro_dimensions||{});loadedItems=repairLoaded(Array.isArray(vrow.items)?vrow.items:[],(vrow.selections||{}).size);setItems(loadedItems);setSelectedId(null);idCounter=Math.max.apply(Math,[idCounter,0].concat(_toConsumableArray(loadedItems.map(function(i){return Number(i.id)||0;}))))+1;setViewingVersion(version);if(!embedded){p=new URLSearchParams(window.location.search);p.set("v",String(version));window.history.replaceState({},"","?".concat(p.toString()));}case 23:case"end":return _context12.stop();}},_callee12);}));return function(_x13){return _ref25.apply(this,arguments);};}(),[supabase,designCode,embedded]);// ─── Page-based geometry: on-screen mirrors the 8.5"×11" export 1:1 ───
// The SVG viewBox IS the export page. Notes/lines live in page coordinates,
// so wherever they sit on screen is exactly where they print.
var PAGE_W=SS_PAGE.W,PAGE_H=SS_PAGE.H;var TEXT_AREA_H=SS_PAGE.TEXT_AREA_H;// bottom band reserved for auto customer info
var TOP_LABEL_PAD=SS_PAGE.TOP_LABEL_PAD;// space for size + FRONT labels above plan
var BOT_LABEL_PAD=SS_PAGE.BOT_LABEL_PAD;// space for size + BACK labels below plan
var RAMP_SPACE_FT=SS_PAGE.RAMP_SPACE_FT;// a ramp shows 2 ft past its wall (visual)
var visibleH=PAGE_H-TEXT_AREA_H;// Plan dynamically scales: caps in three directions ensure a ramp fits
// both north and south plus 70% target sizing.
//   1) width ≤ 70% of page (so the plan never spans the full sheet)
//   2) height ≤ 70% of the visible top area
//   3) plan + 2 ramps + 2 label pads ≤ visibleH (so south + north ramps fit)
// Top-bias: the plan sits RAMP_SPACE_FT*scale + TOP_LABEL_PAD from the top so a north
// ramp + labels fit; the third scale constraint guarantees room for a south ramp below.
// Derived by pageGeom() so the reflow can compute the OLD page geometry the same way.
var _pageGeom=pageGeom(bldgW,bldgH),scale=_pageGeom.scale,pW=_pageGeom.pW,pH=_pageGeom.pH,mgX=_pageGeom.mgX,mgY=_pageGeom.mgY;var cW=PAGE_W,cH=PAGE_H;var TEXT_BAND_TOP=PAGE_H-TEXT_AREA_H;// ─── Display frame: zoom-to-fit crop of the sheet (DISPLAY-ONLY) ───
// Everything above (scale, mgX, mgY) is shared with the print/export path and
// is untouched. The frame only decides which part of the sheet the on-screen
// SVG shows (its viewBox) and how large it renders — the plan plus a wide
// margin band for notes/lines, expanded to include any annotation already
// placed outside it (saved designs), clamped to the sheet. Because the
// element's aspect ratio always matches the frame's, on-screen px-per-page-px
// stays uniform on both axes and getSvgPt's single-ratio math stays exact.
var NOTE_MARGIN=170;// page px kept beside the plan for notes (~20% of sheet width per side)
var frame=function(){var x0=Math.max(0,mgX-NOTE_MARGIN);var x1=Math.min(PAGE_W,mgX+pW+NOTE_MARGIN);var y0=0;var y1=Math.min(TEXT_BAND_TOP,mgY+pH+RAMP_SPACE_FT*scale+BOT_LABEL_PAD+40);items.forEach(function(it){if(it.type==="textNote"){var w=it.widthPx||160,h=it.heightPx||40;// Left pad is wider (28) so the docked leader handle (cx -w/2-18, r7)
// is never clipped out of the frame for notes near the sheet's edge.
x0=Math.min(x0,it.x-w/2-28);x1=Math.max(x1,it.x+w/2+12);y0=Math.min(y0,it.y-h/2-12);y1=Math.max(y1,it.y+h/2+12);if(it.leader){x0=Math.min(x0,it.leader.x-12);x1=Math.max(x1,it.leader.x+12);y0=Math.min(y0,it.leader.y-12);y1=Math.max(y1,it.leader.y+12);}}else if(it.type==="line"){x0=Math.min(x0,Math.min(it.x1,it.x2)-12);x1=Math.max(x1,Math.max(it.x1,it.x2)+12);y0=Math.min(y0,Math.min(it.y1,it.y2)-12);y1=Math.max(y1,Math.max(it.y1,it.y2)+12);}});x0=Math.max(0,x0);y0=Math.max(0,y0);x1=Math.min(PAGE_W,x1);y1=Math.min(TEXT_BAND_TOP,y1);return{x:x0,y:y0,w:x1-x0,h:y1-y0};}();// On-screen size: full container width up to a height cap. maxWidth is derived
// from the cap so the element's aspect always equals the frame's (no letterbox).
var DISP_MAX_H=760;var dispMaxW=Math.round(Math.min(1010,frame.w*DISP_MAX_H/frame.h));// Ref so pointer-math reads the CURRENT frame even from stale-closured handlers.
var frameRef=useRef(frame);frameRef.current=frame;// Get sizes for selected style
var selectedStyle=C.buildingStyles.find(function(s){return s.value===sel.style;});var sizeOpts=selectedStyle&&Array.isArray(selectedStyle.sizes)?selectedStyle.sizes:C.defaultSizes||[];var frontWall=getFrontWall(items);// Detect unattached lofts for warning banner
var lofts=items.filter(function(i){return i.type==="loft";});var unattachedLofts=lofts.filter(function(lf){var w=(lf.widthFt||6)/2,h=(lf.heightFt||4)/2;var cx=(lf.x-mgX)/scale,cy=(lf.y-mgY)/scale;var others=lofts.filter(function(o){return o.id!==lf.id;}).map(function(o){var ow=(o.widthFt||6)/2,oh=(o.heightFt||4)/2;var ocx=(o.x-mgX)/scale,ocy=(o.y-mgY)/scale;return{l:ocx-ow,r:ocx+ow,t:ocy-oh,b:ocy+oh};});return!checkLoftAttached(cx-w,cx+w,cy-h,cy+h,bldgW,bldgH,others);});// A 3D snapshot must always show the layout it ships with: drop it whenever
// anything that feeds the 3D scene changes — any selection counts, since
// options can change the 3D look too (e.g. the lap-siding upgrade). Also
// fires on design load, which is correct — the customer re-captures from
// the reopened design.
useEffect(function(){render3DSnapshotRef.current=null;setHas3DSnapshot(false);},[items,sel,paintColors,bldgW,bldgH]);// ─── INTERACTION HANDLERS ───
var getSvgPt=useCallback(function(e){var svg=svgRef.current;if(!svg)return{x:0,y:0};var r=svg.getBoundingClientRect();var cx=e.touches?e.touches[0].clientX:e.clientX;var cy=e.touches?e.touches[0].clientY:e.clientY;// The viewBox is the zoom-to-fit display frame — a crop of the sheet whose
// aspect ratio always matches the element's, so ONE ratio maps both axes.
// Read the frame through the ref so this stays exact even when a handler
// closed over an older render (the frame moves as items/sizes change).
var f=frameRef.current;var sx=f.w/r.width;return{x:f.x+(cx-r.left)*sx,y:f.y+(cy-r.top)*sx};},[]);var handleClick=useCallback(function(e){if(dragging)return;if(planLockedRef.current)return;// inventory estimate: the building is already built
// Not captured yet: any attempt to work the canvas pops the lead gate instead.
if(gateRequired){setGateOpen(true);return;}// Swallow the click that fires immediately after a drag/resize gesture —
// otherwise the hit test below would deselect items the user just resized.
if(justGesturedRef.current){justGesturedRef.current=false;return;}// Pick-one-to-remove mode: the pulsing overlays are the only click targets
// (they handle their own clicks); every other canvas action is disabled.
if(pendingRemoval)return;var pt=getSvgPt(e);if(!activeTool){var hit=_toConsumableArray(items).reverse().find(function(it){var c=ITEMS[it.type];if(!c)return false;if(c.lineType){// Distance from click to the line segment
var A=pt.x-it.x1,B=pt.y-it.y1;var C2=it.x2-it.x1,D=it.y2-it.y1;var lenSq=C2*C2+D*D;var t=lenSq?(A*C2+B*D)/lenSq:0;t=Math.max(0,Math.min(1,t));var dx=pt.x-(it.x1+t*C2),dy=pt.y-(it.y1+t*D);return Math.sqrt(dx*dx+dy*dy)<8;}if(c.noteType){var w=it.widthPx||160,h=it.heightPx||40;return Math.abs(pt.x-it.x)<w/2+4&&Math.abs(pt.y-it.y)<h/2+4;}// Use the item's actual stored size, not the config default — the
// workbench/loft/RO can be resized past their default and still need
// to be selectable at their visual bounds.
var iwFt=it.widthFt||c.width;var ihFt=it.heightFt||c.height;var iw=iwFt*scale,ih=ihFt*scale;var rot=it.rotation===90||it.rotation===270;var hw=(rot?ih:iw)/2;var hh=(rot?iw:ih)/2;// 270° swaps the visual bbox just like 90° (audit #F5)
return Math.abs(pt.x-it.x)<hw+5&&Math.abs(pt.y-it.y)<hh+5;});// Clicking an ALREADY-selected note starts typing in place (works for
// double-click and tap-tap alike; the justGestured guard above keeps a
// drag's trailing click from triggering it).
if(hit&&hit.id===selectedId&&ITEMS[hit.type]&&ITEMS[hit.type].noteType){setEditingNoteId(hit.id);return;}if(!hit||hit.id!==editingNoteId)setEditingNoteId(null);setSelectedId(hit?hit.id:null);return;}var cfg=ITEMS[activeTool];if(!cfg)return;// The single "Door" tool: don't place yet — remember the wall + click point and open the
// door picker, which chooses the door + swing/operation and then places it (placePickedDoor).
if(cfg.isDoorPicker){var w=getWallFromClick(pt.x,pt.y,pW,pH,mgX,mgY)||getNearestWall(pt.x,pt.y,pW,pH,mgX,mgY);setDoorPick({wall:w,ptx:pt.x,pty:pt.y});setActiveTool(null);setToast(null);return;}// The "Window" tool: like the door picker but no swing/operation — remember the wall + point
// and open the window picker, which places the chosen catalog window (placePickedWindow).
if(cfg.isWindowPicker){var _w4=getWallFromClick(pt.x,pt.y,pW,pH,mgX,mgY)||getNearestWall(pt.x,pt.y,pW,pH,mgX,mgY);setWindowPick({wall:_w4,ptx:pt.x,pty:pt.y});setActiveTool(null);setToast(null);return;}// An INCLUDED catalog door/window chip is armed → drop that EXACT fixture here (no picker).
// Included ramps are doorSnap and handled in the doorSnap branch below.
if(cfg.includedFixture&&!cfg.doorSnap){var fx=cfg.includedFixture;var _w5=getWallFromClick(pt.x,pt.y,pW,pH,mgX,mgY)||getNearestWall(pt.x,pt.y,pW,pH,mgX,mgY);var widthFt=(Number(fx.widthIn)||(fx.category==="window"?24:36))/12;// Wider than the clicked wall = snapToWall's clamp degenerates and the fixture
// overhangs the building corner; refuse up front, as the door picker does
// (audit 2026-08-20).
if(widthFt>(_w5==="north"||_w5==="south"?pW:pH)/scale+1e-6){setToast("That ".concat(fx.category==="window"?"window":"door"," is wider than this wall \u2014 pick a longer wall."));setTimeout(function(){return setToast(null);},4000);return;}var iwPx2=widthFt*scale,ihPx2=0.5*scale;var sn=snapToWall(_w5,pt.x,pt.y,iwPx2,ihPx2,pW,pH,mgX,mgY);var _ni;if(fx.category==="window"){_ni=_objectSpread(_objectSpread({id:idCounter++,type:"window"},sn),{},{widthFt:widthFt,heightFt:0.5,fixtureItemId:fx.id,windowName:fx.name||"Window",planLabel:fx.planLabel&&String(fx.planLabel).trim()||(fx.name||"WIN").toUpperCase().slice(0,6),price:fx.price!=null?fx.price:null,widthIn:Number(fx.widthIn)||null,heightIn:Number(fx.heightIn)||null});}else{var swing=fx.swingDefault||(fx.swingOut?"out":fx.swingIn?"in":null);var operation=fx.opDefault||(fx.opDouble?"double":fx.opSlideUp?"slideup":fx.opRight?"right":fx.opLeft?"left":null);_ni=_objectSpread(_objectSpread({id:idCounter++,type:"fixtureDoor"},sn),{},{widthFt:widthFt,heightFt:0.5,fixtureItemId:fx.id,doorName:fx.name||"Door",planLabel:fx.planLabel&&String(fx.planLabel).trim()||(fx.name||"DOOR").toUpperCase().slice(0,6),price:fx.price!=null?fx.price:null,widthIn:Number(fx.widthIn)||null,heightIn:Number(fx.heightIn)||null,swing:swing,operation:operation});}if(checkDoorCollision(_ni,{width:widthFt},items,ITEMS,scale)){setToast("Something's already there — pick a different spot on the wall.");setTimeout(function(){return setToast(null);},4000);return;}// Workbench check too, as the built-in wall placement below runs — checkDoorCollision
// skips workbenches, so an included chip could drop its door/window straight onto one
// (audit 2026-08-20).
if(checkWorkbenchOverlap(sn,iwPx2,items,ITEMS,scale)){setToast("A workbench is on that wall — place this somewhere else on the wall.");setTimeout(function(){return setToast(null);},4000);return;}setItems(function(p){return[].concat(_toConsumableArray(p),[_ni]);});setSelectedId(_ni.id);setActiveTool(null);setToast(null);return;}var iwPx=cfg.width*scale;var ihPx=cfg.height*scale;var wall=getWallFromClick(pt.x,pt.y,pW,pH,mgX,mgY);// Wall-only items always go on a wall; if the click missed the threshold,
// fall back to the nearest wall so the placement still happens.
if(cfg.wallOnly&&!wall)wall=getNearestWall(pt.x,pt.y,pW,pH,mgX,mgY);// Annotation tools (note + line): free placement anywhere on the visible
// page area (above the auto info band), not just inside the plan rectangle.
if(cfg.noteType){var _ni2={id:idCounter++,type:activeTool,x:Math.max(20,Math.min(pt.x,PAGE_W-20)),y:Math.max(20,Math.min(pt.y,TEXT_BAND_TOP-20)),rotation:0,wall:null,widthPx:160,heightPx:40,// user-resizable box; text flows inside
text:"Note"};setItems(function(p){return[].concat(_toConsumableArray(p),[_ni2]);});setSelectedId(_ni2.id);setEditingNoteId(_ni2.id);// start typing in the note immediately (text pre-selected)
setActiveTool(null);setToast(null);return;}if(cfg.lineType){var halfLenPx=cfg.width/2*scale;var cx=Math.max(20,Math.min(pt.x,PAGE_W-20));var cy=Math.max(20,Math.min(pt.y,TEXT_BAND_TOP-20));var _ni3={id:idCounter++,type:activeTool,wall:null,x1:Math.max(0,cx-halfLenPx),y1:cy,x2:Math.min(PAGE_W,cx+halfLenPx),y2:cy};setItems(function(p){return[].concat(_toConsumableArray(p),[_ni3]);});setSelectedId(_ni3.id);setActiveTool(null);setToast(null);return;}// Door-snap items (ramp): find nearest door and snap to its outside
if(cfg.doorSnap){// fixtureDoor counts as a door here. It is the item type EVERY catalog door placement
// creates, and it is treated as a door everywhere else — getFrontWall, checkDoorCollision via
// wallOnly, the payload doors[] schedule — but the ramp tool filtered it out, so a shopper who
// placed the tenant's own catalog door (a slide-up or garage door, the most natural ramp
// companion) got "Place a door first, then add a ramp to it." with a door plainly on the plan.
// Catalog doors are live: fixture_items has active category='door' rows today.
var doors=items.filter(function(i){return i.type==="singleDoor"||i.type==="doubleDoor"||i.type==="fixtureDoor";});if(doors.length===0){setToast("Place a door first, then add a ramp to it.");setTimeout(function(){return setToast(null);},5000);return;}// Find closest door to click
var closest=null;var minDist=Infinity;doors.forEach(function(d){var dx=pt.x-d.x;var dy=pt.y-d.y;var dist=Math.sqrt(dx*dx+dy*dy);if(dist<minDist){minDist=dist;closest=d;}});if(!closest)return;// Check if this door already has a ramp
var existingRamp=items.find(function(i){return i.type==="ramp"&&i.snapDoorId===closest.id;});if(existingRamp){setToast("This door already has a ramp. Delete it first to replace.");setTimeout(function(){return setToast(null);},5000);return;}// Custom ramp: open the picker for THIS door — placePickedRamp creates the ramp item.
if(cfg.isRampPicker){setRampPick({door:closest});setActiveTool(null);setToast(null);return;}// An INCLUDED catalog ramp chip: attach that EXACT ramp to this door (like a custom ramp).
if(cfg.includedFixture){var _fx=cfg.includedFixture;var _widthFt=(Number(_fx.widthIn)||36)/12;var rDepth=(Number(_fx.heightIn)||0)/12||RAMP_SPACE_FT;var rDepthPx=rDepth*scale;var rx,ry,rot;if(closest.wall==="north"){rx=closest.x;ry=mgY-rDepthPx/2;rot=0;}else if(closest.wall==="south"){rx=closest.x;ry=mgY+pH+rDepthPx/2;rot=0;}else if(closest.wall==="west"){rx=mgX-rDepthPx/2;ry=closest.y;rot=90;}else if(closest.wall==="east"){rx=mgX+pW+rDepthPx/2;ry=closest.y;rot=90;}else return;var _ni4={id:idCounter++,type:"ramp",x:rx,y:ry,rotation:rot,wall:closest.wall,widthFt:_widthFt,heightFt:rDepth,snapDoorId:closest.id,fixtureItemId:_fx.id,rampName:_fx.name||"Ramp",planLabel:_fx.planLabel&&String(_fx.planLabel).trim()||(_fx.name||"RAMP").toUpperCase().slice(0,6),price:_fx.price!=null?_fx.price:null,widthIn:Number(_fx.widthIn)||null,heightIn:Number(_fx.heightIn)||null};setItems(function(p){return[].concat(_toConsumableArray(p),[_ni4]);});setSelectedId(_ni4.id);setActiveTool(null);setToast(null);return;}var doorCfg=ITEMS[closest.type];var doorW=doorCfg?doorCfg.width:3;var rampDepth=RAMP_SPACE_FT;// visual ramp depth in feet
var rp=rampPlacementForDoor(closest,rampDepth,pW,pH,mgX,mgY,scale);if(!rp)return;var _ni5=_objectSpread(_objectSpread({id:idCounter++,type:activeTool},rp),{},{widthFt:doorW,heightFt:rampDepth,snapDoorId:closest.id});setItems(function(p){return[].concat(_toConsumableArray(p),[_ni5]);});setActiveTool(null);setToast(null);return;}// (cfg.wallOnly is always assigned a wall above — no need to abort here)
var ni;if(cfg.wallSnap){var clickedWall=wall||getNearestWall(pt.x,pt.y,pW,pH,mgX,mgY);var _sn4=snapToWallInterior(clickedWall,pt.x,pt.y,iwPx,ihPx,pW,pH,mgX,mgY);var candidate=_objectSpread(_objectSpread({id:idCounter,type:activeTool},_sn4),{},{widthFt:cfg.width,heightFt:cfg.height});var others=items.filter(function(i){return i.id!==candidate.id;});if(checkDoorCollision(candidate,cfg,others,ITEMS,scale)){setToast("A door is blocking this wall! Try clicking a different wall, or move the door first.");setTimeout(function(){return setToast(null);},5000);return;}// Check workbench overlap on same wall during placement
var isH=_sn4.wall==="north"||_sn4.wall==="south";var candPos=isH?_sn4.x:_sn4.y;var candHalf=cfg.width*scale/2;var _iterator15=_createForOfIteratorHelper(others),_step15;try{for(_iterator15.s();!(_step15=_iterator15.n()).done;){var ob=_step15.value;if(ob.type!=="workbench"||ob.wall!==_sn4.wall)continue;var obW=(ob.widthFt||ITEMS[ob.type].width)*scale/2;var obPos=isH?ob.x:ob.y;if(Math.abs(candPos-obPos)<candHalf+obW-2){setToast("Another workbench is in the way. Try a different spot on the wall.");setTimeout(function(){return setToast(null);},4000);return;}}}catch(err){_iterator15.e(err);}finally{_iterator15.f();}ni=candidate;idCounter++;}else if(activeTool==="loft"){// Auto-span wall-to-wall (full building width), positioned at click Y
var loftH=cfg.height;// default 4ft
var cyFtRound=Math.max(loftH/2,Math.min(Math.round((pt.y-mgY)/scale),bldgH-loftH/2));var cxFt=bldgW/2;var nL=0,nR=bldgW,nT=cyFtRound-loftH/2,nB=cyFtRound+loftH/2;// Prevent overlap with other lofts
var otherLofts=items.filter(function(i){return i.type==="loft";});var _iterator16=_createForOfIteratorHelper(otherLofts),_step16;try{for(_iterator16.s();!(_step16=_iterator16.n()).done;){var o=_step16.value;var ow=(o.widthFt||cfg.width)/2,oh=(o.heightFt||cfg.height)/2;var ocx=(o.x-mgX)/scale,ocy=(o.y-mgY)/scale;if(nL<ocx+ow-0.1&&nR>ocx-ow+0.1&&nT<ocy+oh-0.1&&nB>ocy-oh+0.1){setToast("Can't place a loft overlapping another loft. Move the existing one or click a different spot.");setTimeout(function(){return setToast(null);},4000);return;}}}catch(err){_iterator16.e(err);}finally{_iterator16.f();}ni={id:idCounter++,type:"loft",x:mgX+cxFt*scale,y:mgY+cyFtRound*scale,rotation:0,wall:null,widthFt:bldgW,heightFt:loftH,elevationFt:D3.LOFT_ELEV};}else if(wall){var _sn5=snapToWall(wall,pt.x,pt.y,iwPx,ihPx,pW,pH,mgX,mgY);// Placing a door/window/RO had NO overlap check at all, so one could be click-placed straight
// on top of another door, or onto a workbench’s wall span. Both checks now run here, matching
// the wallSnap (workbench) branch, so the invariant holds whichever item is the one moving.
var cand=_objectSpread(_objectSpread({id:-1,type:activeTool},_sn5),{},{widthFt:cfg.width,heightFt:cfg.height});if(checkDoorCollision(cand,cfg,items,ITEMS,scale)){setToast("Something is already on that spot. Pick a clear part of the wall.");setTimeout(function(){return setToast(null);},4000);return;}if(checkWorkbenchOverlap(_sn5,iwPx,items,ITEMS,scale)){setToast("A workbench is on that wall — place this somewhere else on the wall.");setTimeout(function(){return setToast(null);},4000);return;}ni=_objectSpread(_objectSpread({id:idCounter++,type:activeTool},_sn5),{},{widthFt:cfg.width,heightFt:cfg.height},d3OpeningDefaults(activeTool));}else{var x=Math.max(mgX+iwPx/2,Math.min(pt.x,mgX+pW-iwPx/2));var y=Math.max(mgY+ihPx/2,Math.min(pt.y,mgY+pH-ihPx/2));ni={id:idCounter++,type:activeTool,x:x,y:y,rotation:0,wall:null,widthFt:cfg.width,heightFt:cfg.height};}setItems(function(p){return[].concat(_toConsumableArray(p),[ni]);});setActiveTool(null);setToast(null);},[activeTool,dragging,getSvgPt,items,mgX,mgY,pW,pH,scale,ITEMS,pendingRemoval,selectedId,editingNoteId,gateRequired]);// Place the door chosen in the picker at the remembered wall/click point. Snapshots the
// door's spec (so a later catalog edit never changes this saved design) + the shopper's
// swing/operation choice onto a stable `fixtureDoor` item.
var placePickedDoor=useCallback(function(fx,swing,operation){// Swap mode: replace the selected door in place with the chosen door — keeping its wall,
// but RE-LEGALIZED for the new width. The swap used to keep x/y verbatim with no bounds,
// collision, or workbench check — the one mutation path with none — so swapping to a
// wider door overlapped its neighbors, or hung past the building corner near an end,
// straight into the exported PDF (audit 2026-08-20). Same checks + refusals as click
// placement; when the new door cannot fit, nothing changes and the old door stays.
if(swapId!=null&&fx){var wFt=(Number(fx.widthIn)||36)/12;var cur=items.find(function(it){return it.id===swapId;});if(!cur||!cur.wall){setSwapId(null);setDoorPick(null);return;}if(wFt>(cur.wall==="north"||cur.wall==="south"?pW:pH)/scale+1e-6){setToast("That door is wider than this wall — pick a narrower door.");setTimeout(function(){return setToast(null);},4000);setSwapId(null);setDoorPick(null);return;}var _sn6=snapToWall(cur.wall,cur.x,cur.y,wFt*scale,0.5*scale,pW,pH,mgX,mgY);var others=items.filter(function(it){return it.id!==swapId;});if(checkDoorCollision(_objectSpread(_objectSpread(_objectSpread({},cur),_sn6),{},{widthFt:wFt}),{width:wFt},others,ITEMS,scale)){setToast("That door doesn't fit here — something else is in the way on this wall.");setTimeout(function(){return setToast(null);},4000);setSwapId(null);setDoorPick(null);return;}if(checkWorkbenchOverlap(_sn6,wFt*scale,others,ITEMS,scale)){setToast("A workbench is on that wall — the wider door would overlap it.");setTimeout(function(){return setToast(null);},4000);setSwapId(null);setDoorPick(null);return;}setItems(function(p){return p.map(function(it){if(it.id===swapId)return _objectSpread(_objectSpread(_objectSpread({},it),_sn6),{},{type:"fixtureDoor",fixtureItemId:fx.id,doorName:fx.name||"Door",planLabel:fx.planLabel&&String(fx.planLabel).trim()||(fx.name||"DOOR").toUpperCase().slice(0,6),price:fx.price!=null?fx.price:null,widthIn:Number(fx.widthIn)||null,heightIn:Number(fx.heightIn)||null,// Drop the height a BUILT-IN placement stamped: openingHeightFt wins over heightIn
// in openingSpan, so keeping it here would draw this fixture at the old 6'6" no
// matter what the builder's door actually measures.
openingHeightFt:undefined,sillFt:undefined,widthFt:wFt,swing:swing||it.swing||null,operation:operation||it.operation||null});// The re-clamp can shift the door; its ramp is derived geometry and must follow,
// exactly like the drag path — else it detaches into the rasterized PDF.
if(it.type==="ramp"&&it.snapDoorId===swapId){var rp=rampPlacementForDoor(_sn6,it.heightFt,pW,pH,mgX,mgY,scale);return rp?_objectSpread(_objectSpread({},it),rp):it;}return it;});});setSwapId(null);setDoorPick(null);setToast(null);return;}if(!doorPick||!fx)return;var widthFt=(Number(fx.widthIn)||36)/12;// A door wider than the clicked wall can never be legal: snapToWall's clamp degenerates
// (its max bound exceeds its min bound) and parks the door overhanging the building
// corner — on the plan, in the exported PDF, and as an opening wider than the wall in
// 3D. reflowItems' seat() already refuses wFt > wallLen on size change; enforce the
// same invariant at placement (audit 2026-08-20).
if(widthFt>(doorPick.wall==="north"||doorPick.wall==="south"?pW:pH)/scale+1e-6){setToast("That door is wider than this wall — pick a longer wall or a narrower door.");setTimeout(function(){return setToast(null);},4000);setDoorPick(null);return;}var iwPx=widthFt*scale,ihPx=0.5*scale;var sn=snapToWall(doorPick.wall,doorPick.ptx,doorPick.pty,iwPx,ihPx,pW,pH,mgX,mgY);var ni=_objectSpread(_objectSpread({id:idCounter++,type:"fixtureDoor"},sn),{},{widthFt:widthFt,heightFt:0.5,fixtureItemId:fx.id,doorName:fx.name||"Door",planLabel:fx.planLabel&&String(fx.planLabel).trim()||(fx.name||"DOOR").toUpperCase().slice(0,6),price:fx.price!=null?fx.price:null,widthIn:Number(fx.widthIn)||null,heightIn:Number(fx.heightIn)||null,swing:swing||null,operation:operation||null});if(checkDoorCollision(ni,{width:widthFt},items,ITEMS,scale)){setToast("A door is already there — pick a different spot on the wall.");setTimeout(function(){return setToast(null);},4000);setDoorPick(null);return;}// Same second check as the built-in wall placement and the drag path: checkDoorCollision
// skips workbenches (wallSnap, not wallOnly), so a picked catalog door landed straight
// on a workbench — the exact layout checkWorkbenchOverlap exists to prevent
// (audit 2026-08-20).
if(checkWorkbenchOverlap(sn,iwPx,items,ITEMS,scale)){setToast("A workbench is on that wall — place this somewhere else on the wall.");setTimeout(function(){return setToast(null);},4000);setDoorPick(null);return;}setItems(function(p){return[].concat(_toConsumableArray(p),[ni]);});setSelectedId(ni.id);setDoorPick(null);setToast(null);},[swapId,doorPick,items,mgX,mgY,pW,pH,scale,ITEMS]);// Place the window style chosen in the picker at the remembered wall/point. A catalog window is
// a normal type:"window" item (reuses the built-in window render/collision/payload) carrying the
// style's width + a priced snapshot; fixtureItemId is what marks it as a catalog (vs built-in) window.
var placePickedWindow=useCallback(function(fx){// Swap mode: same re-legalization as the door swap above — the new width is re-clamped
// to the wall and collision/workbench-checked before committing; the old swap kept x/y
// verbatim with no checks at all (audit 2026-08-20).
if(swapId!=null&&fx){var wFt=(Number(fx.widthIn)||24)/12;var cur=items.find(function(it){return it.id===swapId;});if(!cur||!cur.wall){setSwapId(null);setWindowPick(null);return;}if(wFt>(cur.wall==="north"||cur.wall==="south"?pW:pH)/scale+1e-6){setToast("That window is wider than this wall — pick a narrower window.");setTimeout(function(){return setToast(null);},4000);setSwapId(null);setWindowPick(null);return;}var _sn7=snapToWall(cur.wall,cur.x,cur.y,wFt*scale,0.5*scale,pW,pH,mgX,mgY);var others=items.filter(function(it){return it.id!==swapId;});if(checkDoorCollision(_objectSpread(_objectSpread(_objectSpread({},cur),_sn7),{},{widthFt:wFt}),{width:wFt},others,ITEMS,scale)){setToast("That window doesn't fit here — something else is in the way on this wall.");setTimeout(function(){return setToast(null);},4000);setSwapId(null);setWindowPick(null);return;}if(checkWorkbenchOverlap(_sn7,wFt*scale,others,ITEMS,scale)){setToast("A workbench is on that wall — the wider window would overlap it.");setTimeout(function(){return setToast(null);},4000);setSwapId(null);setWindowPick(null);return;}setItems(function(p){return p.map(function(it){return it.id===swapId?_objectSpread(_objectSpread(_objectSpread({},it),_sn7),{},{type:"window",fixtureItemId:fx.id,windowName:fx.name||"Window",planLabel:fx.planLabel&&String(fx.planLabel).trim()||(fx.name||"WIN").toUpperCase().slice(0,6),price:fx.price!=null?fx.price:null,widthIn:Number(fx.widthIn)||null,heightIn:Number(fx.heightIn)||null,// As on the door swap: a built-in window stamped openingHeightFt/sillFt, and those
// beat the catalog window's own heightIn in openingSpan.
openingHeightFt:undefined,sillFt:undefined,widthFt:wFt}):it;});});setSwapId(null);setWindowPick(null);setToast(null);return;}if(!windowPick||!fx)return;var widthFt=(Number(fx.widthIn)||24)/12;// Wider than the clicked wall = snapToWall's clamp degenerates and the window overhangs
// the building corner; refuse up front, as the door picker does (audit 2026-08-20).
if(widthFt>(windowPick.wall==="north"||windowPick.wall==="south"?pW:pH)/scale+1e-6){setToast("That window is wider than this wall — pick a longer wall or a narrower window.");setTimeout(function(){return setToast(null);},4000);setWindowPick(null);return;}var iwPx=widthFt*scale,ihPx=0.5*scale;var sn=snapToWall(windowPick.wall,windowPick.ptx,windowPick.pty,iwPx,ihPx,pW,pH,mgX,mgY);var ni=_objectSpread(_objectSpread({id:idCounter++,type:"window"},sn),{},{widthFt:widthFt,heightFt:0.5,fixtureItemId:fx.id,windowName:fx.name||"Window",planLabel:fx.planLabel&&String(fx.planLabel).trim()||(fx.name||"WIN").toUpperCase().slice(0,6),price:fx.price!=null?fx.price:null,widthIn:Number(fx.widthIn)||null,heightIn:Number(fx.heightIn)||null});if(checkDoorCollision(ni,{width:widthFt},items,ITEMS,scale)){setToast("Something's already there — pick a different spot on the wall.");setTimeout(function(){return setToast(null);},4000);setWindowPick(null);return;}// Workbench check too, as the built-in wall placement runs — checkDoorCollision skips
// workbenches, so a picked catalog window landed straight on one (audit 2026-08-20).
if(checkWorkbenchOverlap(sn,iwPx,items,ITEMS,scale)){setToast("A workbench is on that wall — place this somewhere else on the wall.");setTimeout(function(){return setToast(null);},4000);setWindowPick(null);return;}setItems(function(p){return[].concat(_toConsumableArray(p),[ni]);});setSelectedId(ni.id);setWindowPick(null);setToast(null);},[swapId,windowPick,items,mgX,mgY,pW,pH,scale,ITEMS]);// Place the ramp style chosen in the picker on the remembered door. A custom ramp is a normal
// type:"ramp" item (so all ramp machinery applies) but takes the style's OWN width + length
// (length = the ramp's run/depth out from the door) and snapshots its spec + price.
var placePickedRamp=useCallback(function(fx){// Swap mode: the stored x/y were derived from the OLD depth (a ramp sits centered
// depth/2 OUTSIDE the wall), so keeping them while changing heightFt left a deeper
// ramp extending inside the building on the plan — until its door was next dragged,
// the only thing that recomputed it. Re-derive the position from the door for the
// NEW depth, exactly like the drag path (audit 2026-08-20).
if(swapId!=null&&fx){var wFt=(Number(fx.widthIn)||36)/12;var dpt=(Number(fx.heightIn)||0)/12||RAMP_SPACE_FT;setItems(function(p){return p.map(function(it){if(it.id!==swapId)return it;var door=p.find(function(d){return d.id===it.snapDoorId;});var rp=door&&door.wall?rampPlacementForDoor(door,dpt,pW,pH,mgX,mgY,scale):null;return _objectSpread(_objectSpread(_objectSpread({},it),rp||{}),{},{type:"ramp",fixtureItemId:fx.id,rampName:fx.name||"Ramp",planLabel:fx.planLabel&&String(fx.planLabel).trim()||(fx.name||"RAMP").toUpperCase().slice(0,6),price:fx.price!=null?fx.price:null,widthIn:Number(fx.widthIn)||null,heightIn:Number(fx.heightIn)||null,widthFt:wFt,heightFt:dpt});});});setSwapId(null);setRampPick(null);setToast(null);return;}if(!rampPick||!fx)return;var door=rampPick.door;var widthFt=(Number(fx.widthIn)||36)/12;var rampDepth=(Number(fx.heightIn)||0)/12||RAMP_SPACE_FT;// style length = run out from the door
var rampDepthPx=rampDepth*scale;var rx,ry,rot;if(door.wall==="north"){rx=door.x;ry=mgY-rampDepthPx/2;rot=0;}else if(door.wall==="south"){rx=door.x;ry=mgY+pH+rampDepthPx/2;rot=0;}else if(door.wall==="west"){rx=mgX-rampDepthPx/2;ry=door.y;rot=90;}else if(door.wall==="east"){rx=mgX+pW+rampDepthPx/2;ry=door.y;rot=90;}else{setRampPick(null);return;}var ni={id:idCounter++,type:"ramp",x:rx,y:ry,rotation:rot,wall:door.wall,widthFt:widthFt,heightFt:rampDepth,snapDoorId:door.id,fixtureItemId:fx.id,rampName:fx.name||"Ramp",planLabel:fx.planLabel&&String(fx.planLabel).trim()||(fx.name||"RAMP").toUpperCase().slice(0,6),price:fx.price!=null?fx.price:null,widthIn:Number(fx.widthIn)||null,heightIn:Number(fx.heightIn)||null};setItems(function(p){return[].concat(_toConsumableArray(p),[ni]);});setSelectedId(ni.id);setRampPick(null);setToast(null);},[swapId,rampPick,mgX,mgY,pW,pH,scale,RAMP_SPACE_FT]);var onPtrDown=useCallback(function(e,item){e.stopPropagation();if(planLockedRef.current)return;// no selecting or dragging a building that exists
if(gateRequired){setGateOpen(true);return;}if(pendingRemoval)return;// pick mode: overlays handle the pick; no select/drag
if(activeTool)return;movedRef.current=false;gestureStartRef.current={x:e.touches?e.touches[0].clientX:e.clientX,y:e.touches?e.touches[0].clientY:e.clientY};setSelectedId(item.id);var cfg=ITEMS[item.type];if(resizing||cfg&&cfg.doorSnap)return;// don't drag ramps or while resizing
var pt=getSvgPt(e);if(cfg&&cfg.lineType){// Line is stored as two endpoints; track midpoint offset + half-deltas
// so a body drag translates both endpoints rigidly.
var midX=(item.x1+item.x2)/2,midY=(item.y1+item.y2)/2;setDragging({id:item.id,kind:"line",ox:pt.x-midX,oy:pt.y-midY,halfDx:(item.x2-item.x1)/2,halfDy:(item.y2-item.y1)/2});return;}setDragging({id:item.id,ox:pt.x-item.x,oy:pt.y-item.y,startX:item.x,startY:item.y});},[activeTool,getSvgPt,resizing,ITEMS,pendingRemoval,gateRequired]);var startResize=useCallback(function(e,item,handle){e.preventDefault();if(planLockedRef.current)return;movedRef.current=false;gestureStartRef.current={x:e.touches?e.touches[0].clientX:e.clientX,y:e.touches?e.touches[0].clientY:e.clientY};var pt=getSvgPt(e);setResizing({id:item.id,handle:handle,startPt:pt,origWidthFt:item.widthFt,origHeightFt:item.heightFt,origWidthPx:item.widthPx,origHeightPx:item.heightPx,origX:item.x,origY:item.y});},[getSvgPt]);var getResizeBounds=useCallback(function(item){var isHoriz=item.wall==="north"||item.wall==="south";var wallLen=isHoriz?bldgW:bldgH;var minEdge=0;// wall start in ft
var maxEdge=wallLen;// wall end in ft
// Find obstacles on same wall
items.forEach(function(other){if(other.id===item.id||other.wall!==item.wall)return;var oCfg=ITEMS[other.type];if(!oCfg)return;var oW=other.widthFt||oCfg.width;var oPos=isHoriz?(other.x-mgX)/scale:(other.y-mgY)/scale;var oLeft=oPos-oW/2;var oRight=oPos+oW/2;var itemPos=isHoriz?(item.x-mgX)/scale:(item.y-mgY)/scale;if(oRight<=itemPos)minEdge=Math.max(minEdge,oRight);if(oLeft>=itemPos)maxEdge=Math.min(maxEdge,oLeft);});return{minEdge:minEdge,maxEdge:maxEdge,isHoriz:isHoriz};},[items,ITEMS,bldgW,bldgH,mgX,mgY,scale]);var onPtrMove=useCallback(function(e){// Mark the gesture as a real drag/resize once the pointer travels past a
// small jitter threshold — onPtrUp uses this to decide whether the
// trailing click should be swallowed (see movedRef declaration).
if(gestureStartRef.current&&!movedRef.current){var gx=e.touches?e.touches[0].clientX:e.clientX;var gy=e.touches?e.touches[0].clientY:e.clientY;if(Math.abs(gx-gestureStartRef.current.x)>4||Math.abs(gy-gestureStartRef.current.y)>4)movedRef.current=true;}if(resizing){var _pt=getSvgPt(e);var _it5=items.find(function(i){return i.id===resizing.id;});if(!_it5)return;// Note leader (pointer) drag: the target dot follows the cursor anywhere
// on the visible sheet. Dropping it back onto the note removes the
// pointer (handled in onPtrUp so the handle doesn't snap away mid-drag).
if(_it5.type==="textNote"&&resizing.handle==="leader"){var nx=Math.max(0,Math.min(_pt.x,PAGE_W));var ny=Math.max(0,Math.min(_pt.y,TEXT_BAND_TOP));setItems(function(p){return p.map(function(i){return i.id===resizing.id?_objectSpread(_objectSpread({},i),{},{leader:{x:nx,y:ny}}):i;});});return;}// Text-note resize: drag the bottom-right corner. The top-left stays
// pinned, so the box grows toward the cursor and the text reflows live.
if(_it5.type==="textNote"){var tlX=resizing.origX-resizing.origWidthPx/2;var tlY=resizing.origY-resizing.origHeightPx/2;var newW=Math.max(80,Math.min(_pt.x-tlX,PAGE_W-tlX-4));var newH=Math.max(28,Math.min(_pt.y-tlY,TEXT_BAND_TOP-tlY-4));setItems(function(p){return p.map(function(i){return i.id===resizing.id?_objectSpread(_objectSpread({},i),{},{widthPx:newW,heightPx:newH,x:tlX+newW/2,y:tlY+newH/2}):i;});});return;}// Line endpoint drag: snap the endpoint to the cursor, clamped to the visible page area
if(_it5.type==="line"){var newX=Math.max(0,Math.min(_pt.x,PAGE_W));var newY=Math.max(0,Math.min(_pt.y,TEXT_BAND_TOP));setItems(function(p){return p.map(function(i){return i.id===resizing.id?_objectSpread(_objectSpread({},i),resizing.handle==="ep1"?{x1:newX,y1:newY}:{x2:newX,y2:newY}):i;});});return;}// Loft: free-floating 4-sided resize with collision
if(_it5.type==="loft"){var hd=resizing.handle;var mouseXft=Math.round((_pt.x-mgX)/scale);var mouseYft=Math.round((_pt.y-mgY)/scale);var origCxFt=(resizing.origX-mgX)/scale;var origCyFt=(resizing.origY-mgY)/scale;var origW=resizing.origWidthFt;var origH=resizing.origHeightFt;var oL=Math.round(origCxFt-origW/2),oR=oL+origW;var oT=Math.round(origCyFt-origH/2),oB=oT+origH;var nL=oL,nR=oR,nT=oT,nB=oB;if(hd==="right")nR=Math.max(oL+2,Math.min(mouseXft,bldgW));else if(hd==="left")nL=Math.min(oR-2,Math.max(mouseXft,0));else if(hd==="bottom")nB=Math.max(oT+2,Math.min(mouseYft,bldgH));else if(hd==="top")nT=Math.min(oB-2,Math.max(mouseYft,0));// Check overlap with other lofts — clamp edge if it would overlap
var otherLofts=items.filter(function(i){return i.type==="loft"&&i.id!==resizing.id;});var _iterator17=_createForOfIteratorHelper(otherLofts),_step17;try{for(_iterator17.s();!(_step17=_iterator17.n()).done;){var o=_step17.value;var ow=(o.widthFt||6)/2,oh=(o.heightFt||4)/2;var ocx=(o.x-mgX)/scale,ocy=(o.y-mgY)/scale;var olL=ocx-ow,olR=ocx+ow,olT=ocy-oh,olB=ocy+oh;// Only clamp if the other dimensions overlap (2D check)
if(nT<olB&&nB>olT){// vertically overlapping
if(hd==="right"&&nR>olL&&oL<olL)nR=Math.round(olL);if(hd==="left"&&nL<olR&&oR>olR)nL=Math.round(olR);}if(nL<olR&&nR>olL){// horizontally overlapping
if(hd==="bottom"&&nB>olT&&oT<olT)nB=Math.round(olT);if(hd==="top"&&nT<olB&&oB>olB)nT=Math.round(olB);}}}catch(err){_iterator17.e(err);}finally{_iterator17.f();}var nW=Math.max(2,nR-nL),nH=Math.max(2,nB-nT);setItems(function(p){return p.map(function(i){return i.id===resizing.id?_objectSpread(_objectSpread({},i),{},{widthFt:nW,heightFt:nH,x:mgX+(nL+nR)/2*scale,y:mgY+(nT+nB)/2*scale}):i;});});return;}// Wall-attached 1D resize: workbench snaps to integer feet, rough
// opening resizes smoothly to whatever width the user drags to.
var isHoriz=_it5.wall==="north"||_it5.wall==="south";var isRO=_it5.type==="roughOpening";// Mouse position in feet along the wall axis
var mouseFt=isHoriz?(_pt.x-mgX)/scale:(_pt.y-mgY)/scale;var mouseFtVal=isRO?mouseFt:Math.round(mouseFt);var origCenterFt=isHoriz?(resizing.origX-mgX)/scale:(resizing.origY-mgY)/scale;var origLeft=isRO?origCenterFt-resizing.origWidthFt/2:Math.round(origCenterFt-resizing.origWidthFt/2);var origRight=origLeft+resizing.origWidthFt;var origItem=_objectSpread(_objectSpread({},_it5),{},{x:resizing.origX,y:resizing.origY,widthFt:resizing.origWidthFt});var _getResizeBounds=getResizeBounds(origItem),minEdge=_getResizeBounds.minEdge,maxEdge=_getResizeBounds.maxEdge;var minWidth=isRO?0.5:2;var newLeft=origLeft,newRight=origRight;if(resizing.handle==="max"){newRight=Math.max(origLeft+minWidth,Math.min(mouseFtVal,isRO?maxEdge:Math.floor(maxEdge)));}else{newLeft=Math.min(origRight-minWidth,Math.max(mouseFtVal,isRO?minEdge:Math.ceil(minEdge)));}var newWidthFt=newRight-newLeft;var newCenterFt=(newLeft+newRight)/2;var newPos=(isHoriz?mgX:mgY)+newCenterFt*scale;setItems(function(p){return p.map(function(i){return i.id===resizing.id?_objectSpread(_objectSpread({},i),{},{widthFt:newWidthFt,x:isHoriz?newPos:i.x,y:isHoriz?i.y:newPos}):i;});});return;}if(!dragging)return;var pt=getSvgPt(e);var it=items.find(function(i){return i.id===dragging.id;});if(!it)return;var cfg=ITEMS[it.type];if(!cfg)return;// Line body drag: translate both endpoints by the same delta, clamped so
// neither endpoint leaves the visible page area.
if(dragging.kind==="line"){var newMidX=pt.x-dragging.ox,newMidY=pt.y-dragging.oy;var ahx=Math.abs(dragging.halfDx),ahy=Math.abs(dragging.halfDy);var cMidX=Math.max(ahx,Math.min(newMidX,PAGE_W-ahx));var cMidY=Math.max(ahy,Math.min(newMidY,TEXT_BAND_TOP-ahy));setItems(function(p){return p.map(function(i){return i.id===dragging.id?_objectSpread(_objectSpread({},i),{},{x1:cMidX-dragging.halfDx,y1:cMidY-dragging.halfDy,x2:cMidX+dragging.halfDx,y2:cMidY+dragging.halfDy}):i;});});return;}var rx=pt.x-dragging.ox;var ry=pt.y-dragging.oy;var iWidthFt=it.widthFt||cfg.width;if(cfg.wallOnly){// Always snap to nearest wall during drag so the door follows the mouse
// and doesn't get stuck off-wall. The door's ramp (if any) follows too —
// its placement is derived from the door's position/wall.
var w=getWallFromClick(rx,ry,pW,pH,mgX,mgY)||getNearestWall(rx,ry,pW,pH,mgX,mgY);var sn=snapToWall(w,rx,ry,iWidthFt*scale,cfg.height*scale,pW,pH,mgX,mgY);// Refuse the move rather than commit an overlap — same posture as the workbench branch
// below, which simply returns. Without this, dragging a door onto another door or onto a
// workbench silently succeeded, producing the exact layout the workbench-side toast prevents.
var dOthers=items.filter(function(i){return i.id!==dragging.id;});var dCand=_objectSpread(_objectSpread(_objectSpread({},it),sn),{},{widthFt:iWidthFt});if(checkDoorCollision(dCand,_objectSpread(_objectSpread({},cfg),{},{width:iWidthFt}),dOthers,ITEMS,scale))return;if(checkWorkbenchOverlap(sn,iWidthFt*scale,dOthers,ITEMS,scale))return;// A ramp snapped to this door must follow it (position + wall); otherwise it
// detaches and the stale geometry is rasterized into the exported PDF. (audit #F4)
// rampPlacementForDoor honours the ramp's own depth (catalog ramps vary), so it
// supersedes the fixed-depth relocRamp beta shipped for the same fix.
setItems(function(p){return p.map(function(i){if(i.id===dragging.id)return _objectSpread(_objectSpread({},i),sn);if(i.type==="ramp"&&i.snapDoorId===dragging.id){var rp=rampPlacementForDoor(sn,i.heightFt,pW,pH,mgX,mgY,scale);return rp?_objectSpread(_objectSpread({},i),rp):i;}return i;});});}else if(cfg.wallSnap){var nw=getNearestWall(rx,ry,pW,pH,mgX,mgY);var _sn8=snapToWallInterior(nw,rx,ry,iWidthFt*scale,cfg.height*scale,pW,pH,mgX,mgY);var cand=_objectSpread(_objectSpread({},it),_sn8);// Check collision with doors AND other workbenches on same wall
var others=items.filter(function(i){return i.id!==dragging.id;});if(checkDoorCollision(cand,_objectSpread(_objectSpread({},cfg),{},{width:iWidthFt}),others,ITEMS,scale))return;// Check workbench overlap on same wall
var isH=_sn8.wall==="north"||_sn8.wall==="south";var candPos=isH?_sn8.x:_sn8.y;var candHalf=iWidthFt*scale/2;var _iterator18=_createForOfIteratorHelper(others),_step18;try{for(_iterator18.s();!(_step18=_iterator18.n()).done;){var ob=_step18.value;if(ob.type!=="workbench"||ob.wall!==_sn8.wall)continue;var obW=(ob.widthFt||ITEMS[ob.type].width)*scale/2;var obPos=isH?ob.x:ob.y;if(Math.abs(candPos-obPos)<candHalf+obW-2)return;// overlap
}}catch(err){_iterator18.e(err);}finally{_iterator18.f();}setItems(function(p){return p.map(function(i){return i.id===dragging.id?_objectSpread(_objectSpread({},i),_sn8):i;});});}else{// Notes drag anywhere on the visible page (no plan constraint)
if(cfg.noteType){var _x14=Math.max(20,Math.min(rx,PAGE_W-20));var _y5=Math.max(20,Math.min(ry,TEXT_BAND_TOP-20));setItems(function(p){return p.map(function(i){return i.id===dragging.id?_objectSpread(_objectSpread({},i),{},{x:_x14,y:_y5}):i;});});return;}var iHeightFt=it.heightFt||cfg.height;var halfW=iWidthFt/2,halfH=iHeightFt/2;var snapFt=1;// snap threshold in feet
// Convert desired position to feet
var cxFt=(rx-mgX)/scale;var cyFt=(ry-mgY)/scale;// Round to integer feet
cxFt=Math.round(cxFt);cyFt=Math.round(cyFt);// Snap edges to walls
if(cxFt-halfW<snapFt)cxFt=halfW;else if(cxFt+halfW>bldgW-snapFt)cxFt=bldgW-halfW;if(cyFt-halfH<snapFt)cyFt=halfH;else if(cyFt+halfH>bldgH-snapFt)cyFt=bldgH-halfH;// Snap edges to other lofts
if(it.type==="loft"){var _otherLofts=items.filter(function(i){return i.type==="loft"&&i.id!==dragging.id;});var l=cxFt-halfW,r=cxFt+halfW,t=cyFt-halfH,b=cyFt+halfH;var _iterator19=_createForOfIteratorHelper(_otherLofts),_step19;try{for(_iterator19.s();!(_step19=_iterator19.n()).done;){var _o3=_step19.value;var oW=(_o3.widthFt||cfg.width)/2,oH=(_o3.heightFt||cfg.height)/2;var oCx=(_o3.x-mgX)/scale,oCy=(_o3.y-mgY)/scale;var _oL=oCx-oW,_oR=oCx+oW,_oT=oCy-oH,_oB=oCy+oH;if(t<_oB&&b>_oT){if(Math.abs(r-_oL)<snapFt)cxFt=_oL-halfW;else if(Math.abs(l-_oR)<snapFt)cxFt=_oR+halfW;}if(l<_oR&&r>_oL){if(Math.abs(b-_oT)<snapFt)cyFt=_oT-halfH;else if(Math.abs(t-_oB)<snapFt)cyFt=_oB+halfH;}l=cxFt-halfW;r=cxFt+halfW;t=cyFt-halfH;b=cyFt+halfH;}// Constrain to building
}catch(err){_iterator19.e(err);}finally{_iterator19.f();}cxFt=Math.max(halfW,Math.min(cxFt,bldgW-halfW));cyFt=Math.max(halfH,Math.min(cyFt,bldgH-halfH));// Check overlap — reject if overlapping any loft
var fL=cxFt-halfW,fR=cxFt+halfW,fT=cyFt-halfH,fB=cyFt+halfH;var _iterator20=_createForOfIteratorHelper(_otherLofts),_step20;try{for(_iterator20.s();!(_step20=_iterator20.n()).done;){var _o4=_step20.value;var oW2=(_o4.widthFt||cfg.width)/2,oH2=(_o4.heightFt||cfg.height)/2;var oCx2=(_o4.x-mgX)/scale,oCy2=(_o4.y-mgY)/scale;if(fL<oCx2+oW2-0.1&&fR>oCx2-oW2+0.1&&fT<oCy2+oH2-0.1&&fB>oCy2-oH2+0.1)return;}// Validate attachment — both ends of at least one axis must touch walls or other lofts
}catch(err){_iterator20.e(err);}finally{_iterator20.f();}var olEdges=_otherLofts.map(function(o){var ow=(o.widthFt||cfg.width)/2,oh=(o.heightFt||cfg.height)/2;var ox=(o.x-mgX)/scale,oy=(o.y-mgY)/scale;return{l:ox-ow,r:ox+ow,t:oy-oh,b:oy+oh};});if(!checkLoftAttached(fL,fR,fT,fB,bldgW,bldgH,olEdges)){setItems(function(p){return p.map(function(i){return i.id===dragging.id?_objectSpread(_objectSpread({},i),{},{x:mgX+cxFt*scale,y:mgY+cyFt*scale}):i;});});return;}}else{cxFt=Math.max(halfW,Math.min(cxFt,bldgW-halfW));cyFt=Math.max(halfH,Math.min(cyFt,bldgH-halfH));}var x=mgX+cxFt*scale;var y=mgY+cyFt*scale;setItems(function(p){return p.map(function(i){return i.id===dragging.id?_objectSpread(_objectSpread({},i),{},{x:x,y:y}):i;});});}},[dragging,resizing,getSvgPt,items,mgX,mgY,pW,pH,scale,ITEMS,getResizeBounds]);var onPtrUp=useCallback(function(){// Dropping a note's leader handle back onto the note removes the pointer
// ("drag it home to delete it") — checked on release, not mid-drag, so the
// handle doesn't vanish under the cursor while crossing the note.
if(resizing&&resizing.handle==="leader"){setItems(function(p){return p.map(function(i){if(i.id!==resizing.id||!i.leader)return i;var w=i.widthPx||160,h=i.heightPx||40;var inside=Math.abs(i.leader.x-i.x)<w/2+6&&Math.abs(i.leader.y-i.y)<h/2+6;return inside?_objectSpread(_objectSpread({},i),{},{leader:undefined}):i;});});}setDragging(null);setResizing(null);// Swallow the trailing click ONLY if the gesture actually moved — a
// stationary press must remain a click so clicking a selected note can
// enter in-place edit (an unconditional swallow made notes uneditable).
justGesturedRef.current=movedRef.current;movedRef.current=false;gestureStartRef.current=null;},[resizing]);useEffect(function(){if(dragging||resizing){window.addEventListener("mousemove",onPtrMove);window.addEventListener("mouseup",onPtrUp);window.addEventListener("touchmove",onPtrMove,{passive:false});window.addEventListener("touchend",onPtrUp);return function(){window.removeEventListener("mousemove",onPtrMove);window.removeEventListener("mouseup",onPtrUp);window.removeEventListener("touchmove",onPtrMove);window.removeEventListener("touchend",onPtrUp);};}},[dragging,resizing,onPtrMove,onPtrUp]);// Deleting a door also removes its attached ramp — a ramp can't exist
// without the door it's snapped to.
var delSel=function delSel(){if(selectedId){setItems(function(p){return p.filter(function(i){return i.id!==selectedId&&!(i.type==="ramp"&&i.snapDoorId===selectedId);});});setSelectedId(null);setEditingNoteId(null);}};// Rotate the selection.
//
// Lofts are handled by SWAPPING widthFt/heightFt rather than by setting a rotation angle, and
// that is the whole fix for the rotated-loft class of bug. Both renderers honour `rotation` (SVG
// transform, canvas ctx.rotate) and the hit test swaps the bbox for 90/270 — but EVERY piece of
// loft geometry ignored it: the loft-vs-loft overlap checks, the resize clamps, the drag
// containment clamp (halfW/halfH from the UNROTATED widthFt/heightFt) and checkLoftAttached /
// the unattachedLofts banner. So one click on Rotate could leave a 10x4 loft rendering as 4x10,
// sticking 3ft outside the north wall with no warning, visually overlapping another loft, and
// reporting attached/unattached wrongly — and that geometry is what gets rasterized into the PDF
// the customer signs against and the shop builds from.
//
// A loft is an axis-aligned resizable rectangle, so a 90-degree turn IS a width/height swap;
// expressing it that way keeps `rotation` at 0 and leaves every invariant above valid as
// written, instead of teaching six separate places about rotation. The swap is validated exactly
// like a drag: clamp the centre back inside the building, then refuse if the new footprint would
// overlap another loft or no longer fit.
//
// doorSnap items (ramps) are excluded too: a ramp's position and rotation are DERIVED from the
// door it is attached to, and it deliberately cannot be dragged — rotating it only desynced it
// from its door.
var rotSel=function rotSel(){if(!selectedId)return;var sel=items.find(function(i){return i.id===selectedId;});if(!sel)return;var c=ITEMS[sel.type];if(c&&(c.wallOnly||c.wallSnap||c.lineType||c.doorSnap))return;if(sel.type==="loft"){var curW=sel.widthFt||c.width,curH=sel.heightFt||c.height;var newW=curH,newH=curW;if(newW>bldgW||newH>bldgH){setToast("Turning this loft won't fit inside the building. Resize it first.");setTimeout(function(){return setToast(null);},4000);return;}var halfW=newW/2,halfH=newH/2;var cxFt=(sel.x-mgX)/scale,cyFt=(sel.y-mgY)/scale;cxFt=Math.max(halfW,Math.min(cxFt,bldgW-halfW));cyFt=Math.max(halfH,Math.min(cyFt,bldgH-halfH));var fL=cxFt-halfW,fR=cxFt+halfW,fT=cyFt-halfH,fB=cyFt+halfH;var _iterator21=_createForOfIteratorHelper(items),_step21;try{for(_iterator21.s();!(_step21=_iterator21.n()).done;){var o=_step21.value;if(o.id===sel.id||o.type!=="loft")continue;var oW=(o.widthFt||c.width)/2,oH=(o.heightFt||c.height)/2;var oCx=(o.x-mgX)/scale,oCy=(o.y-mgY)/scale;if(fL<oCx+oW-0.1&&fR>oCx-oW+0.1&&fT<oCy+oH-0.1&&fB>oCy-oH+0.1){setToast("Turning this loft would overlap another loft. Move one of them first.");setTimeout(function(){return setToast(null);},4000);return;}}}catch(err){_iterator21.e(err);}finally{_iterator21.f();}setItems(function(p){return p.map(function(i){return i.id!==selectedId?i:_objectSpread(_objectSpread({},i),{},{widthFt:newW,heightFt:newH,rotation:0,x:mgX+cxFt*scale,y:mgY+cyFt*scale});});});return;}setItems(function(p){return p.map(function(i){return i.id!==selectedId?i:_objectSpread(_objectSpread({},i),{},{rotation:((i.rotation||0)+90)%360});});});};var clearAll=function clearAll(){setItems([]);setSelectedId(null);setEditingNoteId(null);};// ─── EXPORT RENDERING (shared by Export modal, PDF, and submit) ───
// The on-screen SVG already uses page coordinates (cW × cH = 850 × 1100),
// so the export is a straight 2× DPR rasterization — same scale, same mgX/mgY,
// same item positions. No coordinate conversion needed.
var renderExportCanvas=function renderExportCanvas(){var dpr=2;var canvas=document.createElement("canvas");canvas.width=cW*dpr;canvas.height=cH*dpr;var ctx=canvas.getContext("2d");ctx.scale(dpr,dpr);ctx.fillStyle="#FFF";ctx.fillRect(0,0,cW,cH);// Grid + plan border
ctx.strokeStyle="#E8ECF1";ctx.lineWidth=0.5;for(var fx=0;fx<=bldgW;fx++){var x=mgX+fx*scale;ctx.beginPath();ctx.moveTo(x,mgY);ctx.lineTo(x,mgY+pH);ctx.stroke();}for(var fy=0;fy<=bldgH;fy++){var y=mgY+fy*scale;ctx.beginPath();ctx.moveTo(mgX,y);ctx.lineTo(mgX+pW,y);ctx.stroke();}ctx.strokeStyle="#1E293B";ctx.lineWidth=WALL_THICKNESS;ctx.strokeRect(mgX,mgY,pW,pH);// Items render in page coordinates directly — same as the SVG.
// Ramps render first so other items (workbench, doors, etc) sit on top of them.
_toConsumableArray(items).sort(function(a,b){return(a.type==="ramp"?0:1)-(b.type==="ramp"?0:1);}).forEach(function(item){var cfg=ITEMS[item.type];if(!cfg)return;// Line: free-angle segment between two endpoints (page coords)
if(cfg.lineType){ctx.save();ctx.strokeStyle=cfg.color;ctx.lineWidth=2.5;ctx.lineCap="round";ctx.beginPath();ctx.moveTo(item.x1,item.y1);ctx.lineTo(item.x2,item.y2);ctx.stroke();ctx.restore();return;}// Text Note: resizable pill with word-wrapped text (page coords)
if(cfg.noteType){var text=(item.text||"").trim()||"Note";var w=item.widthPx||160;var h=item.heightPx||40;var padX=8;// Leader (pointer) line: dashed from the pill's edge to the target dot.
// Drawn first (absolute page coords) so the pill sits on top of it.
if(item.leader){var ep=noteEdgePoint(item.x,item.y,w,h,item.leader.x,item.leader.y);var ldx=item.leader.x-ep.x,ldy=item.leader.y-ep.y;if(Math.sqrt(ldx*ldx+ldy*ldy)>10){ctx.save();ctx.strokeStyle=cfg.color;ctx.lineWidth=1.5;ctx.setLineDash([5,4]);ctx.beginPath();ctx.moveTo(ep.x,ep.y);ctx.lineTo(item.leader.x,item.leader.y);ctx.stroke();ctx.setLineDash([]);ctx.fillStyle=cfg.color;ctx.beginPath();ctx.arc(item.leader.x,item.leader.y,3.5,0,Math.PI*2);ctx.fill();ctx.restore();}}ctx.save();ctx.translate(item.x,item.y);ctx.fillStyle="#FFFBEB";ctx.strokeStyle=cfg.color;ctx.lineWidth=1.25;var r=4;ctx.beginPath();ctx.moveTo(-w/2+r,-h/2);ctx.lineTo(w/2-r,-h/2);ctx.quadraticCurveTo(w/2,-h/2,w/2,-h/2+r);ctx.lineTo(w/2,h/2-r);ctx.quadraticCurveTo(w/2,h/2,w/2-r,h/2);ctx.lineTo(-w/2+r,h/2);ctx.quadraticCurveTo(-w/2,h/2,-w/2,h/2-r);ctx.lineTo(-w/2,-h/2+r);ctx.quadraticCurveTo(-w/2,-h/2,-w/2+r,-h/2);ctx.fill();ctx.stroke();ctx.font="600 12px sans-serif";ctx.fillStyle=cfg.color;ctx.textAlign="center";ctx.textBaseline="middle";// Word-wrap to fit the box width
var maxW=w-padX*2;var words=text.split(/\s+/);var lines=[];var line="";var _iterator22=_createForOfIteratorHelper(words),_step22;try{for(_iterator22.s();!(_step22=_iterator22.n()).done;){var word=_step22.value;var test=line?line+" "+word:word;if(ctx.measureText(test).width>maxW&&line){lines.push(line);line=word;}else{line=test;}}}catch(err){_iterator22.e(err);}finally{_iterator22.f();}if(line)lines.push(line);var lineHeight=14;var totalH=lines.length*lineHeight;var ly=-totalH/2+lineHeight/2;for(var _i19=0,_lines=lines;_i19<_lines.length;_i19++){var ln=_lines[_i19];ctx.fillText(ln,0,ly);ly+=lineHeight;}ctx.restore();return;}// Plan-bound items: position is already in page coords; widths are in feet
var itemW=item.widthFt||cfg.width;var itemH=item.heightFt||cfg.height;var iw=itemW*scale;var ih=itemH*scale;ctx.save();ctx.translate(item.x,item.y);ctx.rotate(item.rotation*Math.PI/180);if(item.type==="loft"){ctx.fillStyle=cfg.color+"25";ctx.strokeStyle=cfg.color;ctx.lineWidth=2;ctx.setLineDash([6,4]);ctx.fillRect(-iw/2,-ih/2,iw,ih);ctx.strokeRect(-iw/2,-ih/2,iw,ih);ctx.setLineDash([]);ctx.save();ctx.beginPath();ctx.rect(-iw/2,-ih/2,iw,ih);ctx.clip();ctx.strokeStyle=cfg.color+"40";ctx.lineWidth=1;for(var d=-iw;d<iw+ih;d+=12){ctx.beginPath();ctx.moveTo(-iw/2+d,-ih/2);ctx.lineTo(-iw/2+d-ih,ih/2);ctx.stroke();}ctx.restore();}else if(cfg.wallOnly){// Rounded rect for door/window bar (matches SVG rx=1)
var barH=10,barR=1;ctx.fillStyle=item.type==="roughOpening"?"#FFFFFF":item.type==="fixtureDoor"?fixtureDoorColor(item):cfg.color;ctx.beginPath();ctx.moveTo(-iw/2+barR,-barH/2);ctx.lineTo(iw/2-barR,-barH/2);ctx.quadraticCurveTo(iw/2,-barH/2,iw/2,-barH/2+barR);ctx.lineTo(iw/2,barH/2-barR);ctx.quadraticCurveTo(iw/2,barH/2,iw/2-barR,barH/2);ctx.lineTo(-iw/2+barR,barH/2);ctx.quadraticCurveTo(-iw/2,barH/2,-iw/2,barH/2-barR);ctx.lineTo(-iw/2,-barH/2+barR);ctx.quadraticCurveTo(-iw/2,-barH/2,-iw/2+barR,-barH/2);ctx.fill();if(item.type==="roughOpening"){ctx.strokeStyle="#000000";ctx.lineWidth=1.5;ctx.stroke();}if(item.type==="singleDoor"){ctx.strokeStyle=cfg.color+"60";ctx.lineWidth=1.5;ctx.setLineDash([4,3]);var out=item.wall==="north"||item.wall==="east";ctx.beginPath();ctx.arc(-iw/2,0,iw*0.8,0,out?-Math.PI/2:Math.PI/2,out);ctx.stroke();ctx.setLineDash([]);}else if(item.type==="doubleDoor"){ctx.strokeStyle=cfg.color+"60";ctx.lineWidth=1.5;ctx.setLineDash([4,3]);var _out2=item.wall==="north"||item.wall==="east";var _r3=iw*0.4;// Left leaf: hinge at left edge of door
ctx.beginPath();ctx.arc(-iw/2,0,_r3,0,_out2?-Math.PI/2:Math.PI/2,_out2);ctx.stroke();// Right leaf: hinge at right edge of door
ctx.beginPath();ctx.arc(iw/2,0,_r3,Math.PI,_out2?3*Math.PI/2:Math.PI/2,!_out2);ctx.stroke();ctx.setLineDash([]);ctx.strokeStyle="#FFF";ctx.lineWidth=1.5;ctx.beginPath();ctx.moveTo(0,-5);ctx.lineTo(0,5);ctx.stroke();}else if(item.type==="fixtureDoor"){fixtureDoorCanvas(ctx,item,iw,fixtureDoorColor(item));}else if(item.type==="window"){ctx.strokeStyle="#FFF";ctx.lineWidth=1.5;[0,-iw/4,iw/4].forEach(function(lx){ctx.beginPath();ctx.moveTo(lx,-4);ctx.lineTo(lx,4);ctx.stroke();});}}else{ctx.fillStyle=cfg.color+(item.type==="ramp"?"12":"30");ctx.strokeStyle=cfg.color+(item.type==="ramp"?"80":"FF");ctx.lineWidth=item.type==="ramp"?1.5:2;ctx.fillRect(-iw/2,-ih/2,iw,ih);ctx.strokeRect(-iw/2,-ih/2,iw,ih);}ctx.fillStyle="#1E293B";ctx.font="bold 11px sans-serif";ctx.textAlign="center";if(item.type==="workbench"){ctx.fillText("".concat(itemW," ft"),0,0);ctx.font="9px sans-serif";ctx.fillText("Workbench",0,13);}else if(item.type==="ramp"){ctx.textAlign="left";ctx.fillText(item.planLabel||"RAMP",-iw/2+5,4);}else if(item.type==="loft"){ctx.fillStyle=cfg.color;ctx.fillText("LOFT",0,0);ctx.font="10px sans-serif";ctx.globalAlpha=0.7;ctx.fillText("".concat(itemW,"\xD7").concat(itemH," ft"),0,14);ctx.globalAlpha=1;}else{var lblY=cfg.wallOnly?item.wall==="north"||item.wall==="east"?14:-10:4;var label=cfg.shortLabel;if(item.type==="fixtureDoor")label=item.planLabel||cfg.shortLabel;if(item.type==="window")label=item.planLabel||cfg.shortLabel;if(item.type==="roughOpening"){var idx=items.filter(function(i){return i.type==="roughOpening";}).findIndex(function(r){return r.id===item.id;});label="RO-".concat(idx+1);}// Doors + windows prefix their width, e.g. "6' DD".
if(item.type==="singleDoor"||item.type==="doubleDoor"||item.type==="fixtureDoor"||item.type==="window"){var _w6=fmtFtIn((item.widthFt||cfg.width)*12);if(_w6)label="".concat(_w6," ").concat(label);}ctx.fillText(label,0,lblY);}ctx.restore();});// Size labels + FRONT/BACK/LEFT/RIGHT — drawn after items so a centered ramp doesn't paint over the building chrome.
ctx.fillStyle="#475569";ctx.font="bold 13px sans-serif";ctx.textAlign="center";ctx.fillText("".concat(bldgW," ft"),mgX+pW/2,mgY-16);ctx.fillText("".concat(bldgW," ft"),mgX+pW/2,mgY+pH+26);ctx.save();ctx.translate(mgX-20,mgY+pH/2);ctx.rotate(-Math.PI/2);ctx.fillText("".concat(bldgH," ft"),0,0);ctx.restore();ctx.save();ctx.translate(mgX+pW+24,mgY+pH/2);ctx.rotate(Math.PI/2);ctx.fillText("".concat(bldgH," ft"),0,0);ctx.restore();if(frontWall){ctx.fillStyle="#94A3B8";ctx.font="10px sans-serif";ctx.fillText(getDisplayLabel("north",frontWall),mgX+pW/2,mgY-32);ctx.fillText(getDisplayLabel("south",frontWall),mgX+pW/2,mgY+pH+42);ctx.save();ctx.translate(mgX-38,mgY+pH/2);ctx.rotate(-Math.PI/2);ctx.fillText(getDisplayLabel("west",frontWall),0,0);ctx.restore();ctx.save();ctx.translate(mgX+pW+42,mgY+pH/2);ctx.rotate(Math.PI/2);ctx.fillText(getDisplayLabel("east",frontWall),0,0);ctx.restore();}// ─── Bottom text band ───
var TEXT_X=36;var TEXT_RIGHT=cW-36;var TEXT_W=TEXT_RIGHT-TEXT_X;var textTop=TEXT_BAND_TOP;var textY=textTop+24;// Separator
ctx.strokeStyle="#E2E8F0";ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(TEXT_X,textTop);ctx.lineTo(TEXT_RIGHT,textTop);ctx.stroke();var customerName=contact.name||"Customer";var customerAddr=[contact.street,contact.city,contact.state,contact.zip].filter(Boolean).join(", ");// Line 1: Name (left) | Size + Style (right)
ctx.fillStyle="#1E293B";ctx.font="bold 16px sans-serif";ctx.textAlign="left";ctx.fillText(customerName,TEXT_X,textY);ctx.textAlign="right";ctx.fillText("".concat(bldgW,"\xD7").concat(bldgH,"  ").concat(sel.style||""),TEXT_RIGHT,textY);textY+=22;// Line 2-3: Contact info + address
ctx.fillStyle="#64748B";ctx.font="13px sans-serif";ctx.textAlign="left";var infoLine=[contact.phone,contact.email].filter(Boolean).join("  •  ");if(infoLine){ctx.fillText(infoLine,TEXT_X,textY);textY+=17;}if(customerAddr){ctx.fillText(customerAddr,TEXT_X,textY);textY+=19;}// Build bullet list (paint + placed items + custom options)
var bullets=[];if(sel.paint==="Painted"){var body=paintColors.body||"TBD";var trim=paintColors.trim||"TBD";bullets.push("Painted \u2014 Body: ".concat(body,", Trim: ").concat(trim));}else{bullets.push("Unpainted");}if(sel.roofType)bullets.push("Roof \u2014 ".concat(sel.roofType).concat(sel.roofColor?": ".concat(sel.roofColor):""));var sdCount=items.filter(function(i){return i.type==="singleDoor";}).length;var ddCount=items.filter(function(i){return i.type==="doubleDoor";}).length;if(sdCount>0)bullets.push("Single Door".concat(sdCount>1?" ×"+sdCount:""));if(ddCount>0)bullets.push("Double Door".concat(ddCount>1?" ×"+ddCount:""));// Catalog fixture doors — one bullet per placed door with its full spec (name, size,
// swing, operation), driven by the placed items so ANY catalog door lists automatically
// (nothing hard-coded per door). Windows/ramps will slot in the same way later.
items.filter(function(i){return i.type==="fixtureDoor";}).forEach(function(d){var parts=[];if(d.widthIn&&d.heightIn)parts.push("".concat(fmtFtIn(d.widthIn),"\xD7").concat(fmtFtIn(d.heightIn)));var sw=d.swing==="in"?"in-swing":d.swing==="out"?"out-swing":"";if(sw)parts.push(sw);var op=d.operation==="slideup"?"slide up":d.operation==="double"?"double":d.operation==="right"?"right hinge":d.operation==="left"?"left hinge":"";if(op)parts.push(op);bullets.push("".concat(d.doorName||"Door").concat(parts.length?" — "+parts.join(", "):""));});var winCount=items.filter(function(i){return i.type==="window";}).length;if(winCount>0)bullets.push("Window".concat(winCount>1?"s ×"+winCount:""));items.filter(function(i){return i.type==="workbench";}).forEach(function(wb){return bullets.push("".concat(wb.widthFt,"ft Workbench"));});var loftItems=items.filter(function(i){return i.type==="loft";});if(loftItems.length>0){var loftSqft=Math.round(loftItems.reduce(function(s,l){return s+(Number(l.widthFt)||0)*(Number(l.heightFt)||0);},0));bullets.push("Loft".concat(loftItems.length>1?" ×"+loftItems.length:""," \u2014 ").concat(loftSqft," sq ft"));}var rampCount=items.filter(function(i){return i.type==="ramp";}).length;if(rampCount>0)bullets.push("Ramp".concat(rampCount>1?" ×"+rampCount:""));items.filter(function(i){return i.type==="roughOpening";}).forEach(function(ro,idx){var d=(roDimensions[ro.id]||"").trim();var label="RO-".concat(idx+1);bullets.push(d?"".concat(label," \u2014 ").concat(d):label);});// Lines and notes are not bulleted — they already render at their position on the page.
customOptions.forEach(function(co){if(co.name&&co.name.trim()){var q=co.qty&&parseInt(co.qty)>0?" (\xD7".concat(co.qty,")"):"";bullets.push(co.name.trim()+q);}});// 2-column bullets
if(bullets.length>0){textY+=6;ctx.fillStyle="#334155";ctx.font="13px sans-serif";ctx.textAlign="left";var colW=TEXT_W/2;var half=Math.ceil(bullets.length/2);for(var i=0;i<half;i++){ctx.fillText("•  "+bullets[i],TEXT_X,textY);if(i+half<bullets.length)ctx.fillText("•  "+bullets[i+half],TEXT_X+colW,textY);textY+=18;}}return canvas;};var generatePNG=function generatePNG(){return renderExportCanvas().toDataURL("image/png");};var exportPNG=function exportPNG(){setExportUrl(generatePNG());setShowExport(true);};// ─── ADMIN: GHL credentials management ───
// Both helpers call the admin-save-settings Edge Function; the password is verified
// server-side against ADMIN_PASSWORD. The API key, once saved, is never returned to
// the browser — checkAdminStatus only reports configured/not + a masked location ID.
var checkAdminStatus=/*#__PURE__*/function(){var _ref26=_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee13(){var _yield$supabase$funct3,data,error;return _regeneratorRuntime().wrap(function _callee13$(_context13){while(1)switch(_context13.prev=_context13.next){case 0:if(supabase){_context13.next=3;break;}setAdminMsg({ok:false,msg:"Supabase not configured."});return _context13.abrupt("return");case 3:if(adminPwd){_context13.next=6;break;}setAdminMsg({ok:false,msg:"Enter the admin password first."});return _context13.abrupt("return");case 6:setAdminBusy(true);setAdminMsg(null);_context13.prev=8;_context13.next=11;return supabase.functions.invoke("admin-save-settings",{body:{adminPassword:adminPwd,clientId:C.clientId,action:"status"}});case 11:_yield$supabase$funct3=_context13.sent;data=_yield$supabase$funct3.data;error=_yield$supabase$funct3.error;if(!error){_context13.next=16;break;}throw new Error(error.message||"Status check failed");case 16:if(data!==null&&data!==void 0&&data.ok){_context13.next=18;break;}throw new Error((data===null||data===void 0?void 0:data.error)||"Status check failed");case 18:setAdminStatus(data);_context13.next=25;break;case 21:_context13.prev=21;_context13.t0=_context13["catch"](8);setAdminMsg({ok:false,msg:_context13.t0.message});setAdminStatus(null);case 25:_context13.prev=25;setAdminBusy(false);return _context13.finish(25);case 28:case"end":return _context13.stop();}},_callee13,null,[[8,21,25,28]]);}));return function checkAdminStatus(){return _ref26.apply(this,arguments);};}();var saveAdminSettings=/*#__PURE__*/function(){var _ref27=_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee14(){var _yield$supabase$funct4,data,error,_yield$supabase$funct5,st;return _regeneratorRuntime().wrap(function _callee14$(_context14){while(1)switch(_context14.prev=_context14.next){case 0:if(supabase){_context14.next=3;break;}setAdminMsg({ok:false,msg:"Supabase not configured."});return _context14.abrupt("return");case 3:if(!(!adminPwd||!adminLocId||!adminApiKey)){_context14.next=6;break;}setAdminMsg({ok:false,msg:"Fill in admin password, location ID, and API key."});return _context14.abrupt("return");case 6:setAdminBusy(true);setAdminMsg(null);_context14.prev=8;_context14.next=11;return supabase.functions.invoke("admin-save-settings",{body:{adminPassword:adminPwd,clientId:C.clientId,ghlLocationId:adminLocId,ghlApiKey:adminApiKey}});case 11:_yield$supabase$funct4=_context14.sent;data=_yield$supabase$funct4.data;error=_yield$supabase$funct4.error;if(!error){_context14.next=16;break;}throw new Error(error.message||"Save failed");case 16:if(data!==null&&data!==void 0&&data.ok){_context14.next=18;break;}throw new Error((data===null||data===void 0?void 0:data.error)||"Save failed");case 18:setAdminMsg({ok:true,msg:"GHL settings saved."});setAdminApiKey("");// Clear the key from React state once it's persisted
// Refresh status indicator
_context14.next=22;return supabase.functions.invoke("admin-save-settings",{body:{adminPassword:adminPwd,clientId:C.clientId,action:"status"}});case 22:_yield$supabase$funct5=_context14.sent;st=_yield$supabase$funct5.data;if(st!==null&&st!==void 0&&st.ok)setAdminStatus(st);_context14.next=30;break;case 27:_context14.prev=27;_context14.t0=_context14["catch"](8);setAdminMsg({ok:false,msg:_context14.t0.message});case 30:_context14.prev=30;setAdminBusy(false);return _context14.finish(30);case 33:case"end":return _context14.stop();}},_callee14,null,[[8,27,30,33]]);}));return function saveAdminSettings(){return _ref27.apply(this,arguments);};}();// ─── ADMIN: 3D style calibration handlers ───
// The four-side photos are the calibration REFERENCE (the IdeaRoom onboarding
// workflow): a human — or the calibrate-style function — reads the roof/
// siding/color parameters off them into the style's d3 spec; the parametric
// engine renders from the spec, never from the photos.
var openCalEditor=function openCalEditor(s){setAdminCalMsg(null);setAdminCalPreview(false);setAdminCal({styleValue:s.value,spec:d3ResolveStyleSpec(s,s.value,C.wallHeightFt),// s.d3Photos is empty as of migration 093: these are a builder's photos of their own
// REAL buildings, and get_config is the call the anonymous customer page makes, so they
// are no longer in that payload. The portal re-fetches them over its authenticated
// session below; the standalone ?admin=1 path has no session and so starts blank.
photos:(s.d3Photos||[]).concat(["","","",""]).slice(0,4)});setScan({busy:false,step:null,err:null,measured:null,file:null,status:"none",aiReady:null});// One authenticated read gives everything the customer config deliberately does not carry:
// the reference photos (a builder's own buildings — see 093), this style's scan status, and
// whether AI drafting is even configured on the server.
if(setup3d&&setup3d.onLoadStyle3D){setup3d.onLoadStyle3D(s.value).then(function(meta){if(!meta)return;setAdminCal(function(p){return p&&p.styleValue===s.value&&Array.isArray(meta.photos)&&meta.photos.length?_objectSpread(_objectSpread({},p),{},{photos:meta.photos.filter(Boolean).concat(["","","",""]).slice(0,4)}):p;});setScan(function(p){return _objectSpread(_objectSpread({},p),{},{status:meta.modelStatus||"none",aiReady:meta.aiReady!==false});});})["catch"](function(){/* a convenience read; never block the editor */});}};var calSet=function calSet(patch){return setAdminCal(function(p){return _objectSpread(_objectSpread({},p),{},{spec:_objectSpread(_objectSpread({},p.spec),patch)});});};var calSetRoof=function calSetRoof(patch){return setAdminCal(function(p){return _objectSpread(_objectSpread({},p),{},{spec:_objectSpread(_objectSpread({},p.spec),{},{roof:_objectSpread(_objectSpread({},p.spec.roof),patch)})});});};var calSetColor=function calSetColor(k,v){return setAdminCal(function(p){return _objectSpread(_objectSpread({},p),{},{spec:_objectSpread(_objectSpread({},p.spec),{},{colors:_objectSpread(_objectSpread({},p.spec.colors),{},_defineProperty({},k,v))})});});};var calSetPhoto=function calSetPhoto(i,v){return setAdminCal(function(p){var ph=p.photos.slice();ph[i]=v;return _objectSpread(_objectSpread({},p),{},{photos:ph});});};// A drafted spec MERGES into the draft rather than replacing it: the model reports only
// what the photos actually show, so anything it leaves out keeps the value the editor
// (or the style default) already had.
var applyDraftedSpec=function applyDraftedSpec(d3){return setAdminCal(function(p){return _objectSpread(_objectSpread({},p),{},{spec:{roof:_objectSpread(_objectSpread({},p.spec.roof),d3.roof||{}),siding:d3.siding!==undefined?d3.siding:p.spec.siding,colors:_objectSpread(_objectSpread({},p.spec.colors),d3.colors||{}),wallHeightFt:d3.wallHeightFt||p.spec.wallHeightFt}});});};var copyCalJson=function copyCalJson(){var out=JSON.stringify({d3:adminCal.spec,d3Photos:adminCal.photos.filter(Boolean)},null,2);try{navigator.clipboard.writeText(out);setAdminCalMsg({ok:true,msg:"d3 JSON copied — paste into this style's building_styles.d3 column."});}catch(_){setAdminCalMsg({ok:false,msg:"Clipboard blocked — JSON: "+out});}};// Photo slots can take an upload in the portal (the host owns the authed call); the
// public page keeps pasting URLs, since it has no session to upload with.
var calUploadPhoto=/*#__PURE__*/function(){var _ref28=_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee15(i,file){var url;return _regeneratorRuntime().wrap(function _callee15$(_context15){while(1)switch(_context15.prev=_context15.next){case 0:if(!(!file||!(setup3d&&setup3d.onUploadPhoto))){_context15.next=2;break;}return _context15.abrupt("return");case 2:if(!(file.size>3000000)){_context15.next=5;break;}setAdminCalMsg({ok:false,msg:"That photo is over 3MB — please use a smaller one."});return _context15.abrupt("return");case 5:setAdminCalBusy(true);setAdminCalMsg(null);_context15.prev=7;_context15.next=10;return setup3d.onUploadPhoto(file);case 10:url=_context15.sent;if(url){_context15.next=13;break;}throw new Error("Upload returned no URL.");case 13:calSetPhoto(i,url);setAdminCalMsg({ok:true,msg:"Photo uploaded."});_context15.next=20;break;case 17:_context15.prev=17;_context15.t0=_context15["catch"](7);setAdminCalMsg({ok:false,msg:_context15.t0.message||"Upload failed"});case 20:_context15.prev=20;setAdminCalBusy(false);return _context15.finish(20);case 23:case"end":return _context15.stop();}},_callee15,null,[[7,17,20,23]]);}));return function calUploadPhoto(_x15,_x16){return _ref28.apply(this,arguments);};}();// ─── Building scan: read it, measure it, then drive the parametric model from the numbers ──
// Measuring happens BEFORE any upload, on purpose: nothing is stored until we know the file
// is a usable GLB of something building-shaped, and the builder has seen the numbers.
var scanDisposeScene=function scanDisposeScene(scene){scene.traverse(function(o){if(o.geometry)o.geometry.dispose();var mats=o.material?Array.isArray(o.material)?o.material:[o.material]:[];mats.forEach(function(mm){if(mm.map)mm.map.dispose();mm.dispose();});});};// Read + gate + measure. Returns the LIVE scene too — the turntable renders
// it — so CALLERS own disposal via scanDisposeScene; a 40MB scan must never
// sit in memory behind the editor.
var scanReadGlb=/*#__PURE__*/function(){var _ref29=_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee16(file){var buf,gate,_yield$loadGLTFLoader,THREE,GLTFLoader,gltf,sample,metric;return _regeneratorRuntime().wrap(function _callee16$(_context16){while(1)switch(_context16.prev=_context16.next){case 0:if(!(file.size>SCAN_MAX_BYTES)){_context16.next=2;break;}throw new Error("That scan is ".concat((file.size/1048576).toFixed(0),"MB. Export it at Medium or High instead of Ultra \u2014 the limit is ").concat(SCAN_MAX_BYTES/1048576,"MB."));case 2:_context16.next=4;return file.arrayBuffer();case 4:buf=_context16.sent;// Header gate first: no three.js, no GPU, works on a phone, and refuses the whole class
// of files that would otherwise fail deep inside the loader with an opaque message.
gate=scanInspectGlb(buf);if(!gate.err){_context16.next=8;break;}throw new Error(gate.err);case 8:_context16.next=10;return loadGLTFLoader();case 10:_yield$loadGLTFLoader=_context16.sent;THREE=_yield$loadGLTFLoader.THREE;GLTFLoader=_yield$loadGLTFLoader.GLTFLoader;_context16.next=15;return new GLTFLoader().parseAsync(buf,"");case 15:gltf=_context16.sent;if(gltf.scene instanceof THREE.Object3D){_context16.next=18;break;}throw new Error("Two copies of three.js loaded — check the version pin.");case 18:sample=scanSamplePoints(THREE,gltf.scene,120000);if(sample){_context16.next=22;break;}scanDisposeScene(gltf.scene);throw new Error("That scan has no mesh in it.");case 22:metric=scanMeasure(sample);if(!metric.err){_context16.next=26;break;}scanDisposeScene(gltf.scene);throw new Error(metric.err);case 26:return _context16.abrupt("return",{THREE:THREE,scene:gltf.scene,metric:metric,measured:scanToFeet(metric)});case 27:case"end":return _context16.stop();}},_callee16);}));return function scanReadGlb(_x17){return _ref29.apply(this,arguments);};}();var scanPick=/*#__PURE__*/function(){var _ref30=_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee17(file){var _yield$scanReadGlb,scene,measured;return _regeneratorRuntime().wrap(function _callee17$(_context17){while(1)switch(_context17.prev=_context17.next){case 0:if(file){_context17.next=2;break;}return _context17.abrupt("return");case 2:// Full replacement on purpose (drops renderUrls — a new file needs new
// renders) but the style's status, the aiReady probe and the builder's
// device pick survive.
setScan({busy:true,step:"Reading the scan…",err:null,measured:null,file:null,status:scan.status,aiReady:scan.aiReady,device:scan.device});_context17.prev=3;_context17.next=6;return scanReadGlb(file);case 6:_yield$scanReadGlb=_context17.sent;scene=_yield$scanReadGlb.scene;measured=_yield$scanReadGlb.measured;scanDisposeScene(scene);setScan({busy:false,step:null,err:null,measured:measured,file:file,status:scan.status,aiReady:scan.aiReady,device:scan.device});_context17.next=16;break;case 13:_context17.prev=13;_context17.t0=_context17["catch"](3);setScan({busy:false,step:null,err:_context17.t0.message||"Could not read that scan.",measured:null,file:null,status:scan.status,aiReady:scan.aiReady,device:scan.device});case 16:case"end":return _context17.stop();}},_callee17,null,[[3,13]]);}));return function scanPick(_x18){return _ref30.apply(this,arguments);};}();// Four three-quarter turntable JPEGs of the raw scan, framed off the
// measured geometry. MeshBasicMaterial swap on purpose: photogrammetry
// textures carry the light they were captured in — lighting them again
// shades them wrong, and an unlit standard material renders black.
var scanRenderTurntable=function scanRenderTurntable(THREE,scene,metric){var W=1024,H=768;var cv=document.createElement("canvas");cv.width=W;cv.height=H;var renderer=new THREE.WebGLRenderer({canvas:cv,antialias:true,preserveDrawingBuffer:true});renderer.setPixelRatio(1);renderer.setClearColor("#E7EEF5",1);var swapped=[];scene.traverse(function(o){if(!o.isMesh)return;var orig=Array.isArray(o.material)?o.material[0]:o.material;var mat=new THREE.MeshBasicMaterial({map:orig&&orig.map||null,vertexColors:Boolean(o.geometry&&o.geometry.getAttribute&&o.geometry.getAttribute("color")),side:THREE.DoubleSide});if(!mat.map&&!mat.vertexColors)mat.color.set("#8A857C");swapped.push({mesh:o,orig:o.material,mat:mat});o.material=mat;});var cam=new THREE.PerspectiveCamera(45,W/H,0.05,1000);var target=new THREE.Vector3(metric.centerX||0,(metric.groundY||0)+(metric.peakM||2)*0.45,metric.centerZ||0);var dist=Math.max(2,2.3*Math.max(metric.widthM||2,metric.peakM||2));var elev=18*Math.PI/180;var shots=[];for(var i=0;i<4;i++){var az=((metric.headingDeg||0)+i*90+22.5)*Math.PI/180;cam.position.set(target.x+dist*Math.cos(elev)*Math.sin(az),target.y+dist*Math.sin(elev),target.z+dist*Math.cos(elev)*Math.cos(az));cam.lookAt(target);renderer.render(scene,cam);shots.push(cv.toDataURL("image/jpeg",0.85));}swapped.forEach(function(s){s.mesh.material=s.orig;s.mat.dispose();});renderer.dispose();if(renderer.forceContextLoss)renderer.forceContextLoss();return shots;};// One button: measure → apply → (when AI is on) render the scan from four
// sides → upload the renders as the style's photos → let the AI read siding
// and colours → merge — then re-assert the measured geometry ON TOP: the
// tape measure beats the vision model wherever they disagree. Renders are
// cached per picked file, so a retry re-spends only the AI call (10/day cap).
var scanGenerate=/*#__PURE__*/function(){var _ref31=_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee18(){var file,_yield$scanReadGlb2,THREE,scene,metric,measured,exact,aiOn,urls,shots,_loop6,i,d3,roofNote;return _regeneratorRuntime().wrap(function _callee18$(_context19){while(1)switch(_context19.prev=_context19.next){case 0:file=scan.file;if(!(!file||scan.busy)){_context19.next=3;break;}return _context19.abrupt("return");case 3:setScan(function(p){return _objectSpread(_objectSpread({},p),{},{busy:true,err:null,step:"Reading the scan…"});});_context19.prev=4;_context19.next=7;return scanReadGlb(file);case 7:_yield$scanReadGlb2=_context19.sent;THREE=_yield$scanReadGlb2.THREE;scene=_yield$scanReadGlb2.scene;metric=_yield$scanReadGlb2.metric;measured=_yield$scanReadGlb2.measured;setScan(function(p){return _objectSpread(_objectSpread({},p),{},{measured:measured,step:"Applying measurements…"});});scanApplyMeasured(measured);exact=scanSizeMatchesFor(measured).find(function(s){return s.score===0;});if(exact)scanApplySize(exact.label);aiOn=scan.aiReady!==false&&setup3d&&setup3d.onDraftFromPhotos&&setup3d.onUploadPhoto;if(aiOn){_context19.next=22;break;}scanDisposeScene(scene);setScan(function(p){return _objectSpread(_objectSpread({},p),{},{busy:false,step:null});});setAdminCalMsg({ok:true,msg:"Measured and applied. AI look-drafting is off for this site — siding and colours stay as they are; tune them by hand, or ask CSM Synergy to switch AI on."});return _context19.abrupt("return");case 22:urls=scan.renderUrls||null;shots=null;if(!urls){setScan(function(p){return _objectSpread(_objectSpread({},p),{},{step:"Rendering the scan…"});});shots=scanRenderTurntable(THREE,scene,metric);}scanDisposeScene(scene);if(urls){_context19.next=36;break;}urls=[];_loop6=/*#__PURE__*/_regeneratorRuntime().mark(function _loop6(i){var f,url;return _regeneratorRuntime().wrap(function _loop6$(_context18){while(1)switch(_context18.prev=_context18.next){case 0:setScan(function(p){return _objectSpread(_objectSpread({},p),{},{step:"Uploading view ".concat(i+1," of 4\u2026")});});f=new File([dataUrlToBytes(shots[i])],"scan-view-".concat(i+1,".jpg"),{type:"image/jpeg"});_context18.next=4;return setup3d.onUploadPhoto(f);case 4:url=_context18.sent;if(url){_context18.next=7;break;}throw new Error("Upload returned no URL.");case 7:urls.push(url);calSetPhoto(i,url);case 9:case"end":return _context18.stop();}},_loop6);});i=0;case 30:if(!(i<shots.length)){_context19.next=35;break;}return _context19.delegateYield(_loop6(i),"t0",32);case 32:i++;_context19.next=30;break;case 35:setScan(function(p){return _objectSpread(_objectSpread({},p),{},{renderUrls:urls});});case 36:setScan(function(p){return _objectSpread(_objectSpread({},p),{},{step:"Reading the look with AI…"});});_context19.next=39;return setup3d.onDraftFromPhotos(urls,adminCal.styleValue);case 39:d3=_context19.sent;applyDraftedSpec(d3);scanApplyMeasured(measured);setScan(function(p){return _objectSpread(_objectSpread({},p),{},{busy:false,step:null});});roofNote=d3&&d3.roof&&d3.roof.type&&d3.roof.type!==measured.roofType?" (the AI read the roof as ".concat(d3.roof.type,"; keeping the measured ").concat(measured.roofType,")"):"";setAdminCalMsg({ok:true,msg:"Generated from the scan: measured geometry + AI siding/colours".concat(roofNote,". Preview it, tweak, then Save.")});_context19.next=50;break;case 47:_context19.prev=47;_context19.t1=_context19["catch"](4);setScan(function(p){return _objectSpread(_objectSpread({},p),{},{busy:false,step:null,err:_context19.t1.message||"Could not generate from that scan."});});case 50:case"end":return _context19.stop();}},_callee18,null,[[4,47]]);}));return function scanGenerate(){return _ref31.apply(this,arguments);};}();// Push the measured numbers into the draft spec. Geometry owns dimensions; the AI photo read
// (and the builder's eye) still own siding and colour, which a scan's phone-white-balanced
// texture is genuinely bad at.
var scanApplyMeasured=function scanApplyMeasured(m){calSet({wallHeightFt:m.eaveFt});var roof={type:m.roofType,pitch:m.pitch,overhang:m.overhangFt||0};if(m.roofType==="gambrel"&&m.gambrel){roof.kneeU=m.gambrel.kneeU;roof.kneeRise=m.gambrel.kneeRise;roof.ridgeRise=m.gambrel.ridgeRise;}if(m.roofType==="gable"&&m.ridgeOffset)roof.ridgeOffset=m.ridgeOffset;calSetRoof(roof);};var scanApply=function scanApply(){var m=scan.measured;if(!m)return;scanApplyMeasured(m);setAdminCalMsg({ok:true,msg:"Using the scan: ".concat(m.widthFt,"\xD7").concat(m.depthFt," ft, ").concat(m.eaveFt," ft walls, ").concat(m.roofType," roof at ").concat(m.pitch).concat(m.overhangFt?", ".concat(m.overhangFt," ft overhang"):"",". Preview it, then save.")});};// Closest sizes this style sells, orientation-agnostic; empty when nothing
// is within 4 ft total. The scan can SUGGEST but never create a size —
// sizes carry pricing (the NULL-base-price contract), so a miss is the
// builder's cue to add the size under Settings → Catalog first.
var scanSizeMatchesFor=function scanSizeMatchesFor(m){if(!m)return[];var st=(C&&C.buildingStyles||[]).find(function(s){return s.value===(adminCal&&adminCal.styleValue);});var labels=st&&Array.isArray(st.sizes)&&st.sizes.length?st.sizes:C&&C.defaultSizes||[];var out=[];labels.forEach(function(label){var p=parseSize(typeof label==="string"?label:label&&label.label);if(!p)return;var score=Math.abs(Math.max(p.w,p.h)-Math.max(m.widthFt,m.depthFt))+Math.abs(Math.min(p.w,p.h)-Math.min(m.widthFt,m.depthFt));out.push({label:typeof label==="string"?label:label.label,score:score});});out.sort(function(a,b){return a.score-b.score;});return out.filter(function(s){return s.score<=4;}).slice(0,3);};var scanApplySize=function scanApplySize(label){setSel(function(p){return _objectSpread(_objectSpread({},p),{},{style:adminCal.styleValue,size:label});});setAdminCalMsg({ok:true,msg:"Canvas set to ".concat(label," on this style \u2014 the resize cleared any placed items.")});};// Store the scan against the style so it can be re-measured later (an algorithm improvement
// should not need the builder to walk round the building again). The capture kind rides in
// the meta: LiDAR and photogrammetry meshes earn different trust later.
var scanUpload=/*#__PURE__*/function(){var _ref32=_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee19(){var path,meta;return _regeneratorRuntime().wrap(function _callee19$(_context20){while(1)switch(_context20.prev=_context20.next){case 0:if(!(!scan.file||!(setup3d&&setup3d.onUploadModel&&setup3d.onSaveModel))){_context20.next=2;break;}return _context20.abrupt("return");case 2:setScan(function(p){return _objectSpread(_objectSpread({},p),{},{busy:true,step:"Uploading…",err:null});});_context20.prev=3;_context20.next=6;return setup3d.onUploadModel(scan.file,adminCal.styleValue);case 6:path=_context20.sent;if(path){_context20.next=9;break;}throw new Error("Upload returned no path.");case 9:meta=scan.device?_objectSpread(_objectSpread({},scan.measured),{},{capture:scan.device==="iphone-pro"?"lidar":"photogrammetry",device:scan.device}):scan.measured;_context20.next=12;return setup3d.onSaveModel(adminCal.styleValue,path,meta);case 12:setScan(function(p){return _objectSpread(_objectSpread({},p),{},{busy:false,step:null,status:"uploaded"});});setAdminCalMsg({ok:true,msg:"Scan saved to this style."});_context20.next=19;break;case 16:_context20.prev=16;_context20.t0=_context20["catch"](3);setScan(function(p){return _objectSpread(_objectSpread({},p),{},{busy:false,step:null,err:_context20.t0.message||"Upload failed"});});case 19:case"end":return _context20.stop();}},_callee19,null,[[3,16]]);}));return function scanUpload(){return _ref32.apply(this,arguments);};}();// Pull the stored scan back down and run it through the same read-and-measure
// path a fresh file takes — after which Generate works on it too.
var scanFetchStored=/*#__PURE__*/function(){var _ref33=_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee20(){var url,resp,blob,file;return _regeneratorRuntime().wrap(function _callee20$(_context21){while(1)switch(_context21.prev=_context21.next){case 0:if(!(!(setup3d&&setup3d.onLoadModelUrl)||scan.busy)){_context21.next=2;break;}return _context21.abrupt("return");case 2:setScan(function(p){return _objectSpread(_objectSpread({},p),{},{busy:true,err:null,step:"Fetching the stored scan…"});});_context21.prev=3;_context21.next=6;return setup3d.onLoadModelUrl(adminCal.styleValue);case 6:url=_context21.sent;if(url){_context21.next=9;break;}throw new Error("No scan is stored for this style yet.");case 9:_context21.next=11;return fetch(url);case 11:resp=_context21.sent;if(resp.ok){_context21.next=14;break;}throw new Error("Could not download the stored scan.");case 14:_context21.next=16;return resp.blob();case 16:blob=_context21.sent;file=new File([blob],"stored-scan.glb",{type:"model/gltf-binary"});_context21.next=20;return scanPick(file);case 20:_context21.next=25;break;case 22:_context21.prev=22;_context21.t0=_context21["catch"](3);setScan(function(p){return _objectSpread(_objectSpread({},p),{},{busy:false,step:null,err:_context21.t0.message||"Could not fetch the stored scan."});});case 25:case"end":return _context21.stop();}},_callee20,null,[[3,22]]);}));return function scanFetchStored(){return _ref33.apply(this,arguments);};}();var scanSetStatus=/*#__PURE__*/function(){var _ref34=_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee21(status){return _regeneratorRuntime().wrap(function _callee21$(_context22){while(1)switch(_context22.prev=_context22.next){case 0:if(setup3d&&setup3d.onSetModelStatus){_context22.next=2;break;}return _context22.abrupt("return");case 2:setScan(function(p){return _objectSpread(_objectSpread({},p),{},{busy:true,err:null});});_context22.prev=3;_context22.next=6;return setup3d.onSetModelStatus(adminCal.styleValue,status);case 6:setScan(function(p){return _objectSpread(_objectSpread({},p),{},{busy:false,status:status});});setAdminCalMsg({ok:true,msg:status==="locked"?"3D setup locked for this style.":"3D setup unlocked."});_context22.next=13;break;case 10:_context22.prev=10;_context22.t0=_context22["catch"](3);setScan(function(p){return _objectSpread(_objectSpread({},p),{},{busy:false,err:_context22.t0.message||"Could not change that."});});case 13:case"end":return _context22.stop();}},_callee21,null,[[3,10]]);}));return function scanSetStatus(_x19){return _ref34.apply(this,arguments);};}();// Both writes below have two callers with different credentials. In the portal the host
// passes `setup3d` and owns the I/O, because it holds the signed-in session — THIS
// component's supabase client is the anon one, and calling portal-settings with it would
// 401 at resolveTenant. On the public page (?admin=1) there is no session at all, so the
// operator path keeps going through admin-save-settings with the shared password.
var saveCalSpec=/*#__PURE__*/function(){var _ref35=_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee22(){var _yield$supabase$funct6,data,error;return _regeneratorRuntime().wrap(function _callee22$(_context23){while(1)switch(_context23.prev=_context23.next){case 0:if(!(setup3d&&setup3d.onSaveSpec)){_context23.next=16;break;}setAdminCalBusy(true);setAdminCalMsg(null);_context23.prev=3;_context23.next=6;return setup3d.onSaveSpec(adminCal.styleValue,adminCal.spec,adminCal.photos.filter(Boolean));case 6:setAdminCalMsg({ok:true,msg:"Saved. Customers see this on their next page load; reopen the Designer tab to refresh it here."});_context23.next=12;break;case 9:_context23.prev=9;_context23.t0=_context23["catch"](3);setAdminCalMsg({ok:false,msg:_context23.t0.message||"Save failed"});case 12:_context23.prev=12;setAdminCalBusy(false);return _context23.finish(12);case 15:return _context23.abrupt("return");case 16:if(adminPwd){_context23.next=19;break;}setAdminCalMsg({ok:false,msg:"Enter the admin password first (top row)."});return _context23.abrupt("return");case 19:setAdminCalBusy(true);setAdminCalMsg(null);_context23.prev=21;_context23.next=24;return supabase.functions.invoke("admin-save-settings",{body:{adminPassword:adminPwd,clientId:C.clientId,action:"save_style_d3",styleValue:adminCal.styleValue,d3:adminCal.spec,d3Photos:adminCal.photos.filter(Boolean)}});case 24:_yield$supabase$funct6=_context23.sent;data=_yield$supabase$funct6.data;error=_yield$supabase$funct6.error;if(!error){_context23.next=29;break;}throw new Error(error.message||"Save failed");case 29:if(!(!data||!data.ok)){_context23.next=31;break;}throw new Error(data&&data.error||"Save failed");case 31:setAdminCalMsg({ok:true,msg:"Saved — reload the page to see it live."});_context23.next=37;break;case 34:_context23.prev=34;_context23.t1=_context23["catch"](21);setAdminCalMsg({ok:false,msg:_context23.t1.message+" (Or use Copy d3 JSON and set building_styles.d3 by hand.)"});case 37:_context23.prev=37;setAdminCalBusy(false);return _context23.finish(37);case 40:case"end":return _context23.stop();}},_callee22,null,[[3,9,12,15],[21,34,37,40]]);}));return function saveCalSpec(){return _ref35.apply(this,arguments);};}();var calibrateFromPhotos=/*#__PURE__*/function(){var _ref36=_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee23(){var photos,d3,_yield$supabase$funct7,data,error;return _regeneratorRuntime().wrap(function _callee23$(_context24){while(1)switch(_context24.prev=_context24.next){case 0:photos=adminCal.photos.filter(Boolean);if(!(photos.length===0)){_context24.next=4;break;}setAdminCalMsg({ok:false,msg:"Add at least one photo first."});return _context24.abrupt("return");case 4:if(!(setup3d&&setup3d.onDraftFromPhotos)){_context24.next=24;break;}setAdminCalBusy(true);setAdminCalMsg(null);_context24.prev=7;_context24.next=10;return setup3d.onDraftFromPhotos(photos,adminCal.styleValue);case 10:d3=_context24.sent;if(d3){_context24.next=13;break;}throw new Error("The draft came back empty.");case 13:applyDraftedSpec(d3);setAdminCalMsg({ok:true,msg:"Draft read from your photos — preview it, tweak anything, then save."});_context24.next=20;break;case 17:_context24.prev=17;_context24.t0=_context24["catch"](7);setAdminCalMsg({ok:false,msg:_context24.t0.message||"Drafting failed"});case 20:_context24.prev=20;setAdminCalBusy(false);return _context24.finish(20);case 23:return _context24.abrupt("return");case 24:if(adminPwd){_context24.next=27;break;}setAdminCalMsg({ok:false,msg:"Enter the admin password first (top row)."});return _context24.abrupt("return");case 27:setAdminCalBusy(true);setAdminCalMsg(null);_context24.prev=29;_context24.next=32;return supabase.functions.invoke("calibrate-style",{body:{adminPassword:adminPwd,photoUrls:photos,styleLabel:adminCal.styleValue}});case 32:_yield$supabase$funct7=_context24.sent;data=_yield$supabase$funct7.data;error=_yield$supabase$funct7.error;if(!error){_context24.next=37;break;}throw new Error(error.message||"Calibration failed");case 37:if(!(!data||!data.ok||!data.d3)){_context24.next=39;break;}throw new Error(data&&data.error||"Calibration failed");case 39:applyDraftedSpec(data.d3);setAdminCalMsg({ok:true,msg:"Draft spec read from the photos — preview it, tweak, then save."});_context24.next=46;break;case 43:_context24.prev=43;_context24.t1=_context24["catch"](29);setAdminCalMsg({ok:false,msg:_context24.t1.message+" (On the public page this needs calibrate-style deployed; in the portal it runs through Settings.)"});case 46:_context24.prev=46;setAdminCalBusy(false);return _context24.finish(46);case 49:case"end":return _context24.stop();}},_callee23,null,[[7,17,20,23],[29,43,46,49]]);}));return function calibrateFromPhotos(){return _ref36.apply(this,arguments);};}();// ─── SUBMIT QUOTE ───
var submitQuote=/*#__PURE__*/function(){var _ref37=_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee24(){var FIELD_LABEL,missing,declinedKeys,unplacedIncluded,names,canvas,shortCode,filePath,pdfPages,shot3d,blob,_yield$supabase$stora,upErr,_supabase$storage$fro,urlData,imageUrl,_yield$supabase$rpc6,dbErr,shareParams,tenantParam,viewUrl,unitToLink,payload,betaMode,_yield$supabase$funct8,result,fnErr,detail,errBody;return _regeneratorRuntime().wrap(function _callee24$(_context25){while(1)switch(_context25.prev=_context25.next){case 0:if(!(inventoryMaster&&currentDesignIdRef.current===inventoryMaster.code)){_context25.next=3;break;}setSubmitError("This is an inventory building. Use “Send estimate” on the Inventory tab to quote it to a customer, or “Update Inventory Building” to save design changes.");return _context25.abrupt("return");case 3:// Validate every contact field that's enabled in the config. Address fields
// are required because downstream tax calc needs the full address.
FIELD_LABEL={name:"Name",email:"Email",phone:"Phone",street:"Street Address",city:"City",state:"State",zip:"Zip"};missing=["name","email","phone","street","city","state","zip"].filter(function(f){return C.contactFields.includes(f)&&!String(contact[f]||"").trim();}).map(function(f){return FIELD_LABEL[f];});if(!(missing.length>0)){_context25.next=8;break;}setSubmitError("Please fill in: ".concat(missing.join(", "),"."));return _context25.abrupt("return");case 8:if(!(C.contactFields.includes("phone")&&contact.phone.replace(/\D/g,"").length!==10)){_context25.next=11;break;}setSubmitError("Phone number must be 10 digits.");return _context25.abrupt("return");case 11:if(!(C.contactFields.includes("zip")&&!/^\d{5}$/.test(contact.zip))){_context25.next=14;break;}setSubmitError("Zip must be 5 digits.");return _context25.abrupt("return");case 14:if(!(!sel.style||!sel.size)){_context25.next=17;break;}setSubmitError("Please select a Building Style and Size.");return _context25.abrupt("return");case 17:// Every included item must be placed on the layout, or explicitly declined.
declinedKeys=Array.isArray(sel.declinedItems)?sel.declinedItems:[];unplacedIncluded=includedItemKeys.filter(function(k){return!declinedKeys.includes(k)&&!items.some(function(it){return it.type===k||it.fixtureItemId===k;});});if(!(unplacedIncluded.length>0)){_context25.next=23;break;}names=unplacedIncluded.map(function(k){return ITEMS[k]&&ITEMS[k].label||k;}).join(", ");setSubmitError("Please place all included items on your layout, or decline the ones you don't want: ".concat(names,"."));return _context25.abrupt("return");case 23:if(supabase){_context25.next=26;break;}setSubmitError("Storage isn't configured. Contact support.");return _context25.abrupt("return");case 26:setSubmitting(true);setSubmitError(null);_context25.prev=28;// 1. Render the export canvas — page 1 of the quote PDF. If the customer
//    opened the 3D view, the captured snapshot rides along as page 2 of the
//    SAME PDF (same storage path, so the {client_id}/SS-….pdf storage policy
//    and the Edge Function's attachment handling are untouched).
canvas=renderExportCanvas();// 2. Reuse the existing short_code if we loaded one; otherwise mint a fresh one
shortCode=currentDesignIdRef.current||genShortCode();// Store the PDF under a per-tenant prefix ({client_id}/<code>-<ts>.pdf). The
// timestamp suffix keeps each submitted version's PDF instead of overwriting the
// previous one (design_versions history); the storage policy allows the -<digits>.
filePath="".concat(C.clientId,"/").concat(shortCode,"-").concat(Date.now(),".pdf");// 3. Upload the PDF to the floor-plans bucket. The filename is unique per
//    submit (short_code + timestamp) so there is never a conflict — use a
//    plain insert (upsert:false), NOT an upsert. This matters for security:
//    a storage upsert's RETURNING requires a public SELECT policy, and that
//    same SELECT policy is what lets anyone list() a tenant prefix and
//    enumerate every design short_code. A plain insert needs no SELECT
//    policy, so the listable policy can be dropped (see 042_floor_plans_no_list).
//    Uses the same hand-built JPEG-in-PDF wrapper that downloadPDF uses.
pdfPages=[{bytes:dataUrlToBytes(canvas.toDataURL("image/jpeg",0.92)),w:canvas.width,h:canvas.height}];shot3d=render3DSnapshotRef.current;if(shot3d)pdfPages.push({bytes:dataUrlToBytes(shot3d.url),w:shot3d.w,h:shot3d.h});blob=buildPdfFromJpegPages(pdfPages);_context25.next=38;return supabase.storage.from("floor-plans").upload(filePath,blob,{upsert:false,contentType:"application/pdf",cacheControl:"0"});case 38:_yield$supabase$stora=_context25.sent;upErr=_yield$supabase$stora.error;if(!upErr){_context25.next=42;break;}throw new Error("PDF upload failed: ".concat(upErr.message));case 42:_supabase$storage$fro=supabase.storage.from("floor-plans").getPublicUrl(filePath),urlData=_supabase$storage$fro.data;imageUrl=urlData.publicUrl;// 4. Save the design row via the capability RPC (insert on first save,
//    update on subsequent saves; keyed by the unguessable short code).
//    The RPC also pins client_id — a saved design can never be re-homed
//    to a different tenant.
_context25.next=46;return supabase.rpc("save_design",{p_code:shortCode,p_client_id:C.clientId,p_contact:contact,p_selections:sel,p_paint_colors:paintColors,p_items:items,p_custom_options:customOptions,p_ro_dimensions:roDimensions,p_bldg_w:bldgW,p_bldg_h:bldgH,p_image_url:imageUrl});case 46:_yield$supabase$rpc6=_context25.sent;dbErr=_yield$supabase$rpc6.error;if(!dbErr){_context25.next=50;break;}throw new Error("Save failed: ".concat(dbErr.message));case 50:// 5. Update the URL so a refresh / share-link reopens the same design.
//    Keep the ?client= tenant param so the link reopens with the right branding.
//    Embedded (in-portal): the page URL is /portal.html and carries no ?client,
//    so build the share link from the config's tenant + the public root instead
//    — and never rewrite the host page's URL.
shareParams=new URLSearchParams();tenantParam=embedded?C.clientId:new URLSearchParams(window.location.search).get("client");if(tenantParam)shareParams.set("client",tenantParam);shareParams.set("id",shortCode);viewUrl="".concat(window.location.origin).concat(embedded?"/":window.location.pathname,"?").concat(shareParams.toString());if(!embedded)window.history.replaceState({},"","?".concat(shareParams.toString()));currentDesignIdRef.current=shortCode;// If this code began life as a silent draft, save_design just promoted it to 'sent'
// — from here on it is a submitted design and draft saves must leave it alone.
isDraftRef.current=false;draftStateRef.current=null;// Estimate sent from an inventory unit ("Send estimate"): tie the new design to its
// unit so the Inventory tab lists it. Best-effort — a link failure must never break
// a submitted estimate; the fire-and-forget catch keeps it silent.
// Tie this submission to its inventory building — or deliberately UNTIE it when
// staff designed a fresh build for the same customer ("Design a new build
// instead"), so the new version reads New rather than inheriting Inventory.
// newBuildMode is the ONLY thing that unties. An ordinary resubmit of a reopened
// inventory estimate (adding a discount, say) must RE-STAMP the same unit: the
// openDesign path deliberately clears inventoryUnitRef, so reading only that ref
// sent unitId:null and silently severed the quote from its building — the lock then
// survived exactly one submit and the unit never flipped Sold on acceptance.
unitToLink=newBuildMode?null:inventoryUnitRef.current||designUnit&&designUnit.id||null;if(embedded&&(unitToLink||newBuildMode&&designUnit)){try{supabase.functions.invoke("portal-settings",{body:{action:"link_design_to_unit",targetClientId:C.clientId,shortCode:shortCode,unitId:unitToLink}})["catch"](function(){});}catch(_e){/* never block the estimate on the label */}if(unitToLink)setDesignUnit(function(d){return d&&d.id===unitToLink?d:{id:unitToLink,serial:null};});else{setDesignUnit(null);setNewBuildMode(false);}}setDesignCode(shortCode);setViewingVersion(null);payload={// New fields — n8n can use these for GHL linking + image embed
designId:shortCode,imageUrl:imageUrl,viewUrl:viewUrl,source:"StructureStudio",clientId:C.clientId,deliveryFee:Number(sel.deliveryFee)||0,// Included items the customer declined → the estimate adds a deduction line per item.
declinedItems:(Array.isArray(sel.declinedItems)?sel.declinedItems:[]).filter(function(k){return includedItemKeys.includes(k);}).map(function(k){return{key:k,label:ITEMS[k]&&ITEMS[k].label||k};}),contact:{name:contact.name,email:contact.email,// Strip display formatting; n8n/GHL store raw digits.
phone:contact.phone.replace(/\D/g,""),street:contact.street,city:contact.city,state:contact.state,zip:contact.zip},selections:_objectSpread(_objectSpread(_objectSpread(_objectSpread({buildingStyle:sel.style,buildingSize:sel.size,paint:sel.paint||"No Paint"},sel.paint==="Painted"?{paintBodyColor:paintColors.body||"TBD",paintTrimColor:paintColors.trim||"TBD"}:{}),sel.wallHeight?{wallHeightFt:sel.wallHeight}:{}),Array.isArray(C.colors)&&C.colors.some(function(c){return c.shingle||c.metal;})?{roofType:sel.roofType||"",roofColor:sel.roofColor||""}:{}),sel.cladding&&D3_CLADDING[sel.cladding]?{cladding:D3_CLADDING[sel.cladding].label,claddingId:sel.cladding}:{}),floorPlanItems:items.map(function(item){var displayLabel=getDisplayLabel(item.wall,frontWall);if(item.type==="line"){var dxFt=(item.x2-item.x1)/scale;var dyFt=(item.y2-item.y1)/scale;return{type:"line",wall:null,lengthFt:Math.round(Math.sqrt(dxFt*dxFt+dyFt*dyFt)*100)/100,angleDeg:Math.round(Math.atan2(dyFt,dxFt)*180/Math.PI*10)/10};}if(item.type==="textNote"){return{type:"textNote",wall:null,text:(item.text||"").trim()};}return _objectSpread(_objectSpread(_objectSpread(_objectSpread({type:item.type,wall:displayLabel?displayLabel.toLowerCase():item.wall||null},item.type==="workbench"?{lengthFt:item.widthFt}:{}),item.type==="fixtureDoor"?{name:item.doorName,widthIn:item.widthIn,heightIn:item.heightIn,swing:item.swing,operation:item.operation,price:item.price!=null?Number(item.price):null,fixtureItemId:item.fixtureItemId||null}:{}),item.type==="ramp"?{name:item.rampName||null,widthIn:item.widthIn||null,heightIn:item.heightIn||null,price:item.price!=null?Number(item.price):null,fixtureItemId:item.fixtureItemId||null}:{}),item.type==="window"&&item.fixtureItemId?{name:item.windowName||null,widthIn:item.widthIn||null,heightIn:item.heightIn||null,price:item.price!=null?Number(item.price):null,fixtureItemId:item.fixtureItemId}:{});}),// Catalog door schedule: one row per placed fixture door, with its snapshotted spec +
// price. submit-estimate turns each into a priced estimate line. Kept separate from
// itemSummary (which counts the built-in door types) so the estimate engine has the
// full per-door detail, not just a count.
doors:items.filter(function(i){return i.type==="fixtureDoor";}).map(function(d){var lbl=getDisplayLabel(d.wall,frontWall);return{name:d.doorName||"Door",widthIn:d.widthIn!=null?Number(d.widthIn):null,heightIn:d.heightIn!=null?Number(d.heightIn):null,swing:d.swing||null,operation:d.operation||null,price:d.price!=null?Number(d.price):null,wall:lbl?lbl.toLowerCase():d.wall||null,fixtureItemId:d.fixtureItemId||null};}),// Ramp schedule: one row per placed ramp. Custom ramps carry their snapshot price; simple
// ramps leave price null and submit-estimate prices them from the tenant's ramp settings
// (each, or per_ft × the attached door width, passed here as doorWidthFt).
ramps:items.filter(function(i){return i.type==="ramp";}).map(function(r){var door=items.find(function(d){return d.id===r.snapDoorId;});var doorWidthFt=r.widthFt!=null?Number(r.widthFt):null;if(door&&door.type==="fixtureDoor"&&door.widthIn)doorWidthFt=Number(door.widthIn)/12;var lbl=getDisplayLabel(r.wall,frontWall);return{name:r.rampName||null,widthIn:r.widthIn!=null?Number(r.widthIn):null,heightIn:r.heightIn!=null?Number(r.heightIn):null,price:r.price!=null?Number(r.price):null,doorWidthFt:doorWidthFt!=null?Math.round(doorWidthFt*100)/100:null,wall:lbl?lbl.toLowerCase():r.wall||null,fixtureItemId:r.fixtureItemId||null};}),// Catalog window schedule: one row per placed catalog window (has fixtureItemId), with its
// snapshot price. Built-in windows aren't here — they're counted in itemSummary.windows.
windows:items.filter(function(i){return i.type==="window"&&i.fixtureItemId;}).map(function(w){var lbl=getDisplayLabel(w.wall,frontWall);return{name:w.windowName||"Window",widthIn:w.widthIn!=null?Number(w.widthIn):null,heightIn:w.heightIn!=null?Number(w.heightIn):null,price:w.price!=null?Number(w.price):null,wall:lbl?lbl.toLowerCase():w.wall||null,fixtureItemId:w.fixtureItemId||null};}),itemSummary:{singleDoors:items.filter(function(i){return i.type==="singleDoor";}).length,doubleDoors:items.filter(function(i){return i.type==="doubleDoor";}).length,// Built-in windows only (catalog windows are priced from windows[] by snapshot).
windows:items.filter(function(i){return i.type==="window"&&!i.fixtureItemId;}).length,workbenches:items.filter(function(i){return i.type==="workbench";}).map(function(i){var lbl=getDisplayLabel(i.wall,frontWall);return{wall:lbl?lbl.toLowerCase():i.wall,lengthFt:i.widthFt};}),lofts:items.filter(function(i){return i.type==="loft";}).length,loftSqft:Math.round(items.filter(function(i){return i.type==="loft";}).reduce(function(s,i){return s+(i.widthFt||0)*(i.heightFt||0);},0)),ramp:items.filter(function(i){return i.type==="ramp";}).length,// count — ramp is priced "each" (one per door)
lines:items.filter(function(i){return i.type==="line";}).length,notes:items.filter(function(i){return i.type==="textNote";}).map(function(n){return(n.text||"").trim();}).filter(Boolean)},customOptions:customOptions.filter(function(co){return co.name&&co.name.trim();}).map(function(co){return{name:co.name.trim(),qty:co.qty?parseInt(co.qty)||0:0,amount:co.amount?parseFloat(co.amount)||0:0};}),// Discounts → GHL invoice discount total (each shows as a $0 "Discount — <desc>" line).
discounts:(Array.isArray(sel.discounts)?sel.discounts:[]).map(function(d){return{description:String(d.description||"").trim(),amount:Math.abs(parseFloat(d.amount)||0)};}).filter(function(d){return d.amount>0;}),roughOpenings:items.filter(function(i){return i.type==="roughOpening";}).map(function(ro,idx){return{name:"RO-".concat(idx+1),dimensions:(roDimensions[ro.id]||"").trim(),qty:1};}),submittedAt:new Date().toISOString()};// Call the submit-estimate Edge Function. It looks up the GHL credentials for
// this clientId in Supabase (admin-configured), then either creates a new GHL
// estimate or updates the existing one for this design and emails it.
// ⚠️ This `betaMode` flag is TELEMETRY ONLY and does not redirect anything. It is
// detected from the deploy host (beta.* on either apex, or a beta--* branch preview),
// which nobody opts into — so it must never gain a side effect, or every submission
// from the beta host would divert (and hard-fail for tenants with no test inbox).
//
// What DOES redirect is the tenant's own beta_mode switch in Settings → Branding →
// Testing (restored 2026-08-07): with it on, submit-estimate mails that tenant's
// beta_email instead of the customer, and refuses the submission outright if no valid
// test inbox is set rather than falling back to the customer. So on a tenant WITHOUT
// that switch on, a verification submit carrying a real lead's details still emails
// that customer a live branded quote — turn beta mode on first, or use an address you
// control. An earlier QA-inbox redirect pointed at one hard-coded non-deliverable
// address so beta estimates silently failed to send; the per-tenant address plus the
// refuse-if-unset rule are what make this version safe to have back.
betaMode=typeof window!=="undefined"&&/(^|\.)beta(\.|--)/.test(window.location.hostname);_context25.next=67;return supabase.functions.invoke("submit-estimate",{body:_objectSpread(_objectSpread({},payload),{},{betaMode:betaMode})});case 67:_yield$supabase$funct8=_context25.sent;result=_yield$supabase$funct8.data;fnErr=_yield$supabase$funct8.error;if(!fnErr){_context25.next=83;break;}detail=fnErr.message||"Submit failed";_context25.prev=72;if(!(fnErr.context&&typeof fnErr.context.json==="function")){_context25.next=78;break;}_context25.next=76;return fnErr.context.json();case 76:errBody=_context25.sent;if(errBody&&errBody.error)detail=errBody.error;case 78:_context25.next=82;break;case 80:_context25.prev=80;_context25.t0=_context25["catch"](72);case 82:throw new Error(detail);case 83:if(result!==null&&result!==void 0&&result.ok){_context25.next=85;break;}throw new Error((result===null||result===void 0?void 0:result.error)||"Submit failed");case 85:// Persist the returned GHL IDs so subsequent edits update the same estimate.
if(result.contactId)ghlContactIdRef.current=result.contactId;if(result.estimateId){ghlEstimateIdRef.current=result.estimateId;setHasExistingEstimate(true);}if(result.estimateNumber)ghlEstimateNumberRef.current=result.estimateNumber;setSavedDesign({code:shortCode,viewUrl:viewUrl,imageUrl:imageUrl,estimateNumber:result.estimateNumber||null,updated:!!result.updated});setSubmitted(true);// Embedded (in-portal) mounts: tell the host page a design was submitted so it
// can refresh its lists. Fired only after the full submit-estimate success so
// estimateNumber/updated are real; purely additive — no payload change.
if(typeof onSaved==="function"){try{onSaved({code:shortCode,clientId:C.clientId,viewUrl:viewUrl,imageUrl:imageUrl,estimateNumber:result.estimateNumber||null,updated:!!result.updated});}catch(_e){}}_context25.next=98;break;case 93:_context25.prev=93;_context25.t1=_context25["catch"](28);setSubmitError(_context25.t1.message||"Something went wrong submitting your quote. Please try again.");console.error("Submit error:",_context25.t1);if(window.ssLogError)window.ssLogError("designer",_context25.t1&&_context25.t1.message||"submit failed",null,{phase:"submitQuote",stack:_context25.t1&&_context25.t1.stack?String(_context25.t1.stack).slice(0,2000):null});case 98:_context25.prev=98;setSubmitting(false);return _context25.finish(98);case 101:case"end":return _context25.stop();}},_callee24,null,[[28,93,98,101],[72,80]]);}));return function submitQuote(){return _ref37.apply(this,arguments);};}();var downloadPNG=function downloadPNG(){if(!exportUrl)return;var a=document.createElement("a");a.href=exportUrl;var nameSlug=contact.name.trim().replace(/\s+/g,"-").toLowerCase()||"customer";a.download="structurestudio-".concat(nameSlug,"-").concat(bldgW,"x").concat(bldgH,".png");a.click();};var dataUrlToBytes=function dataUrlToBytes(dataUrl){var bin=atob(dataUrl.split(",")[1]);var bytes=new Uint8Array(bin.length);for(var i=0;i<bin.length;i++)bytes[i]=bin.charCodeAt(i);return bytes;};// Build a US-Letter PDF embedding one JPEG per page ({ bytes, w, h } each),
// aspect-fit. Page 1 (the floor-plan canvas) is letter-shaped already so it
// fills the page edge-to-edge exactly as before; other aspect ratios (the 3D
// snapshot) get a small margin and center on the page. Self-contained — no
// external PDF library.
var buildPdfFromJpegPages=function buildPdfFromJpegPages(pages){var PT_W=612,PT_H=792;var enc=new TextEncoder();var chunks=[];var totalLen=0;var offsets=[];var pushStr=function pushStr(s){var b=enc.encode(s);chunks.push(b);totalLen+=b.length;};var pushBytes=function pushBytes(b){chunks.push(b);totalLen+=b.length;};// Header + binary marker so PDF readers treat the file as binary
pushStr("%PDF-1.4\n%\xC4\xE5\xF2\xE5\xEB\xA7\xF3\xA0\xD0\xC4\xC6\n");offsets[1]=totalLen;pushStr("1 0 obj\n<< /Type /Catalog /Pages 2 0 R >>\nendobj\n");offsets[2]=totalLen;var kids=pages.map(function(_,i){return"".concat(3+i*3," 0 R");}).join(" ");pushStr("2 0 obj\n<< /Type /Pages /Kids [".concat(kids,"] /Count ").concat(pages.length," >>\nendobj\n"));pages.forEach(function(pg,i){var pageObj=3+i*3,imgObj=pageObj+1,contObj=pageObj+2;// Letter-ratio images (the 2D canvas) fill the page; others get a margin.
var margin=Math.abs(pg.w/pg.h-PT_W/PT_H)<0.01?0:18;var s=Math.min((PT_W-margin*2)/pg.w,(PT_H-margin*2)/pg.h);var dw=pg.w*s,dh=pg.h*s;var dx=(PT_W-dw)/2,dy=(PT_H-dh)/2;var contentBytes=enc.encode("q ".concat(dw.toFixed(2)," 0 0 ").concat(dh.toFixed(2)," ").concat(dx.toFixed(2)," ").concat(dy.toFixed(2)," cm /Im0 Do Q\n"));offsets[pageObj]=totalLen;pushStr("".concat(pageObj," 0 obj\n<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ").concat(PT_W," ").concat(PT_H,"] /Resources << /XObject << /Im0 ").concat(imgObj," 0 R >> /ProcSet [/PDF /ImageC] >> /Contents ").concat(contObj," 0 R >>\nendobj\n"));offsets[imgObj]=totalLen;pushStr("".concat(imgObj," 0 obj\n<< /Type /XObject /Subtype /Image /Width ").concat(pg.w," /Height ").concat(pg.h," /ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ").concat(pg.bytes.length," >>\nstream\n"));pushBytes(pg.bytes);pushStr("\nendstream\nendobj\n");offsets[contObj]=totalLen;pushStr("".concat(contObj," 0 obj\n<< /Length ").concat(contentBytes.length," >>\nstream\n"));pushBytes(contentBytes);pushStr("endstream\nendobj\n");});var maxObj=2+pages.length*3;var xrefOffset=totalLen;var xref="xref\n0 ".concat(maxObj+1,"\n0000000000 65535 f \n");for(var i=1;i<=maxObj;i++)xref+=String(offsets[i]).padStart(10,"0")+" 00000 n \n";pushStr(xref);pushStr("trailer\n<< /Size ".concat(maxObj+1," /Root 1 0 R >>\nstartxref\n").concat(xrefOffset,"\n%%EOF\n"));var out=new Uint8Array(totalLen);var off=0;for(var _i20=0,_chunks=chunks;_i20<_chunks.length;_i20++){var c=_chunks[_i20];out.set(c,off);off+=c.length;}return new Blob([out],{type:"application/pdf"});};var downloadPDF=function downloadPDF(){var canvas=renderExportCanvas();var pages=[{bytes:dataUrlToBytes(canvas.toDataURL("image/jpeg",0.92)),w:canvas.width,h:canvas.height}];var shot3d=render3DSnapshotRef.current;if(shot3d)pages.push({bytes:dataUrlToBytes(shot3d.url),w:shot3d.w,h:shot3d.h});var blob=buildPdfFromJpegPages(pages);var url=URL.createObjectURL(blob);var a=document.createElement("a");a.href=url;var nameSlug=contact.name.trim().replace(/\s+/g,"-").toLowerCase()||"customer";a.download="structurestudio-".concat(nameSlug,"-").concat(bldgW,"x").concat(bldgH,".pdf");a.click();setTimeout(function(){return URL.revokeObjectURL(url);},1000);};// ─── STYLES ───
var S={sel:{border:"1px solid #CBD5E1",borderRadius:6,padding:"5px 8px",fontSize:13,fontWeight:600,background:"#FFF",minWidth:90},lbl:{fontSize:11,fontWeight:700,color:"#64748B",textTransform:"uppercase",letterSpacing:"0.06em"},btn:function btn(bg,fg){return{background:bg,color:fg,border:"none",borderRadius:6,padding:"5px 12px",fontSize:12,fontWeight:700,cursor:"pointer"};},card:function card(active){return{cursor:"pointer",borderRadius:10,overflow:"hidden",transition:"all 0.2s",border:"3px solid ".concat(active?accent:"#E2E8F0"),boxShadow:active?"0 0 0 2px ".concat(accent,", 0 4px 12px ").concat(accent,"40"):"0 2px 8px rgba(0,0,0,0.06)",transform:active?"scale(1.03)":"scale(1)"};},cardLabel:function cardLabel(active){return{padding:"6px 8px",textAlign:"center",fontWeight:700,fontSize:11,background:active?"#FFFBEB":"#FAFBFC",color:active?"#92400E":"#334155"};},check:{position:"absolute",top:4,right:4,width:20,height:20,borderRadius:99,background:accent,display:"flex",alignItems:"center",justifyContent:"center",color:"#FFF",fontSize:11,fontWeight:800},pill:function pill(active){return{padding:"8px 14px",borderRadius:8,cursor:"pointer",fontWeight:700,fontSize:13,transition:"all 0.15s",border:"2px solid ".concat(active?accent:"#E2E8F0"),background:active?"#FFFBEB":"#FAFBFC",color:active?"#92400E":"#334155",boxShadow:active?"0 0 0 2px ".concat(accent):"none"};}};// ─── PAINT FIELDS (inline, beside Roof Options) ───
// Body/Trim color pickers backed by the tenant palette (portal Colors tab).
// Moved out of renderOption so the paint option can sit beside the roof
// colors in the Size row, while other counter options keep rendering as
// pill rows below. Logic is unchanged: "Unpainted" is just the tenant's
// default palette color (owner-priced in the Colors tab) — it is NOT
// synthesized here. sel.paint ("No Paint"/"Painted") stays the
// save/load/estimate contract and is derived from the picks: the build is
// "Painted" once a chosen Body/Trim color differs from that side's default
// color (or is a custom color).
var renderPaintFields=function renderPaintFields(opt){var palette=Array.isArray(C.colors)?C.colors:[];// flex-basis 170px (not flex:1) so on a phone each color field wraps onto
// its own full-width row instead of overflowing the page horizontally.
var PAINT_LBL={display:"flex",alignItems:"center",gap:4,flex:"1 1 170px",fontSize:12,fontWeight:600,color:"#475569",minWidth:0};var PAINT_INPUT={flex:1,minWidth:0,border:"1px solid #CBD5E1",borderRadius:6,padding:"5px 8px",fontSize:12,outline:"none"};var defaultLabel=function defaultLabel(k){var d=palette.find(function(c){return(k==="body"?c.siding:c.trim)&&c.isDefault;});return d?d.label:"";};var sidePainted=function sidePainted(k,v,custom){return custom||!!v&&v!==defaultLabel(k);};var paintField=function paintField(kind){var colors=palette.filter(function(c){return kind==="body"?c.siding:c.trim;});var val=paintColors[kind]||"";var set=function set(v){return setPaintColors(function(p){return _objectSpread(_objectSpread({},p),{},_defineProperty({},kind,v));});};var labelTxt=kind==="body"?"Body:":"Trim:";var other=kind==="body"?"trim":"body";// No palette configured for this side → free-text. Any text on either side = painted.
if(colors.length===0){return/*#__PURE__*/React.createElement("label",{style:PAINT_LBL},labelTxt,/*#__PURE__*/React.createElement("input",{type:"text",value:val,onChange:function onChange(e){var v=e.target.value;set(v);setSel(function(p){return _objectSpread(_objectSpread({},p),{},_defineProperty({},opt.id,v||paintColors[other]?"Painted":"No Paint"));});},placeholder:"Enter color or leave blank",style:PAINT_INPUT}));}var match=colors.find(function(c){return c.label===val&&!c.allowCustom;});var customColor=colors.find(function(c){return c.allowCustom;});var isCustom=paintCustom[kind]||!match&&!!val&&!!customColor;var selectVal=isCustom&&customColor?customColor.label:match?match.label:"";var onSel=function onSel(label){var c=colors.find(function(x){return x.label===label;});var custom=!!(c&&c.allowCustom);if(custom){setPaintCustom(function(p){return _objectSpread(_objectSpread({},p),{},_defineProperty({},kind,true));});set("");}else{setPaintCustom(function(p){return _objectSpread(_objectSpread({},p),{},_defineProperty({},kind,false));});set(label);}// Recompute the build's paint state from both sides (a custom pick counts as painted).
var painted=sidePainted(kind,custom?"":label,custom)||sidePainted(other,paintColors[other],paintCustom[other]);setSel(function(p){return _objectSpread(_objectSpread({},p),{},_defineProperty({},opt.id,painted?"Painted":"No Paint"));});};return/*#__PURE__*/React.createElement("div",{style:_objectSpread(_objectSpread({},PAINT_LBL),{},{gap:4})},/*#__PURE__*/React.createElement("span",null,labelTxt),/*#__PURE__*/React.createElement(ColorSelect,{value:selectVal,colors:colors,onPick:onSel}),isCustom&&/*#__PURE__*/React.createElement("input",{type:"text",value:val,onChange:function onChange(e){return set(e.target.value);},placeholder:"Exact color",style:PAINT_INPUT}));};return/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:6,flexWrap:"wrap",alignItems:"center",minWidth:0}},opt.img&&/*#__PURE__*/React.createElement("div",{style:{flex:"0 0 auto",width:100,borderRadius:10,overflow:"hidden",border:"2px solid #E2E8F0"}},/*#__PURE__*/React.createElement("img",{src:opt.img,alt:opt.label,style:{width:"100%",height:80,objectFit:"cover",display:"block"}})),paintField("body"),paintField("trim"));};// ─── OPTION RENDERER ───
var renderOption=function renderOption(opt){if(opt.type==="image_cards"){return/*#__PURE__*/React.createElement("div",{key:opt.id,style:{marginBottom:14}},/*#__PURE__*/React.createElement("span",{style:_objectSpread(_objectSpread({},S.lbl),{},{display:"block",marginBottom:8})},opt.label),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:10,flexWrap:"wrap"}},opt.choices.map(function(ch){var active=sel[opt.id]===ch.value;return/*#__PURE__*/React.createElement("div",{key:ch.value,onClick:function onClick(){return setSel(function(p){return _objectSpread(_objectSpread({},p),{},_defineProperty({},opt.id,ch.value));});},style:_objectSpread(_objectSpread({},S.card(active)),{},{width:130,flex:"0 0 auto"})},ch.img?/*#__PURE__*/React.createElement("div",{style:{position:"relative"}},/*#__PURE__*/React.createElement("img",{src:ch.img,alt:ch.label,style:{width:"100%",height:85,objectFit:"cover",display:"block"}}),active&&/*#__PURE__*/React.createElement("div",{style:S.check},"\u2713")):/*#__PURE__*/React.createElement("div",{style:{height:85,background:active?"#FEF3C7":"#F1F5F9",display:"flex",alignItems:"center",justifyContent:"center",fontSize:13,fontWeight:700,color:active?"#92400E":"#64748B",position:"relative"}},ch.label.includes("None")||ch.label.includes("No ")?"None":ch.label,active&&/*#__PURE__*/React.createElement("div",{style:S.check},"\u2713")),/*#__PURE__*/React.createElement("div",{style:S.cardLabel(active)},ch.label));})));}if(opt.type==="counter"){// Paint renders inline beside Roof Options (see the Size/Roof/Paint row
// and renderPaintFields); the map below filters it out — guard anyway.
if(opt.id==="paint")return null;var hasImage=!!opt.img;return/*#__PURE__*/React.createElement("div",{key:opt.id,style:{marginBottom:14}},/*#__PURE__*/React.createElement("span",{style:_objectSpread(_objectSpread({},S.lbl),{},{display:"block",marginBottom:8})},opt.label),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:10,flexWrap:"wrap",alignItems:"flex-start"}},hasImage&&/*#__PURE__*/React.createElement("div",{style:{flex:"0 0 auto",width:100,borderRadius:10,overflow:"hidden",border:"2px solid #E2E8F0"}},/*#__PURE__*/React.createElement("img",{src:opt.img,alt:opt.label,style:{width:"100%",height:80,objectFit:"cover",display:"block"}})),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:6,flexWrap:"wrap",flex:1,alignItems:"center",minWidth:0}},opt.options.map(function(o){return/*#__PURE__*/React.createElement("div",{key:o,onClick:function onClick(){return setSel(function(p){return _objectSpread(_objectSpread({},p),{},_defineProperty({},opt.id,o));});},style:_objectSpread(_objectSpread({},S.pill(sel[opt.id]===o)),{},{flexShrink:0})},o);}))));}return null;};// ─── RENDER ───
// Lead-capture gate (name + phone), shown as a dimmed/blurred modal over the live page.
// While the modal is open the designer subtree is marked `inert` and page scroll is
// locked; the gate is portaled to <body> so it stays interactive outside that subtree.
// The gate is INTERACTION-triggered (Ahsan 2026-07-24): the page loads fully
// visible and browsable; the popup appears only when a not-yet-captured visitor
// tries to work the 2D canvas (arm a tool, place, or drag an item). gatePassed
// (remembered browsers, ?id= reopens), the operator preview (isAdmin), and
// embedded portal mounts never see it.
var gateRequired=!gatePassed&&!isAdmin&&!embedded;var _useState173=useState(false),_useState174=_slicedToArray(_useState173,2),gateOpen=_useState174[0],setGateOpen=_useState174[1];var showGate=gateRequired&&gateOpen;// Gate identity chip (public page only): who this browser is remembered as, plus a
// reset. contact.name is live right after passing the gate; the localStorage copy
// covers return visits (the gate flag alone carries no name).
var gateName=useMemo(function(){var live=(contact.name||"").trim();if(live)return live.split(/\s+/)[0];try{return(localStorage.getItem("ss_gate_name_"+(C.clientId||""))||"").trim().split(/\s+/)[0]||"";}catch(_e){return"";}},[contact.name,C.clientId]);var resetGate=function resetGate(){try{localStorage.removeItem("ss_gate_"+(C.clientId||""));localStorage.removeItem("ss_gate_name_"+(C.clientId||""));}catch(_e){}// Strip the design code (and version) from the URL — a bare reload would keep
// ?id=, which re-passes the gate and rehydrates the same contact, making the
// button a no-op on share-link reopens and post-submit pages.
var p=new URLSearchParams(window.location.search);p["delete"]("id");p["delete"]("v");window.location.replace(window.location.pathname+(p.toString()?"?"+p.toString():""));};var gateBgRef=useRef(null);useEffect(function(){var el=gateBgRef.current;if(el){if(showGate)el.setAttribute("inert","");else el.removeAttribute("inert");}document.body.style.overflow=showGate?"hidden":"";return function(){document.body.style.overflow="";};},[showGate]);var gateEl=showGate?/*#__PURE__*/React.createElement(LeadGate,{config:C,supabase:supabase,accent:accent,onClose:function onClose(){return setGateOpen(false);},onPass:function onPass(info){if(info&&(info.name||info.phone))setContact(function(p){return _objectSpread(_objectSpread({},p),{},{name:info.name||p.name,phone:info.phone||p.phone});});try{localStorage.setItem("ss_gate_"+(C.clientId||""),"1");if(info&&info.name)localStorage.setItem("ss_gate_name_"+(C.clientId||""),info.name);}catch(_e){}setGatePassed(true);setGateOpen(false);}}):null;// ── 3D Style Calibration ───────────────────────────────────────────────
// 3D Style Calibration. Two ways in: the operator panel on the public page
//     (?admin=1), and -- through the setup3d contract -- the BUILDER inside their own
//     portal, which is the point of the feature. Deliberately a sibling of the admin
//     panel rather than a child: that panel also carries the GHL credentials, which
//     must never surface inside a tenant portal.
// Held in a variable rather than written inline: Settings → Designer → 3D mounts
// this component with `calibrationOnly` and renders ONLY this panel, so the panel
// has to be reachable from two different returns.
var cal3dPanel=showCal3D&&/*#__PURE__*/React.createElement("div",{style:{background:"#FFFBEB",borderBottom:"1px solid #FCD34D",padding:"12px 20px"}},/*#__PURE__*/React.createElement("span",{style:{fontWeight:700,fontSize:13,color:"#92400E"}},"\uD83E\uDDCA 3D Style Calibration"),/*#__PURE__*/React.createElement("span",{style:{fontSize:11,color:"#92400E",marginLeft:8}},setup3d?"Pick one of your styles, add photos of a real building, tune it against the live 3D preview, then Save. This is what your customers see in 3D.":"Pick a style, paste its four-side photo URLs, tune the spec against the live preview, then Save (or Copy JSON into building_styles.d3)."),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:6,flexWrap:"wrap",margin:"8px 0"}},C.buildingStyles.map(function(s){return/*#__PURE__*/React.createElement("button",{key:s.value,onClick:function onClick(){return openCalEditor(s);},style:_objectSpread(_objectSpread({},S.btn(adminCal&&adminCal.styleValue===s.value?"#92400E":"#FFF",adminCal&&adminCal.styleValue===s.value?"#FFF":"#92400E")),{},{border:"1px solid #FCD34D"})},s.label);})),adminCal&&/*#__PURE__*/React.createElement("div",null,setup3d&&setup3d.onUploadModel&&/*#__PURE__*/React.createElement("div",{style:{border:"1px solid #FCD34D",borderRadius:8,background:"#FFF",padding:"10px 12px",marginBottom:10}},/*#__PURE__*/React.createElement("div",{style:{display:"flex",alignItems:"center",gap:8,flexWrap:"wrap"}},/*#__PURE__*/React.createElement("span",{style:{fontWeight:800,fontSize:12.5,color:"#92400E"}},"\uD83D\uDCD0 Scan of a real building"),scan.status!=="none"&&/*#__PURE__*/React.createElement("span",{style:{fontSize:11,fontWeight:800,borderRadius:5,padding:"2px 6px",background:scan.status==="locked"?"#ECFDF5":"#F1F5F9",color:scan.status==="locked"?"#047857":"#475569"}},scan.status==="locked"?"locked":scan.status),/*#__PURE__*/React.createElement("span",{style:{flex:1}}),scan.status==="locked"?/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return scanSetStatus("uploaded");},disabled:scan.busy,style:_objectSpread(_objectSpread({},S.btn("#FFF","#92400E")),{},{border:"1px solid #FCD34D",fontSize:11.5})},"Unlock"):/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return scanSetStatus("locked");},disabled:scan.busy||scan.status==="none",style:_objectSpread(_objectSpread({},S.btn("#FFF","#047857")),{},{border:"1px solid #A7F3D0",fontSize:11.5})},"Lock this 3D setup")),/*#__PURE__*/React.createElement("p",{style:{margin:"6px 0 8px",fontSize:11.5,color:"#92400E",lineHeight:1.5}},"Walk around one of your real buildings with a phone scanning app and export a ",/*#__PURE__*/React.createElement("b",null,".glb"),". We read its size and roof shape and set the 3D up to match \u2014 the scan itself is never shown to customers."),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:6,alignItems:"center",flexWrap:"wrap",marginBottom:6}},/*#__PURE__*/React.createElement("span",{style:{fontSize:11,fontWeight:700,color:"#92400E"}},"Your phone:"),[["iphone-pro","iPhone Pro (LiDAR)"],["iphone","other iPhone"],["android","Android"]].map(function(_ref38){var _ref39=_slicedToArray(_ref38,2),k=_ref39[0],lbl=_ref39[1];return/*#__PURE__*/React.createElement("button",{key:k,onClick:function onClick(){return setScan(function(p){return _objectSpread(_objectSpread({},p),{},{device:p.device===k?null:k});});},style:_objectSpread(_objectSpread({},S.btn(scan.device===k?"#92400E":"#FFF",scan.device===k?"#FFF":"#92400E")),{},{border:"1px solid #FCD34D",fontSize:11,padding:"3px 8px"})},lbl);})),scan.device&&/*#__PURE__*/React.createElement("p",{style:{margin:"0 0 8px",fontSize:11,color:"#78350F",lineHeight:1.5}},scan.device==="iphone-pro"&&/*#__PURE__*/React.createElement(React.Fragment,null,"Scaniverse (free, no account): ",/*#__PURE__*/React.createElement("b",null,"New Scan \u2192 Mesh"),", walk slowly all the way around, process on-device, then ",/*#__PURE__*/React.createElement("b",null,"Share \u2192 Export Model \u2192 GLB"),". LiDAR gives the cleanest mesh."),scan.device==="iphone"&&/*#__PURE__*/React.createElement(React.Fragment,null,"No LiDAR on this model \u2014 Scaniverse still works in photo mode: circle the building slowly in good light, expect a rougher mesh, then ",/*#__PURE__*/React.createElement("b",null,"Share \u2192 Export Model \u2192 GLB"),"."),scan.device==="android"&&/*#__PURE__*/React.createElement(React.Fragment,null,"Android phones scan by photos: use ",/*#__PURE__*/React.createElement("b",null,"Scaniverse")," (free) or ",/*#__PURE__*/React.createElement("b",null,"Kiri Engine")," (free account) \u2014 circle the building slowly in good light, then export as ",/*#__PURE__*/React.createElement("b",null,"GLB"),".")),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:8,alignItems:"center",flexWrap:"wrap"}},/*#__PURE__*/React.createElement("label",{style:_objectSpread(_objectSpread({},S.btn("#92400E","#FFF")),{},{fontSize:12,cursor:scan.busy||scan.status==="locked"?"default":"pointer",opacity:scan.status==="locked"?0.5:1,marginBottom:0})},scan.busy?scan.step||"Working…":"Choose a .glb scan",/*#__PURE__*/React.createElement("input",{type:"file",accept:".glb,model/gltf-binary",disabled:scan.busy||scan.status==="locked",onChange:function onChange(e){var f=e.target.files&&e.target.files[0];e.target.value="";scanPick(f);},style:{display:"none"}})),scan.status!=="none"&&!scan.measured&&!scan.busy&&setup3d&&setup3d.onLoadModelUrl&&/*#__PURE__*/React.createElement("button",{onClick:scanFetchStored,style:_objectSpread(_objectSpread({},S.btn("#FFF","#0E7490")),{},{border:"1px solid #A5F3FC",fontSize:12})},"Re-measure the stored scan"),scan.measured&&!scan.busy&&/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("button",{onClick:scanGenerate,disabled:adminCalPreview,title:scan.aiReady===false?"AI is off for this site — this applies the measurements only.":"Measure, render the scan from four sides, and let AI read the siding + colours.",style:_objectSpread(_objectSpread({},S.btn("#7C3AED","#FFF")),{},{fontSize:12,opacity:adminCalPreview?0.5:1})},"\u2728 Generate 3D from this scan"),/*#__PURE__*/React.createElement("button",{onClick:scanApply,style:_objectSpread(_objectSpread({},S.btn("#0E7490","#FFF")),{},{fontSize:12})},"Use these measurements"),/*#__PURE__*/React.createElement("button",{onClick:scanUpload,style:_objectSpread(_objectSpread({},S.btn("#FFF","#92400E")),{},{border:"1px solid #FCD34D",fontSize:12})},"Save the scan to this style"))),scan.err&&/*#__PURE__*/React.createElement("div",{style:{marginTop:8,fontSize:11.5,color:"#DC2626",fontWeight:600}},scan.err),scan.measured&&/*#__PURE__*/React.createElement("div",{style:{marginTop:8,fontSize:12,color:"#0F172A"}},/*#__PURE__*/React.createElement("b",null,scan.measured.widthFt," \xD7 ",scan.measured.depthFt," ft")," · ","walls ",/*#__PURE__*/React.createElement("b",null,scan.measured.eaveFt," ft")," · ","peak ",/*#__PURE__*/React.createElement("b",null,scan.measured.peakFt," ft")," · ",/*#__PURE__*/React.createElement("b",null,scan.measured.roofType)," roof, pitch ",/*#__PURE__*/React.createElement("b",null,scan.measured.pitch),scan.measured.overhangFt?/*#__PURE__*/React.createElement(React.Fragment,null,", overhang ",/*#__PURE__*/React.createElement("b",null,scan.measured.overhangFt," ft")):null,scan.measured.gambrel&&/*#__PURE__*/React.createElement("span",{style:{color:"#64748B"}}," ","(knee ",scan.measured.gambrel.kneeU," / rise ",scan.measured.gambrel.kneeRise," / ridge ",scan.measured.gambrel.ridgeRise,")"),/*#__PURE__*/React.createElement("span",{style:{color:"#64748B"}}," ","(from ",scan.measured.sampled.toLocaleString()," surface points)"),scan.measured.warn&&/*#__PURE__*/React.createElement("div",{style:{marginTop:4,color:"#B45309",fontWeight:600}},"\u26A0 ",scan.measured.warn),function(){var matches=scanSizeMatchesFor(scan.measured);if(matches.length){return/*#__PURE__*/React.createElement("div",{style:{marginTop:6,display:"flex",gap:6,alignItems:"center",flexWrap:"wrap"}},/*#__PURE__*/React.createElement("span",{style:{fontSize:11,fontWeight:700,color:"#92400E"}},"Closest sizes you sell:"),matches.map(function(s){return/*#__PURE__*/React.createElement("button",{key:s.label,onClick:function onClick(){return scanApplySize(s.label);},style:_objectSpread(_objectSpread({},S.btn("#FFF","#0E7490")),{},{border:"1px solid #A5F3FC",fontSize:11.5,padding:"3px 8px"})},s.label,s.score===0?" ✓":" (\xB1".concat(s.score," ft)"));}));}return/*#__PURE__*/React.createElement("div",{style:{marginTop:6,fontSize:11.5,color:"#B45309",fontWeight:600}},"You don't sell a ",scan.measured.widthFt,"\xD7",scan.measured.depthFt," \u2014 add it under Settings \u2192 Catalog, or pick your closest size.");}())),/*#__PURE__*/React.createElement("div",{style:{display:"grid",gap:8,gridTemplateColumns:"repeat(auto-fit, minmax(150px, 1fr))",marginBottom:8}},/*#__PURE__*/React.createElement("label",{style:{fontSize:11,color:"#92400E",fontWeight:700}},"Roof type",/*#__PURE__*/React.createElement("select",{value:adminCal.spec.roof.type,onChange:function onChange(e){return calSetRoof({type:e.target.value});},style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box"})},/*#__PURE__*/React.createElement("option",{value:"gable"},"gable"),/*#__PURE__*/React.createElement("option",{value:"shed"},"shed"),/*#__PURE__*/React.createElement("option",{value:"gambrel"},"gambrel"))),/*#__PURE__*/React.createElement("label",{style:{fontSize:11,color:"#92400E",fontWeight:700}},"Pitch (rise/run)",/*#__PURE__*/React.createElement("input",{type:"number",step:"0.05",value:adminCal.spec.roof.pitch!=null?adminCal.spec.roof.pitch:0.4,onChange:function onChange(e){return calSetRoof({pitch:parseFloat(e.target.value)||0});},style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box"})})),/*#__PURE__*/React.createElement("label",{style:{fontSize:11,color:"#92400E",fontWeight:700}},"Overhang (ft)",/*#__PURE__*/React.createElement("input",{type:"number",step:"0.05",value:adminCal.spec.roof.overhang!=null?adminCal.spec.roof.overhang:0.6,onChange:function onChange(e){return calSetRoof({overhang:parseFloat(e.target.value)||0});},style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box"})})),/*#__PURE__*/React.createElement("label",{style:{fontSize:11,color:"#92400E",fontWeight:700}},"Ridge offset (\u22120.35\u20260.35)",/*#__PURE__*/React.createElement("input",{type:"number",step:"0.05",value:adminCal.spec.roof.ridgeOffset!=null?adminCal.spec.roof.ridgeOffset:0,onChange:function onChange(e){return calSetRoof({ridgeOffset:parseFloat(e.target.value)||0});},style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box"})})),/*#__PURE__*/React.createElement("label",{style:{fontSize:11,color:"#92400E",fontWeight:700}},"Wall height (ft)",/*#__PURE__*/React.createElement("input",{type:"number",step:"0.5",value:adminCal.spec.wallHeightFt||8,onChange:function onChange(e){return calSet({wallHeightFt:parseFloat(e.target.value)||0});},style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box"})})),/*#__PURE__*/React.createElement("label",{style:{fontSize:11,color:"#92400E",fontWeight:700}},"Siding (standard look)",/*#__PURE__*/React.createElement("select",{value:adminCal.spec.siding||"",onChange:function onChange(e){return calSet({siding:e.target.value||null});},style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box"})},/*#__PURE__*/React.createElement("option",{value:""},"plain"),/*#__PURE__*/React.createElement("option",{value:"batten"},"batten (vertical)"),/*#__PURE__*/React.createElement("option",{value:"lap"},"lap (horizontal)")))),adminCal.spec.roof.type==="gambrel"&&/*#__PURE__*/React.createElement("div",{style:{display:"grid",gap:8,gridTemplateColumns:"repeat(auto-fit, minmax(150px, 1fr))",marginBottom:8}},/*#__PURE__*/React.createElement("label",{style:{fontSize:11,color:"#92400E",fontWeight:700}},"Gambrel knee position (0\u20131)",/*#__PURE__*/React.createElement("input",{type:"number",step:"0.05",value:adminCal.spec.roof.kneeU!=null?adminCal.spec.roof.kneeU:0.55,onChange:function onChange(e){return calSetRoof({kneeU:parseFloat(e.target.value)||0});},style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box"})})),/*#__PURE__*/React.createElement("label",{style:{fontSize:11,color:"#92400E",fontWeight:700}},"Knee rise (\xD7 half-span)",/*#__PURE__*/React.createElement("input",{type:"number",step:"0.05",value:adminCal.spec.roof.kneeRise!=null?adminCal.spec.roof.kneeRise:0.55,onChange:function onChange(e){return calSetRoof({kneeRise:parseFloat(e.target.value)||0});},style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box"})})),/*#__PURE__*/React.createElement("label",{style:{fontSize:11,color:"#92400E",fontWeight:700}},"Ridge rise (\xD7 half-span)",/*#__PURE__*/React.createElement("input",{type:"number",step:"0.05",value:adminCal.spec.roof.ridgeRise!=null?adminCal.spec.roof.ridgeRise:0.8,onChange:function onChange(e){return calSetRoof({ridgeRise:parseFloat(e.target.value)||0});},style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box"})}))),/*#__PURE__*/React.createElement("div",{style:{display:"grid",gap:8,gridTemplateColumns:"repeat(auto-fit, minmax(150px, 1fr))",marginBottom:8}},["body","trim","roof"].map(function(k){return/*#__PURE__*/React.createElement("label",{key:k,style:{fontSize:11,color:"#92400E",fontWeight:700,textTransform:"capitalize"}},k," color (unpainted)",/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:4,alignItems:"center"}},/*#__PURE__*/React.createElement("input",{type:"text",placeholder:"#hex or blank",value:adminCal.spec.colors[k]||"",onChange:function onChange(e){return calSetColor(k,e.target.value);},style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box"})}),/*#__PURE__*/React.createElement("span",{style:{width:22,height:22,borderRadius:4,border:"1px solid #FCD34D",background:adminCal.spec.colors[k]||"#EEE",flexShrink:0}})));})),/*#__PURE__*/React.createElement("div",{style:{display:"grid",gap:8,gridTemplateColumns:"repeat(auto-fit, minmax(220px, 1fr))",marginBottom:8}},["Front","Back","Left","Right"].map(function(side,i){return/*#__PURE__*/React.createElement("label",{key:side,style:{fontSize:11,color:"#92400E",fontWeight:700}},side," photo",setup3d?"":" URL",/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:4,alignItems:"center"}},/*#__PURE__*/React.createElement("input",{type:"text",placeholder:"https://\u2026",value:adminCal.photos[i]||"",onChange:function onChange(e){return calSetPhoto(i,e.target.value);},style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box"})}),setup3d&&setup3d.onUploadPhoto&&/*#__PURE__*/React.createElement("label",{title:"Upload a photo",style:_objectSpread(_objectSpread({},S.btn("#FFF","#92400E")),{},{border:"1px solid #FCD34D",fontSize:11,padding:"6px 8px",cursor:adminCalBusy?"wait":"pointer",flexShrink:0,marginBottom:0})},"\u2B06",/*#__PURE__*/React.createElement("input",{type:"file",accept:"image/jpeg,image/png,image/webp,image/gif",disabled:adminCalBusy,onChange:function onChange(e){var f=e.target.files&&e.target.files[0];e.target.value="";calUploadPhoto(i,f);},style:{display:"none"}})),adminCal.photos[i]?/*#__PURE__*/React.createElement("img",{src:adminCal.photos[i],alt:side,style:{width:44,height:32,objectFit:"cover",borderRadius:4,border:"1px solid #FCD34D",flexShrink:0}}):null));})),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:8,alignItems:"center",flexWrap:"wrap"}},/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return setAdminCalPreview(true);},style:_objectSpread(_objectSpread({},S.btn("#7C3AED","#FFF")),{},{padding:"8px 14px",fontSize:13})},"\uD83E\uDDCA Preview in 3D"),!setup3d&&/*#__PURE__*/React.createElement("button",{onClick:copyCalJson,style:_objectSpread(_objectSpread({},S.btn("#FFF","#92400E")),{},{border:"1px solid #FCD34D",fontSize:12})},"Copy d3 JSON"),/*#__PURE__*/React.createElement("button",{onClick:calibrateFromPhotos,disabled:adminCalBusy||scan.aiReady===false,title:scan.aiReady===false?"AI drafting isn't switched on for this site yet — tune the numbers by hand, or ask CSM Synergy to enable it.":"Read the roof, siding and colours off the photos",style:_objectSpread(_objectSpread({},S.btn(adminCalBusy||scan.aiReady===false?"#9CA3AF":"#0E7490","#FFF")),{},{fontSize:12,cursor:adminCalBusy?"wait":"pointer"})},adminCalBusy?"Working…":"✨ Draft from photos (AI)"),/*#__PURE__*/React.createElement("button",{onClick:saveCalSpec,disabled:adminCalBusy,style:_objectSpread(_objectSpread({},S.btn(adminCalBusy?"#9CA3AF":"#92400E","#FFF")),{},{padding:"8px 14px",fontSize:13,cursor:adminCalBusy?"wait":"pointer"})},adminCalBusy?"Saving…":setup3d?"Save 3D look":"Save to config")),adminCalMsg&&/*#__PURE__*/React.createElement("div",{style:{marginTop:8,fontSize:12,color:adminCalMsg.ok?"#166534":"#DC2626",fontWeight:600,wordBreak:"break-all"}},adminCalMsg.msg)));// Operator calibration preview: the current layout rendered with the
//     DRAFT spec being edited — tune numbers against the reference photos.
var cal3dPreview=showCal3D&&adminCal&&adminCalPreview&&/*#__PURE__*/React.createElement(Structure3DViewer,{bldgW:bldgW,bldgH:bldgH,items:items,itemTypes:ITEMS,styleValue:adminCal.styleValue,frontWall:frontWall,painted:false,paintBody:"",paintTrim:"",scale:scale,mgX:mgX,mgY:mgY,accent:accent,style3d:adminCal.spec,fixtures:C.fixtures,paletteKeys:Object.keys(ITEMS).filter(function(k){return ITEMS[k]&&!ITEMS[k].noPalette&&(embedded||!ITEMS[k].internalOnly);}),placeableDoors:placeableDoors,placeableWindows:placeableWindows,placeableRamps:placeableRamps,paintEnabled:false,onSnapshot:function onSnapshot(){},onClose:function onClose(){return setAdminCalPreview(false);}});// Settings → Designer → 3D. The calibration editor needs everything this component
// already computes (the style list, the resolved config, the live 3D preview), so the
// portal mounts the whole component and takes just this surface — no toolbar, no
// canvas, no estimate. Every hook above still runs, which is what keeps hook order
// identical between this return and the full designer below.
if(calibrationOnly){return/*#__PURE__*/React.createElement("div",{style:{fontFamily:"'Segoe UI', system-ui, -apple-system, sans-serif"}},showCal3D?cal3dPanel:/*#__PURE__*/React.createElement("div",{style:{fontSize:13,color:"#64748B"}},"3D isn't turned on for this account yet."),cal3dPreview);}return/*#__PURE__*/React.createElement("div",{ref:gateBgRef,style:{fontFamily:"'Segoe UI', system-ui, -apple-system, sans-serif",background:"#F8FAFC",minHeight:embedded?"100%":"100vh"}},gateEl&&createPortal(gateEl,document.body),doorPick&&createPortal(/*#__PURE__*/React.createElement(DoorPicker,{doors:placeableDoors,showPricing:!!C.showPricing,onCancel:function onCancel(){setDoorPick(null);setSwapId(null);},onPlace:placePickedDoor}),document.body),rampPick&&createPortal(/*#__PURE__*/React.createElement(RampPicker,{ramps:placeableRamps,showPricing:!!C.showPricing,onCancel:function onCancel(){setRampPick(null);setSwapId(null);},onPlace:placePickedRamp}),document.body),windowPick&&createPortal(/*#__PURE__*/React.createElement(WindowPicker,{windows:placeableWindows,showPricing:!!C.showPricing,onCancel:function onCancel(){setWindowPick(null);setSwapId(null);},onPlace:placePickedWindow}),document.body),sizeBlock&&createPortal(/*#__PURE__*/React.createElement("div",{onClick:function onClick(){return setSizeBlock(null);},style:{position:"fixed",inset:0,background:"rgba(15,23,42,0.45)",zIndex:9000,display:"flex",alignItems:"center",justifyContent:"center",padding:16}},/*#__PURE__*/React.createElement("div",{onClick:function onClick(e){return e.stopPropagation();},style:{background:"#FFF",borderRadius:14,width:"min(520px, 96vw)",maxHeight:"88vh",overflow:"auto",padding:20,boxShadow:"0 20px 60px rgba(0,0,0,0.3)"}},/*#__PURE__*/React.createElement("div",{style:{fontSize:17,fontWeight:800,color:"#1E293B",marginBottom:6}},sizeBlock.items.length===1?"One item won't fit":"".concat(sizeBlock.items.length," items won't fit")," in a ",sizeBlock.to),/*#__PURE__*/React.createElement("div",{style:{fontSize:13,color:"#64748B",lineHeight:1.5,marginBottom:14}},"Everything else would have moved across fine, but there's nowhere to put:"),/*#__PURE__*/React.createElement("ul",{style:{margin:"0 0 16px",paddingLeft:20,fontSize:13.5,color:"#1E293B",lineHeight:1.7}},sizeBlock.items.map(function(e){return/*#__PURE__*/React.createElement("li",{key:e.id},/*#__PURE__*/React.createElement("b",null,e.label));})),/*#__PURE__*/React.createElement("div",{style:{fontSize:13,color:"#64748B",lineHeight:1.5,marginBottom:16}},"Your building is still ",/*#__PURE__*/React.createElement("b",null,sizeBlock.from)," and nothing on the plan has changed. Delete ",sizeBlock.items.length===1?"it":"them"," \u2014 or make room by removing something else \u2014 then pick the size again."),/*#__PURE__*/React.createElement("div",{style:{display:"flex",justifyContent:"flex-end"}},/*#__PURE__*/React.createElement("button",{type:"button",onClick:function onClick(){return setSizeBlock(null);},style:{background:accent,color:"#FFF",border:"none",borderRadius:8,padding:"9px 18px",fontSize:13.5,fontWeight:700,cursor:"pointer",fontFamily:"inherit"}},"Got it")))),document.body),!embedded&&/*#__PURE__*/React.createElement("div",{style:{background:C.branding.headerBg||"linear-gradient(135deg, #1E293B 0%, #334155 100%)",color:"#FFF",padding:"14px 20px",display:"flex",alignItems:"center",gap:12}},C.branding.logo?/*#__PURE__*/React.createElement("img",{src:C.branding.logo,alt:C.branding.companyName||"logo",style:{width:34,height:34,borderRadius:8,objectFit:"contain",flexShrink:0,background:"rgba(255,255,255,0.12)"}}):/*#__PURE__*/React.createElement("div",{style:{width:34,height:34,background:accent,borderRadius:8,display:"flex",alignItems:"center",justifyContent:"center",fontSize:15,fontWeight:800,flexShrink:0,letterSpacing:"-0.05em",color:"#FFF"}},initials),/*#__PURE__*/React.createElement("div",{style:{flex:1}},/*#__PURE__*/React.createElement("div",{style:{fontSize:17,fontWeight:700,letterSpacing:"-0.02em"}},C.branding.companyName||"Design Studio"),/*#__PURE__*/React.createElement("div",{style:{fontSize:11,color:"#94A3B8",marginTop:1}},C.branding.tagline||"Design & Quote")),/*#__PURE__*/React.createElement("div",{style:{display:"flex",flexDirection:"column",alignItems:"flex-end",gap:4,flexShrink:0}},gatePassed&&!isAdmin&&/*#__PURE__*/React.createElement("div",{style:{display:"flex",alignItems:"center",gap:8,whiteSpace:"nowrap"}},/*#__PURE__*/React.createElement("span",{style:{fontSize:12,color:"#E2E8F0"}},gateName?"Designing as ".concat(gateName):"Welcome back"),/*#__PURE__*/React.createElement("button",{type:"button",onClick:resetGate,title:"Clear this browser's saved visitor and start fresh",style:{fontSize:11,fontWeight:700,color:"#FFF",background:"rgba(255,255,255,0.14)",border:"1px solid rgba(255,255,255,0.3)",borderRadius:8,padding:"4px 10px",cursor:"pointer"}},"Not you? Start over")),/*#__PURE__*/React.createElement("div",{style:{fontSize:10,color:"#94A3B8",whiteSpace:"nowrap"}},"Powered by Structure Studio"))),isAdmin&&/*#__PURE__*/React.createElement("div",{style:{background:"#FEF3C7",borderBottom:"2px solid #F59E0B",padding:"14px 20px"}},/*#__PURE__*/React.createElement("div",{style:{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:8}},/*#__PURE__*/React.createElement("span",{style:{fontWeight:700,fontSize:14,color:"#92400E"}},"\uD83D\uDD12 GHL Integration \u2014 Admin"),/*#__PURE__*/React.createElement("span",{style:{fontSize:11,color:"#92400E"}},"Client: ",/*#__PURE__*/React.createElement("code",null,C.clientId))),/*#__PURE__*/React.createElement("p",{style:{margin:"0 0 10px",fontSize:12,color:"#92400E"}},"Set the GHL Location ID and Private Integration Token for this client. Once saved, credentials live in Supabase and are only read server-side \u2014 they never reach customer browsers."),/*#__PURE__*/React.createElement("div",{style:{display:"grid",gap:8,gridTemplateColumns:"repeat(auto-fit, minmax(200px, 1fr))"}},/*#__PURE__*/React.createElement(PasswordInput,{value:adminPwd,onChange:function onChange(e){return setAdminPwd(e.target.value);},placeholder:"Admin password",style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box"})}),/*#__PURE__*/React.createElement("input",{type:"text",value:adminLocId,onChange:function onChange(e){return setAdminLocId(e.target.value);},placeholder:"GHL Location ID",style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box"})}),/*#__PURE__*/React.createElement(PasswordInput,{value:adminApiKey,onChange:function onChange(e){return setAdminApiKey(e.target.value);},placeholder:"GHL API Key (pit-\u2026)",style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box"})})),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:8,alignItems:"center",marginTop:10,flexWrap:"wrap"}},/*#__PURE__*/React.createElement("button",{onClick:saveAdminSettings,disabled:adminBusy,style:_objectSpread(_objectSpread({},S.btn(adminBusy?"#9CA3AF":"#92400E","#FFF")),{},{padding:"8px 18px",fontSize:13,cursor:adminBusy?"wait":"pointer"})},adminBusy?"Saving…":"Save GHL Settings"),/*#__PURE__*/React.createElement("button",{onClick:checkAdminStatus,disabled:adminBusy||!adminPwd,style:_objectSpread(_objectSpread({},S.btn("#FFF","#92400E")),{},{border:"1px solid #FCD34D",fontSize:12})},"Check status"),adminStatus&&adminStatus.configured&&/*#__PURE__*/React.createElement("span",{style:{fontSize:12,color:"#166534",fontWeight:600}},"\u2713 Configured \u2014 Loc ",adminStatus.ghlLocationIdMasked,", saved ",new Date(adminStatus.updatedAt).toLocaleString()),adminStatus&&!adminStatus.configured&&/*#__PURE__*/React.createElement("span",{style:{fontSize:12,color:"#92400E",fontWeight:600}},"Not yet configured for this client.")),adminMsg&&/*#__PURE__*/React.createElement("div",{style:{marginTop:8,fontSize:12,color:adminMsg.ok?"#166534":"#DC2626",fontWeight:600}},adminMsg.msg)),cal3dPanel,planLocked&&/*#__PURE__*/React.createElement("div",{style:{background:"#EFF6FF",borderBottom:"1px solid #BFDBFE",padding:"11px 20px",display:"flex",alignItems:"center",gap:10,flexWrap:"wrap"}},/*#__PURE__*/React.createElement("span",{style:{fontSize:13,fontWeight:700,color:"#1E3A8A"}},"\uD83D\uDD12 Inventory building",designUnit&&designUnit.serial!=null?" #".concat(designUnit.serial):""," \u2014 already built, so the plan can't be changed."),/*#__PURE__*/React.createElement("span",{style:{fontSize:12.5,color:"#1E40AF",fontWeight:600}},"Custom options, a discount and a delivery fee can still be added below."),embedded&&/*#__PURE__*/React.createElement("button",{type:"button",onClick:function onClick(){if(!window.confirm("Design a brand-new building for this customer instead?\n\nThe plan unlocks so you can change anything. Submitting saves it as another version of this quote, no longer tied to the inventory building."))return;setNewBuildMode(true);inventoryUnitRef.current=null;},style:{marginLeft:"auto",background:"#FFF",color:"#1D4ED8",border:"1.5px solid #93C5FD",borderRadius:8,padding:"7px 14px",fontSize:12.5,fontWeight:800,cursor:"pointer",whiteSpace:"nowrap"}},"Design a new build instead")),embedded&&newBuildMode&&designUnit&&/*#__PURE__*/React.createElement("div",{style:{background:"#F0FDF4",borderBottom:"1px solid #BBF7D0",padding:"10px 20px",fontSize:12.5,fontWeight:700,color:"#15803D"}},"\u270E Designing a new build for this customer \u2014 submitting saves it as another version, no longer tied to building",designUnit.serial!=null?" #".concat(designUnit.serial):"","."),embedded&&!planLocked&&!newBuildMode&&designUnit&&designUnit.lifecycle&&((_INV_RANKS$designUnit2=INV_RANKS[designUnit.lifecycle])!==null&&_INV_RANKS$designUnit2!==void 0?_INV_RANKS$designUnit2:INV_BUILT_RANK)<INV_BUILT_RANK&&/*#__PURE__*/React.createElement("div",{style:{background:"#FFFBEB",borderBottom:"1px solid #FDE68A",padding:"10px 20px",fontSize:12.5,fontWeight:700,color:"#92400E"}},"\u26A0 Building",designUnit.serial!=null?" #".concat(designUnit.serial):""," isn\u2019t built yet \u2014 what you change here is what gets built."),/*#__PURE__*/ // A real <fieldset disabled> — pointerEvents alone leaves every <select>/<input>
// in the tab order, so a keyboard user could still change Building Size, and the
// size effect wipes every item off a plan that describes a building already built.
// fieldset disables form controls including via keyboard; pointerEvents covers the
// style cards, which are clickable divs rather than controls. Both, deliberately.
React.createElement("fieldset",{disabled:planLocked||undefined,"aria-disabled":planLocked||undefined,style:_objectSpread({border:"none",margin:0,minWidth:0,background:"#FFF",borderBottom:"2px solid #E2E8F0",padding:"14px 20px"},planLocked?{pointerEvents:"none",opacity:0.62}:{})},/*#__PURE__*/React.createElement("div",{style:{marginBottom:14}},/*#__PURE__*/React.createElement("span",{style:_objectSpread(_objectSpread({},S.lbl),{},{display:"block",marginBottom:8})},"Select Your Building Style"),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:10,flexWrap:"wrap"}},C.buildingStyles.map(function(s){var active=sel.style===s.value;return/*#__PURE__*/React.createElement("div",{key:s.value,onClick:function onClick(){return setSel(function(p){return _objectSpread(_objectSpread({},p),{},{style:s.value,size:""});});},style:_objectSpread(_objectSpread({},S.card(active)),{},{flex:"1 1 120px",maxWidth:160})},/*#__PURE__*/React.createElement("div",{style:{position:"relative"}},/*#__PURE__*/React.createElement("img",{src:s.img,alt:s.label,style:{width:"100%",height:100,objectFit:"cover",display:"block"}}),active&&/*#__PURE__*/React.createElement("div",{style:S.check},"\u2713")),/*#__PURE__*/React.createElement("div",{style:S.cardLabel(active)},s.label));}))),(sizeOpts.length>0||roofTypes.length>0||claddingChoices.length>0||paintOpt)&&/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:24,flexWrap:"wrap",alignItems:"flex-start",marginBottom:14}},sizeOpts.length>0&&/*#__PURE__*/React.createElement("div",null,/*#__PURE__*/React.createElement("span",{style:_objectSpread(_objectSpread({},S.lbl),{},{display:"block",marginBottom:8})},"Building Size"),/*#__PURE__*/React.createElement("select",{value:sel.size||"",onChange:function onChange(e){return setSel(function(p){return _objectSpread(_objectSpread({},p),{},{size:e.target.value});});},style:{minWidth:160,border:"1px solid #CBD5E1",borderRadius:6,padding:"5px 8px",fontSize:12,color:sel.size?"#334155":"#94A3B8",background:"#FFF",cursor:"pointer"}},/*#__PURE__*/React.createElement("option",{value:"",disabled:true},"Select a size\u2026"),sizeOpts.map(function(s){return/*#__PURE__*/React.createElement("option",{key:s,value:s},s);}))),roofTypes.length>0&&function(){// Roof color list depends on the chosen type. Custom-color handling mirrors paint.
var roofList=roofColorsFor(sel.roofType);var rMatch=roofList.find(function(c){return c.label===sel.roofColor&&!c.allowCustom;});var rCustomColor=roofList.find(function(c){return c.allowCustom;});var rIsCustom=roofCustom||!rMatch&&!!sel.roofColor&&!!rCustomColor;var rSelectVal=rIsCustom&&rCustomColor?rCustomColor.label:rMatch?rMatch.label:"";var onRoofType=function onRoofType(type){var dflt=roofColorsFor(type).find(function(c){return c.isDefault;});setRoofCustom(false);setSel(function(p){return _objectSpread(_objectSpread({},p),{},{roofType:type,roofColor:dflt?dflt.label:""});});};var onRoofColor=function onRoofColor(label){var c=roofList.find(function(x){return x.label===label;});if(c&&c.allowCustom){setRoofCustom(true);setSel(function(p){return _objectSpread(_objectSpread({},p),{},{roofColor:""});});}else{setRoofCustom(false);setSel(function(p){return _objectSpread(_objectSpread({},p),{},{roofColor:label});});}};return/*#__PURE__*/React.createElement("div",{style:{flex:1,minWidth:240}},/*#__PURE__*/React.createElement("span",{style:_objectSpread(_objectSpread({},S.lbl),{},{display:"block",marginBottom:8})},"Roof Options"),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:10,flexWrap:"wrap",alignItems:"center"}},/*#__PURE__*/React.createElement("label",{style:{display:"flex",alignItems:"center",gap:6,fontSize:12,fontWeight:600,color:"#475569"}},"Type:",/*#__PURE__*/React.createElement("select",{value:sel.roofType||"",onChange:function onChange(e){return onRoofType(e.target.value);},style:{minWidth:130,border:"1px solid #CBD5E1",borderRadius:6,padding:"5px 8px",fontSize:12,color:sel.roofType?"#334155":"#94A3B8",background:"#FFF",cursor:"pointer"}},/*#__PURE__*/React.createElement("option",{value:""},"Select\u2026"),roofTypes.map(function(t){return/*#__PURE__*/React.createElement("option",{key:t,value:t},t);}))),/*#__PURE__*/React.createElement("div",{style:{display:"flex",alignItems:"center",gap:6,fontSize:12,fontWeight:600,color:"#475569",flex:"1 1 200px",minWidth:0}},/*#__PURE__*/React.createElement("span",null,"Color:"),sel.roofType?/*#__PURE__*/React.createElement(ColorSelect,{value:rSelectVal,colors:roofList,onPick:onRoofColor}):/*#__PURE__*/React.createElement("span",{style:{flex:1,fontSize:12,color:"#94A3B8",fontStyle:"italic",fontWeight:500}},"pick a roof type first"),rIsCustom&&sel.roofType&&/*#__PURE__*/React.createElement("input",{type:"text",value:sel.roofColor||"",onChange:function onChange(e){return setSel(function(p){return _objectSpread(_objectSpread({},p),{},{roofColor:e.target.value});});},placeholder:"Exact color",style:{flex:1,minWidth:0,border:"1px solid #CBD5E1",borderRadius:6,padding:"5px 8px",fontSize:12,outline:"none"}}))));}(),claddingChoices.length>0&&/*#__PURE__*/React.createElement("div",null,/*#__PURE__*/React.createElement("span",{style:_objectSpread(_objectSpread({},S.lbl),{},{display:"block",marginBottom:8})},"Cladding"),/*#__PURE__*/React.createElement("select",{value:sel.cladding||"",onChange:function onChange(e){return setSel(function(p){return _objectSpread(_objectSpread({},p),{},{cladding:e.target.value||""});});},style:{minWidth:160,border:"1px solid #CBD5E1",borderRadius:6,padding:"5px 8px",fontSize:12,color:sel.cladding?"#334155":"#94A3B8",background:"#FFF",cursor:"pointer"}},/*#__PURE__*/React.createElement("option",{value:""},"Builder's standard"),claddingChoices.map(function(id){return/*#__PURE__*/React.createElement("option",{key:id,value:id},D3_CLADDING[id].label);}))),paintOpt&&/*#__PURE__*/React.createElement("div",{style:{flex:1,minWidth:260}},/*#__PURE__*/React.createElement("span",{style:_objectSpread(_objectSpread({},S.lbl),{},{display:"block",marginBottom:8})},paintOpt.label),renderPaintFields(paintOpt))),visibleOptions.filter(function(o){return o!==paintOpt;}).map(function(opt){return renderOption(opt);})),unattachedLofts.length>0&&/*#__PURE__*/React.createElement("div",{style:{background:"#FEF3C7",borderBottom:"1px solid #FCD34D",padding:"10px 16px",fontSize:12,color:"#92400E"}},/*#__PURE__*/React.createElement("div",{style:{fontWeight:700,marginBottom:4}},"\u26A0\uFE0F Loft support warning \u2014 ",unattachedLofts.length," loft",unattachedLofts.length>1?"s":""," not properly supported"),/*#__PURE__*/React.createElement("div",{style:{fontWeight:500}},"Each loft must have ",/*#__PURE__*/React.createElement("b",null,"both ends")," of at least one axis (left+right OR top+bottom) resting on a wall or another loft. Adjust position or size to fix.")),reflowNote&&reflowNote.length>0&&/*#__PURE__*/React.createElement("div",{style:{background:"#EFF6FF",borderBottom:"1px solid #BFDBFE",padding:"10px 16px",fontSize:12,color:"#1E3A8A",display:"flex",gap:10,alignItems:"flex-start"}},/*#__PURE__*/React.createElement("span",{style:{flexShrink:0}},"\uD83D\uDCD0"),/*#__PURE__*/React.createElement("div",{style:{flex:1}},/*#__PURE__*/React.createElement("div",{style:{fontWeight:700,marginBottom:4}},"Your layout moved to fit the new size"),/*#__PURE__*/React.createElement("div",{style:{fontWeight:500}},reflowNote.map(function(e,i){return/*#__PURE__*/React.createElement("span",{key:e.id},i>0?"; ":"",/*#__PURE__*/React.createElement("b",null,e.label),e.kind==="movedWall"?" moved to ".concat(wallPhrase(e.to,frontWall)):" was shortened to ".concat(e.to," ft"));}),". Everything else kept its place.")),/*#__PURE__*/React.createElement("button",{type:"button",onClick:function onClick(){return setReflowNote(null);},style:{background:"none",border:"none",color:"#1E3A8A",cursor:"pointer",fontSize:15,fontWeight:800,lineHeight:1,flexShrink:0}},"\u2715")),/*#__PURE__*/React.createElement("div",{style:{background:"#FFF",borderBottom:"1px solid #E2E8F0",padding:"10px 20px",display:"flex",gap:6,flexWrap:"wrap",alignItems:"center"}},!planLocked&&function(){var btn=function btn(_ref40){var _ref41=_slicedToArray(_ref40,2),key=_ref41[0],cfg=_ref41[1];return/*#__PURE__*/React.createElement("button",{key:key,onClick:function onClick(){if(gateRequired){setGateOpen(true);return;}setActiveTool(activeTool===key?null:key);setSelectedId(null);},style:{display:"inline-flex",alignItems:"center",gap:4,padding:"5px 10px",borderRadius:7,fontSize:12,fontWeight:600,cursor:"pointer",transition:"all 0.15s",position:"relative",background:activeTool===key?cfg.color:"#F8FAFC",color:activeTool===key?"#FFF":"#334155",border:"2px solid ".concat(activeTool===key?cfg.color:"#E2E8F0")}},/*#__PURE__*/React.createElement("span",{style:{fontSize:14,display:"inline-flex",alignItems:"center"}},key==="singleDoor"||key==="doorPicker"?/*#__PURE__*/React.createElement(DoorIcon,null):key==="doubleDoor"?/*#__PURE__*/React.createElement(DoorIcon,{"double":true}):cfg.icon),cfg.label,(cfg.wallOnly||cfg.wallSnap)&&/*#__PURE__*/React.createElement("span",{style:{fontSize:9,opacity:0.7,background:activeTool===key?"rgba(255,255,255,0.25)":"#F1F5F9",borderRadius:3,padding:"1px 4px"}},"wall"));};var entries=Object.entries(ITEMS).filter(function(_ref42){var _ref43=_slicedToArray(_ref42,2),c=_ref43[1];return c&&!c.noPalette&&(embedded||!c.internalOnly);});var incl=includedItemKeys.length?entries.filter(function(_ref44){var _ref45=_slicedToArray(_ref44,1),k=_ref45[0];return includedItemKeys.includes(k);}):[];var addl=includedItemKeys.length?entries.filter(function(_ref46){var _ref47=_slicedToArray(_ref46,1),k=_ref47[0];return!includedItemKeys.includes(k);}):entries;// Decline control for an included item: X it off (a deduction line is added on the
// estimate). Declined items don't have to be placed on the layout.
var declined=Array.isArray(sel.declinedItems)?sel.declinedItems:[];var toggleDecline=function toggleDecline(key){var cur=Array.isArray(sel.declinedItems)?sel.declinedItems:[];var declining=!cur.includes(key);if(declining){// Declining removes it from the layout (like Delete) — a declined item can't be placed,
// so any already-placed instances are cleared (cascading a door's snapped ramp, like
// delSel) and the tool is deselected if active.
setItems(function(its){var removedIds=new Set(its.filter(function(it){return it.type===key||it.fixtureItemId===key;}).map(function(it){return it.id;}));return its.filter(function(it){return!(it.type===key||it.fixtureItemId===key)&&!(it.type==="ramp"&&removedIds.has(it.snapDoorId));});});setActiveTool(function(t){return t===key?null:t;});setSelectedId(null);}setSel(function(p){var c=Array.isArray(p.declinedItems)?p.declinedItems:[];return _objectSpread(_objectSpread({},p),{},{declinedItems:c.includes(key)?c.filter(function(k){return k!==key;}):[].concat(_toConsumableArray(c),[key])});});};// Included chips show the included quantity when it's more than a single unit
// (loft quantities are square footage; everything else is a count).
var withQty=function withQty(key,cfg){var q=includedItemQty[key]||1;if(q<=1)return cfg;return _objectSpread(_objectSpread({},cfg),{},{label:key==="loft"?"".concat(cfg.label," (").concat(q," sq ft)"):"".concat(cfg.label," \xD7").concat(q)});};var inclBtn=function inclBtn(_ref48){var _ref49=_slicedToArray(_ref48,2),key=_ref49[0],rawCfg=_ref49[1];var cfg=withQty(key,rawCfg);return declined.includes(key)?/*#__PURE__*/React.createElement("span",{key:key,title:"You declined this included item \u2014 it'll show as a deduction on your estimate",style:{display:"inline-flex",alignItems:"center",gap:6,padding:"5px 10px",borderRadius:7,fontSize:12,fontWeight:600,background:"#F1F5F9",color:"#94A3B8",border:"2px dashed #CBD5E1"}},/*#__PURE__*/React.createElement("span",{style:{textDecoration:"line-through"}},cfg.label),/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return toggleDecline(key);},title:"Add it back",style:{background:"transparent",border:"none",cursor:"pointer",color:"#334155",fontWeight:700,fontSize:11}},"Undo")):/*#__PURE__*/React.createElement("span",{key:key,style:{display:"inline-flex",alignItems:"center"}},btn([key,cfg]),/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return toggleDecline(key);},title:"Decline ".concat(cfg.label," (deduction)"),style:{marginLeft:2,background:"transparent",border:"none",cursor:"pointer",color:"#94A3B8",fontWeight:800,fontSize:13,lineHeight:1}},"\u2715"));};if(incl.length===0){return/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("span",{style:_objectSpread(_objectSpread({},S.lbl),{},{marginRight:4,fontSize:10})},"Place:"),addl.map(btn));}// Included items on their own row, a full-width horizontal rule, then the additional
// options below (width:100% children force line breaks inside the wrapping flex row).
return/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("div",{style:{display:"flex",flexWrap:"wrap",alignItems:"center",gap:6,width:"100%"}},/*#__PURE__*/React.createElement("span",{style:_objectSpread(_objectSpread({},S.lbl),{},{marginRight:4,fontSize:10,color:"#15803D"})},"\u2713 Included \u2014 place or decline:"),incl.map(inclBtn)),/*#__PURE__*/React.createElement("div",{style:{width:"100%",borderTop:"1px solid #CBD5E1",margin:"2px 0"}}),/*#__PURE__*/React.createElement("span",{style:_objectSpread(_objectSpread({},S.lbl),{},{marginRight:4,fontSize:10})},"Additional options:"),addl.map(btn));}(),activeTool&&/*#__PURE__*/React.createElement("span",{style:{fontSize:11,color:accent,fontWeight:600,marginLeft:6}},"\u2190 ",ITEMS[activeTool]&&ITEMS[activeTool].doorSnap?"Click near a door":"Click ".concat(ITEMS[activeTool]&&(ITEMS[activeTool].wallOnly||ITEMS[activeTool].wallSnap)?"a wall":"the layout")),/*#__PURE__*/React.createElement("div",{style:{marginLeft:"auto",display:"flex",gap:6,alignItems:"center"}},selectedId&&function(){// Swap: change a placed door/window/ramp (built-in OR catalog) to a current catalog one,
// in place. Deliberate click only — dragging/nudging never opens it. Essential for
// replacing an ARCHIVED item; hidden if there's nothing to swap to.
var si=items.find(function(i){return i.id===selectedId;});if(!si)return null;if(planLocked)return null;var isDoor=si.type==="fixtureDoor"||si.type==="singleDoor"||si.type==="doubleDoor";var isWin=si.type==="window";var isRamp=si.type==="ramp";if(!(isDoor||isWin||isRamp))return null;var pool=isDoor?placeableDoors:isWin?placeableWindows:placeableRamps;if(!pool||pool.length===0)return null;var archived=isArchivedItem(si);var openSwap=function openSwap(){setSwapId(si.id);setActiveTool(null);setToast(null);if(isDoor)setDoorPick({swap:true});else if(isWin)setWindowPick({swap:true});else setRampPick({swap:true});};return/*#__PURE__*/React.createElement(React.Fragment,null,archived&&/*#__PURE__*/React.createElement("span",{style:{fontSize:11,fontWeight:700,color:"#B45309"}},"\u26A0 Archived \u2014 swap it \u2192"),/*#__PURE__*/React.createElement("button",{onClick:openSwap,style:_objectSpread(_objectSpread({},S.btn(archived?"#FEF3C7":"#ECFEFF",archived?"#B45309":"#0891B2")),{},{border:"1px solid ".concat(archived?"#FCD34D":"#A5F0FC")})},"\u21C4 Swap"));}(),selectedId&&!planLocked&&/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("button",{onClick:rotSel,style:_objectSpread(_objectSpread({},S.btn("#EEF2FF","#4F46E5")),{},{border:"1px solid #C7D2FE"})},"\u21BB Rotate"),/*#__PURE__*/React.createElement("button",{onClick:delSel,style:_objectSpread(_objectSpread({},S.btn("#FEF2F2","#DC2626")),{},{border:"1px solid #FECACA"})},"\u2715 Delete")),!planLocked&&/*#__PURE__*/React.createElement("button",{onClick:clearAll,style:_objectSpread(_objectSpread({},S.btn("#F1F5F9","#64748B")),{},{border:"1px solid #E2E8F0"})},"Clear"),(!planLocked||dockOn)&&view3dOn&&/*#__PURE__*/React.createElement("button",{onClick:function onClick(){if(gateRequired){setGateOpen(true);return;}if(dockOn)setDock3D(function(v){return!v;});else setShow3D(true);// narrow or touch: byte-identical to before
},style:S.btn(dock3D?"#5B21B6":"#7C3AED","#FFF")},dock3D?"🧊 Hide 3D":dockOn?"🧊 3D View":has3DSnapshot?"🧊 3D ✓":"🧊 3D View"),!view3dOn&&customerFacing&&/*#__PURE__*/React.createElement("button",{disabled:true,title:"See your building in 3D \u2014 coming soon",style:_objectSpread(_objectSpread({},S.btn("#F8FAFC","#94A3B8")),{},{border:"1px dashed #CBD5E1",cursor:"default",display:"inline-flex",alignItems:"center",gap:5})},"3D",/*#__PURE__*/React.createElement("span",{style:{fontSize:9,fontWeight:800,letterSpacing:0.4,textTransform:"uppercase",background:"#75E6DA",color:"#0F4C46",borderRadius:5,padding:"1.5px 5px"}},"Coming\xA0soon")),/*#__PURE__*/React.createElement("button",{onClick:exportPNG,style:S.btn("#059669","#FFF")},"\uD83D\uDCF7 Export"))),pendingRemoval&&function(){var prCfg=ITEMS[pendingRemoval.type];// Row label first: catalog rows' keys (fx:<id> …) are not ITEMS keys, and a raw
// key is not a user-facing name (audit 2026-08-20).
var prLbl=pendingRemoval.label||prCfg&&prCfg.label||pendingRemoval.type;return/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("div",{style:{position:"fixed",inset:0,background:"rgba(15,23,42,0.45)",zIndex:900}}),/*#__PURE__*/React.createElement("div",{style:{position:"fixed",top:14,left:"50%",transform:"translateX(-50%)",zIndex:902,background:"#1E293B",color:"#FFF",borderRadius:10,padding:"10px 16px",display:"flex",alignItems:"center",gap:12,boxShadow:"0 8px 30px rgba(0,0,0,0.35)",maxWidth:"92vw",boxSizing:"border-box"}},/*#__PURE__*/React.createElement("span",{style:{fontSize:13,fontWeight:600}},"Removing one ",prLbl," \u2014 click a highlighted item on the plan."),/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return setPendingRemoval(null);},style:{background:"rgba(255,255,255,0.12)",color:"#FFF",border:"1px solid rgba(255,255,255,0.35)",borderRadius:6,padding:"4px 12px",fontSize:12,fontWeight:700,cursor:"pointer",flexShrink:0}},"Cancel")));}(),!(show3D||adminCalPreview)&&/*#__PURE__*/React.createElement("div",{ref:canvasRowRef,style:{display:"flex",justifyContent:dock3D?"flex-start":"center",alignItems:"flex-start",gap:dock3D?12:0,padding:"16px 20px",background:"#F1F5F9",cursor:activeTool?"crosshair":dragging?"grabbing":"default"}},/*#__PURE__*/React.createElement("div",{style:{flex:"1 1 auto",minWidth:0,display:"flex",justifyContent:"center"}},/*#__PURE__*/React.createElement("svg",{ref:svgRef,viewBox:"".concat(frame.x," ").concat(frame.y," ").concat(frame.w," ").concat(frame.h),style:{width:"100%",maxWidth:dispMaxW,height:"auto",background:"#FFF",borderRadius:12,boxShadow:pendingRemoval?"0 0 0 3px #F59E0B, 0 4px 24px rgba(0,0,0,0.35)":"0 4px 24px rgba(0,0,0,0.08)",border:"1px solid #E2E8F0",userSelect:"none",position:"relative",zIndex:pendingRemoval?901:"auto"},onClick:handleClick},/*#__PURE__*/React.createElement("rect",{x:0,y:0,width:cW,height:TEXT_BAND_TOP,fill:"#FFF"}),/*#__PURE__*/React.createElement("rect",{x:mgX,y:mgY,width:pW,height:pH,fill:"#FAFBFD"}),Array.from({length:Math.floor(bldgW)+1},function(_,i){return/*#__PURE__*/React.createElement("line",{key:"gx".concat(i),x1:mgX+i*scale,y1:mgY,x2:mgX+i*scale,y2:mgY+pH,stroke:"#E8ECF1",strokeWidth:0.5});}),Array.from({length:Math.floor(bldgH)+1},function(_,i){return/*#__PURE__*/React.createElement("line",{key:"gy".concat(i),x1:mgX,y1:mgY+i*scale,x2:mgX+pW,y2:mgY+i*scale,stroke:"#E8ECF1",strokeWidth:0.5});}),/*#__PURE__*/React.createElement("rect",{x:mgX,y:mgY,width:pW,height:pH,fill:"none",stroke:"#1E293B",strokeWidth:WALL_THICKNESS}),_toConsumableArray(items).sort(function(a,b){return(a.type==="ramp"?0:1)-(b.type==="ramp"?0:1);}).map(function(item){var cfg=ITEMS[item.type];if(!cfg)return null;var isSel=item.id===selectedId;// ─── Line: rendered as a free-angle segment with two endpoint handles ───
if(cfg.lineType){var midX=(item.x1+item.x2)/2,midY=(item.y1+item.y2)/2;return/*#__PURE__*/React.createElement("g",{key:item.id,style:{cursor:activeTool?"crosshair":"grab"}},/*#__PURE__*/React.createElement("line",{x1:item.x1,y1:item.y1,x2:item.x2,y2:item.y2,stroke:"transparent",strokeWidth:14,strokeLinecap:"round",onMouseDown:function onMouseDown(e){return onPtrDown(e,item);},onTouchStart:function onTouchStart(e){return onPtrDown(e,item);}}),/*#__PURE__*/React.createElement("line",{x1:item.x1,y1:item.y1,x2:item.x2,y2:item.y2,stroke:cfg.color,strokeWidth:isSel?3:2.5,strokeLinecap:"round",pointerEvents:"none"}),isSel&&/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("line",{x1:item.x1,y1:item.y1,x2:item.x2,y2:item.y2,stroke:"#3B82F6",strokeWidth:1,strokeDasharray:"4 3",pointerEvents:"none"}),/*#__PURE__*/React.createElement("circle",{cx:item.x1,cy:item.y1,r:7,fill:"#FFF",stroke:"#3B82F6",strokeWidth:2,style:{cursor:"move"},onMouseDown:function onMouseDown(e){e.stopPropagation();startResize(e,item,"ep1");},onTouchStart:function onTouchStart(e){e.stopPropagation();startResize(e,item,"ep1");}}),/*#__PURE__*/React.createElement("circle",{cx:item.x2,cy:item.y2,r:7,fill:"#FFF",stroke:"#3B82F6",strokeWidth:2,style:{cursor:"move"},onMouseDown:function onMouseDown(e){e.stopPropagation();startResize(e,item,"ep2");},onTouchStart:function onTouchStart(e){e.stopPropagation();startResize(e,item,"ep2");}})),isSel&&resizing&&resizing.id===item.id&&function(){var lenFt=Math.sqrt(Math.pow((item.x2-item.x1)/scale,2)+Math.pow((item.y2-item.y1)/scale,2));return/*#__PURE__*/React.createElement("g",{transform:"translate(".concat(midX,",").concat(midY-14,")"),pointerEvents:"none"},/*#__PURE__*/React.createElement("rect",{x:-26,y:-11,width:52,height:20,rx:5,fill:"#1E293B"}),/*#__PURE__*/React.createElement("text",{x:0,y:3,textAnchor:"middle",fill:"#FFF",fontSize:11,fontWeight:"700"},lenFt.toFixed(1)," ft"));}());}// ─── Text Note: resizable box; text is edited IN PLACE on the canvas ───
if(cfg.noteType){var w=item.widthPx||160;var h=item.heightPx||40;var isEditing=editingNoteId===item.id;// Leader target in coords relative to this g's translate.
var lt=item.leader?{x:item.leader.x-item.x,y:item.leader.y-item.y}:null;return/*#__PURE__*/React.createElement("g",{key:item.id,transform:"translate(".concat(item.x,",").concat(item.y,")"),onMouseDown:function onMouseDown(e){if(isEditing)return;onPtrDown(e,item);},onTouchStart:function onTouchStart(e){if(isEditing)return;onPtrDown(e,item);},style:{cursor:isEditing?"text":activeTool?"crosshair":"grab"}},lt&&function(){var ep=noteEdgePoint(0,0,w,h,lt.x,lt.y);var dx=lt.x-ep.x,dy=lt.y-ep.y;if(Math.sqrt(dx*dx+dy*dy)<=10)return null;return/*#__PURE__*/React.createElement("g",{pointerEvents:"none"},/*#__PURE__*/React.createElement("line",{x1:ep.x,y1:ep.y,x2:lt.x,y2:lt.y,stroke:cfg.color,strokeWidth:1.5,strokeDasharray:"5 4"}),/*#__PURE__*/React.createElement("circle",{cx:lt.x,cy:lt.y,r:3.5,fill:cfg.color}));}(),isSel&&/*#__PURE__*/React.createElement("rect",{x:-w/2-4,y:-h/2-4,width:w+8,height:h+8,fill:"none",stroke:"#3B82F6",strokeWidth:2,strokeDasharray:"4 2",rx:6}),/*#__PURE__*/React.createElement("rect",{x:-w/2,y:-h/2,width:w,height:h,fill:"#FFFBEB",stroke:cfg.color,strokeWidth:1.25,rx:4}),/*#__PURE__*/React.createElement("foreignObject",{x:-w/2,y:-h/2,width:w,height:h},isEditing?/*#__PURE__*/React.createElement("div",{xmlns:"http://www.w3.org/1999/xhtml",key:"edit"+item.id,contentEditable:true,suppressContentEditableWarning:true,ref:function ref(el){if(el&&el.dataset.init!=="1"){el.dataset.init="1";el.textContent=item.text||"";setTimeout(function(){el.focus();// Select-all so typing replaces the "Note" placeholder.
try{var s=window.getSelection();var rg=document.createRange();rg.selectNodeContents(el);s.removeAllRanges();s.addRange(rg);}catch(_e){/* selection APIs unavailable */}},0);}},onMouseDown:function onMouseDown(e){return e.stopPropagation();},onTouchStart:function onTouchStart(e){return e.stopPropagation();},onClick:function onClick(e){return e.stopPropagation();},onPaste:function onPaste(e){// Paste as PLAIN text: rich-HTML pastes would render their own
// markup mid-edit, and textContent drops element boundaries so
// multi-line pastes would silently concatenate without spaces.
e.preventDefault();var t=(e.clipboardData&&e.clipboardData.getData("text/plain")||"").replace(/\s+/g," ");document.execCommand("insertText",false,t);},onInput:function onInput(e){var v=e.currentTarget.textContent;setItems(function(p){return p.map(function(i){return i.id===item.id?_objectSpread(_objectSpread({},i),{},{text:v}):i;});});},onKeyDown:function onKeyDown(e){e.stopPropagation();if(e.key==="Enter"||e.key==="Escape"){e.preventDefault();e.currentTarget.blur();}},onBlur:function onBlur(e){var v=(e.currentTarget.textContent||"").trim();setItems(function(p){return p.map(function(i){return i.id===item.id?_objectSpread(_objectSpread({},i),{},{text:v}):i;});});setEditingNoteId(null);},style:{width:"100%",height:"100%",padding:"4px 8px",boxSizing:"border-box",font:"600 12px sans-serif",color:cfg.color,lineHeight:1.3,textAlign:"center",wordWrap:"break-word",overflowWrap:"break-word",overflow:"hidden",whiteSpace:"pre-wrap",outline:"none",cursor:"text",userSelect:"text",WebkitUserSelect:"text"}}):/*#__PURE__*/React.createElement("div",{xmlns:"http://www.w3.org/1999/xhtml",key:"view"+item.id,style:{width:"100%",height:"100%",padding:"4px 8px",boxSizing:"border-box",font:"600 12px sans-serif",color:cfg.color,lineHeight:1.3,textAlign:"center",wordWrap:"break-word",overflowWrap:"break-word",overflow:"hidden",display:"flex",alignItems:"center",justifyContent:"center"}},item.text||"Note")),isSel&&/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("rect",{x:w/2-14,y:h/2-14,width:28,height:28,fill:"transparent",style:{cursor:"nwse-resize"},onClick:function onClick(e){return e.stopPropagation();},onMouseDown:function onMouseDown(e){e.stopPropagation();startResize(e,item,"br");},onTouchStart:function onTouchStart(e){e.stopPropagation();startResize(e,item,"br");}}),/*#__PURE__*/React.createElement("rect",{x:w/2-7,y:h/2-7,width:14,height:14,fill:"#3B82F6",stroke:"#FFF",strokeWidth:1.5,rx:2,pointerEvents:"none"}),/*#__PURE__*/React.createElement("g",{transform:"translate(".concat(w/2+2,",").concat(-h/2-2,")"),style:{cursor:"pointer"},onMouseDown:function onMouseDown(e){return e.stopPropagation();},onTouchStart:function onTouchStart(e){return e.stopPropagation();},onClick:function onClick(e){e.stopPropagation();delSel();}},/*#__PURE__*/React.createElement("circle",{r:9,fill:"#DC2626",stroke:"#FFF",strokeWidth:1.5}),/*#__PURE__*/React.createElement("line",{x1:-3.5,y1:-3.5,x2:3.5,y2:3.5,stroke:"#FFF",strokeWidth:1.8,strokeLinecap:"round"}),/*#__PURE__*/React.createElement("line",{x1:-3.5,y1:3.5,x2:3.5,y2:-3.5,stroke:"#FFF",strokeWidth:1.8,strokeLinecap:"round"})),!isEditing&&function(){var hx=lt?lt.x:-w/2-18,hy=lt?lt.y:0;return/*#__PURE__*/React.createElement("g",null,!lt&&/*#__PURE__*/React.createElement("line",{x1:-w/2,y1:0,x2:hx+7,y2:hy,stroke:"#94A3B8",strokeWidth:1,strokeDasharray:"2 3",pointerEvents:"none"}),/*#__PURE__*/React.createElement("circle",{cx:hx,cy:hy,r:7,fill:"#FFF",stroke:"#3B82F6",strokeWidth:2,style:{cursor:"move"},onClick:function onClick(e){return e.stopPropagation();},onMouseDown:function onMouseDown(e){e.stopPropagation();startResize(e,item,"leader");},onTouchStart:function onTouchStart(e){e.stopPropagation();startResize(e,item,"leader");}},/*#__PURE__*/React.createElement("title",null,"Drag to point this note at something \xB7 drop back on the note to remove")));}()));}var itemW=item.widthFt||cfg.width;var itemH=item.heightFt||cfg.height;var iw=itemW*scale;var ih=itemH*scale;var isWB=item.type==="workbench";return/*#__PURE__*/React.createElement("g",{key:item.id,transform:"translate(".concat(item.x,",").concat(item.y,") rotate(").concat(item.rotation,")"),onMouseDown:function onMouseDown(e){return onPtrDown(e,item);},onTouchStart:function onTouchStart(e){return onPtrDown(e,item);},style:{cursor:activeTool?"crosshair":"grab"}},isSel&&/*#__PURE__*/React.createElement("rect",{x:-iw/2-4,y:(cfg.wallOnly?-8:-ih/2)-4,width:iw+8,height:(cfg.wallOnly?16:ih)+8,fill:"none",stroke:"#3B82F6",strokeWidth:2,strokeDasharray:"4 2",rx:3}),isArchivedItem(item)&&/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("rect",{x:-iw/2-3,y:(cfg.wallOnly?-8:-ih/2)-3,width:iw+6,height:(cfg.wallOnly?16:ih)+6,fill:"none",stroke:"#F59E0B",strokeWidth:2,strokeDasharray:"2 2",rx:3}),/*#__PURE__*/React.createElement("text",{x:0,y:cfg.wallOnly?item.wall==="north"||item.wall==="east"?27:-23:-ih/2-6,textAnchor:"middle",fontSize:9,fontWeight:"800",fill:"#B45309"},"\u26A0 archived")),item.type==="loft"?/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("defs",null,/*#__PURE__*/React.createElement("clipPath",{id:"loftClip".concat(item.id)},/*#__PURE__*/React.createElement("rect",{x:-iw/2,y:-ih/2,width:iw,height:ih,rx:2}))),/*#__PURE__*/React.createElement("rect",{x:-iw/2,y:-ih/2,width:iw,height:ih,fill:cfg.color+"18",stroke:cfg.color,strokeWidth:2,strokeDasharray:"6 4",rx:2}),/*#__PURE__*/React.createElement("g",{opacity:0.15,clipPath:"url(#loftClip".concat(item.id,")")},Array.from({length:Math.ceil((iw+ih)/10)+2},function(_,d){return/*#__PURE__*/React.createElement("line",{key:d,x1:-iw/2+d*10,y1:-ih/2,x2:-iw/2+d*10-ih,y2:ih/2,stroke:cfg.color,strokeWidth:1});})),/*#__PURE__*/React.createElement("text",{x:0,y:4,textAnchor:"middle",fill:cfg.color,fontSize:10,fontWeight:"700"},"LOFT"),/*#__PURE__*/React.createElement("text",{x:0,y:16,textAnchor:"middle",fill:cfg.color,fontSize:9,opacity:0.7},itemW,"\xD7",itemH," ft"),isSel&&function(){var hz=Math.min(Math.max(ih/3,22),36,ih*0.5);var vz=Math.min(Math.max(iw/3,22),36,iw*0.5);return/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("rect",{x:-vz/2,y:-ih/2-1,width:vz,height:hz/2+1,fill:"transparent",style:{cursor:"ns-resize"},onMouseDown:function onMouseDown(e){e.stopPropagation();startResize(e,item,"top");},onTouchStart:function onTouchStart(e){e.stopPropagation();startResize(e,item,"top");}}),/*#__PURE__*/React.createElement("rect",{x:-vz/2,y:ih/2-hz/2,width:vz,height:hz/2+1,fill:"transparent",style:{cursor:"ns-resize"},onMouseDown:function onMouseDown(e){e.stopPropagation();startResize(e,item,"bottom");},onTouchStart:function onTouchStart(e){e.stopPropagation();startResize(e,item,"bottom");}}),/*#__PURE__*/React.createElement("rect",{x:-iw/2-1,y:-hz/2,width:vz/2+1,height:hz,fill:"transparent",style:{cursor:"ew-resize"},onMouseDown:function onMouseDown(e){e.stopPropagation();startResize(e,item,"left");},onTouchStart:function onTouchStart(e){e.stopPropagation();startResize(e,item,"left");}}),/*#__PURE__*/React.createElement("rect",{x:iw/2-vz/2,y:-hz/2,width:vz/2+1,height:hz,fill:"transparent",style:{cursor:"ew-resize"},onMouseDown:function onMouseDown(e){e.stopPropagation();startResize(e,item,"right");},onTouchStart:function onTouchStart(e){e.stopPropagation();startResize(e,item,"right");}}),/*#__PURE__*/React.createElement("line",{x1:-vz/3,y1:-ih/2,x2:vz/3,y2:-ih/2,stroke:"#3B82F6",strokeWidth:3,strokeLinecap:"round",pointerEvents:"none"}),/*#__PURE__*/React.createElement("line",{x1:-vz/3,y1:ih/2,x2:vz/3,y2:ih/2,stroke:"#3B82F6",strokeWidth:3,strokeLinecap:"round",pointerEvents:"none"}),/*#__PURE__*/React.createElement("line",{x1:-iw/2,y1:-hz/3,x2:-iw/2,y2:hz/3,stroke:"#3B82F6",strokeWidth:3,strokeLinecap:"round",pointerEvents:"none"}),/*#__PURE__*/React.createElement("line",{x1:iw/2,y1:-hz/3,x2:iw/2,y2:hz/3,stroke:"#3B82F6",strokeWidth:3,strokeLinecap:"round",pointerEvents:"none"}));}()):cfg.wallOnly?/*#__PURE__*/React.createElement(React.Fragment,null,item.type==="roughOpening"?/*#__PURE__*/React.createElement("rect",{x:-iw/2,y:-5,width:iw,height:10,fill:"#FFFFFF",stroke:"#000000",strokeWidth:1.5,rx:1}):/*#__PURE__*/React.createElement("rect",{x:-iw/2,y:-5,width:iw,height:10,fill:item.type==="fixtureDoor"?fixtureDoorColor(item):cfg.color,rx:1}),item.type==="singleDoor"&&function(){var r=iw*0.8,out=item.wall==="north"||item.wall==="east";return/*#__PURE__*/React.createElement("path",{d:"M ".concat(-iw/2+r," 0 A ").concat(r," ").concat(r," 0 0 ").concat(out?0:1," ").concat(-iw/2," ").concat(out?-r:r),fill:"none",stroke:cfg.color+"60",strokeWidth:1.5,strokeDasharray:"4 3"});}(),item.type==="doubleDoor"&&function(){var r=iw*0.4,out=item.wall==="north"||item.wall==="east";var s=out?-1:1;return/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("path",{d:"M ".concat(-iw/2+r," 0 A ").concat(r," ").concat(r," 0 0 ").concat(out?0:1," ").concat(-iw/2," ").concat(s*r),fill:"none",stroke:cfg.color+"60",strokeWidth:1.5,strokeDasharray:"4 3"}),/*#__PURE__*/React.createElement("path",{d:"M ".concat(iw/2-r," 0 A ").concat(r," ").concat(r," 0 0 ").concat(out?1:0," ").concat(iw/2," ").concat(s*r),fill:"none",stroke:cfg.color+"60",strokeWidth:1.5,strokeDasharray:"4 3"}),/*#__PURE__*/React.createElement("line",{x1:0,y1:-5,x2:0,y2:5,stroke:"#FFF",strokeWidth:1.5}));}(),item.type==="fixtureDoor"&&fixtureDoorSVG(item,iw,fixtureDoorColor(item)),item.type==="window"&&/*#__PURE__*/React.createElement("g",null,/*#__PURE__*/React.createElement("line",{x1:0,y1:-4,x2:0,y2:4,stroke:"#FFF",strokeWidth:1.5}),/*#__PURE__*/React.createElement("line",{x1:-iw/4,y1:-4,x2:-iw/4,y2:4,stroke:"#FFF",strokeWidth:1}),/*#__PURE__*/React.createElement("line",{x1:iw/4,y1:-4,x2:iw/4,y2:4,stroke:"#FFF",strokeWidth:1})),/*#__PURE__*/React.createElement("text",{x:0,y:item.wall==="north"||item.wall==="east"?14:-10,textAnchor:"middle",fill:"#1E293B",fontSize:9,fontWeight:"700"},function(){if(item.type==="roughOpening"){var idx=items.filter(function(i){return i.type==="roughOpening";}).findIndex(function(r){return r.id===item.id;});return"RO-".concat(idx+1);}var base=(item.type==="fixtureDoor"||item.type==="window")&&item.planLabel?item.planLabel:cfg.shortLabel;// Doors + windows prefix their width, e.g. "6' DD", so the size reads off the plan.
var isDoorOrWin=item.type==="singleDoor"||item.type==="doubleDoor"||item.type==="fixtureDoor"||item.type==="window";var w=isDoorOrWin?fmtFtIn((item.widthFt||cfg.width)*12):"";return w?"".concat(w," ").concat(base):base;}()),item.type==="roughOpening"&&isSel&&function(){var cursor=item.wall==="north"||item.wall==="south"?"ew-resize":"ns-resize";var endZoneW=Math.min(Math.max(iw/5,10),22,iw*0.4);return/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("rect",{x:-iw/2+1,y:-4,width:endZoneW-2,height:8,fill:"#3B82F640",stroke:"#3B82F680",strokeWidth:1,pointerEvents:"none",rx:1}),/*#__PURE__*/React.createElement("rect",{x:iw/2-endZoneW+1,y:-4,width:endZoneW-2,height:8,fill:"#3B82F640",stroke:"#3B82F680",strokeWidth:1,pointerEvents:"none",rx:1}),/*#__PURE__*/React.createElement("rect",{x:-iw/2-4,y:-9,width:endZoneW+4,height:18,fill:"transparent",style:{cursor:cursor},onMouseDown:function onMouseDown(e){e.stopPropagation();startResize(e,item,"min");},onTouchStart:function onTouchStart(e){e.stopPropagation();startResize(e,item,"min");}}),/*#__PURE__*/React.createElement("rect",{x:iw/2-endZoneW,y:-9,width:endZoneW+4,height:18,fill:"transparent",style:{cursor:cursor},onMouseDown:function onMouseDown(e){e.stopPropagation();startResize(e,item,"max");},onTouchStart:function onTouchStart(e){e.stopPropagation();startResize(e,item,"max");}}));}()):/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("rect",{x:-iw/2,y:-ih/2,width:iw,height:ih,fill:cfg.color+(item.type==="ramp"?"12":"30"),stroke:cfg.color+(item.type==="ramp"?"80":"FF"),strokeWidth:item.type==="ramp"?1.5:2,rx:2}),item.type==="ramp"?/*#__PURE__*/React.createElement("text",{x:-iw/2+5,y:4,textAnchor:"start",fill:cfg.color,fontSize:9,fontWeight:"700"},item.planLabel||"RAMP"):isWB?/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("text",{x:0,y:0,textAnchor:"middle",fill:cfg.color,fontSize:11,fontWeight:"700"},itemW," ft"),/*#__PURE__*/React.createElement("text",{x:0,y:13,textAnchor:"middle",fill:cfg.color,fontSize:8,opacity:0.7},"Workbench")):/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("text",{x:0,y:2,textAnchor:"middle",fill:cfg.color,fontSize:10,fontWeight:"700"},cfg.shortLabel),/*#__PURE__*/React.createElement("text",{x:0,y:14,textAnchor:"middle",fill:cfg.color,fontSize:8,opacity:0.7},itemW,"\xD7",itemH)),isWB&&isSel&&function(){var isHoriz=item.wall==="north"||item.wall==="south";var cursor=isHoriz?"ew-resize":"ns-resize";var endZoneW=Math.min(Math.max(iw/4,16),30,iw*0.45);var handleX1=-iw/2+endZoneW/2;var handleX2=iw/2-endZoneW/2;return/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("rect",{x:-iw/2+1,y:-ih/2+1,width:endZoneW-2,height:ih-2,fill:"#3B82F618",stroke:"#3B82F680",strokeWidth:1.5,pointerEvents:"none",rx:2}),/*#__PURE__*/React.createElement("rect",{x:iw/2-endZoneW+1,y:-ih/2+1,width:endZoneW-2,height:ih-2,fill:"#3B82F618",stroke:"#3B82F680",strokeWidth:1.5,pointerEvents:"none",rx:2}),/*#__PURE__*/React.createElement("text",{x:handleX1,y:5,textAnchor:"middle",fill:"#3B82F6",fontSize:14,fontWeight:"700",pointerEvents:"none"},"\u25C4"),/*#__PURE__*/React.createElement("text",{x:handleX2,y:5,textAnchor:"middle",fill:"#3B82F6",fontSize:14,fontWeight:"700",pointerEvents:"none"},"\u25BA"),/*#__PURE__*/React.createElement("rect",{x:-iw/2,y:-ih/2,width:endZoneW,height:ih,fill:"transparent",style:{cursor:cursor},onMouseDown:function onMouseDown(e){e.stopPropagation();startResize(e,item,"min");},onTouchStart:function onTouchStart(e){e.stopPropagation();startResize(e,item,"min");}}),/*#__PURE__*/React.createElement("rect",{x:iw/2-endZoneW,y:-ih/2,width:endZoneW,height:ih,fill:"transparent",style:{cursor:cursor},onMouseDown:function onMouseDown(e){e.stopPropagation();startResize(e,item,"max");},onTouchStart:function onTouchStart(e){e.stopPropagation();startResize(e,item,"max");}}));}()));}),/*#__PURE__*/React.createElement("text",{x:mgX+pW/2,y:mgY-16,textAnchor:"middle",fill:"#475569",fontSize:13,fontWeight:"bold"},bldgW," ft"),/*#__PURE__*/React.createElement("text",{x:mgX+pW/2,y:mgY+pH+26,textAnchor:"middle",fill:"#475569",fontSize:13,fontWeight:"bold"},bldgW," ft"),/*#__PURE__*/React.createElement("text",{x:mgX-20,y:mgY+pH/2,textAnchor:"middle",fill:"#475569",fontSize:13,fontWeight:"bold",transform:"rotate(-90,".concat(mgX-20,",").concat(mgY+pH/2,")")},bldgH," ft"),/*#__PURE__*/React.createElement("text",{x:mgX+pW+24,y:mgY+pH/2,textAnchor:"middle",fill:"#475569",fontSize:13,fontWeight:"bold",transform:"rotate(90,".concat(mgX+pW+24,",").concat(mgY+pH/2,")")},bldgH," ft"),frontWall&&/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("text",{x:mgX+pW/2,y:mgY-32,textAnchor:"middle",fill:"#94A3B8",fontSize:10,fontWeight:"600",letterSpacing:"0.1em"},getDisplayLabel("north",frontWall)),/*#__PURE__*/React.createElement("text",{x:mgX+pW/2,y:mgY+pH+42,textAnchor:"middle",fill:"#94A3B8",fontSize:10,fontWeight:"600",letterSpacing:"0.1em"},getDisplayLabel("south",frontWall)),/*#__PURE__*/React.createElement("text",{x:mgX-38,y:mgY+pH/2,textAnchor:"middle",fill:"#94A3B8",fontSize:10,fontWeight:"600",letterSpacing:"0.1em",transform:"rotate(-90,".concat(mgX-38,",").concat(mgY+pH/2,")")},getDisplayLabel("west",frontWall)),/*#__PURE__*/React.createElement("text",{x:mgX+pW+42,y:mgY+pH/2,textAnchor:"middle",fill:"#94A3B8",fontSize:10,fontWeight:"600",letterSpacing:"0.1em",transform:"rotate(90,".concat(mgX+pW+42,",").concat(mgY+pH/2,")")},getDisplayLabel("east",frontWall))),resizing&&function(){var ri=items.find(function(i){return i.id===resizing.id;});if(!ri||ri.type==="line"||!Number.isFinite(ri.widthFt))return null;// line shows its own length inline; notes have no widthFt → skip the 'ft' badge (audit #F3)
return/*#__PURE__*/React.createElement("g",{transform:"translate(".concat(ri.x,",").concat(ri.y-28,")")},/*#__PURE__*/React.createElement("rect",{x:-30,y:-12,width:60,height:24,rx:6,fill:"#1E293B"}),/*#__PURE__*/React.createElement("text",{x:0,y:4,textAnchor:"middle",fill:"#FFF",fontSize:13,fontWeight:"700"},Math.round(ri.widthFt*10)/10," ft"));}(),pendingRemoval&&items.filter(priceRowMatcher(pendingRemoval.type)).map(function(it){var c=ITEMS[it.type];if(!c)return null;var iwFt=it.widthFt||c.width,ihFt=it.heightFt||c.height;var iw=iwFt*scale,ih=ihFt*scale;var rot=it.rotation===90||it.rotation===270;var hw=(rot?ih:iw)/2,hh=(rot?iw:ih)/2;return/*#__PURE__*/React.createElement("rect",{key:"pr-".concat(it.id),x:it.x-hw-5,y:it.y-hh-5,width:hw*2+10,height:hh*2+10,rx:5,fill:"rgba(220,38,38,0.10)",stroke:"#DC2626",strokeWidth:2.5,style:{cursor:"pointer"},onMouseDown:function onMouseDown(e){return e.stopPropagation();},onTouchStart:function onTouchStart(e){return e.stopPropagation();},onClick:function onClick(e){e.stopPropagation();setItems(function(p){return p.filter(function(x){return x.id!==it.id&&!(x.type==="ramp"&&x.snapDoorId===it.id);});});setPendingRemoval(null);setSelectedId(null);}},/*#__PURE__*/React.createElement("animate",{attributeName:"stroke-opacity",values:"1;0.25;1",dur:"1.1s",repeatCount:"indefinite"}));}))),view3dOn&&dock3D&&/*#__PURE__*/ /* Width and height are pure CSS — never derived from `frame` or dispMaxW,
             which recompute on every render and would realloc the drawing buffer at
             mousemove rate. sticky (not fixed) because the designer host is the
             scroller; fixed would escape it and float over the whole portal. */React.createElement("div",{style:{flex:"0 0 clamp(320px, 36%, 520px)",height:"min(620px, 68vh)",position:"sticky",top:12}},/*#__PURE__*/React.createElement(Structure3DPanel/* Remount on building size and NOTHING else: every camera, shadow and
                 orbit constant in the panel bakes from bldgW/bldgH at mount. Size
                 comes from a <select>, so this is rare and correct. Style, cladding,
                 roof and paint all rebuild in place with no context churn. */,{key:"".concat(bldgW,"x").concat(bldgH),bldgW:bldgW,bldgH:bldgH,items:items,itemTypes:ITEMS,painted:sel.paint==="Painted",paintBody:paintColors.body,paintTrim:paintColors.trim,frontWall:frontWall,scale:scale,mgX:mgX,mgY:mgY,style3d:d3ResolveStyleSpec(selectedStyle,sel.style,C.wallHeightFt,d3SidingOverride(C,sel),sel.wallHeight),roofType:sel.roofType,roofColorHex:function(){var rc=(Array.isArray(C.colors)?C.colors:[]).find(function(c){return c.label===sel.roofColor&&(sel.roofType==="Metal"?c.metal:c.shingle);});return rc&&rc.hex?rc.hex:"";}(),fixtures:C.fixtures,suspended:Boolean(dragging||resizing),canEdit:!planLocked,onEdit:function onEdit(){setDock3D(false);setShow3D(true);},onClose:function onClose(){return setDock3D(false);}}))),!submitted&&/*#__PURE__*/React.createElement("div",{style:{background:"#FFF",borderTop:"2px solid #E2E8F0",padding:"14px 20px"}},/*#__PURE__*/React.createElement("span",{style:_objectSpread(_objectSpread({},S.lbl),{},{display:"block",marginBottom:10,fontSize:12})},"Customer Information"),/*#__PURE__*/React.createElement("div",{style:{display:"flex",flexWrap:"wrap",gap:10,marginBottom:10}},C.contactFields.includes("name")&&/*#__PURE__*/React.createElement("div",{style:{flex:"1 1 180px"}},/*#__PURE__*/React.createElement("span",{style:_objectSpread(_objectSpread({},S.lbl),{},{fontSize:10,display:"block",marginBottom:3})},"Name *"),/*#__PURE__*/React.createElement("input",{type:"text",value:contact.name,onChange:function onChange(e){return setContact(function(p){return _objectSpread(_objectSpread({},p),{},{name:e.target.value});});},placeholder:"Full Name",style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box"})})),C.contactFields.includes("email")&&/*#__PURE__*/React.createElement("div",{style:{flex:"1 1 200px"}},/*#__PURE__*/React.createElement("span",{style:_objectSpread(_objectSpread({},S.lbl),{},{fontSize:10,display:"block",marginBottom:3})},"Email *"),/*#__PURE__*/React.createElement("input",{type:"email",value:contact.email,onChange:function onChange(e){return setContact(function(p){return _objectSpread(_objectSpread({},p),{},{email:e.target.value});});},placeholder:"email@example.com",style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box"})})),C.contactFields.includes("phone")&&/*#__PURE__*/React.createElement("div",{style:{flex:"1 1 140px"}},/*#__PURE__*/React.createElement("span",{style:_objectSpread(_objectSpread({},S.lbl),{},{fontSize:10,display:"block",marginBottom:3})},"Phone *"),/*#__PURE__*/React.createElement("input",{type:"tel",inputMode:"tel",autoComplete:"tel",value:formatPhoneDisplay(contact.phone),onChange:function onChange(e){return setContact(function(p){return _objectSpread(_objectSpread({},p),{},{phone:formatPhoneDisplay(e.target.value)});});},placeholder:"(555) 555-5555",style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box"})}))),(C.googleMapsApiKey||DEFAULT_GOOGLE_MAPS_API_KEY)&&C.contactFields.includes("street")&&/*#__PURE__*/React.createElement("div",{style:{display:"flex",alignItems:"center",gap:10,marginBottom:10}},/*#__PURE__*/React.createElement("span",{style:_objectSpread(_objectSpread({},S.lbl),{},{fontSize:10,whiteSpace:"nowrap"})},"Search for address"),/*#__PURE__*/React.createElement("div",{ref:attachStreetAutocomplete,style:{flex:1,minWidth:0,display:"flex",alignItems:"stretch",boxSizing:"border-box"}}),function(){// Open Google Maps directly to the address typed in the fields below. Built from the
// customer's own street/city/state/zip; disabled until at least one is filled.
var addr=[contact.street,contact.city,contact.state,contact.zip].map(function(s){return(s||"").trim();}).filter(Boolean).join(", ");return/*#__PURE__*/React.createElement("button",{type:"button",disabled:!addr,onClick:function onClick(){if(addr)window.open("https://www.google.com/maps/search/?api=1&query=".concat(encodeURIComponent(addr)),"_blank","noopener,noreferrer");},title:addr?"Open this address in Google Maps":"Enter an address below first",style:_objectSpread(_objectSpread({},S.btn("#EEF2FF","#4F46E5")),{},{border:"1px solid #C7D2FE",flexShrink:0,whiteSpace:"nowrap",display:"inline-flex",alignItems:"center",gap:4,opacity:addr?1:0.5,cursor:addr?"pointer":"not-allowed"})},"\uD83D\uDCCD View Property");}()),(C.contactFields.includes("street")||C.contactFields.includes("city"))&&/*#__PURE__*/React.createElement("div",{style:{display:"flex",flexWrap:"wrap",gap:10}},C.contactFields.includes("street")&&/*#__PURE__*/React.createElement("div",{style:{flex:"2 1 200px"}},/*#__PURE__*/React.createElement("span",{style:_objectSpread(_objectSpread({},S.lbl),{},{fontSize:10,display:"block",marginBottom:3})},"Street Address *"),/*#__PURE__*/React.createElement("input",{type:"text",autoComplete:"street-address",value:contact.street,onChange:function onChange(e){return setContact(function(p){return _objectSpread(_objectSpread({},p),{},{street:e.target.value});});},placeholder:"123 Main St",style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box"})})),C.contactFields.includes("city")&&/*#__PURE__*/React.createElement("div",{style:{flex:"1 1 130px"}},/*#__PURE__*/React.createElement("span",{style:_objectSpread(_objectSpread({},S.lbl),{},{fontSize:10,display:"block",marginBottom:3})},"City *"),/*#__PURE__*/React.createElement("input",{type:"text",autoComplete:"address-level2",value:contact.city,onChange:function onChange(e){return setContact(function(p){return _objectSpread(_objectSpread({},p),{},{city:e.target.value});});},placeholder:"City",style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box"})})),C.contactFields.includes("state")&&/*#__PURE__*/React.createElement("div",{style:{flex:"1 1 160px"}},/*#__PURE__*/React.createElement("span",{style:_objectSpread(_objectSpread({},S.lbl),{},{fontSize:10,display:"block",marginBottom:3})},"State *"),/*#__PURE__*/React.createElement("select",{autoComplete:"address-level1",value:contact.state,onChange:function onChange(e){return setContact(function(p){return _objectSpread(_objectSpread({},p),{},{state:e.target.value});});},style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box",color:contact.state?undefined:"#94A3B8"})},/*#__PURE__*/React.createElement("option",{value:""},"Select state\u2026"),["Alabama","Alaska","Arizona","Arkansas","California","Colorado","Connecticut","Delaware","District of Columbia","Florida","Georgia","Hawaii","Idaho","Illinois","Indiana","Iowa","Kansas","Kentucky","Louisiana","Maine","Maryland","Massachusetts","Michigan","Minnesota","Mississippi","Missouri","Montana","Nebraska","Nevada","New Hampshire","New Jersey","New Mexico","New York","North Carolina","North Dakota","Ohio","Oklahoma","Oregon","Pennsylvania","Rhode Island","South Carolina","South Dakota","Tennessee","Texas","Utah","Vermont","Virginia","Washington","West Virginia","Wisconsin","Wyoming"].map(function(s){return/*#__PURE__*/React.createElement("option",{key:s,value:s,style:{color:"#1E293B"}},s);}))),C.contactFields.includes("zip")&&/*#__PURE__*/React.createElement("div",{style:{flex:"0 1 100px"}},/*#__PURE__*/React.createElement("span",{style:_objectSpread(_objectSpread({},S.lbl),{},{fontSize:10,display:"block",marginBottom:3})},"Zip *"),/*#__PURE__*/React.createElement("input",{type:"text",inputMode:"numeric",autoComplete:"postal-code",value:contact.zip,onChange:function onChange(e){return setContact(function(p){return _objectSpread(_objectSpread({},p),{},{zip:e.target.value.replace(/\D/g,"").slice(0,5)});});},placeholder:"00000",maxLength:5,style:_objectSpread(_objectSpread({},S.sel),{},{width:"100%",boxSizing:"border-box"})})))),!submitted&&/*#__PURE__*/React.createElement("div",{style:{background:"#FFF",borderTop:"2px solid #E2E8F0",padding:"14px 20px"}},/*#__PURE__*/React.createElement("div",{onClick:function onClick(){if(!detailsLocked)setAdditionalOpen(function(o){return!o;});},style:_objectSpread({display:"flex",alignItems:"center",justifyContent:"space-between",gap:10,cursor:detailsLocked?"default":"pointer",userSelect:"none"},customerFacing?{// Unlocked = a SOLID accent bar with the submit button's shadow — it is the
// page's "see your price" call to action and reads like one. Locked stays
// quiet: the contact form is the customer's current job, not this bar.
background:detailsLocked?"#F8FAFC":accent,border:"1.5px solid ".concat(detailsLocked?"#E2E8F0":accent),borderRadius:10,padding:"14px 18px",boxShadow:detailsLocked?"none":"0 4px 14px ".concat(accent,"50"),transition:"all 0.2s"}:{})},/*#__PURE__*/React.createElement("span",{style:{fontSize:customerFacing?14.5:12,fontWeight:customerFacing?800:700,color:customerFacing&&!detailsLocked?textOnAccent(accent):"#64748B",letterSpacing:0.2}},"Details"),detailsLocked?/*#__PURE__*/React.createElement("span",{style:{fontSize:customerFacing?12.5:11.5,fontWeight:600,color:customerFacing?"#64748B":"#94A3B8",textAlign:"right"}},"\uD83D\uDD12 Enter all your contact information to see the quote details."):customerFacing?/*#__PURE__*/React.createElement("span",{style:{fontSize:13,fontWeight:800,color:textOnAccent(accent),textAlign:"right"}},additionalOpen?"Hide quote details ▾":"See your quote details ▸"):/*#__PURE__*/React.createElement("span",{style:{fontSize:11,color:"#94A3B8"}},additionalOpen?"▾":"▸")),additionalOpen&&!detailsLocked&&function(){// ── Invoice-style detail rows ─────────────────────────────────────────
// Every row shares the same right-anchored grid: [qty 50px] [amount 85px]
// [action 28px], gap 6 — so every amount lines up in one column. Rows with
// no action get a 28px spacer; rows with no qty just omit that cell.
var selRows=computeSelectionRows(sel,paintColors,C,items);var priceRows=C.showPricing?computeLayoutPricingRows(items,sel,customOptions,C,paintColors).rows:[];var roList=items.filter(function(i){return i.type==="roughOpening";});// Rough-opening rate: same per-style resolution as the estimate (layoutPricing,
// byStyle override wins) — the old C.layoutPrices read was a stale key that
// showed $0.00 while the estimate charged the real rate.
var roRate=function(){var lp=C.layoutPricing&&C.layoutPricing.roughOpening;if(!lp)return 0;var ov=lp.byStyle&&sel.style?lp.byStyle[sel.style]:null;return Number(ov&&ov.rate!=null?ov.rate:lp.rate)||0;}();var customTotal=customOptions.reduce(function(s,r){if(!r||!r.name||!String(r.name).trim())return s;var amt=Math.max(0,parseFloat(r.amount)||0);var q=r.qty?Math.abs(parseInt(r.qty,10))||1:1;// abs: the edge bills |qty|
return s+amt*q;},0);var discountTotal=(sel.discounts||[]).reduce(function(s,r){return s+Math.max(0,parseFloat(r&&r.amount)||0);},0);var deliveryAmt=parseFloat(sel.deliveryFee)||0;var showDelivery=deliveryOpen||String(sel.deliveryFee||"")!=="";// Mirrors the estimate's pre-tax total: all line items + delivery − discounts.
var subtotal=Math.max(0,selRows.reduce(function(s,r){return s+(Number(r.total)||0);},0)+priceRows.reduce(function(s,r){return s+(Number(r.total)||0);},0)+(C.showPricing?roList.length*roRate:0)+customTotal+deliveryAmt-discountTotal);var qtyCell={width:50,flex:"0 0 auto",textAlign:"center",fontSize:12,color:"#64748B",border:"1px solid #E2E8F0",borderRadius:6,padding:"6px 0",background:"#F8FAFC",boxSizing:"border-box"};var amtCell={width:85,flex:"0 0 auto",textAlign:"right",fontSize:12,fontWeight:600,color:"#334155",border:"1px solid #E2E8F0",borderRadius:6,padding:"6px 8px",background:"#F8FAFC",boxSizing:"border-box"};var amtInputWrap={display:"flex",alignItems:"center",border:"1px solid #CBD5E1",borderRadius:6,padding:"0 6px",background:"#FFF",width:85,flex:"0 0 auto",boxSizing:"border-box"};var actSpacer={width:28,flex:"0 0 auto"};var delBtn={background:"#FEF2F2",color:"#DC2626",border:"1px solid #FECACA",borderRadius:6,width:28,height:30,cursor:"pointer",fontSize:14,fontWeight:700,flexShrink:0};var dashBtn={background:"#F1F5F9",color:"#334155",border:"1px dashed #94A3B8",borderRadius:6,padding:"6px 12px",fontSize:12,fontWeight:600,cursor:"pointer"};return/*#__PURE__*/React.createElement("div",{style:{marginTop:8}},/*#__PURE__*/React.createElement("div",{style:{marginBottom:4}},selRows.map(function(r){return/*#__PURE__*/React.createElement("div",{key:r.key,style:{display:"flex",gap:6,alignItems:"center",marginBottom:6}},/*#__PURE__*/React.createElement("div",{style:{flex:1,minWidth:0}},/*#__PURE__*/React.createElement("div",{style:{fontSize:12,fontWeight:700,color:"#334155"}},r.label),/*#__PURE__*/React.createElement("div",{style:{fontSize:10.5,color:"#94A3B8",whiteSpace:"pre-line"}},r.detail)),r.total!=null&&/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("div",{style:amtCell},fmtMoney2(r.total)),/*#__PURE__*/React.createElement("div",{style:actSpacer})));})),priceRows.length>0&&/*#__PURE__*/React.createElement("div",{style:{marginTop:14}},/*#__PURE__*/React.createElement("div",{style:_objectSpread(_objectSpread({},S.lbl),{},{marginBottom:8})},"Options on your plan"),priceRows.map(function(r){return/*#__PURE__*/React.createElement("div",{key:r.key,style:{display:"flex",gap:6,alignItems:"center",marginBottom:6}},/*#__PURE__*/React.createElement("div",{style:{flex:1,minWidth:0}},/*#__PURE__*/React.createElement("div",{style:{fontSize:12,fontWeight:700,color:"#334155"}},r.label),/*#__PURE__*/React.createElement("div",{style:{fontSize:10.5,color:"#94A3B8"}},r.unit)),/*#__PURE__*/React.createElement("div",{style:qtyCell},Number.isInteger(r.qty)?r.qty:Number(r.qty).toFixed(1)),/*#__PURE__*/React.createElement("div",{style:amtCell},fmtMoney2(r.total)),!planLocked&&/*#__PURE__*/React.createElement("button",{title:r.method==="each"?"Remove one from the plan":"Remove from the plan",onClick:function onClick(){// "each"-priced items step down one at a time (when several are
// placed, the plan asks which one); everything else clears the line
// and removes everything on it from the layout. priceRowMatcher, not
// `i.type === r.key`: catalog rows carry fx:/win:/ramp: keys that
// match no item's type, so this × removed nothing for exactly the
// rows that carry a price (audit 2026-08-20). The row label rides
// along so pick mode can name the item (its key is not ITEMS-keyed).
var match=priceRowMatcher(r.key);var placed=items.filter(match);if(r.method==="each"&&placed.length>1){setPendingRemoval({type:r.key,label:r.label});setSelectedId(null);setActiveTool(null);setTimeout(function(){try{svgRef.current&&svgRef.current.scrollIntoView({behavior:"smooth",block:"center"});}catch(_){}},0);}else{// Cascade like delSel: removing a door also removes its snapped ramp.
var removedIds=new Set(placed.map(function(i){return i.id;}));setItems(function(p){return p.filter(function(i){return!match(i)&&!(i.type==="ramp"&&removedIds.has(i.snapDoorId));});});setSelectedId(null);}},style:delBtn},"\xD7"));})),roList.length>0&&/*#__PURE__*/React.createElement("div",{style:{marginTop:14}},roList.map(function(ro,idx){var dim=roDimensions[ro.id]||"";var invalid=!dim.trim();return/*#__PURE__*/React.createElement("div",{key:ro.id,style:{display:"flex",gap:6,alignItems:"center",marginBottom:6}},/*#__PURE__*/React.createElement("span",{style:{flex:"0 0 auto",fontSize:12,fontWeight:700,color:"#334155",minWidth:60}},"RO-",idx+1),/*#__PURE__*/React.createElement("input",{type:"text",value:dim,placeholder:"Enter Rough Opening size: e.g. 3 x 6 or 29\u215E \xD7 34\xBD\"",readOnly:planLocked||undefined,onChange:function onChange(e){if(planLocked)return;setRoDimensions(function(p){return _objectSpread(_objectSpread({},p),{},_defineProperty({},ro.id,e.target.value));});},style:{flex:1,minWidth:0,border:"1px solid ".concat(invalid?"#DC2626":"#CBD5E1"),borderRadius:6,padding:"6px 8px",fontSize:12,outline:"none",background:invalid?"#FEF2F2":"#FFF"}}),C.showPricing&&/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("div",{style:qtyCell},"1"),/*#__PURE__*/React.createElement("div",{style:amtCell},fmtMoney2(roRate))),!planLocked&&/*#__PURE__*/React.createElement("button",{title:"Remove this rough opening from the plan",onClick:function onClick(){setItems(function(p){return p.filter(function(i){return i.id!==ro.id;});});setSelectedId(null);},style:delBtn},"\xD7"));})),customOptions.length>0&&/*#__PURE__*/React.createElement("div",{style:{marginTop:14}},customOptions.map(function(row,idx){var invalid=!row.name||!row.name.trim();return/*#__PURE__*/React.createElement("div",{key:idx,style:{display:"flex",gap:6,alignItems:"center",marginBottom:6}},/*#__PURE__*/React.createElement("input",{type:"text",value:row.name,placeholder:"Item name (required)",onChange:function onChange(e){return setCustomOptions(function(p){return p.map(function(r,i){return i===idx?_objectSpread(_objectSpread({},r),{},{name:e.target.value}):r;});});},style:{flex:1,minWidth:0,border:"1px solid ".concat(invalid?"#DC2626":"#CBD5E1"),borderRadius:6,padding:"6px 8px",fontSize:12,outline:"none",background:invalid?"#FEF2F2":"#FFF",wordBreak:"break-word"}}),/*#__PURE__*/React.createElement("input",{type:"number",min:"0",value:row.qty,placeholder:"Qty",onChange:function onChange(e){var v=e.target.value.replace(/[^0-9]/g,"");setCustomOptions(function(p){return p.map(function(r,i){return i===idx?_objectSpread(_objectSpread({},r),{},{qty:v}):r;});});},style:{width:50,flex:"0 0 auto",border:"1px solid #CBD5E1",borderRadius:6,padding:"6px 4px",fontSize:12,outline:"none",textAlign:"center",boxSizing:"border-box"}}),/*#__PURE__*/React.createElement("div",{style:amtInputWrap},/*#__PURE__*/React.createElement("span",{style:{fontSize:12,color:"#64748B",marginRight:2,flexShrink:0}},"$"),/*#__PURE__*/React.createElement("input",{type:"number",min:"0",value:row.amount,placeholder:"0.00",onChange:function onChange(e){return setCustomOptions(function(p){return p.map(function(r,i){return i===idx?_objectSpread(_objectSpread({},r),{},{amount:e.target.value.replace(/[^0-9.]/g,"")}):r;});});},style:{flex:1,minWidth:0,width:"100%",border:"none",padding:"6px 0",fontSize:12,outline:"none"}})),/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return setCustomOptions(function(p){return p.filter(function(_,i){return i!==idx;});});},style:delBtn},"\xD7"));})),(sel.discounts||[]).length>0&&/*#__PURE__*/React.createElement("div",{style:{marginTop:14}},(sel.discounts||[]).map(function(row,idx){return/*#__PURE__*/React.createElement("div",{key:idx,style:{display:"flex",gap:6,alignItems:"center",marginBottom:6}},embedded?/*#__PURE__*/React.createElement("input",{type:"text",value:row.description||"",placeholder:"Discount description",onChange:function onChange(e){return setSel(function(p){return _objectSpread(_objectSpread({},p),{},{discounts:(p.discounts||[]).map(function(r,i){return i===idx?_objectSpread(_objectSpread({},r),{},{description:e.target.value}):r;})});});},style:{flex:1,minWidth:0,border:"1px solid #CBD5E1",borderRadius:6,padding:"6px 8px",fontSize:12,outline:"none",background:"#FFF",wordBreak:"break-word"}}):/*#__PURE__*/React.createElement("div",{style:{flex:1,minWidth:0,fontSize:12,fontWeight:700,color:"#334155",padding:"6px 0",wordBreak:"break-word"}},row.description||"Discount"),embedded?/*#__PURE__*/React.createElement("div",{style:amtInputWrap},/*#__PURE__*/React.createElement("span",{style:{fontSize:12,color:"#64748B",marginRight:2,flexShrink:0,whiteSpace:"nowrap"}},"\u2212$"),/*#__PURE__*/React.createElement("input",{type:"number",min:"0",value:row.amount||"",placeholder:"0.00",onChange:function onChange(e){var v=e.target.value.replace(/[^0-9.]/g,"");setSel(function(p){return _objectSpread(_objectSpread({},p),{},{discounts:(p.discounts||[]).map(function(r,i){return i===idx?_objectSpread(_objectSpread({},r),{},{amount:v}):r;})});});},style:{flex:1,minWidth:0,width:"100%",border:"none",padding:"6px 0",fontSize:12,outline:"none"}})):/*#__PURE__*/React.createElement("div",{style:{width:85,textAlign:"right",fontSize:12,fontWeight:700,color:"#059669",flexShrink:0}},"\u2212$",Number(row.amount||0).toFixed(2)),embedded?/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return setSel(function(p){return _objectSpread(_objectSpread({},p),{},{discounts:(p.discounts||[]).filter(function(_,i){return i!==idx;})});});},style:delBtn},"\xD7"):/*#__PURE__*/React.createElement("span",{style:{width:28,flexShrink:0}}));})),showDelivery&&/*#__PURE__*/React.createElement("div",{style:{marginTop:14,display:"flex",gap:6,alignItems:"center",marginBottom:6}},/*#__PURE__*/React.createElement("div",{style:{flex:1,minWidth:0}},/*#__PURE__*/React.createElement("div",{style:{fontSize:12,fontWeight:700,color:"#334155"}},"Delivery Fee"),/*#__PURE__*/React.createElement("div",{style:{fontSize:10.5,color:"#94A3B8"}},"Non-taxable line on the estimate")),embedded?/*#__PURE__*/React.createElement("div",{style:amtInputWrap},/*#__PURE__*/React.createElement("span",{style:{fontSize:12,color:"#64748B",marginRight:2,flexShrink:0}},"$"),/*#__PURE__*/React.createElement("input",{type:"text",inputMode:"decimal",value:sel.deliveryFee||"",placeholder:"0.00",onChange:function onChange(e){var v=e.target.value.replace(/[^0-9.]/g,"");setSel(function(p){return _objectSpread(_objectSpread({},p),{},{deliveryFee:v});});},style:{flex:1,minWidth:0,width:"100%",border:"none",padding:"6px 0",fontSize:12,outline:"none"}})):/*#__PURE__*/React.createElement("div",{style:{width:85,textAlign:"right",fontSize:12,fontWeight:700,color:"#334155",flexShrink:0}},"$",Number(sel.deliveryFee||0).toFixed(2)),embedded?/*#__PURE__*/React.createElement("button",{title:"Remove the delivery fee",onClick:function onClick(){setDeliveryOpen(false);setSel(function(p){return _objectSpread(_objectSpread({},p),{},{deliveryFee:""});});},style:delBtn},"\xD7"):/*#__PURE__*/React.createElement("span",{style:{width:28,flexShrink:0}})),C.showPricing&&/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:6,alignItems:"center",borderTop:"2px solid #E2E8F0",marginTop:12,paddingTop:8}},/*#__PURE__*/React.createElement("div",{style:{flex:1,minWidth:0,fontSize:12,fontWeight:800,color:"#1E293B"}},"Subtotal ",/*#__PURE__*/React.createElement("span",{style:{fontWeight:600,color:"#94A3B8",fontSize:10.5}},"(before tax)")),/*#__PURE__*/React.createElement("div",{style:{width:85,flex:"0 0 auto",textAlign:"right",fontSize:13,fontWeight:800,color:"#1E293B",padding:"6px 8px",boxSizing:"border-box"}},fmtMoney2(subtotal)),/*#__PURE__*/React.createElement("div",{style:actSpacer})),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:8,flexWrap:"wrap",marginTop:12}},/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return setCustomOptions(function(p){return[].concat(_toConsumableArray(p),[{name:"",qty:"",amount:""}]);});},style:dashBtn},"+ Add Custom Option"),embedded&&/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return setSel(function(p){return _objectSpread(_objectSpread({},p),{},{discounts:[].concat(_toConsumableArray(p.discounts||[]),[{description:"",amount:""}])});});},style:dashBtn},"+ Add Discount"),embedded&&!showDelivery&&/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return setDeliveryOpen(true);},style:dashBtn},"+ Add Delivery Fee")),/*#__PURE__*/React.createElement("div",{style:{fontSize:10.5,color:"#94A3B8",marginTop:6}},"Custom options add charges \xB7 discounts reduce the estimate total \xB7 delivery is added as a non-taxable line."));}()),embedded&&invDialog&&/*#__PURE__*/React.createElement("div",{onClick:function onClick(){return setInvDialog(null);},style:{position:"fixed",inset:0,background:"rgba(15,23,42,0.45)",zIndex:9000,display:"flex",alignItems:"center",justifyContent:"center",padding:16}},/*#__PURE__*/React.createElement("div",{onClick:function onClick(e){return e.stopPropagation();},style:{background:"#FFF",borderRadius:14,width:"min(440px, 96vw)",padding:20,boxShadow:"0 20px 60px rgba(0,0,0,0.3)",fontFamily:"system-ui, -apple-system, sans-serif"}},invDialog.done?/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("div",{style:{fontSize:17,fontWeight:800,color:"#15803D",marginBottom:6}},invDialog.done.updated?"Inventory building updated":"Requested".concat(invDialog.done.serial!=null?" \u2014 Serial #".concat(invDialog.done.serial):"")),/*#__PURE__*/React.createElement("div",{style:{fontSize:13,color:"#475569",marginBottom:16}},invDialog.done.updated?/*#__PURE__*/React.createElement(React.Fragment,null,"Find it on your portal's Inventory tab."):/*#__PURE__*/React.createElement(React.Fragment,null,"Find it on your portal's Inventory tab, and put it on the ",/*#__PURE__*/React.createElement("strong",null,"Build Schedule")," when you're ready to make it. You can quote it to a customer at any time \u2014 a building can be sold before it's built.")),/*#__PURE__*/React.createElement("div",{style:{display:"flex",justifyContent:"flex-end"}},/*#__PURE__*/React.createElement("button",{type:"button",onClick:function onClick(){return setInvDialog(null);},style:{background:"#1E293B",color:"#FFF",border:"none",borderRadius:8,padding:"9px 18px",fontSize:13,fontWeight:700,cursor:"pointer"}},"Done"))):/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("div",{style:{fontSize:17,fontWeight:800,color:"#1E293B",marginBottom:4}},inventoryMaster&&inventoryMaster.unitId?"Update inventory building":"Request this build"),/*#__PURE__*/React.createElement("div",{style:{fontSize:12.5,color:"#64748B",marginBottom:14,lineHeight:1.5}},inventoryMaster&&inventoryMaster.unitId?"Saves your design changes to this building. Its serial number and any estimates already sent are unaffected.":"No customer needed. This goes on your Inventory list as a REQUEST and takes the next serial number automatically — it isn't on the lot yet, and won't show as available to sell until it's built and brought to a location."),invDialog.err&&/*#__PURE__*/React.createElement("div",{style:{background:"#FEF2F2",border:"1px solid #FECACA",borderRadius:8,padding:"8px 12px",marginBottom:12,color:"#DC2626",fontSize:12.5,fontWeight:600}},invDialog.err),function(){var loc=invLocations.find(function(l){return String(l.id)===String(invLocationId);});var name=loc?loc.city&&loc.city!==loc.name?"".concat(loc.name," \u2014 ").concat(loc.city):loc.name:"none yet";return/*#__PURE__*/React.createElement("div",{style:{fontSize:12.5,color:"#475569",marginBottom:12}},"Location: ",/*#__PURE__*/React.createElement("b",null,name));}(),/*#__PURE__*/React.createElement("label",{style:{display:"block",fontSize:10.5,fontWeight:800,letterSpacing:0.5,textTransform:"uppercase",color:"#94A3B8",marginBottom:4}},"Asking price"),/*#__PURE__*/React.createElement("input",{value:invDialog.price,inputMode:"decimal",placeholder:"0.00",onChange:function onChange(e){return setInvDialog(function(d){return d&&_objectSpread(_objectSpread({},d),{},{price:e.target.value});});},disabled:invDialog.busy,style:{width:"100%",boxSizing:"border-box",border:"1px solid #E2E8F0",borderRadius:8,padding:"9px 10px",fontSize:13.5,background:"#FFF",color:"#1E293B"}}),/*#__PURE__*/React.createElement("div",{style:{fontSize:11,color:"#94A3B8",margin:"5px 0 14px"}},"Starts at this design's quoted price \u2014 a markdown here never changes your catalog."),/*#__PURE__*/React.createElement("div",{style:{display:"flex",justifyContent:"flex-end",gap:8}},/*#__PURE__*/React.createElement("button",{type:"button",onClick:function onClick(){return setInvDialog(null);},style:{background:"#F1F5F9",color:"#334155",border:"1px solid #E2E8F0",borderRadius:8,padding:"9px 16px",fontSize:13,fontWeight:700,cursor:"pointer"}},"Cancel"),/*#__PURE__*/React.createElement("button",{type:"button",onClick:saveInventory,disabled:invDialog.busy,style:{background:invDialog.busy?"#9CA3AF":accent,color:"#FFF",border:"none",borderRadius:8,padding:"9px 18px",fontSize:13,fontWeight:800,cursor:invDialog.busy?"wait":"pointer"}},invDialog.busy?"Saving…":inventoryMaster&&inventoryMaster.unitId?"Save changes":"Send request"))))),!submitted&&/*#__PURE__*/React.createElement("div",{style:{background:"#FFF",borderTop:"2px solid #E2E8F0",padding:"16px 20px"}},submitError&&/*#__PURE__*/React.createElement("div",{style:{background:"#FEF2F2",border:"1px solid #FECACA",borderRadius:8,padding:"10px 14px",marginBottom:12,color:"#DC2626",fontSize:13,fontWeight:600}},submitError),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:10,alignItems:"center",justifyContent:"space-between"}},/*#__PURE__*/React.createElement("p",{style:{margin:0,fontSize:12,color:"#64748B",flex:1}},inventoryNew||inventoryMaster?inventoryMaster&&inventoryMaster.unitId?/*#__PURE__*/React.createElement(React.Fragment,null,"Design the building and pick its location, then click ",/*#__PURE__*/React.createElement("strong",null,"Update Inventory Building"),"."):/*#__PURE__*/React.createElement(React.Fragment,null,"Design the building and pick where it will sit, then click ",/*#__PURE__*/React.createElement("strong",null,"Request this build"),". It lands on your Inventory list as a request \u2014 put it on the Build Schedule when you're ready to make it."):hasExistingEstimate?/*#__PURE__*/React.createElement(React.Fragment,null,"Update your selections, then click ",/*#__PURE__*/React.createElement("strong",null,"Resubmit for Updated Estimate")," to refresh and re-send your quote."):/*#__PURE__*/React.createElement(React.Fragment,null,"Place your options on the layout above, then click ",/*#__PURE__*/React.createElement("strong",null,"Get Quote")," to receive a detailed estimate.")),embedded&&(inventoryNew||inventoryMaster)&&/*#__PURE__*/React.createElement("div",{style:{display:"flex",alignItems:"center",gap:8,whiteSpace:"nowrap"}},/*#__PURE__*/React.createElement("select",{value:invLocationId,onChange:function onChange(e){return setInvLocationId(e.target.value);},title:"Location \u2014 where this building sits on your lot",style:{border:"1.5px solid #CBD5E1",borderRadius:10,padding:"12px 12px",fontSize:14,fontWeight:700,color:"#334155",background:"#FFF",cursor:"pointer",maxWidth:210}},/*#__PURE__*/React.createElement("option",{value:""},invLocations.length?"No location yet":"Loading locations…"),invLocations.map(function(l){return/*#__PURE__*/React.createElement("option",{key:l.id,value:l.id},l.name,l.city?" \u2014 ".concat(l.city):"");})),/*#__PURE__*/React.createElement("button",{type:"button",onClick:openInventoryDialog,disabled:submitting||Boolean(invDialog&&invDialog.busy),style:{background:submitting||invDialog&&invDialog.busy?"#9CA3AF":accent,color:"#FFF",border:"none",borderRadius:10,padding:"12px 22px",fontSize:14,fontWeight:800,cursor:submitting||invDialog&&invDialog.busy?"wait":"pointer",letterSpacing:"-0.01em",whiteSpace:"nowrap",boxShadow:submitting||invDialog&&invDialog.busy?"none":"0 4px 14px ".concat(accent,"50")}},inventoryMaster&&inventoryMaster.unitId?"Update Inventory Building":"Request this build")),!(inventoryNew||inventoryMaster)&&/*#__PURE__*/React.createElement("button",{onClick:submitQuote,disabled:submitting,style:{background:submitting?"#9CA3AF":accent,color:"#FFF",border:"none",borderRadius:10,padding:"12px 32px",fontSize:16,fontWeight:800,cursor:submitting?"wait":"pointer",letterSpacing:"-0.01em",boxShadow:submitting?"none":"0 4px 14px ".concat(accent,"50"),transition:"all 0.2s",minWidth:160}},submitting?"Submitting...":hasExistingEstimate?"Resubmit for Updated Estimate":"Get Quote")),estimateVersions.length>0&&function(){var cur=viewingVersion==null?estimateVersions[0]:estimateVersions.find(function(v){return v.version===viewingVersion;})||estimateVersions[0];var others=estimateVersions.filter(function(v){return v.version!==cur.version;});var csel=cur.selections||{};return/*#__PURE__*/React.createElement("div",{style:{marginTop:14,borderTop:"1px solid #F1F5F9",paddingTop:12}},/*#__PURE__*/React.createElement("div",{style:{fontSize:11,fontWeight:700,color:"#64748B",textTransform:"uppercase",letterSpacing:"0.06em",marginBottom:4}},"All designs on this estimate"),/*#__PURE__*/React.createElement("div",{style:{display:"flex",alignItems:"center",justifyContent:"space-between",gap:8,padding:"8px 0"}},/*#__PURE__*/React.createElement("div",{style:{minWidth:0}},/*#__PURE__*/React.createElement("span",{style:{fontSize:13,fontWeight:700,color:"#1E293B"}},[capWords(csel.style),csel.size].filter(Boolean).join(" ")||"Design"),/*#__PURE__*/React.createElement("span",{style:{fontSize:12,color:"#94A3B8",fontWeight:600}}," \xB7 v",cur.version," (viewing)"),others.length>0&&/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return setVersionsOpen(function(o){return!o;});},style:{marginLeft:8,background:"transparent",border:"none",padding:0,cursor:"pointer",color:accent,fontSize:12,fontWeight:700}},versionsOpen?"▴ hide":"\u25BE ".concat(estimateVersions.length," versions"))),/*#__PURE__*/React.createElement("div",{style:{whiteSpace:"nowrap",flexShrink:0}},/*#__PURE__*/React.createElement("span",{style:{color:"#94A3B8",fontWeight:700,marginRight:12,fontSize:13}},"Viewing"),ssSafeUrl(cur.image_url)&&/*#__PURE__*/React.createElement("a",{href:ssSafeUrl(cur.image_url),target:"_blank",rel:"noopener",style:{color:"#334155",fontWeight:700,textDecoration:"none",fontSize:13}},"PDF"))),versionsOpen&&others.map(function(v){var vsel=v.selections||{};var dstr="";try{dstr=new Date(v.created_at).toLocaleDateString("en-US",{month:"short",day:"numeric",year:"numeric"});}catch(_unused3){/* ignore */}return/*#__PURE__*/React.createElement("div",{key:v.version,style:{display:"flex",alignItems:"center",justifyContent:"space-between",gap:8,padding:"7px 0 7px 12px",borderTop:"1px solid #F1F5F9",background:"#F8FAFC"}},/*#__PURE__*/React.createElement("div",{style:{minWidth:0,fontSize:13,color:"#64748B"}},"\u21B3 v",v.version," \xB7 ",[capWords(vsel.style),vsel.size].filter(Boolean).join(" ")||"Design",dstr?" \xB7 ".concat(dstr):""),/*#__PURE__*/React.createElement("div",{style:{whiteSpace:"nowrap",flexShrink:0}},/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return openVersion(v.version);},style:{background:"transparent",border:"none",padding:0,cursor:"pointer",color:accent,fontWeight:700,marginRight:12,fontSize:13}},"Open"),ssSafeUrl(v.image_url)&&/*#__PURE__*/React.createElement("a",{href:ssSafeUrl(v.image_url),target:"_blank",rel:"noopener",style:{color:"#334155",fontWeight:700,textDecoration:"none",fontSize:13}},"PDF")));}));}()),submitted&&/*#__PURE__*/React.createElement("div",{style:{background:"#F0FDF4",borderTop:"2px solid #BBF7D0",padding:"32px 20px",textAlign:"center"}},/*#__PURE__*/React.createElement("div",{style:{fontSize:48,marginBottom:12}},"\u2705"),/*#__PURE__*/React.createElement("h3",{style:{margin:"0 0 8px",fontSize:20,fontWeight:700,color:"#166534"}},savedDesign&&savedDesign.updated?"Estimate Updated!":"Quote Request Submitted!"),/*#__PURE__*/React.createElement("p",{style:{margin:0,fontSize:14,color:"#15803D",maxWidth:460,marginLeft:"auto",marginRight:"auto"}},savedDesign&&savedDesign.updated?"Thank you, ".concat(contact.name||"","! Your existing estimate has been updated and re-sent by email."):"Thank you, ".concat(contact.name||"","! We've received your building configuration and layout. A team member will prepare your detailed estimate and reach out shortly.")),savedDesign&&/*#__PURE__*/React.createElement("div",{style:{maxWidth:520,margin:"20px auto 0",background:"#FFF",border:"1px solid #BBF7D0",borderRadius:10,padding:14,textAlign:"left"}},/*#__PURE__*/React.createElement("div",{style:{display:"flex",alignItems:"center",justifyContent:"space-between",marginBottom:10}},/*#__PURE__*/React.createElement("span",{style:{fontSize:11,fontWeight:700,color:"#64748B",textTransform:"uppercase",letterSpacing:"0.06em"}},"Design ID"),/*#__PURE__*/React.createElement("span",{style:{fontSize:16,fontWeight:800,color:"#1E293B",letterSpacing:"0.05em",fontFamily:"monospace"}},savedDesign.code)),savedDesign.estimateNumber&&/*#__PURE__*/React.createElement("div",{style:{display:"flex",alignItems:"center",justifyContent:"space-between",marginBottom:10}},/*#__PURE__*/React.createElement("span",{style:{fontSize:11,fontWeight:700,color:"#64748B",textTransform:"uppercase",letterSpacing:"0.06em"}},"Estimate #"),/*#__PURE__*/React.createElement("span",{style:{fontSize:14,fontWeight:700,color:"#1E293B",fontFamily:"monospace"}},"EST-",savedDesign.estimateNumber))),estimateVersions.length>0&&function(){var cur=estimateVersions[0];var others=estimateVersions.slice(1);var csel=cur.selections||{};return/*#__PURE__*/React.createElement("div",{style:{maxWidth:520,margin:"16px auto 0",background:"#FFF",border:"1px solid #BBF7D0",borderRadius:10,padding:14,textAlign:"left"}},/*#__PURE__*/React.createElement("div",{style:{fontSize:11,fontWeight:700,color:"#64748B",textTransform:"uppercase",letterSpacing:"0.06em",marginBottom:4}},"All designs on this estimate"),/*#__PURE__*/React.createElement("div",{style:{display:"flex",alignItems:"center",justifyContent:"space-between",gap:8,padding:"8px 0"}},/*#__PURE__*/React.createElement("div",{style:{minWidth:0}},/*#__PURE__*/React.createElement("span",{style:{fontSize:13,fontWeight:700,color:"#1E293B"}},[capWords(csel.style),csel.size].filter(Boolean).join(" ")||"Design"),/*#__PURE__*/React.createElement("span",{style:{fontSize:12,color:"#94A3B8",fontWeight:600}}," \xB7 v",cur.version," (current)"),others.length>0&&/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return setVersionsOpen(function(o){return!o;});},style:{marginLeft:8,background:"transparent",border:"none",padding:0,cursor:"pointer",color:accent,fontSize:12,fontWeight:700}},versionsOpen?"▴ hide":"\u25BE ".concat(estimateVersions.length," versions"))),ssSafeUrl(cur.image_url)&&/*#__PURE__*/React.createElement("a",{href:ssSafeUrl(cur.image_url),target:"_blank",rel:"noopener",style:{color:"#334155",fontWeight:700,textDecoration:"none",fontSize:13,whiteSpace:"nowrap",flexShrink:0}},"PDF")),versionsOpen&&others.map(function(v){var vsel=v.selections||{};var dstr="";try{dstr=new Date(v.created_at).toLocaleDateString("en-US",{month:"short",day:"numeric",year:"numeric"});}catch(_unused4){/* ignore */}return/*#__PURE__*/React.createElement("div",{key:v.version,style:{display:"flex",alignItems:"center",justifyContent:"space-between",gap:8,padding:"7px 0 7px 12px",borderTop:"1px solid #F1F5F9",background:"#F8FAFC"}},/*#__PURE__*/React.createElement("div",{style:{minWidth:0,fontSize:13,color:"#64748B"}},"\u21B3 v",v.version," \xB7 ",[capWords(vsel.style),vsel.size].filter(Boolean).join(" ")||"Design",dstr?" \xB7 ".concat(dstr):""),/*#__PURE__*/React.createElement("div",{style:{whiteSpace:"nowrap",flexShrink:0}},/*#__PURE__*/React.createElement("button",{onClick:function onClick(){setSubmitted(false);openVersion(v.version);},style:{background:"transparent",border:"none",padding:0,cursor:"pointer",color:accent,fontWeight:700,marginRight:12,fontSize:13}},"Open"),ssSafeUrl(v.image_url)&&/*#__PURE__*/React.createElement("a",{href:ssSafeUrl(v.image_url),target:"_blank",rel:"noopener",style:{color:"#334155",fontWeight:700,textDecoration:"none",fontSize:13}},"PDF")));}));}(),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:10,justifyContent:"center",flexWrap:"wrap",marginTop:20}},/*#__PURE__*/React.createElement("button",{onClick:function onClick(){setSubmitted(false);},style:_objectSpread(_objectSpread({},S.btn("#FFF",accent)),{},{border:"2px solid ".concat(accent),padding:"10px 24px",fontSize:14})},"Review to make additional changes"),/*#__PURE__*/React.createElement("button",{onClick:function onClick(){setSubmitted(false);setSavedDesign(null);setItems([]);setSel(function(p){var n=_objectSpread({},p);Object.keys(n).forEach(function(k){return n[k]="";});return n;});setContact({name:"",phone:"",email:"",street:"",city:"",state:"",zip:""});setPaintColors({body:"",trim:""});setCustomOptions([]);setRoDimensions({});currentDesignIdRef.current=null;isDraftRef.current=false;draftStateRef.current=null;ghlContactIdRef.current=null;ghlEstimateIdRef.current=null;ghlEstimateNumberRef.current=null;// Inventory state MUST reset with everything else: a stale inventoryUnitRef
// would silently link the NEXT, unrelated customer's estimate to the last
// unit quoted — and that estimate going accepted would then flip a building
// that never sold to Sold, false-warning every real prospect on it.
inventoryUnitRef.current=null;setInventoryMaster(null);setDesignUnit(null);setNewBuildMode(false);setHasExistingEstimate(false);setDesignCode(null);setEstimateVersions([]);setViewingVersion(null);if(!embedded)window.history.replaceState({},"",window.location.pathname);},style:_objectSpread(_objectSpread({},S.btn(accent,"#FFF")),{},{padding:"10px 24px",fontSize:14})},"Start New Quote"))),toast&&/*#__PURE__*/React.createElement("div",{style:{position:"fixed",top:20,left:"50%",transform:"translateX(-50%)",zIndex:1100,maxWidth:460,width:"90%"}},/*#__PURE__*/React.createElement("div",{style:{background:"#FFFBEB",border:"2px solid #F59E0B",borderRadius:12,padding:"14px 20px",boxShadow:"0 8px 30px rgba(0,0,0,0.15)",display:"flex",gap:12,alignItems:"flex-start"}},/*#__PURE__*/React.createElement("span",{style:{fontSize:24,lineHeight:1,flexShrink:0}},"\u26A0\uFE0F"),/*#__PURE__*/React.createElement("div",{style:{flex:1}},/*#__PURE__*/React.createElement("div",{style:{fontWeight:700,fontSize:14,color:"#92400E",marginBottom:4}},"Can't place here"),/*#__PURE__*/React.createElement("div",{style:{fontSize:13,color:"#A16207",lineHeight:1.4}},toast)),/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return setToast(null);},style:{background:"none",border:"none",fontSize:18,cursor:"pointer",color:"#92400E",flexShrink:0,padding:0}},"\u2715"))),cal3dPreview,show3D&&/*#__PURE__*/React.createElement(Structure3DViewer,{bldgW:bldgW,bldgH:bldgH,items:items,itemTypes:ITEMS,styleValue:sel.style,frontWall:frontWall,painted:sel.paint==="Painted",paintBody:paintColors.body,paintTrim:paintColors.trim,scale:scale,mgX:mgX,mgY:mgY,accent:accent,style3d:d3ResolveStyleSpec(selectedStyle,sel.style,C.wallHeightFt,d3SidingOverride(C,sel),sel.wallHeight),roofType:sel.roofType,roofColorHex:function(){var rc=(Array.isArray(C.colors)?C.colors:[]).find(function(c){return c.label===sel.roofColor&&(sel.roofType==="Metal"?c.metal:c.shingle);});return rc&&rc.hex?rc.hex:"";}(),fixtures:C.fixtures,paletteKeys:Object.keys(ITEMS).filter(function(k){return ITEMS[k]&&!ITEMS[k].noPalette&&(embedded||!ITEMS[k].internalOnly);}),placeableDoors:placeableDoors,placeableWindows:placeableWindows,placeableRamps:placeableRamps,paintEnabled:C.options.some(function(o){return o.id==="paint"&&isOptionApplicable(o,sel.style);}),onPaintChange:function onPaintChange(pc){setPaintColors({body:pc.body,trim:pc.trim});setSel(function(p){return _objectSpread(_objectSpread({},p),{},{paint:pc.body||pc.trim?"Painted":"No Paint"});});},onWallHeight:function onWallHeight(h){return setSel(function(p){return _objectSpread(_objectSpread({},p),{},{wallHeight:h});});},onItemAdd:function onItemAdd(ni){return setItems(function(p){return[].concat(_toConsumableArray(p),[ni]);});},onItemSelect:function onItemSelect(id){return setSelectedId(id);},onItemMove:function onItemMove(id,sn){return setItems(function(p){return p.map(function(i){return i.id===id?_objectSpread(_objectSpread({},i),sn):i;});});},onItemDelete:function onItemDelete(id){setItems(function(p){return p.filter(function(i){return i.id!==id&&!(i.type==="ramp"&&i.snapDoorId===id);});});setSelectedId(null);},onSnapshot:function onSnapshot(shot){render3DSnapshotRef.current=shot||null;setHas3DSnapshot(Boolean(shot));},onClose:function onClose(){return setShow3D(false);}}),showExport&&/*#__PURE__*/React.createElement("div",{style:{position:"fixed",inset:0,background:"rgba(0,0,0,0.6)",display:"flex",alignItems:"center",justifyContent:"center",zIndex:1000,padding:20},onClick:function onClick(){setShowExport(false);setExportUrl(null);}},/*#__PURE__*/React.createElement("div",{style:{background:"#FFF",borderRadius:16,padding:24,maxWidth:580,width:"100%",boxShadow:"0 20px 60px rgba(0,0,0,0.2)"},onClick:function onClick(e){return e.stopPropagation();}},/*#__PURE__*/React.createElement("div",{style:{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:16}},/*#__PURE__*/React.createElement("h3",{style:{margin:0,fontSize:18,fontWeight:700,color:"#1E293B"}},C.branding.companyName||"Design Studio"," Export"),/*#__PURE__*/React.createElement("button",{onClick:function onClick(){setShowExport(false);setExportUrl(null);},style:{background:"none",border:"none",fontSize:20,cursor:"pointer",color:"#94A3B8"}},"\u2715")),exportUrl&&/*#__PURE__*/React.createElement(React.Fragment,null,/*#__PURE__*/React.createElement("img",{src:exportUrl,alt:"Floor Plan",style:{width:"100%",borderRadius:8,border:"1px solid #E2E8F0"}}),/*#__PURE__*/React.createElement("div",{style:{display:"flex",gap:8,marginTop:16}},/*#__PURE__*/React.createElement("button",{onClick:downloadPDF,style:_objectSpread(_objectSpread({flex:1},S.btn("#B91C1C","#FFF")),{},{padding:10,fontSize:14})},"\u2B07 Download PDF"),/*#__PURE__*/React.createElement("button",{onClick:downloadPNG,style:_objectSpread(_objectSpread({flex:1},S.btn("#1E293B","#FFF")),{},{padding:10,fontSize:14})},"\u2B07 Download PNG"),/*#__PURE__*/React.createElement("button",{onClick:function onClick(){fetch(exportUrl).then(function(r){return r.blob();}).then(function(b){navigator.clipboard.write([new ClipboardItem({"image/png":b})])["catch"](function(){});});},style:_objectSpread(_objectSpread({flex:1},S.btn("#F1F5F9","#334155")),{},{border:"1px solid #E2E8F0",padding:10,fontSize:14})},"\uD83D\uDCCB Copy")),/*#__PURE__*/React.createElement("p",{style:{fontSize:11,color:"#94A3B8",marginTop:12,textAlign:"center"}},"8.5\"\xD711\" letter \u2014 attach to your GHL estimate or invoice")))));}// ─── FeedbackWidget: REMOVED from the designer (2026-07-26) ───
// The bug/feature widget now lives in portal.html only. A submission has to be
// attributable to a signed-in portal user and their tenant (portal-feedback resolves
// both server-side from the JWT), and the public designer's visitors are anonymous
// shed-shoppers who should never have seen a "Report a bug" button on a tenant's
// customer-facing page. Mirror note: StructureStudio.jsx dropped its
// `import FeedbackWidget from "./FeedbackWidget.jsx"` in the same change.
// ─── Client-config loader (default export) ───
// Every page load fetches the tenant's config from public.client_configs — there
// is no in-source copy of any client. Resolution order:
//   1. `config` prop (e.g. supplied by index.html's postMessage re-render handler)
//      wins and is used as-is, no fetch.
//   2. `?client=<id>` URL param — explicit override, wins over hostname.
//   3. Subdomain — `juniorbarns.structurestudio.app` → "juniorbarns", on either
//      apex we serve (structurestudio.app and structurestudiosuite.com — both live
//      during the migration). Skipped for the apexes themselves, IPs, localhost,
//      *.pages.dev / *.netlify.app / *.workers.dev deploy hosts, and the reserved
//      env labels (www/beta/dev/staging/app).
//   4. `?id=<short_code>` share-link — the design row records its owning tenant;
//      resolved via the load_design RPC (NOT a direct table read — that dies at
//      cutover) so a rep clicking someone else's link gets that tenant's branding.
//   5. Fallback: DEFAULT_CLIENT_ID.
// On fetch failure (network error or unknown client_id) we render an error screen
// with a retry button rather than silently falling back to a wrong-tenant config.
// The fetched config's clientId is always forced to the row key so a config blob
// can never point a tenant's designs at another tenant.
// A partial config row (e.g. branding-only) would crash the designer mid-render,
// so fail loud on the error screen instead. Every row must be authored complete —
// see the onboarding runbook in CLAUDE.md.
var REQUIRED_CONFIG_KEYS=["branding","contactFields","buildingStyles","defaultSizes","options","layoutItems"];// Catches render-time throws inside the designer (e.g. a malformed-but-complete
// config row that passes REQUIRED_CONFIG_KEYS but has a bad nested shape) so the
// user gets a recoverable message instead of a blank white screen.
var DesignerErrorBoundary=/*#__PURE__*/function(_Component){_inherits(DesignerErrorBoundary,_Component);function DesignerErrorBoundary(props){var _this;_classCallCheck(this,DesignerErrorBoundary);_this=_callSuper(this,DesignerErrorBoundary,[props]);_this.state={err:null};return _this;}_createClass(DesignerErrorBoundary,[{key:"componentDidCatch",value:function componentDidCatch(err){console.error("[StructureStudio] designer render error:",err);if(window.ssLogError)window.ssLogError("designer",err&&err.message||"render error",err&&err.name,{phase:"render",stack:err&&err.stack?String(err.stack).slice(0,2000):null});}},{key:"render",value:function render(){if(this.state.err){return/*#__PURE__*/React.createElement("div",{style:{display:"flex",flexDirection:"column",alignItems:"center",justifyContent:"center",minHeight:this.props.embedded?"40vh":"100vh",padding:"0 24px",fontFamily:"system-ui, -apple-system, sans-serif",color:"#1E293B",textAlign:"center"}},/*#__PURE__*/React.createElement("div",{style:{fontSize:18,fontWeight:700,marginBottom:8}},"This designer couldn't be displayed"),/*#__PURE__*/React.createElement("div",{style:{fontSize:13,color:"#64748B",maxWidth:480,marginBottom:4}},"There's a problem with this builder's configuration. Please contact support."),/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return window.location.reload();},style:{marginTop:20,padding:"8px 16px",background:"#1E293B",color:"#FFF",border:"none",borderRadius:6,fontSize:13,cursor:"pointer"}},"Reload"));}return this.props.children;}}],[{key:"getDerivedStateFromError",value:function getDerivedStateFromError(err){return{err:err};}}]);return DesignerErrorBoundary;}(Component);// Cutover marker: lets us verify from the deployed site which data path this
// bundle uses (multi-tenant RPC vs. legacy direct table access).
console.log("[StructureStudio] multi-tenant build: config-loader + RPC data path");function StructureStudio(_ref50){var _ref50$config=_ref50.config,configProp=_ref50$config===void 0?null:_ref50$config,_ref50$clientId=_ref50.clientId,clientIdProp=_ref50$clientId===void 0?null:_ref50$clientId,_ref50$embedded=_ref50.embedded,embedded=_ref50$embedded===void 0?false:_ref50$embedded,_ref50$onSaved=_ref50.onSaved,onSaved=_ref50$onSaved===void 0?null:_ref50$onSaved,_ref50$openDesign=_ref50.openDesign,openDesign=_ref50$openDesign===void 0?null:_ref50$openDesign,_ref50$setup3d=_ref50.setup3d,setup3d=_ref50$setup3d===void 0?null:_ref50$setup3d,_ref50$view3d=_ref50.view3d,view3d=_ref50$view3d===void 0?false:_ref50$view3d,_ref50$calibrationOnl=_ref50.calibrationOnly,calibrationOnly=_ref50$calibrationOnl===void 0?false:_ref50$calibrationOnl;// state shape: { status: "ready", config } | { status: "loading" } | { status: "error", clientId, message }
var _useState175=useState(function(){return configProp?{status:"ready",config:configProp}:{status:"loading"};}),_useState176=_slicedToArray(_useState175,2),state=_useState176[0],setState=_useState176[1];// White-label the browser tab: show the tenant's business name once config loads.
// Skipped when embedded — the host page (the portal) owns its own tab title.
useEffect(function(){if(embedded)return;if(state.status==="ready"&&typeof document!=="undefined"){document.title=state.config.branding&&state.config.branding.companyName||"Design Studio";}},[state]);useEffect(function(){if(state.status!=="loading")return;if(typeof window==="undefined")return;var params=new URLSearchParams(window.location.search);// Embedded hosts (the owner portal) pass the tenant directly; the URL never
// decides the tenant for an embedded mount.
var clientId=clientIdProp||params.get("client");var designShortCode=params.get("id");// Tenant subdomains: derive a client_id from <sub>.<apex> on EITHER apex we serve.
// Both are live during the Netlify → Cloudflare migration (structurestudio.app is
// production today; structurestudiosuite.com is where we are moving, beta already
// on it), so a branded link must resolve identically on both — supporting only the
// old apex breaks every tenant's subdomain the day production cuts over, and only
// the new one breaks them all today. Retire an entry here when its apex is retired.
// Anything else (either apex itself, *.pages.dev / *.netlify.app / *.workers.dev
// deploy hosts, localhost, IPs, env labels) falls through — a deploy hostname is
// never a tenant.
if(!clientId){var host=window.location.hostname.toLowerCase();var TENANT_APEXES=["structurestudio.app","structurestudiosuite.com"];var RESERVED_SUBDOMAINS=["www","beta","dev","staging","app"];for(var _i21=0,_TENANT_APEXES=TENANT_APEXES;_i21<_TENANT_APEXES.length;_i21++){var base=_TENANT_APEXES[_i21];if(!host.endsWith("."+base))continue;var sub=host.slice(0,host.length-base.length-1);if(sub&&!sub.includes(".")&&!RESERVED_SUBDOMAINS.includes(sub))clientId=sub;break;}}// Bare product root: no tenant link (?client= / subdomain) and no design code.
// This isn't any tenant's page — it's where business owners land, so send
// them to the portal; they copy their customer design link from the dashboard.
if(!clientId&&!designShortCode){if(embedded){// An embedded mount must never navigate the host page — redirecting to
// /portal.html from inside the portal would loop. Show the error screen.
setState({status:"error",clientId:"",message:"No client id was supplied to the embedded designer."});return;}// Carry the query AND hash across. Supabase delivers auth-email outcomes in the
// URL — `#access_token=…&type=invite|recovery` (implicit), `?code=…` (PKCE), or
// `#error=…&error_code=otp_expired` — and a bare replace("/portal") DESTROYS them,
// so the portal booted with a clean URL, found no session, and showed a login form
// instead of the set-password screen. That is the "invite/reset link just takes me
// to login" bug (Carolyn, 2026-07-28). It reaches this page at all whenever the
// link's redirect_to is not in Supabase's allow-list, because Supabase then falls
// back to Site URL (the apex root). portal.html already handles all three shapes.
window.location.replace("/portal"+window.location.search+window.location.hash);return;}var cancelled=false;_asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee25(){var sb,_yield$sb$rpc,rows,dErr,design,_yield$sb$rpc2,cfg,error,missing,fixtures,rampSettings,fxRes,fx;return _regeneratorRuntime().wrap(function _callee25$(_context26){while(1)switch(_context26.prev=_context26.next){case 0:_context26.prev=0;sb=createClient(SUPABASE_URL,SUPABASE_ANON_KEY);// Share-link path: ?id=<short_code> without ?client= or a tenant subdomain
// means someone opened a saved design's bare link. The design row records
// which tenant owns it; look that up so the right config wraps the load.
if(!(!clientId&&designShortCode)){_context26.next=12;break;}_context26.next=5;return sb.rpc("load_design",{p_code:designShortCode});case 5:_yield$sb$rpc=_context26.sent;rows=_yield$sb$rpc.data;dErr=_yield$sb$rpc.error;design=Array.isArray(rows)?rows[0]:rows;if(!cancelled){_context26.next=11;break;}return _context26.abrupt("return");case 11:if(dErr||!design||!design.client_id){console.warn("Design \"".concat(designShortCode,"\" not found while resolving client; using default."),dErr);clientId=DEFAULT_CLIENT_ID;}else{clientId=design.client_id;}case 12:if(!clientId)clientId=DEFAULT_CLIENT_ID;// Fetch this tenant's config via the get_config RPC (capability read),
// not a direct client_configs table query: anon can no longer bulk-read
// every tenant's config — only the one client_id it asks for. The RPC is
// SECURITY DEFINER, so it keeps working after the table's anon SELECT is
// revoked at cutover. Returns the config jsonb, or null for an unknown
// client (→ the error screen, same as a missing row used to do).
_context26.next=15;return sb.rpc("get_config",{p_client_id:clientId});case 15:_yield$sb$rpc2=_context26.sent;cfg=_yield$sb$rpc2.data;error=_yield$sb$rpc2.error;if(!cancelled){_context26.next=20;break;}return _context26.abrupt("return");case 20:if(!(error||!cfg)){_context26.next=24;break;}console.warn("Could not load config for client \"".concat(clientId,"\":"),error);setState({status:"error",clientId:clientId,message:error&&error.message||"Configuration not found."});return _context26.abrupt("return");case 24:missing=REQUIRED_CONFIG_KEYS.filter(function(k){return!cfg[k];});if(!(missing.length>0)){_context26.next=28;break;}setState({status:"error",clientId:clientId,message:"Configuration row is incomplete (missing: ".concat(missing.join(", "),").")});return _context26.abrupt("return");case 28:// Fixtures catalog (Options → Doors; windows/ramps later) — best-effort: a failure
// just means no catalog doors in the palette, it never blocks the designer.
fixtures=[],rampSettings=null;_context26.prev=29;_context26.next=32;return sb.rpc("get_fixtures",{p_client_id:clientId});case 32:fxRes=_context26.sent;fx=fxRes&&fxRes.data;if(!cancelled&&fx){// get_fixtures returns either the legacy array or { items, ramp }.
if(Array.isArray(fx))fixtures=fx;else{if(Array.isArray(fx.items))fixtures=fx.items;if(fx.ramp)rampSettings=fx.ramp;}}_context26.next=39;break;case 37:_context26.prev=37;_context26.t0=_context26["catch"](29);case 39:if(!cancelled){_context26.next=41;break;}return _context26.abrupt("return");case 41:setState({status:"ready",config:_objectSpread(_objectSpread({},cfg),{},{clientId:clientId,fixtures:fixtures,rampSettings:rampSettings})});_context26.next=50;break;case 44:_context26.prev=44;_context26.t1=_context26["catch"](0);if(!cancelled){_context26.next=48;break;}return _context26.abrupt("return");case 48:console.warn("Client config fetch error:",_context26.t1);setState({status:"error",clientId:clientId||DEFAULT_CLIENT_ID,message:_context26.t1&&_context26.t1.message||"Network error."});case 50:case"end":return _context26.stop();}},_callee25,null,[[0,44],[29,37]]);}))();return function(){cancelled=true;};},[state.status]);if(state.status==="loading"){return/*#__PURE__*/React.createElement("div",{style:{display:"flex",alignItems:"center",justifyContent:"center",minHeight:embedded?"40vh":"100vh",fontFamily:"system-ui, -apple-system, sans-serif",color:"#64748B",fontSize:14}},"Loading\u2026");}if(state.status==="error"){return/*#__PURE__*/React.createElement("div",{style:{display:"flex",flexDirection:"column",alignItems:"center",justifyContent:"center",minHeight:embedded?"40vh":"100vh",padding:"0 24px",fontFamily:"system-ui, -apple-system, sans-serif",color:"#1E293B",textAlign:"center"}},/*#__PURE__*/React.createElement("div",{style:{fontSize:18,fontWeight:700,marginBottom:8}},"Could not load configuration"),/*#__PURE__*/React.createElement("div",{style:{fontSize:13,color:"#64748B",marginBottom:4}},"Client: ",/*#__PURE__*/React.createElement("code",null,state.clientId)),/*#__PURE__*/React.createElement("div",{style:{fontSize:13,color:"#64748B",maxWidth:480}},state.message),/*#__PURE__*/React.createElement("button",{onClick:function onClick(){return setState({status:"loading"});},style:{marginTop:20,padding:"8px 16px",background:"#1E293B",color:"#FFF",border:"none",borderRadius:6,fontSize:13,cursor:"pointer"}},"Retry"));}return/*#__PURE__*/React.createElement(DesignerErrorBoundary,{embedded:embedded},/*#__PURE__*/React.createElement(StructureStudioInner,{config:state.config,embedded:embedded,onSaved:onSaved,openDesign:openDesign,setup3d:setup3d,view3d:view3d,calibrationOnly:calibrationOnly}));}// Publish for the host pages' thin mount blocks (cross-block const sharing does not
// exist under Babel-standalone; JSX also needs a capitalized in-scope identifier).
window.StructureStudio=StructureStudio;window.ssAllowedOrigin=ssAllowedOrigin;
}).call(window);
