# ┌──────────────────────────────────────────────────────────────────────────────┐
# │  ⛔ NEVER RUN `supabase config push` WITH THIS FILE.                          │
# │                                                                              │
# │  `config push` is DECLARATIVE, not a patch: anything absent here is applied  │
# │  to the remote project as the CLI's DEFAULT. There is no `config pull`, so   │
# │  the CLI cannot snapshot the live settings first.                            │
# │                                                                              │
# │  This file has NO [auth] section. Pushing it would set                       │
# │  site_url = "http://127.0.0.1:3000", de-allow-list the real redirect, and    │
# │  clear custom SMTP — undoing BOTH the canonical auth-redirect fix and the    │
# │  SPF/sender-authentication work in a single command.                         │
# │                                                                              │
# │  This file exists for ONE reason: `supabase functions deploy` reads the      │
# │  [functions.*] blocks below, so verify_jwt can no longer depend on somebody  │
# │  remembering a CLI flag. That is all it is for.                              │
# │                                                                              │
# │  For one-off auth changes (email templates, SMTP, redirect URLs) use the     │
# │  dashboard. If you ever genuinely need `config push`, first reproduce EVERY  │
# │  current remote value here by reading them off the dashboard by hand.        │
# └──────────────────────────────────────────────────────────────────────────────┘

project_id = "jzeamjbhdrsbygdnphbm"

# ── Edge function JWT gating ───────────────────────────────────────────────────
# Values below mirror the LIVE state verified via list_edge_functions on 2026-07-30.
#
# verify_jwt = false means the API gateway does NOT require a JWT. It is correct only where
# the function authenticates by some other means, and each one below says which. Everything
# else is true — note that true is NOT itself authentication: the public anon key is a valid
# JWT, so those functions still do their own auth (resolveTenant / adminAuth / a shared
# secret) inside the handler.
#
# This history is why the file exists: billing-webhook was once redeployed without
# --no-verify-jwt, which flipped it to true and made every real gateway callback fail at the
# edge — invisibly, because the gateway rejects before any of our logging runs.

# --- Public / non-JWT callers: verify_jwt MUST stay false ---

[functions.billing-webhook]
# Deposyt/NMI posts here server-to-server. Authenticated by an HMAC signature over the raw
# body (DEPOSYT_WEBHOOK_SIGNING_KEY), not by a JWT. Payments break if this flips to true.
verify_jwt = false

[functions.feedback-monday-webhook]
# monday.com posts here. Authenticated by a shared secret (FEEDBACK_SYNC_SECRET).
verify_jwt = false

[functions.email-inbound]
# The inbound-email return leg: a customer's reply POSTs here server-to-server and cannot
# carry a Supabase JWT, so verification stays OFF and auth is a shared secret in the URL
# (?key= <-> EMAIL_INBOUND_SECRET) compared in constant time -- the postmark-events pattern.
# Flip this to true and every customer reply silently stops reaching the CRM: the gateway
# 401s the delivery BEFORE the function runs, so nothing is logged anywhere and the
# conversation just quietly goes one-way again. That is exactly how billing-webhook broke
# for months in 2026-07.
verify_jwt = false

[functions.postmark-events]
# Postmark posts delivery/bounce/spam-complaint events here server-to-server. Authenticated
# by a shared secret in the URL (?key= ⇄ POSTMARK_WEBHOOK_SECRET) compared in constant time —
# the same pattern as feedback-monday-webhook.
verify_jwt = false

[functions.admin-save-settings]
# Called by the designer's ?admin=1 panel, which sends the anon key and authenticates with
# the shared ADMIN_PASSWORD via _shared/adminAuth.ts (plus a per-operator can_write check).
verify_jwt = false

# --- Everything else: JWT required at the gateway, real auth inside ---

[functions.admin-catalog]
# Dual-credential (operator JWT OR ADMIN_PASSWORD step-up). true is correct: admin.html
# reaches it through sb.functions.invoke, so the anon key satisfies the gateway and the
# password gate does the real work. Do NOT "fix" this to false.
verify_jwt = true

[functions.portal-settings]
verify_jwt = true

[functions.portal-billing]
verify_jwt = true

[functions.portal-feedback]
verify_jwt = true

[functions.portal-commissions]
# JWT-authenticated: the function resolves the real user via auth.getUser() and maps them
# through client_users to their tenant + role. The confidential commission_members table is
# service-role only, so all rate/grant access is gated here.
verify_jwt = true

[functions.portal-schedule]
# Build/Delivery Scheduling + Repairs backend. JWT-authenticated via resolveTenant
# (auth.getUser() → client_users; READ/STAFF/ADMIN action tiers; operator override with
# can_write). driver_profiles is service-role only, so all driver setup goes through here.
verify_jwt = true

[functions.operator-portal]
verify_jwt = true

[functions.portal-projects]
# Internal "Projects" module (Monday.com replacement) — operators only. Same auth shape
# as operator-portal: auth.getUser() + service-role app_operators lookup; writes need
# can_write. Not tenant-scoped: never add it to portal.html's SS_TENANT_SCOPED_FNS.
verify_jwt = true

[functions.submit-estimate]
verify_jwt = true

[functions.sync-design-status]
verify_jwt = true

[functions.capture-lead]
# Public in practice — the anon key passes the gateway — so this is NOT auth. Its real
# protections are body validation, a tenant existence check, and the per-tenant rate cap
# added 2026-07-30. Kept true so a bare unauthenticated request is still refused early.
verify_jwt = true

[functions.customer-auth]
# Public in practice — the anon key passes the gateway — so this is NOT auth. Its real
# protections are the Twilio Verify OTP (the caller proves control of the phone), the
# per-phone + per-IP throttle buckets with lockout (customer_otp_throttle), a tenant
# existence check, and US-only numbers. Kept true so a bare unauthenticated request is
# still refused early.
verify_jwt = true

[functions.customer-quotes]
# Session-token auth inside (customer_sessions, hash-at-rest): the function resolves the
# presented bearer token and returns ONLY the verified phone's rows for the named tenant.
# The anon key passes the gateway here too — the token check is the real gate.
verify_jwt = true

[functions.customer-accept]
# The customer's e-signature (migration 124). Same auth story as customer-quotes: the
# session token is the real gate, and the signature only attaches to a design whose
# contact phone matches the session's verified phone. SS-mode tenants only.
verify_jwt = true

[functions.qbo-oauth-connect]
# Starting an OAuth flow attaches a QuickBooks company to a tenant — an owner/admin action.
verify_jwt = true

[functions.qbo-oauth-callback]
# ⛔ MUST STAY FALSE. Intuit redirects the user's BROWSER here with no Supabase JWT; with
# verification on, the gateway 401s every callback before the function runs and connecting
# QuickBooks silently becomes impossible — the exact shape of the billing-webhook outage.
# Auth is the one-shot nonce stored on the tenant row, compared in constant time.
verify_jwt = false

# NOTE: `github-monday-bug` and `migrate-style-images` are deployed on the project but have
# no directory in this repo, so they are deliberately absent here — a [functions] block for
# a function the CLI cannot find is not something to invent. `github-monday-bug` runs with
# verify_jwt = false; leave it alone unless it is brought into the repo.
